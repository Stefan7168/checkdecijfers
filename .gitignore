# OS cruft
.DS_Store
Thumbs.db
Desktop.ini

# Competitor research — reference material, not versioned
Archive/

# Editor/local
*.local
.idea/
.vscode/

# Secrets — never in git (docs/RUNBOOK.md, secrets register)
.env
.env.*
!.env.example

# Dependencies / build output
node_modules/
.next/
coverage/

# Vercel CLI project link (org/project IDs — not secret, just not committed)
.vercel

# Generated benchmark-run artifacts (npm run benchmark:run / :live) — the
# hermetic dump is regenerated by CI on every push; the live dump is one run's
# scoring material. Committed provenance lives in docs/STATUS.md and
# benchmark/live-benchmark-report.json (scorer --report), never here
benchmark/audit-run.json
benchmark/audit-run-live.json
benchmark/validation-run.json
