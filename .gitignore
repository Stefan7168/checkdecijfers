# OS cruft
.DS_Store
Thumbs.db
Desktop.ini

# Competitor research — reference material, not versioned
Archive/

# Editor/local
*.local
.idea/
.vscode/

# Secrets — never in git (docs/RUNBOOK.md, secrets register)
.env
.env.*
!.env.example

# Dependencies / build output
node_modules/
.next/
coverage/

# Generated benchmark-run artifact (npm run benchmark:run) — regenerated by CI
# on every push; committed provenance lives in docs/STATUS.md, never here
benchmark/audit-run.json
