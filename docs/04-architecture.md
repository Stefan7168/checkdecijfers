# Architecture

Recommended architecture for Phase 0–2, per the ADRs in [decisions/](decisions/). Summary: **one full-stack TypeScript app** (ADR [001](decisions/001-single-app-vs-split.md)) over **one Postgres database** (ADR [002](decisions/002-postgres-system-of-record.md)), with CBS data **bulk-ingested behind an adapter** (ADR [003](decisions/003-cbs-access-layer.md)) and the LLM confined to **schema-validated roles that only ever propose or veto, never decide** (ADR [004](decisions/004-llm-usage.md)): intent parsing, answer phrasing, and — #144, flag-gated — the reject-only semantic checker (ADR [034](decisions/034-semantic-fabrication-check.md)).

## System shape

```
                 ┌────────────────────────────────────────────────┐
                 │  Next.js app (single deployment)               │
                 │                                                │
 user ──chat──▶  │  UI (chat, answer view, chart renderer)        │
                 │        │                                       │
                 │  orchestrator (server)                         │
                 │   1. intent parse ──────────▶ LLM (schema)     │
                 │   2. plan against table registry               │
                 │   3. deterministic SQL query ──▶ Postgres      │
                 │   4. validation (existence/units/period)       │
                 │   5. cost-estimation step (no-op in Phase 0)   │
                 │   6. answer phrasing ───────▶ LLM (schema)     │
                 │   7. verbatim-number check                     │
                 │   7b. semantic veto (#144) ─▶ LLM (schema)     │
                 │   8. chart spec (deterministic)                │
                 │   9. audit record ──────────▶ Postgres         │
                 │      ▲ any failed step → clarify or refuse     │
                 └────────────────────────────────────────────────┘
                          ▲
   ingestion script ──────┘   (out-of-band, same repo, CLI/scheduled)
   CbsSource adapter ◀── CBS OData v4 / bulk channels
```

Refusal and clarification text is **deterministic templates, never an LLM** (ADR [015](decisions/015-refusal-clarification-composition.md)); a clarification opens exactly **one** reply round, whose free-text answer re-enters at step 1 merged with the pending partial intent (still-unresolved → refusal-with-guidance, never a second question).

Since WP15 (ADR [021](decisions/021-conversation-memory-structured-context.md)): a follow-up question re-enters at step 1 with the **previous turn's resolved intent** — the stored query plan, mapped back to registry vocabulary and validated server-side — offered as a structured merge candidate ("En in Rotterdam?" inherits topic and period). Never raw chat history in any prompt. Step 1's low-confidence echo suggestions are also **dry-run through steps 2–4** before being offered (#56): an unservable suggestion is replaced by a clarification naming what is actually loaded.

## Components and why they earn their place

| Component | Justification | ADR |
|---|---|---|
| Next.js full-stack app | One repo/language/deploy for a solo non-developer + AI sessions; streaming chat UI ecosystem | [001](decisions/001-single-app-vs-split.md) |
| Postgres (managed) | System of record for CBS data, registry, audit records; boring and sufficient | [002](decisions/002-postgres-system-of-record.md) |
| `CbsSource` adapter + ingestion script | Bulk ingestion per principle (b); isolates the SDMX migration risk; schema-fingerprint defense against table redesigns. Since session 50 (2026-07-17): curated phantom-measure exclusion (`Phase0Table.excludeMeasures`, [#167](open-questions.md) — CBS metadata entries with zero observations table-wide are registered as not-published, fingerprint deliberately unfiltered) + the slow-stream escape hatch (`scripts/capture-observations-chunked.ts` + `scripts/sync-from-capture.ts`, RUNBOOK step 5 — ⚠ **as-built 2026-08-07 (session 60): that hatch could NOT complete a RELEASE sync**, because `sync-from-capture.ts` passed no options to `syncTable` so `acceptNewCodes` was unreachable; **✅ FIXED the same day in session 61** — the script now mirrors `src/ingestion/cli.ts` with exported `parseArgs`/`runSyncFromCapture` and threads both flags, pinned by `tests/ingestion/sync-from-capture.test.ts` ([#192](open-questions.md)); the ~6KB/s slow stream it exists for also measured network-specific, 100,240 rows in 77 s on the direct path). Since session 53: the exclusion mechanism also covers SLICE-EMPTY measures (rows table-wide, zero within the registered slice — 85828NED's Productie family; docs/11 quirk #4), and the curated seed set spans Phase 0 + coverage tables #1-#9 (#4-#9 dormant pending [#168](open-questions.md)). Since session 55 (2026-07-24, #154): per-cell freshness truth — `observations.last_seen_batch_id` NULL-sentinel (migration 021, applied BEFORE the code deploy): syncs mark newly-RETAINED cells with the last provably-confirming batch and clear the marker on reappearance; the answer path dates a result by the OLDEST effective cell date, so retained cells trip the staleness net with the honest "niet opnieuw bevestigd" wording (tests/ingestion/retained-cells.test.ts) | [003](decisions/003-cbs-access-layer.md), [010](decisions/010-registry-canonical-measures.md) |
| `cbs_catalog` mirror + `src/catalog/` table finder | Demand-driven discovery (WP16 sub-part 1): finds which CBS table answers a not-yet-loaded topic — Dutch FTS recall + a rerank picking from a hard-allowlisted shortlist, over our own mirrored metadata, never a live CBS call in the request path. Built and live-calibrated; **wired into the answer flow and LIVE in production since the session-28 go-live (2026-07-06, `ONBOARDING_ENABLED=1`)** — see [STATUS.md](STATUS.md). **Since WP27 stage A (PR #17, merged + deployed 2026-07-09): question-aware rerank (full question + topic, prompt v2 incl. the vague-question honesty rule) + Regulier-first shortlist quotas (20 current + 4 historic). Stage B (PR #18, merged + deployed 2026-07-10) adds the candidate chain (`candidate_ids`, cap 3) through the trigger into `pending_table_requests`; stage C (PR #21, merged + deployed 2026-07-10) adds the in-job measure-fit gate (Haiku closed-choice + A3 deterministic pre-checks) that consumes it. **Stage D (session 33, 2026-07-10, owner-supervised, PR #22): migration 015 applied to production — the whole chain is LIVE and PROVEN (threshold calibrated at 0.8; the #111 bijstand acceptance test answered live in 88s)** | [025](decisions/025-cbs-catalog-table-discovery.md), [026](decisions/026-on-demand-fetch-job-architecture.md), [027](decisions/027-finder-shape-fit-gate.md) |
| Claude API, narrow calls only | Intent parsing + phrasing (strict schemas; verbatim-number check), plus the #144 reject-only semantic checker (below) — every role proposes or vetoes, deterministic code decides | [004](decisions/004-llm-usage.md) |
| Semantic fabrication checker (`src/answer/compose/semantic-check.ts`) | **LIVE + ACTIVE (#144, session 46, 2026-07-16: PR #47 merged + supervised go-live executed same session — calibration 9/9 ×3, prompt v2, replay leg on the gate, smoke row 253 ✓).** The additive, REJECT-ONLY cheap-tier second pass over validated bodies that leaned on a residual-prone exemption (the two proven deterministic ceilings, ADR 013 §6: descriptor-echo reuse and temporal-marker+un-listed-noun). Deterministic suspect gate (`ClassifiedToken.soft`, corpus-measured: 0% of stored legit bodies trigger, both residual shapes do); a fabricated verdict takes the same R3 ladder rung (regenerate → template); verdict stored on the envelope, recorded-not-rederived, scope re-derived by R8 with tamper teeth. Checker errors: FAIL-OPEN + admin alert per skip (owner decision at the flip; alerts.ts e-mails audit row/user/question/error via ADMIN_ALERT_EMAIL). Since session 55 (2026-07-24, #121): alerts.ts carries two more fail-soft hooks at the same respond-audited site — a served 'internal' refusal alerts (the honest catch-all became loud), and a template answer served with a failing validator verdict alerts + gets the distinct R8 label "KNOWN-failing at serve time" (owner option A; the −39 precedent) | [034](decisions/034-semantic-fabrication-check.md) |
| Durable error log (`src/db/error-log.ts`, migration 024) + auth-free health route (`web/app/api/health/route.ts`) | **BUILT hermetic session 66 (2026-08-27, autonomous); MERGED session 67 (2026-08-28, PR #110, `5cccaa3`) — live-verified: `/api/health` returns `{"ok":true,"checks":[...]}` naming only check names, no error text or row data. Migration 024 APPLIED to production 2026-09-02 (session 69, owner present, together with 022/023/025 — `schema_migrations` re-checked 2026-09-03).** A session-67 review caught and fixed one latent bug before merge: the retention job's admin-alert email hardcoded "the trial leg failed" regardless of which leg actually threw, self-contradicting its own error message on an error_log-leg failure — `RetentionPurgePartialError` now carries a `leg: 'trial' | 'errorLog'` field both composition roots branch on, with the missing symmetric test added. #65/WP25: insert-only `error_log` written fail-OPEN (the deliberate reverse of R8's fail-closed audit store — a broken logger never breaks the product path or masks the original error) from the chat actions' outermost catches, the Stripe webhook, the auth callback and the health route; structurally NO personal data (never question/answer text — reference by request id); 90-day DELETE via the retention job's third leg. #114 option (b): `/api/health` (public exact-match, force-dynamic) runs the signed-in dashboard's REAL reads (balance/history incl. the session-27 `pending_table_requests` join, pricing, signup-grant, flag-mirrored websearch/workspace probes) against the nil-uuid synthetic user; the CI post-deploy smoke curls it WITHOUT following redirects (a proxy regression must fail, not be masked by /login's 200). Accepted residual: auth-LAYER breakage still invisible ([#114](open-questions.md)) | [open-questions #65+#114](open-questions.md), WP25 in [08-build-plan](08-build-plan.md) |
| On-demand table eviction/TTL, hermetic half (#110, `src/ingestion/eviction.ts`, migration 025) | **BUILT hermetic session 66 (2026-08-27, autonomous); MERGED session 67 (2026-08-28, PR #111, `18134aa`) — manual/supervised `tables:evict --apply` only, no cron, no live apply yet.** Per-table eviction under a `select ... for update` lock on `cbs_tables`, guarded against an active `pending_table_requests` job, R8-safe (audit/ledger/pending rows structurally untouched — verified end-to-end against a real onboarded table, pre/post snapshots byte-identical). **Two residuals a session-67 adversarial review found, NOT merge-blockers (nothing live yet) but MUST close before any live/automated `--apply`:** [#195](open-questions.md) — the disclosed cost ("+1 DB round-trip per served turn") is understated; the real `respond.ts` path touches `touchLastQueriedAt` ~4-6x per turn via follow-up-suggestion and disambiguation probes, and those same probes also keep a table artificially "warm" even when never actually served — undermining the eviction premise; [#196](open-questions.md) — a concurrent eviction transaction can serve a live query a false "no data" refusal (the guard protects against an active onboarding job, not an in-flight read). **Both ✅ FIXED (session 72, 2026-09-03, branch `fix/195-196-eviction-probe-touch`; round 2 session 73 `ebd341f`; PR #121 MERGED + LIVE 2026-09-03, session 75, owner-approved in chat, squash `527ef2e`):** the servability-probe primitive (`echoServability`) now runs every dry-run with `probe: true`, and `runQuery` skips the `last_queried_at` bump entirely on a probe — `last_queried_at` now means "last SERVED/DELIVERED read", never "last touched by any internal machinery" (closes #195); the bump also moved to directly after the observations fetch, so a live read no longer waits behind an eviction's row lock, and a coordinate found missing is re-checked against `cbs_tables` before diagnosis — an eviction racing a read now yields an honest `table_not_registered` refusal instead of a false `no_data`/`not_published` (closes #196). See ADR [029](decisions/029-follow-up-suggestion-chips.md)'s as-built note for the mechanism and the re-measured statement counts. **Round 2 (session 73) as merged:** the bump takes the row `for no key update skip locked` (a locked, eviction-eligible row is skipped, never waited on), the observations fetch LEFT-JOINs period labels and retained-batch dates in one snapshot, the registration check runs LAST on the one `diagnoseMissing` branch an evicted table can reach, and the refusal is its own kind `table_evicted` → reason `evicted` (honest Dutch copy, never the owner alert). The "MUST close before" gate this row pointed at is CLOSED for these two residuals; what still stands before any live/automated `--apply` is the structural follow-up in [#196](open-questions.md): eviction must YIELD to in-flight reads (`resolveIntent`'s earlier reads race the same eviction). **PR #128 (session 76, 2026-09-04, branch `fix/196-eviction-resolve-race`, head `ef4e35a`) closes this**: `resolveIntent`'s registry reads now take a per-table `pg_advisory_xact_lock_shared`, matched by an `EXCLUSIVE` lock in eviction's transaction — eviction yields to in-flight reads by construction, no DDL. HIGH review found 3 findings, all fixed (a documented lock-coupling trade-off with `pipeline.ts`'s `--rebaseline` EXCLUSIVE lock, corrected query-count tripwire numbers, an added pinning test). OPEN, gate-green, not yet merged — per #118(b) an autonomous session cannot merge core-product code | [open-questions #110](open-questions.md), [#195](open-questions.md), [#196](open-questions.md) |
| `src/threads/` + workspace UI (WP135) | Persisted conversation threads (`chat_threads`, migration 019 — timestamps only, NO text columns: titles derive read-time from audit rows so #14/#120 redaction covers the sidebar for free) + deterministic envelope replay/context-rebuild for resume, and the chat workspace (sidebar, right-pane visual dock, site shell w/ logout). **Built session 41 (2026-07-12); **LIVE in production since the supervised go-live (session 42, 2026-07-13 — as-executed record in RUNBOOK § "WP135 chat workspace")**; web section never docks (ADR 032)** | [033](decisions/033-chat-workspace-redesign.md), [021](decisions/021-conversation-memory-structured-context.md) |
| Supabase Auth (magic link + Google SSO) | The identity seam the credit ledger keys on (`auth.users.id`); magic link since WP13; **"Doorgaan met Google" added by WP28 (2026-07-10)** — Supabase's built-in provider, server-initiated, reusing the SAME provider-agnostic `/auth/callback` + proxy allowlist (zero changes there); one verified e-mail = one account = one signup grant (**VERIFIED empirically on live production, 2026-07-10** — same-e-mail Google login linked to the existing user with no duplicate grant, fresh e-mail minted one user + one grant; ADR 028 D2 as-built, [#122](open-questions.md) resolved) | [006](decisions/006-auth-billing-seams.md), [028](decisions/028-google-sso.md) |
| Chart spec + dumb renderer | "Dom en voorspelbaar" viz layer; the seam for every future export format | [007](decisions/007-chart-spec-rendering.md) |
| Curated homepage discovery charts (`src/chart/curated.ts`) | **BUILT (session 52, 2026-07-17); extended session 66, 2026-08-27 (#170(4), ADR 035 D5).** "Ontdek Nederland in grafieken" on the public landing: **5** canonical-key chart definitions (consumentenvertrouwen, bbp-groei yoy, inflatie, gemiddelde verkoopprijs, **werkloosheid** — added session 66) built through the REAL pipeline minus the LLM parser — `freshestForCanonical` anchor → hand-authored `StructuredIntent` window → `runQuery` → `buildChartSpec` — rendered by the product's own ChartView. Zero LLM, zero money path, no R8 audit rows (answer-scoped, ADR 035 D4). Web side: 30-min in-process TTL cache + stale-over-nothing + per-chart skip (`web/lib/ontdek.ts`) — the public landing never breaks on a data problem. Gate: `tests/chart/curated.test.ts` pins zero skips + windows + R4/R11 against the fixtures. **Session 66 additions:** curated event annotations (`src/chart/annotations.ts`, optional `ChartSpec.annotations`, rendered in `render.ts` + `chart.tsx`) and a narrow definition toggle (`CuratedChartDefinition.alternateReading`, only on `werkloosheid` — seizoengecorrigeerd ↔ ongecorrigeerd, `web/components/chart-toggle.tsx`) — both additive/optional, R6 unaffected | [035](decisions/035-homepage-discovery-charts.md) |
| Visibility smalls 1+2 (`web/components/source-badge.tsx`, `src/registry/coverage.ts` + `web/app/llms.txt/route.ts`) | **✅ LIVE (2026-07-18: built autonomous on PR #57, owner-present merge `12381e3`, deploy verified — /llms.txt HTTP 200 on prod, badges on all four Ontdek charts).** #170(1) source badge: table id + MEASURED last-sync date + StatLine deep link on every answer chip and every ChartView render (Ontdek included) — envelope/registry data only, zero prompt bytes, no cadence promises. #170(2) honest `/llms.txt`: coverage generated live from `cbs_tables`+`canonical_measures` (quarantined tables excluded, honest NB), public-claim wording exact, ontdek-style fail-safe (TTL cache, stale-over-nothing, 503 only when never built), proxy allowlist entry + post-deploy CI smoke. **Item (4) done session 66 (see the curated-charts row above); item (3) — chart download as PNG or SVG with the attribution baked into the file — pulled forward by the owner and shipped session 69 (2026-09-02, `web/components/chart-download.tsx`, `4d7ac2d`)** | [open-questions #170](open-questions.md), sketch: [session-briefs/2026-07-18-parked-ideas-architecture-sketches.md](session-briefs/2026-07-18-parked-ideas-architecture-sketches.md) |
| Chart UX step 1 — numbers + accessibility baseline (`web/components/chart.tsx`, `chart-download.tsx`, `chart-toggle.tsx`, `app/globals.css`) | **BUILT session 69 (2026-09-02), [#197](open-questions.md) step 1 of the chart-UX proposal.** Web renderer only — spec, builder, SVG renderer and stored audit rows untouched. Y-axis ticks at the plotted min/max, an end-of-line "periode: waarde" label per series, a value label per bar (≤ 15 bars), all `formattedValue` strings bound via `data-label-for` (which points get a label is a count-based presentation rule; what they say is never computed client-side). Colour-blind-safe series palette (`--series-1..4`, docs/12-huisstijl.md exception) with dash patterns per series, hollow/hatched provisional markers and a "○ = voorlopig cijfer" key. Accessible name + keyboard hint on the svg, the tooltip as a polite live region (Recharts' accessibility layer already walks points with arrow keys), tap-to-pin tooltips on touch-only devices, `touch-pan-y`, ≥ 24 px targets. Download menu got real menu-button semantics (Escape/outside-click close, focus management, `role="alert"` failure) and now inlines COMPUTED paint into the exported SVG/PNG — `var(--token)` strokes previously serialized verbatim, which no standalone file can resolve. Toggle is a `radiogroup`. Web renderer now refuses non-v1 specs like `render.ts` does. Tests render the real svg in jsdom via `initialDimension`. **Step 2 (same session): a Grafiek/Tabel switch on every ChartView** — `tableModel` is a second dumb renderer over the same spec (period × series; comparisons one row per region under their period; null cells `— (CBS reason)`; every cell bound via `data-label-for`); comparisons with > 15 series open on the table; image download offered in chart view only | ADR [014](decisions/014-chart-spec-v1-and-renderer.md) as-built notes; proposal: [session-briefs/2026-09-02-session-69-chart-ux-research.md](session-briefs/2026-09-02-session-69-chart-ux-research.md) |
| System map page (`/systeemoverzicht`, `web/app/systeemoverzicht/`) | **✅ LIVE, built on direct owner request (not a queued WP), spanning sessions 2026-08-28 → 2026-08-31 (commits `0fbd37a`, `d328213`, `f2b3975`).** A public, noindexed architecture reference page mirroring the equivalent page on the owner's other project: the big-picture system diagram (`web/components/system-map-diagram.tsx`, hand-drawn inline SVG), the 8-step journey of one question, an allowed/never AI-scope list, every external service with cost/status, scheduled automations, and what's built-but-flag-off — reachable via a gear icon in `SiteFooter`. Static content only, no DB reads, no auth; added to the proxy's exact-path allowlist (`isPublicPath`, pinned in `proxy.test.ts`) since it would otherwise redirect to `/login` like any unlisted route. **Bilingual (added `d328213`): an EN/NL toggle, English canonical/default, Dutch second** — the page (`page.tsx`) stays a Server Component only to export `metadata` (English, static); the actual bilingual content dictionary + the toggle's `useState` live in a Client Component (`system-map-content.tsx`), since `metadata` can't be exported from one. Choice persists per-viewer in `localStorage` only; `document.documentElement.lang` follows the active language while on the page and resets to the site's Dutch default on navigating away. **Every status label is a hand-written constant, NOT read live** from flags or the database — whoever ships a new external service or flips a flag must update this file by hand in the same change, same convention as the sibling project's own system map | — |
| Anonymous trial pot (`src/billing/trial-pot.ts` + `web/app/trial-actions.ts`) | **LIVE (session 52, 2026-07-17, supervised go-live).** 2 free questions for anonymous visitors on `/`: deterministic QUESTIONS pot (atomic check-before-serve under a global advisory lock, idempotent takes, refund only on a thrown pipeline error), per-visitor cookie budget (2) + per-ip-hash backstop (5/day, HMAC — raw IPs never stored), ALL trial LLM calls (incl. the #144 checker) on a SEPARATE hard-capped Anthropic key via the `AnthropicLlmClient(sdk)` seam — zero prompt bytes, main ledger untouched. R8 rows: `source_tag 'anonymous_trial'` + user_id NULL; since #181 their CONTENT is redacted after 90 days — the same window as the trial bookkeeping DELETE, one shared constant (both in `gdpr:purge`; account rows keep 2 years). v1: no clarification-reply round; every served response consumes a question. Empty pot/dormant ⇒ honest login degrade; refill re-opens without deploy (since #186, to within a 20 s in-process pot-read cache — pot only, never the per-visitor count). Landing gate also reports the per-IP backstop at render time (#184), both counts in one round trip ⇒ one query per steady-state anonymous render (two on a pot refresh, ≤1 per TTL per instance). Both anonymous reads are bounded at 5 s (#190(b), `web/lib/deadline.ts`) so a saturated pool degrades to the login nudge instead of hanging — that frees the visitor, not the pooler session | [036](decisions/036-anonymous-trial-pot.md) |
| Follow-up suggestion chips (`src/answer/respond/suggestions.ts`) | **BUILT (WP29, 2026-07-11).** Four deterministic generators over the answered intent + registry (adjacent period, trend window, region variant, same-table topic) — zero LLM, copy is a templated QUESTION over labels, never a number. Every chip passes the `echoServability` dry-run in the same request before it may render (the R7-options rule; unservable → silently dropped, zero survivors → no chips). Rides `AnswerResponse.suggestions` (structural, like `chart` — the R8 `text` is byte-untouched); v1 click FILLS the chat input (#75 convention, no new charged entry point), v2 was designed as a handler swap to WP26 Mechanism A — **that swap never happened and cannot: WP26 shipped take-path A2, so v1 and v2 are the SAME handler** (ADR 029 D3 as-built, session 56). **#134(a)/#137 (session 43) + #134(b) (session 44): the SAME mechanism also runs on period-coverage REFUSALS** — one dry-run-gated retry chip on `freshness` / period-axis `outside_loaded_slice` / too-old `not_published` pointing at a working period (the single floor period, or — #137 — the clamped WORKING sub-range for a range ask), on a structural `RefusalResponse.suggestions` field with the refusal `text` byte-untouched; rendered on the live turn AND on WP135 thread-resume. #134(b) adds the too-old `not_published` case (earliest-served floor computed in `run.ts`; a mid-gap stays prose). **#197 step 3 (session 70, 2026-09-02, branch `feat/197-3-comparison-chips`; MERGED + LIVE 2026-09-03, session 71, squash `83f790e`, after the WP26 smoke test passed in production): two COMPARISON generators — the answered regions + the national row / the country + the G4, and the same period a year earlier as the `difference` derivation — placed ahead of the region variant (which is skipped once a comparison surfaced). Their chips are the first taken WITHOUT an LLM re-parse: each rides a `ClickOption` on a present-only `AnswerResponse.pending` (the WP26c chip-carrier shape), and a click runs a NEW validated comparison through the `templateOnly` take-path at the normal reply price. Only with `CLARIFY_CLICK_ENABLED` on; off is byte-identical to before. Carrier-bound labels are dropped on thread resume (no pending is restored there). #73 v2 (session 72, 2026-09-03, autonomous; review round 2 by the parallel cloud session 74; PR #122 MERGED + LIVE 2026-09-03, session 75, owner-approved in chat, squash `4fd6ea5`): the carrier is now GENERAL — every takeable follow-up chip (adjacent period, trend, region variant, same topic) rides it and a click on ANY chip is the zero-LLM take (a new validated result, a real audit row, 20 credits); a chip the click-time schema rejects (an onboarded key) stays a fill-the-input label; on thread resume the question-shaped chips still replay as fill-the-input (their `ClickOption.questionShaped` bit) and only the comparison labels are dropped; flag off byte-identical; dry-run count per turn unchanged** | [029](decisions/029-follow-up-suggestion-chips.md) |
| Unit-expansion derivation + display splice (`deriveUnitExpansion`, `src/answer/compose/expand.ts`) | **LIVE (#125a, session 36, 2026-07-11; PR #25 merged + deployed).** Pure numeric factor units (`x 1 000`) pre-register the exact expanded figure as a `unit_expansion` DerivationRecord (integer-scaled arithmetic, never float; refusal = fail-open) and a deterministic post-validation splice shows it alongside CBS's verbatim notation — `390,2 x 1000 (= 390.200)`. The LLM never sees the record (payload filter keeps every fixture byte-identical) and never renders it; the spliced body is what R8 stores; old audit rows are structurally out of reach | [031](decisions/031-unit-expansion-derivation.md) |
| Alternate-reading disclosure (#39, `buildAlternatesLine`, `src/answer/compose/format.ts`) | **LIVE (owner policy 2026-07-04; BUILT + MERGED session 66→67, PR #103, `d7f0d20`, 2026-08-28) — no feature flag, unconditional default behavior.** When a canonical measure resolves to a registry-recorded default that has documented alternates, a deterministic disclosure line ("Er is ook een andere lezing beschikbaar: …") is appended to the answer — built entirely from `canonical_measures.alternates` (already in the registry, no new migration), never the LLM. Threaded through `ResolvedQuery → Attribution → ComposedAnswer.alternatesLine` (present-only, nullable, docs/13), R8 reconstruction, the envelope-key manifest, thread replay, and `chat.tsx` rendering. Registry-internal "eigen key: …" cross-references are stripped for display (removal only, tested per label). **Session 72 update (2026-09-03; PR #123 MERGED + LIVE 2026-09-03, session 75, owner-approved in chat, squash `ddca024`):** the clickable "waarom dit antwoord" info-icon affordance the scope note below used to flag as unbuilt is now the source drill-through cluster's Depth 1 — see the new row below | [open-questions #39](open-questions.md) |
| Source drill-through cluster — "Bewijs dit cijfer" (#70/#79/#89/#90-deep, `web/lib/answer-proof.ts`, `web/components/answer-proof.tsx`) | **BUILT session 72, 2026-09-03 (autonomous); review round 2 by the parallel cloud session 74 (`92d4db7`: a plural-honest "geen bewerking" step, `marked` on the same R5 rule as compose/citation, unknown-kind belts, `memo`); PR #123 MERGED + LIVE 2026-09-03, session 75, owner-approved in chat, squash `ddca024`.** One client-side disclosure panel over the SAME validated envelope the chat already holds, collapsed by default under the answer, three depths stacked behind one trigger: Depth 1 "Waarom dit antwoord" (chosen `definitionLabel`/`periodSemantics` + not-chosen `Attribution.alternates`, upgrading the existing plain-text `alternatesLine` above without duplicating it), Depth 2 "De gebruikte cellen" (the full `ResultCell` table — coordinates, sync date, batch id, CBS status), Depth 3 "Stap voor stap" (`result.derivations[]` as a followable Dutch step list — difference/max/direction/unit_expansion; `first_last` skipped). Ids/codes behind one "Technische details" toggle (D2 of the design brief). `buildAnswerProof` is a pure leaf built ONCE at receive time (`chat.tsx`) and at replay time (`replay-assemble.ts`) over the same envelope — parity by construction (ADR 033 ⟨A3⟩), exactly the `buildCitation`/`buildAnswerCsv`/`statCardData` pattern. No arithmetic (R1): every digit is a shared formatter over a stored cell/derivation value, pinned by a token-scan test. No backend/pipeline change, zero prompt bytes, zero DDL, no flag — purely additive over data the client already holds. Chat answers only (live turn + thread resume); the dashboard history read-model has no proof panel yet (tracked separately) | [session-briefs/2026-09-03-source-drill-through-design.md](session-briefs/2026-09-03-source-drill-through-design.md), [open-questions #70](open-questions.md)/[#79](open-questions.md)/[#89](open-questions.md) |
| Source registry (`src/sources/registry.ts`) | **LIVE (WP30a, session 36, 2026-07-11; PR #26 merged + deployed).** The single code-level authority for source identity/display: name, attribution label, license, deep link, per-status provisional wording (map), null-reason texts, catalog-lifecycle 'current' set (A6, field-only). `Attribution.source` (additive) + the A1 rule — absent source (every pre-WP30a stored row) resolves to 'cbs', keeping R8 reconstruction byte-identical forever. Eight display sites consolidated; migration 016 = FILE, apply supervised (RUNBOOK) | [030](decisions/030-multi-source-architecture.md) |
| Adapter conformance harness (`src/sources/conformance.ts`) + [how-to-add-a-source guide](how-to-add-a-source.md) | **BUILT (WP30b, session 37, 2026-07-11).** The executable done-definition for adding a source (D6 as amended): six check families over per-source fixture manifests — registry coherence, real-parse-code replay + the D4 id discipline, period-grammar round-trip (`encodePeriodCode`), null-reason/status completeness (R11), catalog-lifecycle completeness (A6), the five ingestion validators. CBS passes as the positive control; a 23-case suite proves the harness can FAIL. Rode along: the provisional rule + finder 'current' partition are now registry-driven (`definitiveStatuses`, `currentCatalogStatuses` — byte-identical for CBS, pinned), `adapterFor` routing seam (CLIs only; cron route byte-untouched until WP30c) | [030](decisions/030-multi-source-architecture.md) |
| Answer feedback (`answer_feedback`, `src/answer/audit/feedback.ts`, `web/components/feedback-buttons.tsx`) | **BUILT (WP128/#128, session 37-continued, 2026-07-12).** 👍/👎 on every chat answer + an inline free-text panel on 👎 — the raw signal store the WP26 answer-quality lane reads (👎 rows = the experience-audit shortlist). WRITE-ONLY pointer at the audit row (R8 untouched; the anchor `auditId` rides the action result OUTSIDE the stored envelope); ownership+kind+source_tag guard inside the insert statement; fail-soft everywhere (free — never a billing call, never breaks an answer); GDPR paths hard-delete feedback in the same transaction as the redaction. Migration 017 = FILE until the supervised window (applies together with 016) | frozen brief: [session-briefs/2026-07-12-wp128-brief.md](session-briefs/2026-07-12-wp128-brief.md), spec: [open-questions #128](open-questions.md) |
| Web-search augmentation channel (`src/websearch/`) + source-tags chips | **LIVE (WP129+130, session 40, 2026-07-12: merged + supervised go-live run same day, smoke tests ledger-verified both modes).** The #129 chips ("CBS data" rendered from the source registry + "Internet") make the per-question source selection a STRUCTURAL pipeline input that rides the audit envelope — R8 reconstructs what was searched. The #130 channel is a NEW self-contained LLM call (Anthropic native web_search — the basic `web_search_20250305` variant after the measured go-live correction (ADR 032): the 20260209 filtering variant returns citation-less blocks; `WEBSEARCH_MODEL`/`WEBSEARCH_TOOL_TYPE` constants, max 3 searches, NL-localized) whose ≤4 cited findings render in a visually separated block — "Van het web (niet door checkdecijfers geverifieerd)" — below the validated body or refusal, domain-name-only links. Web findings never enter the validated body, validators, attribution, charts, conversation context, or the benchmark (web-OFF by construction). +10-credit add-on as its own `websearch_cost` ledger debit, auto-refunded whenever no section is delivered (fail-soft — the CBS answer always ships); the separation IS the honesty model | [032](decisions/032-websearch-augmentation.md) |
| UI foundation (Tailwind, shadcn/ui, Recharts, zod as shared schema layer) | Recorded defaults so build sessions don't re-litigate; binds the two-surface budget rule and the no-token-streaming rule | [008](decisions/008-ui-foundation.md) |
| **Absent on purpose:** Redis, vector DB, Python service, queue | Each has a named trigger instead of a standing cost | [005](decisions/005-caching-strategy.md), [002](decisions/002-postgres-system-of-record.md), [001](decisions/001-single-app-vs-split.md) |

Components from the notes **kept as input but not adopted (yet)**: separate Python/FastAPI backend (migration target with triggers, ADR 001), Supabase-as-platform (Postgres hosting candidate only; no feature may depend on Supabase-specific behavior), Upstash Redis (trigger-based, ADR 005), pgvector (trigger-based, ADR 002), Stripe+iDEAL (Phase 2, ADR 006), Vercel AI SDK (fine as a chat-UI library at implementation time; the pipeline does not depend on it).

## Future-build seams (Stefan's Q1 requirement: don't lose the ideas)

Each parked feature has a named seam it will attach to — future sessions should extend these, not invent new paths:

| Future feature (roadmap) | Seam that already anticipates it |
|---|---|
| Visualisatie Studio (social exports, sizes, interactive, huisstijl) | Chart spec: new renderers over the same spec (ADR 007) |
| Shareable answer pages + OpenGraph (programmatic SEO) | Audit record = permanent answer snapshot to render a page from; static-image renderer over chart spec; the ingestion correction-diff log is the page-invalidation feed (correction policy: [open-questions #22](open-questions.md)) |
| Free browse layer (public data pages) | Pre-rendered — static generation with revalidation — from the same Postgres registry + observations; table-sync events trigger regeneration; charts reuse the spec + renderer (ADR 007); correction handling shares the page-invalidation feed (#22); **no LLM anywhere in this path** |
| User-facing audit trail ("Ironclad Audit Trail") + saved/pinned charts dashboard | `audit_answers` already stores question→plan→result IDs→numbers **and the emitted chart spec**; the history view is a read-only list over it, and "saving" a chart is just a pointer/flag on one row — a frozen snapshot by construction, since a stored row never changes ([05-data-rules.md](05-data-rules.md)). No new storage model needed ([open-questions #60](open-questions.md)) |
| Scoop alerts | Ingestion batches record what changed per sync — an alert is a subscription over batch diffs; alert value is bounded by sync polling cadence (explicit Phase 3 design input), delivery rides the notification seam below |
| Notifications (alerts, onboarding "table ready", owner ops-alerts) | **Notification seam**: subscription/preferences table keyed to the identity seam; delivery via the transactional-email provider that Phase 1's magic-link auth already requires; owner alerting (batch failure, `needs_review`, missed sync, health check) is its first consumer |
| Newsroom licenses / huisstijl | Identity seam (ADR 006) + theme object in chart spec (ADR 007) |
| Answer-first defaults on a safelisted axis (WP26 B, ADR 024) | **✅ BUILT session 56, 2026-07-25 — LIVE behind `ANSWER_FIRST_ENABLED` (ON since 2026-09-03, session 71, owner go; the anonymous trial receives the same flag since the #175 fix of the same day).** `resolveIntent` pins the national row when no place is named and one exists for that measure (`regionDefaulted`); `resolvePeriod`'s `none` branch walks a bounded gap-free trend window (`periodDefaulted`). Both flags ride the ValidatedResult; `buildAssumptionLine` (format.ts) is the ONE builder compose.ts and audit/reconstruct.ts share, so the disclosure the user saw re-derives byte-identically at audit time (R8). Never widens WHICH axes may default — that safelist is code, not configuration. **#191 as-built (2026-08-07, session 61): the clarification REPLY turn now runs under the same flag.** It previously ran HALF of B — B-region (query layer) already reached replies via the `{...options}` spread, B-period (intent layer) did not — so flag-on a reply defaulted the region the user never named and refused over the period it was allowed to default. R7's third branch draws no first/reply distinction, so that was a conformance gap, not caution. |
| One-click resolution of a clarification (WP26 A, ADR 024, take-path A2) | **✅ BUILT session 56, 2026-07-25 — LIVE behind `CLARIFY_CLICK_ENABLED` (ON since 2026-09-02, proven 2026-09-03).** Options carry a dry-run-verified `ClickOption` (id/label/intent/impliedRecency) on the pending state; `respondToClarificationReply` takes a byte-matching reply deterministically with NO LLM call, composing via the ADR-013 template rung. No new entry point and no new UI surface — the labels ride the existing kind-agnostic `suggestions` chip field. Client-held payload re-validated fail-closed (`respond/validate-pending.ts`). **Flipped ON in production session 69 (2026-09-02, `83aa84c`); the owner.s chip-click smoke test PASSED 2026-09-03 (session 71: audit rows 261/262, `parse.model = deterministic/wp26-click-option`, 0 tokens, R8 clean). #197 step 3 (session 70) reuses the take-path for the comparison chips under an ANSWER: the `rescueOnly` chip-carrier pending is now minted on answers too, and `isRescuePending` accepts 1..4 label-bound chips (ADR 024 addendum).** |
| Trust boundary on the client-held pending (WP26, ADR 024 as-built addendum) | **✅ HARDENED session 57, 2026-07-25 (`29e9e8b`)** after a four-lens adversarial review. Confirmed sound: a forged `ClickOption` cannot make the product serve a number it would otherwise refuse — the take path re-runs the real query layer (quarantine gate included) and discards the offer-time dry run. Closed: `rescueOnly` now granted on the pending's SHAPE rather than the client's word (it was reachable with both flags off); chip labels bound to the offered `options[]`; `guardPending` bounds `axes` entries + validates `referenceDate` shape; the pending rebuilt from a key allowlist instead of a spread. Residuals recorded as #174-#178. | [024](decisions/024-answer-first-defaults-and-clickable-options.md) |
| Number-free phrasing via typed slots (#162, ADR-DRAFT slot-filling) | **Hermetic half BUILT session 66, 2026-08-27→28; MERGED session 67, 2026-08-28 (PR #113, `6fdae6f`) — DORMANT behind `SLOT_PHRASING_ENABLED` (unset in Vercel Production, verified; flag-off proven byte-identical by test).** Inverts the anti-fabrication risk model: the phrasing LLM writes the body with typed placeholders and zero digits (`src/answer/compose/slots.ts`), deterministic code fills them from the validated result through the same formatters the template rung already proves — R10/R11 become structural on this path, a fabricated digit becomes unrepresentable rather than caught. New first rung in the R3 ladder (`composeViaSlots`), falls to the unchanged template floor; R1/R8 store the raw placeholder body + slot map, present-only. **Merge note (session 67):** landing after #102 (options dedup) and #103 (#39 alternate-reading disclosure), both also touching `compose.ts`, needed a hand-resolved conflict plus two test-file fixes the review caught in advance — `tests/audit/envelope-key-manifest.test.ts`'s `ComposedAnswer` member count (15→16) and `tests/audit/slot-phrasing-r8.test.ts`'s `rebuildTexts()` helper (missing `alternatesLine` in its manual text reassembly); both fixed as part of the merge commit, full suites re-verified green (697 answer + 138 audit + 464 web tests). **Not done: the §6 A/B (blind pairwise phrasing-quality judge + owner read-back, ~€1-2 live spend) — no quality claim exists yet, and the ADR stays a draft until it runs.** | [session-briefs/2026-07-19-adr-draft-slot-filling.md](session-briefs/2026-07-19-adr-draft-slot-filling.md) |
| Per-process database connection ceiling | **✅ `max: 2` since session 57, 2026-07-25 (`e334590`)** — the free tier caps session-mode pooling at 15 clients project-wide and every process opens its own pool (#173). At 4, three busy processes could exhaust the project; at 2 it takes seven. Pinned by `tests/db/pool-config.test.ts`. `connectionTimeoutMillis` deliberately unset — see the counter-argument recorded on PR #60. | [002](decisions/002-postgres-system-of-record.md) |
| Drill-down buttons on answers | The audit record's stored query plan + registry dimension metadata: a button is a deterministic transform of the current plan (extend period, swap a dimension value — region or otherwise, group by) that re-enters the pipeline at the query step — no new components; shares its UI mechanism with clarification options (R7) |
| Demand-driven table onboarding (user-triggered fetch of not-yet-loaded tables) | **✅ BUILT + LIVE IN PRODUCTION (WP16, go-live session 28, 2026-07-06, ADRs [025](decisions/025-cbs-catalog-table-discovery.md)+[026](decisions/026-on-demand-fetch-job-architecture.md)).** Discovery half: `findTable` over the `cbs_catalog` mirror (sub-part 1). Fetch half: the async cron job (`src/ingestion/onboarding.ts`) — same `CbsSource` adapter + ingestion/validation pipeline, triggered by a user action (the kick, `web/lib/onboarding-kick.ts`) + a daily backstop cron; 100-credit debit + full refund on failure; delivery through the normal audited pipeline + Resend notify. Both paths verified live. Owner-flagged follow-ups: finder stock-vs-flow precision ([#111](open-questions.md) — ✅ CLOSED via WP27, sessions 31–33: the bijstand question answers live), onboarded-answer quality ([#115](open-questions.md) — ✅ done + live-verified session 29: CBS's real per-measure definition now renders as the "Definitie:" line, `canonical_measures.definition_text` / migration 014) |
| Enrichment sources (PDOK/Kadaster, RIVM, UWV, Waarstaatjegemeente, open.overheid.nl; added 2026-07-18: rijksfinancien.nl (also a [#123](open-questions.md) second-source candidate), DNB (long-term — API behind eHerkenning→KvK)) | Additional adapters beside `CbsSource` — but honestly: non-tabular sources (documents, geodata) need their **own** adapter interfaces and storage models (e.g. PostGIS, object store), designed at Phase 3 behind the same query-module boundary. `CbsSource` is the pattern, not the contract. Likely trigger for the Python split (ADR 001) |
| Credits/billing | Ledger + pricing-config + cost-estimation step (ADR 006) |

## Cost picture (indicative)

| Scale | Monthly cost | Dominated by |
|---|---|---|
| 0 users (Phase 0) | ~€0–10 | Free tiers; LLM only for dev + benchmark runs (cents/question) |
| ~100 active users | ~€25–75 | Managed Postgres paid tier; LLM at ~30 q/user/mo × ~€0.01–0.02 |
| ~1,000 active users | ~€200–600 | LLM tokens → triggers: prompt caching, answer cache (ADRs 004, 005) |

## GDPR / AVG — reserved section (activated in the phase that introduces accounts/payments)

Phase 0 processes no personal data by design: no accounts, no payment data, and benchmark questions are our own. From **Phase 1 (accounts)** and **Phase 2 (payments)** onward, three flows become personal-data processing and must be handled before launch of those phases — reserved here, deliberately not designed yet:

1. **Account data** (email, usage history) → privacy policy, lawful basis, retention schedule, export/delete capability.
2. **Payment data** → Stripe as processor (DPA with Stripe); we store references, never card/bank data.
3. **User questions sent to the LLM provider** — the sensitive one: journalists type investigative queries (the notes flag this explicitly). Requires: DPA with the LLM provider (Anthropic) covering no-training and retention terms; a decision on question-log retention and anonymization (identity seam keeps `user_id` separable from question text); disclosure in the privacy policy that questions transit an LLM provider.

Seams reserved now: nullable `user_id` on audit records (separable identity), pricing/billing isolated behind the ledger, question text stored in a small, known set of places so retention policy has one enforcement point. That set is now TWO write points: the audit record (`audit_answers`) and — since on-demand onboarding (WP16, migration 012) — `pending_table_requests.question_text`/`topic_term`/`failure_summary`, which deliberately re-stores the question so the background job can re-run it verbatim at delivery. Both are covered by the SINGLE retention seam (`src/answer/audit/retention.ts`) since #120 (2026-07-12): the self-service deletion and the age-based purge each redact the audit rows (`source_tag in ('user', 'onboarding_delivery', 'anonymous_trial')` — the #53 anonymous-trial answers joined the scope with migration 020; since #181 the purge runs TWO windows over that scope, 2 years for account rows and 90 days for anonymous-trial content, matching its bookkeeping table `trial_questions`, which gets its own 90-day DELETE sweep, ADR 036 D4) AND the pending-request free text in one transaction — so "one enforcement point" holds even though the text lives in two tables. Since #151 (2026-07-17) that pending-request redaction covers `question_text`/`topic_term`/`failure_summary`/`fit_note` (the fit-gate LLM note, migration 015) on every in-scope row, plus the topic-disclosing CBS table identity (`table_id`/`candidate_ids`/`resolved_table_id`) on TERMINAL rows only — a `pending`/`running` row's in-flight job still needs its table id (the documented in-flight residual). Forward-only: pre-#151-deploy rows carry the un-redacted columns until a purge/re-deletion sweeps them.

**Trigger to activate:** the Phase 1 go decision. Add "GDPR checklist complete" to the Phase 1 definition of done in [06-roadmap.md](06-roadmap.md).
