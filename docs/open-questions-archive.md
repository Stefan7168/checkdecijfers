# Open questions & assumptions — archive (terminally-closed rows)

> Moved out of [open-questions.md](open-questions.md) on 2026-07-12 (session 41) so a fresh session can read the live list in fewer calls.
> Rows below are **verbatim** — nothing summarized, nothing edited. Every row here is terminally CLOSED (done/built/shipped/rejected/decided
> with nothing left open at move time). **Numbering is global and never reused**; a `[#NN](open-questions.md)` link elsewhere may point to a row
> that now lives here — check this file when a number is missing from the live list. Rows are in numeric order.
> **Second move, 2026-09-03 (session 71):** rows #15, #16, #24, #35, #36, #121, #129, #130, #139, #142, #145, #155, #156, #165, #167, #168, #177, #179, #182, #183, #187, #191, #192, #193, #194 moved here after a full triage of the live list (every row read; a residual of any kind — a follow-up, an unbuilt part, an owner decision, a revisit trigger — kept the row live).

> **Third move, 2026-09-07 (session 87):** rows #137, #138, #147, #148, #149, #150, #151, #162, #189, #199, #203 moved here after confirming each is genuinely terminally closed (built/merged/live/decided, nothing left open), found during a docs-hygiene triage.

> **Fourth move, 2026-09-07 (session 87, same day as the third move above):** rows #3, #4, #39, #58, #65, #66, #70, #74, #79, #89, #98, #117, #119, #133, #143, #144, #154, #175, #180, #195, #204 moved here after a full re-triage of the ENTIRE live list (every row read, in 5 parallel chunks, cross-checked against git history) — the first full re-triage since session 71's, per that session's own recommendation.

| # | Area | Question / ambiguity | Assumption made (until answered) |
|---|---|---|---|
| 1 | Data | Which exact CBS tables (IDs) make up the Phase 0 set? Notes cite `85552NED` (solar), `83765NED`, `86103NED`, `70072NED`, `85458NED`, `03759NED` — some may be archived/redesigned by now. | **Resolved** (2026-07-02, implementation session): 8-table set validated against both live catalogs and adversarially re-verified — see [07-phase0-table-set.md](07-phase0-table-set.md). Of the cited IDs only `03759NED` made the set; `85552NED` doesn't exist (notes error). All 8 tables reachable via OData v4 — ADR 003's v3-fallback contingency not needed in Phase 0 |
| 3 | Pricing | Free allowance: one-time or monthly? Notes contradict (2 one-time credits vs. 2/month vs. 3/day vs. 5/day). | **Resolved** (Stefan, 2026-07-05): 5 free credits at signup, one-time, never refreshed — bounded abuse surface, no subscription-like mechanic, consistent with "credits never expire". Chat requires an account (#47); the separate anonymous-trial experiment stays tracked at [#53](open-questions.md). **Revised (Stefan, 2026-07-04): 100 free credits**, to hold the same value (5 free simple questions) at the new ×20 pricing scale from [#4](open-questions.md)/[#58](open-questions.md) — mechanism unchanged (one-time, never refreshed). Encoded in the WP13 brief ([08-build-plan.md](08-build-plan.md)) and migration [005](../migrations/005_credit_ledger.sql)'s `signup_grant_config` |
| 4 | Pricing | Heavy-analysis credit price: notes say 3, 3–5, and 5 in different places. | **Resolved for launch** (Stefan, 2026-07-05): action classes **1/3/5** credits (simple/analysis/heavy — in Phase 1 nearly everything is simple = 1); launch packs **€5 / 10 credits · €10 / 25 · €30 / 100** (€0.50→€0.30 per credit). **Superseded (Stefan, 2026-07-04):** widened to a **10–100 scale** so future small a-la-carte features (chart edits, exports) can later be priced in whole credits without rescaling everything again — **clarification 10 / simple 20 / analysis 60 / heavy 100** credits (a new fourth class; reasoning in [#58](open-questions.md)); launch packs' credits scaled ×20 at the same three €-price-points so the euro cost per question is unchanged: **€5 / 200 · €10 / 500 · €30 / 2,000** (still €0.50→€0.30 per simple question, depending on pack). **Fourth pack added same day (Stefan, 2026-07-04): €250 / 25,000 credits** (€0.01/credit) — the first three packs' price-per-credit already fell in a clean €0.005 step each tier (€0.025 → €0.020 → €0.015); this tier continues that step rather than picking an arbitrary discount, aimed at power-user/agency volume. Lives in the WP13 pricing-config seam — deliberately easy to change (ADR [006](decisions/006-auth-billing-seams.md)); these are starting values, not frozen law |
| 10 | Product | Interview Q7 (v1 must-haves from the feature pool) wasn't explicitly answered. | **Resolved** (Stefan, 2026-07-02): confirmed — none are v1 must-haves; all features preserved in the roadmap feature pool + architecture seams table |
| 15 | Execution | Stefan noted interest in build partner ship.studio. | Docs are written to be executable by any competent builder or AI session; partnership is a business decision outside doc scope |
| 16 | Competitive | Nederland in Beeld self-reports "82+", "90+", and "167" sources in different places — figure unreliable. | Treated as positioning color only; no doc claim depends on the number |
| 18 | Product | Does serving covered historical periods from a cadence-overdue table *with a staleness warning* satisfy principle 3 (c), or should it refuse outright? | **Resolved** (Stefan, 2026-07-02): warn, not refuse — warn-and-serve stands as written in [05-data-rules.md](05-data-rules.md) |
| 19 | Engineering | Intent-confidence threshold values and calibration procedure for R7 (clarify vs. answer). | **Resolved** (2026-07-03, WP6): `answerThreshold 0.9` / `runnerUpThreshold 0.35`, calibrated against a 45-case labelled set ([benchmark/intent-labelled-set.json](../benchmark/intent-labelled-set.json)) — measured: correct parses at 0.92–0.98, the model's shaky readings at 0.75–0.85; final state 45/45 with zero outcome flips over 3 repeats. Procedure + measured basis in ADR [012](decisions/012-intent-parsing-llm-harness.md); re-runnable via `npm run intent:eval` |
| 20 | Roadmap | Trojan-Horse demand-data reports and citation-funding/ads are marked "Recommended reject" — architect judgments, not interview decisions. | **Resolved** (Stefan, 2026-07-02): rejections confirmed; recorded in the business-model section of [01-product-vision.md](01-product-vision.md) |
| 24 | Product | Demand-driven onboarding: review throughput/SLA, and what users are told about wait times for "your table is being prepared". | Decided at Phase 2–3 design; the table onboarding rule ([05-data-rules.md](05-data-rules.md)) already bounds quality per table. **Revised (Stefan, 2026-07-04), per [#21](open-questions.md)'s new automated design:** on the high-confidence path there is no human queue to wait on at all — the bound is ingestion + verification run time (seconds to minutes for a small table; existing ingests already measure 4–6 min even for small tables, [#34](open-questions.md); large tables slower). On the low-confidence path there is no wait either — the honest multi-candidate answer ships immediately instead of the table being held back. "Your table is being prepared" messaging is now only realistically needed for genuinely large first-time ingests (multi-million-row tables) or v3-only tables needing the second adapter (ADR [003](decisions/003-cbs-access-layer.md)) — a narrower case than originally scoped here. **Wording + pricing decided (Stefan, 2026-07-04, session 18):** the message must never promise a specific time bound (it varies too much to be honest) — approved copy: *"Dat onderwerp staat nog niet in onze database. We vragen de cijfers nu automatisch op bij het CBS en controleren ze — meestal een kwestie van minuten. Je krijgt een e-mail zodra je vraag beantwoord kan worden. Heb je ondertussen nog een andere vraag?"* (names no fixed minute count on purpose). **This response costs credits — it is NOT free like an outright refusal** (owner decision, reversing the assumption floated mid-discussion that it should refund like a refusal): triggering real onboarding work is a rendered service, not a non-answer. **Resolved (Stefan, session 26, ADR [026](decisions/026-on-demand-fetch-job-architecture.md)): priced at 100 credits (the existing "heavy" tier — reuse, not a new tier).** Refund-on-verification-failure needs no new policy: `src/billing/gate.ts` already compensates in full on any refusal outcome or thrown exception (verified session 26) — a verification failure just routes through the existing refusal path. The e-mail notification uses the Resend API directly (the account already exists per RUNBOOK for the SMTP relay; this is a new direct send, not a reuse of the SMTP path). |
| 35 | Product | B6 ("Hoeveel woningen telde Nederland in 2024?") is ambiguous between the 1-januari-2024 stock (Beginstand voorraad) and the 31-december-2024 stock (Eindstand Voorraad) — both exist in `82235NED`, ~70k apart. | **Assumption, now live** (2026-07-03, benchmark-key freeze WP3; encoded in the registry WP4): pinned to Beginstand (1 januari), matching the "op 1 januari" snapshot convention already used for the population table in this same benchmark. Recorded in [benchmark/answer-key.json](../benchmark/answer-key.json) and as `canonical_measures.housing_stock_start_of_year` ([src/registry/defaults.ts](../src/registry/defaults.ts), ADR [010](decisions/010-registry-canonical-measures.md)), Eindstand kept as a visible alternate in both. Registry-internal variant choice (R7) — owner may still override; a one-row change + `npm run registry:apply` |
| 36 | Product | B9 ("Hoeveel faillissementen werden er in 2025 uitgesproken?") is ambiguous between "Totaal rechtsvormen" (4,105, includes personal bankruptcies) and "Bedrijven en instellingen" (3,226, businesses only) in `82242NED`. | **Assumption, now live** (2026-07-03, benchmark-key freeze WP3; encoded in the registry WP4): pinned to "Bedrijven en instellingen" per docs/07's note that press coverage usually cites this reading. Recorded in [benchmark/answer-key.json](../benchmark/answer-key.json) and as `canonical_measures.bankruptcies_businesses` ([src/registry/defaults.ts](../src/registry/defaults.ts), ADR [010](decisions/010-registry-canonical-measures.md)), Totaal kept as a visible alternate in both. Registry-internal variant choice (R7) — owner may still override; a one-row change + `npm run registry:apply` |
| 37 | Engineering | B20's freshness refusal: when the freshest *available* CPI month (currently 2026MM06, Voorlopig) and the freshest *Definitief* month (2026MM04) diverge, should the refusal offer the freshest available period regardless of status, or only the freshest Definitief one? | **Resolved** (2026-07-03, query WP5): freshest-available is the primary offer, carrying its CBS status so the answer layer can mark it per R11; freshest Definitief is exposed alongside as `freshestDefinitief` ([src/query/types.ts](../src/query/types.ts), `FreshnessInfo`). A freshness refusal offers **period + status only, never a value** — a refusal carries no numbers (principle c). Verified by the B20 test in [tests/query/benchmark-intents.test.ts](../tests/query/benchmark-intents.test.ts) |
| 39 | Product | When we resolve a registry-internal variant to its canonical default (R7), should the answer also proactively surface that a *non-chosen* alternate reading exists (e.g. "woningvoorraad per 1 januari — een cijfer per 31 december bestaat ook"), or only state the reading we used? Relates to [#35](open-questions.md) (housing 1-jan vs 31-dec) and [#36](open-questions.md) (bankruptcies businesses vs total). | **Resolved, reopened and reversed** (Stefan, 2026-07-04 — supersedes the 2026-07-03 "maybe later" deferral): **yes, as a standing policy** — never silently pick a definition when a registry-internal alternate exists; state both values with their definition labels. Applies generally (any future `canonical_measures` entry with alternates), not just B6/B9. Matches the transparency stance already taken for future table onboarding ([#21](open-questions.md)). **Not yet built**: the alternates exist in the registry (`canonical_measures[].alternates`, [src/registry/defaults.ts](../src/registry/defaults.ts)) but aren't threaded into `Attribution` in the query layer yet — needs that plumbing + a WP7 copy decision on phrasing. Small, well-scoped follow-up, queued not built. **✅ RESOLVED — BUILT (2026-08-27, session 66, autonomous): the plumbing + plain-text disclosure.** `canonical_measures.alternates` now rides the resolved query (`ResolvedQuery.alternates`, read from the SAME registry row as `definitionLabel`) onto `Attribution.alternates` (present-only, docs/13 discipline), and a new deterministic builder `buildAlternatesLine` ([src/answer/compose/format.ts](../src/answer/compose/format.ts)) renders it as its own structural line — "Er is ook een andere lezing beschikbaar: stand per 31 december (Eindstand Voorraad)." — outside the LLM-scanned body (principle a / R2 / R3), placed after the Definitie line, re-derived byte-identically at audit time (R8, `ComposedAnswer.alternatesLine` in the envelope-key manifest as `rederived`), replayed in threads, and rendered in the chat. Registry-internal "eigen key: …" cross-references are deterministically STRIPPED for display (removal only, never rewording — tested per real label in tests/answer/compose-alternates.test.ts). **Scope note: this is the PLUMBING + plain-text disclosure ONLY — the clickable "waarom dit antwoord" info-icon affordance is [#89](open-questions.md), deliberately NOT built here; it now has the `Attribution.alternates` data to build from.** **✅ PR #103 merged into `main`, session 67 (2026-08-28, `d7f0d20`).** |
| 40 | Product | Intent-parser period policy (WP6): a question with an explicit present/recency signal ("Hoe hoog is de werkloosheid nu?") answers with the freshest **published** period — stated explicitly in the answer — instead of asking "welke periode?". Past tense without a named period, and change questions without a baseline, DO clarify. Is answer-with-stated-period the right default, or should present-tense questions also clarify? | **Confirmed** (Stefan, 2026-07-04): keep the current behavior — present-tense questions answer with the freshest period stated explicitly, rather than always clarifying first. No prompt/threshold change |
| 44 | Ops | Benchmark runs write real rows into the live `audit_answers` table (ADR [017](decisions/017-live-benchmark-run.md), decision 3) with `user_id = null` — once real user traffic exists, nothing distinguishes benchmark rows from user rows in reporting or retention tooling. Tag benchmark runs? | **Resolved** (Stefan, 2026-07-05): add a source-tag column (benchmark / validation / user) **in WP13's migration** — that WP already ships a migration and is the moment real users arrive. Existing rows 1–76 (benchmark + validation runs; 74–76 are WP14's live spot-check, 2026-07-04) get back-tagged. In the WP13 brief ([08-build-plan.md](08-build-plan.md)) |
| 47 | Ops | Abuse protection for the public chat: no login, no rate limiting, and every question costs real money (~€0.02). The €25/mo spend cap bounds the worst case, but also means one script could exhaust the month's budget in an afternoon and silently stop the product for everyone, including the owner. | **Resolved** (Stefan, 2026-07-04): the integrated chat (going on a real website page) requires an account before any question — no anonymous free-question trial there, closing the scripted-abuse vector structurally. Reaffirms and hardens [#3](open-questions.md)'s existing "chat requires an account" assumption. Prompted a roadmap reprioritization ([06-roadmap.md](06-roadmap.md)): billing/credits (previously Phase 2) is pulled forward alongside auth, since a real public page is going live imminently rather than staying a recruited-only beta. A *separate* unmetered anonymous-trial idea is tracked apart from this at [#53](open-questions.md) — it must not reopen this abuse vector on the main product |
| 48 | Product | Y-axis honesty policy for charts: a bar chart whose axis doesn't start at zero can make a small difference look dramatic. Both renderers currently just fit the data range to the values — nobody has decided a policy. | **Resolved + implemented** (policy Stefan 2026-07-04; both renderers verified/implemented 2026-07-04): bar charts must start their value axis at zero (true proportion); line charts may zoom to show real movement, since position rather than area carries the meaning there. ⚠ The problem statement above was inaccurate: the deterministic **SVG renderer already implemented this at WP8** — `makeScale` in [src/chart/render.ts](../src/chart/render.ts) floors bar domains at 0 with the exact rationale ("Bars encode length … a non-zero baseline visually lies about ratios") and lets lines zoom (min−pad/max+pad); only the **Recharts wrapper** (the sole live user-facing path — the SVG renderer is the deferred Phase-2 static-image path, not mounted in the chat UI) lacked it. Now implemented there too: `yAxisDomain(kind)` in [web/components/chart.tsx](../web/components/chart.tsx) returns `[0, 'auto']` for bar / `['auto', 'auto']` for line, wired into the `YAxis` and unit-test-pinned (bar→0, line→zoom). What genuinely remains for the chart **styling** pass (#29) is visual polish (tick presentation, colours, spacing), not the honesty policy, which is done |
| 58 | Pricing | WP13's brief originally assumed clarification rounds (doorvragen) cost 0 credits, same as outright refusals. But a free clarification round still costs Stefan real Anthropic API money, and nothing stopped a user fishing across unrelated topics for free (ask about an unloaded topic, get a free doorvraag, try a different topic, repeat) without ever spending a credit. | **Resolved** (Stefan, 2026-07-04): **only outright refusals stay free** (full compensation back to 0 — there's nothing to explore, the system just doesn't have the topic). **A clarification round costs its own flat, small price**: a new `clarification` action class in the pricing config. First drafted same-session at 1 credit (equal to `simple`, so the debit needed no compensation at all); **revised same day** once the whole scale widened ×20 for headroom ([#4](open-questions.md)) — final launch value **10 credits**, deliberately *half* of `simple`'s 20, so a `simple`-assumed debit now genuinely compensates 10 credits back when the outcome turns out to be a clarification (no longer a same-value no-op). Deliberately kept as a small, fixed set of classes (now spanning 10–100) rather than a continuously-computed complexity score: there is no complexity classifier yet (analysis/heavy are still inert placeholder rows), so finer-than-four granularity today would just be arbitrary numbers; the schema is a plain `credits integer` column per class, so a finer scale can be added later without a migration once a real classifier exists. Encoded in the WP13 brief ([08-build-plan.md](08-build-plan.md)) and migrations [005](../migrations/005_credit_ledger.sql)/[006](../migrations/006_pricing_config.sql). Structural invariant: the `clarification` price must never exceed the cheapest answer-class price (10 ≤ 20), or a partial-compensation row would need a non-positive delta, which the ledger's CHECK constraint forbids by design |
| 65 | Ops/Engineering | Live testing (session 18, 2026-07-04): a real production error left **zero trace** — no `audit_answers` row, no ledger debit (it failed before the billing gate ran), and the server-side stack trace itself was already gone from `vercel logs` by the time the session checked minutes later (short retention on the current Vercel tier). Root cause unrecoverable. Should there be durable, persistent error logging beyond Vercel's short-lived logs? | **Decided (Stefan, 2026-07-04, session 18): yes, build this.** **Execute-ready brief written (2026-07-05 overnight, session 22): WP25 in [08-build-plan.md](08-build-plan.md)** — Postgres `error_log` table (insert-only, fail-OPEN logging), catch-site writes, owner-only access; needs one live migration → next supervised session. Original scoping notes: where errors persist (a new Postgres table alongside `audit_answers`, vs. an external error-tracking service), what triggers a write (at minimum, the catch blocks in `askQuestion`/`replyToClarification`, `web/app/actions.ts`), and access/retention (owner-only). Standing interim practice already recorded in [lessons-learned.md](lessons-learned.md): check `vercel logs` immediately in the same turn a live test error is observed, don't defer. **✅ RESOLVED — BUILT (2026-08-27, session 66, autonomous, per the WP25 brief): migration `024_error_log.sql` (FILE-ONLY) + `src/db/error-log.ts` (fail-open `logError`, 90-day retention adopted from the brief's suggested default) + catch-site writes in `askQuestion`/`replyToClarification`, the Stripe webhook, the auth callback and the new #114 health route (`web/lib/error-report.ts`) + a third leg in `runRetentionPurge` (skips `table-absent` honestly pre-apply). Two brief deviations, both documented in the migration header: `context` never holds question text even optionally (hardened from "not by default" — the table has no redaction machinery so it must never need it), and non-uuid request ids are preserved in `context` instead of failing the uuid column. Hermetic pins: `tests/db/migration-024.test.ts`, `tests/db/error-log.test.ts`, `web/app/actions-errorlog.test.ts`, retention-job tests. STILL SUPERVISED: the live `npm run db:migrate` apply + grants/RLS check — [RUNBOOK](RUNBOOK.md) "migration 024" step; until then every write fails open into `console.error` (today's behavior, by design).** **✅ PR #110 merged into `main`, session 67 (2026-08-28, `5cccaa3`).** **✅ Migration 024 APPLIED to production 2026-09-02 (session 69, owner present; RLS/grants re-audited clean 2026-09-03, 18/18 tables incl. `error_log`) — the "STILL SUPERVISED" apply above is done; `error_log` writes are live. Noted 2026-09-03 (session 72).** |
| 66 | Product | Clarifications currently offer a suggestion as plain text, requiring a free-text reply the clarify-mode LLM re-parses and merges — which can fail (live examples this session: "Beiden" and "Jan modaal inkomen 2024" both dead-ended in `still_ambiguous`, see [#64](open-questions.md) and the modaal-inkomen case in [STATUS.md](STATUS.md)). Since WP15 ([#56](open-questions.md)), every offered suggestion is already dry-run checked for servability before being shown. Could offered suggestions instead be clickable options that resolve directly, skipping a second LLM parse entirely? | **Decided (Stefan, 2026-07-04, session 18): yes, build this.** Because `echoServability` already proves a suggestion is answerable before it's offered, clicking it jumps straight to its known-good structured intent — cheaper (no second LLM call), faster, and immune to clarify-reply-merge failures. **Not yet scoped into a WP**: needs a UI decision (chat.tsx renders clickable option chips alongside/instead of the free-text field when a clarification carries structured options) and a backend decision (a new "resolve this exact offered option" entry point bypassing `replyToClarification`'s LLM merge for clicks specifically, while free-text stays the fallback for when no offered option matches). **Pricing decided (Stefan, 2026-07-04, session 18): yes, cheaper — target ~5 credits** (half the 10-credit free-text clarification-reply price), reflecting that resolving a click needs no second LLM call at all. Exact value to confirm against `docs/09-pricing.md` conventions at build time; needs a new distinct reason/action-class-price row (today's ledger has one flat `clarification` price for the whole reply round, not split by reply mechanism). **Scoped into WP26 as Mechanism A (session 23, 2026-07-05); design in ADR [024](decisions/024-answer-first-defaults-and-clickable-options.md).** Owner deferred WP26 pricing (session 23) → the ~5-credit click price is **not** built into WP26: the click mechanism ships at the normal answer price with no ledger change, and the previously-decided ~5 price lands as a fast-follow config change tracked in [#101](open-questions.md). **Execute-brief written (2026-07-19 overnight design marathon): [session-briefs/2026-07-19-wp26-execute-brief.md](session-briefs/2026-07-19-wp26-execute-brief.md) — incl. read-back item: take-path A2 (deterministic option-label match inside replyToClarification, no new entry point) recommended over ADR-024-as-written A1; jsonb pending state confirmed → no live DDL.** **➡ BUILT + LIVE-READY (session 56, 2026-07-25): WP26 mechanism A shipped on take-path A2** (owner read back and chose A2 over ADR-024's A1): clarification options now carry a fully resolved, dry-run-verified intent, and a reply byte-equal to an offered label is taken deterministically — zero LLM calls, composed through the template rung. Chips ride the existing kind-agnostic suggestions surface (#75 fill-don't-send). Built at policy rules 3/4 and the region_ambiguous branch (the Utrecht case). Client-held payload re-validated fail-closed (validate-pending.ts). Flag `CLARIFY_CLICK_ENABLED`, dormant. Review fix: no region chip when the question named OTHER regions too (the chip would have answered a narrower question). Commit `8ee71c8`. **WP26c also shipped (`1a4ca89`): the two measured trial misfires carry one deterministic rescue chip beside the unchanged refusal.** **Remaining: the owner-supervised flag flip + live smoke — ✅ DONE: `CLARIFY_CLICK_ENABLED` set 2026-09-02 (session 69), smoke test PASSED 2026-09-03 (session 71: audit rows 261/262 — the Utrecht clarification and its click, `parse.model = deterministic/wp26-click-option`, 0 tokens, `audit:verify` 2/2 clean; rows 263/264 the comparison chip). Nothing left open here.** |
| 67 | Product/UX | WP17's dashboard question history shows a failed clarification round (the clarifying-question audit row and the follow-up refusal row) as **two separate entries sharing the identical original question text** — looks like the same question was answered twice, differently. Confirmed live (Rotterdam 2020/2022 example, audit rows 79–80). | **Decided (Stefan, 2026-07-04, session 18): yes, collapse a clarification round into one folded history item** (question → clarifying question → reply → final outcome shown together), rather than one row per audit record. **Built (2026-07-05, session 20, WP19):** rounds collapse at read time in `getQuestionHistory` (no migration — value-match on the shared original question + the pending's `questionNl`; known two-identical-tabs limitation documented and test-pinned, see the [WP19 brief](08-build-plan.md)); the dashboard renders the exchange inside one fold with the round total labeled "totaal" |
| 69 | Product/UX | Users currently only learn their balance is too low when a question is refused with `insufficient_credits` — no proactive warning beforehand, so running out can surprise a user mid-conversation. | **Decided (Stefan, 2026-07-04, session 18): yes, build a low-balance warning.** Threshold: show it once the balance can cover at most one more `simple` question but not two (today: `20 <= balance < 40`) — the existing `insufficient_credits` refusal already covers the point below that. **Built (2026-07-05, session 20, WP19):** amber banner in the account panel, visible exactly on `simple ≤ balance < 2×simple` with `simple` read live from `action_class_prices` (the threshold tracks a price change automatically) and reacting live to the #68 balance updates; boundary behaviour test-pinned at 19/20/39/40 |
| 70 | Product/UX | "Next-level UX" brainstorm (Stefan's request, 2026-07-04, session 18): source attribution is plain text ("Bron: CBS StatLine, tabel 86141NED...") — could it be a clickable drill-through showing the exact cell, coordinates, and sync date? | **Decided: yes, build.** All the needed data (table id, coordinates, sync date) already exists on every answer — a light first version needs no new backend. Connects to the already-roadmapped "drill-down buttons" (Phase 1) and the Phase 2 user-facing audit-trail view; this can ship as a small standalone piece ahead of either **✅ BUILT 2026-09-03 (session 72, autonomous); PR #123 MERGED + LIVE 2026-09-03, session 75, owner-approved in chat, squash `ddca024`:** shipped as Depth 2 of the source drill-through cluster, per the design brief [session-briefs/2026-09-03-source-drill-through-design.md](session-briefs/2026-09-03-source-drill-through-design.md) — the "De gebruikte cellen" block of the "Bewijs dit cijfer"/"Bewijs deze cijfers" panel shows the exact cell(s): table id, measure, full coordinates (region/period/dims with labels), sync date, batch id, CBS status; ids/codes behind a "Technische details" toggle. Built in `web/lib/answer-proof.ts` (`buildAnswerProof`) + `web/components/answer-proof.tsx` (`AnswerProof`), rendered inline under the chat answer, collapsed by default. |
| 71 | Product/UX | Same brainstorm: the "voorlopig" (provisional) marking is only in the answer's prose — easy to miss when scanning quickly. A visual badge instead/in addition? | **Decided: yes, build.** Small and visual; the full design pass is deferred ([#29](open-questions.md)), but "ugly is acceptable" pre-launch ([03-mvp-scope.md](03-mvp-scope.md)) means a plain badge can ship now rather than waiting for that session. **Built (2026-07-05, session 22 overnight, WP23):** an amber "voorlopig" pill in every answer's source-chip row whenever any quoted cell is provisional — the message-level generalization of the WP20 stat-card badge (which stays); test-pinned on and off |
| 74 | Product/UX | Same brainstorm: WP16's "we're fetching this" response (once built) only notifies by e-mail — no way to see progress while still in the app. | **Decided: yes, build.** A small "mijn aanvragen" panel in the dashboard showing pending onboarding requests and their rough status; pairs directly with WP16, should land alongside it rather than as a separate follow-up. **✅ RESOLVED (2026-08-27, session 66, autonomous): built as a small at-a-glance LINE above the history list, not a separate panel.** Reasoning: the per-item amber status boxes (WP16 sub-part 2, `web/components/question-history.tsx`) already carried all of "mijn aanvragen"'s information — pending/running/failed state, topic, cost, refund — inside the history list, so a dedicated panel would render the same rows twice; the remaining REAL gap was at-a-glance-ness (history sorts by most-recent activity, so a pending request slides under anything asked after it). Closed by one amber summary line above the list — "Er is 1 aanvraag / zijn N aanvragen bij het CBS in behandeling — de status hieronder wordt automatisch bijgewerkt" (`web/components/onboarding-live-status.tsx`, deterministic template per the #84 convention) — rendered by `QuestionHistory` itself, so it appears on BOTH history surfaces (`/geschiedenis` with WORKSPACE_ENABLED on — production today — and the flag-off Dashboard) and disappears when nothing is in flight. The line doubles as the host of #117's live poll, which is what makes the shown status actually track reality **✅ PR #107 merged into `main`, session 67 (2026-08-28, `352f4de`).** |
| 76 | Product/UX | Same brainstorm: a new user has no explanation of the credit economy (what 100 signup credits means, roughly what a question costs) until they run out. | **Decided: yes, build. Placement specified by owner (2026-07-04): in the right-hand account panel (`web/components/account-panel.tsx`), directly under the "Credits kopen" button** — not a modal or first-login popup. One or two sentences: signup grant size and roughly what it buys, in credits and in questions. **Built (2026-07-05, session 20, WP19):** two sentences directly under the button, every number read live from `signup_grant_config` + `action_class_prices` (new `getSignupGrantCredits`, test-pinned to track a plain UPDATE); the question count is `floor(grant/simple)` phrased with "zo'n" so it understates, never overpromises |
| 78 | Product/UX | Second creative-brainstorm batch (2026-07-05, six expert lenses, owner-filtered — rows 78–93): **"Kopieer als citaat"** — one button under every answer copies a ready-to-paste, editorially formatted sentence with the source baked in (e.g. *"… (CBS StatLine, tabel 82931NED, gesynchroniseerd 3 juli 2026)"*). Pure text template over existing answer + attribution fields — no new data, no computation. Surfaced independently by three of six lenses. | **Approved (Stefan, 2026-07-05): build — top-5 #1, effort small.** The quote must stay honest: built only from the validated `answer.body` + structured `Attribution` fields, plus a "voorlopige cijfers" flag when any quoted cell is provisional and the CC BY bewerking-marking when derivations are present — never a recomposed sentence. **Built (2026-07-05, session 20, WP20):** `web/lib/citation.ts` + "Kopieer als citaat" button under every answer, full-string test pins on all flag variants |
| 79 | Product/UX | Same batch: **"Bewijs dit cijfer"** — a button exposing the existing audit record (WP10/R8) as a clickable, followable step list: which table cell, which coordinates, which sync date, every applied derivation (R5), including an explicit notice when a cell is unpublished/suppressed by CBS. Moves trust from "we say so" to "you can recompute it". Reconstruction already exists internally for CI. | **Approved (Stefan, 2026-07-05): build — top-5 #2, effort medium.** Needs a WP brief first: overlaps heavily with the already-approved drill-through ([#70](open-questions.md)) and the Phase 2 user-facing audit-trail view — one design should serve all three, not three separate buttons **✅ BUILT 2026-09-03 (session 72, autonomous); PR #123 MERGED + LIVE 2026-09-03, session 75, owner-approved in chat, squash `ddca024`:** shipped as Depth 3 of the source drill-through cluster, per the design brief [session-briefs/2026-09-03-source-drill-through-design.md](session-briefs/2026-09-03-source-drill-through-design.md) — the "Stap voor stap" block walks `result.derivations[]` into a followable Dutch step list (difference/max/direction/unit_expansion; `first_last` skipped, it carries no value of its own), each step's stored number traced back through `resultId`s behind the "Technische details" toggle, with the CC BY derived-data marking shown whenever the result carries a derivation record — the same rule as the answer's marking line and the citation (review round 2; a binding-only `first_last` counts, exactly as it does there). Built in `web/lib/answer-proof.ts` (`buildAnswerProof`) + `web/components/answer-proof.tsx` (`AnswerProof`) — one panel with all three depths stacked, not three separate buttons. **Review round 2 (session 74, 2026-09-03, the HIGH pass before merge — RUNBOOK batch item 10):** the "Geen bewerking toegepast" step now agrees with the read step for MULTI-cell answers (a comparison — the G4 chips live since rows 263/264 — a series whose null cell blocked its derivations — read the singular "de waarde uit de cel"; now "toont de gelezen waarden van deze cellen"; a `first_last`-only result (no live producer is known — run.ts registers direction and first_last under the same gate and refuses mixed units before either; a belt for stored rows this build did not write — **Assumption:** such a row, possibly beside a null cell, may exist from an older build; ADR 011 says the pre-registration is omitted when a cell is null, and the shallow history available to session 74 could not settle whether that always held, so the belt is pinned for both shapes rather than argued away), which the answer line and the citation mark as derived, now reads "Geen bewerking met een eigen uitkomst: het antwoord duidt de reeks als geheel …" and stays marked — the panel follows the same R5 rule as the surfaces around it); two never-throws belts (a stored derivation of an unknown kind, or a direction word outside up/down/flat, yields an honest "kon niet worden weergegeven" step instead of `undefined` in `steps[]` — which threw in the component, outside the builder's try/catch — or the literal word "undefined" in prose); `AnswerProof` memoized (an open panel rebuilt its cell table on every keystroke); the `direction` fixtures now diverge from a naive recomputation (they coincided, so the R1 canary could not tell a stored read from live arithmetic) and the two count-shaped R1 exemptions are pinned as exact sentences; a resumed-thread render test through `replayParts` → `assembleMessages` → `<Chat initialMessages>`. The design brief's §4 template list records the plural variant. **Follow-up (LOW pass):** the R5 predicate `derivations.length > 0` now lives in four places (compose.ts, reconstruct.ts, citation.ts, the panel) while csv.ts applies a narrower explicit-only rule — one shared `isDerivedResult(result)` beside `DERIVED_DATA_MARKING` would make "same rule" true by construction and surface the CSV divergence; not built here (a shared `src/query` change is outside this web-only PR). **✅ Follow-up BUILT 2026-09-04 (autonomous session, branch `fix/79-shared-derived-predicate`, PR #127, MERGED + LIVE session 78, squash `676facf`):** added `isDerivedResult(result)` beside `DERIVED_DATA_MARKING` in `src/query/types.ts` and switched all four call sites (compose.ts, audit/reconstruct.ts, citation.ts, answer-proof.ts) to call it — pure extraction, behavior byte-identical there (confirmed: R5/R8 invariant tests — `tests/invariants/invariants.test.ts`'s independent `derivations.length > 0` re-check, `tests/answer/compose-template.test.ts`'s marking-line test, the audit R8 byte-match tests — pass unchanged). The real fix is csv.ts: its "Bewerking: ..." marking-line decision now uses `isDerivedResult(result)` (ANY derivation) instead of `exportableDerivations(result.derivations).length > 0` (explicit difference/max only) — closing exactly the gap this row named: a `direction`- or `first_last`-only export used to omit the CC BY marking line while chat/citation/proof panel all showed it. `exportableDerivations()` is UNCHANGED and still gates the separate "Afgeleide waarden" data-table section (a direction/first_last derivation has no number of its own to tabulate) — two questions, two rules, now both correct instead of one silently wrong. Two existing csv.test.ts cases encoded the OLD (under-disclosing) behavior via `not.toContain('bewerking')` on a direction-only and an implicit-max-only fixture — both updated to assert the marking line NOW appears while the data table stays absent; a new dedicated first_last-only test added (the PR #123 round-2 case this row itself flagged: first_last "carries no value of its own" but still counts as derived). Verification block run on the branch (see PR for the full measured log). |
| 82 | Product/UX | Same batch: **cost transparency before sending** — (a) estimated cost next to the send button ("Versturen — kost ~20 credits, je hebt er 100"), (b) a hint that a clarification round may follow, (c) that cost line repeated at the clarification message itself. Complements the built low-balance banner ([#69](open-questions.md)). | **Approved (Stefan, 2026-07-05): build — top-5 #5, effort small.** All numbers from the live pricing tables (ADR 006), never hardcoded. Honest limit on (b): a *confidence-conditional* pre-send hint is impossible before the parse runs — v1 ships a static hint ("een verduidelijkingsvraag kost X, de rest krijg je terug"); a conditional version belongs to the [#72](open-questions.md) design work. **Built (2026-07-05, session 20, WP20):** pre-send cost line with the LIVE balance (#68 state) + static hint under the chat form, and the reply price repeated client-side at every clarification message; all numbers from the live pricing tables |
| 84 | Product/UX | Same batch: **visually distinct message types** — answer (neutral + source chip) / clarification (orange-yellow + question icon) / refusal (own header "Dit kon ik niet beantwoorden" + a "geen antwoord = geen gok" badge). | **Approved (Stefan, 2026-07-05): build — effort small.** The chat already branches on `kind`; this is styling + two fixed Dutch strings. **Built (2026-07-05, session 22 overnight, WP23):** clarifications render amber with a decorative question marker (kept OUTSIDE the bubble so the pipeline's message text stays byte-exact); refusals carry the fixed header "Dit kon ik niet beantwoorden" + badge "geen antwoord = geen gok"; both strings test-pinned, answers pinned free of them |
| 86 | Product/UX | Same batch: **live "Verifieer bij CBS" deep-link** — button linking straight to the exact, pre-filtered CBS StatLine table viewer; pure URL construction over existing registry coordinates, no live CBS call (ADR 003 intact). | **Approved (Stefan, 2026-07-05): build — effort small.** Needs one design check: the StatLine viewer's URL/filter syntax must be pinned by tests against real examples, and the link must land on the TABLE (cell-level deep-linking on StatLine is unreliable) with our coordinates shown alongside ([#70](open-questions.md)/[#79](open-questions.md) show the exact cell). **Built (2026-07-05, session 22 overnight, WP23):** "Bekijk bij CBS StatLine" inside every answer's source chip — pure URL construction to the dataset TABLE view (`web/lib/statline.ts`), pinned against two real Phase-0 table URLs incl. the lowercase-id case; our coordinates stay alongside in the chip/answer (the exact-cell view is the #70/#79 cluster) |
| 89 | Product/UX | Same batch: **"Waarom dit antwoord"** — an info icon showing which canonical default definition was chosen and which alternative existed, clickable instead of only named in the text. | **Approved (Stefan, 2026-07-05): build — effort small.** The data already exists (`Attribution.definitionLabel` + the [#39](open-questions.md) alternate-readings policy — **#39's plumbing was built 2026-08-27 (session 66): `Attribution.alternates` + the plain-text `alternatesLine` disclosure now exist to build the affordance from**; combining them in one affordance is the natural shape) **✅ BUILT 2026-09-03 (session 72, autonomous); PR #123 MERGED + LIVE 2026-09-03, session 75, owner-approved in chat, squash `ddca024`:** shipped as Depth 1 of the source drill-through cluster, per the design brief [session-briefs/2026-09-03-source-drill-through-design.md](session-briefs/2026-09-03-source-drill-through-design.md) — the "Waarom dit antwoord" block shows the chosen `definitionLabel`/`periodSemantics` and every not-chosen `Attribution.alternates` entry (label cleaned via `displayAlternateLabel`, the registry's internal cross-reference stripped; measure/dim code behind "Technische details"), upgrading the existing plain-text `alternatesLine` to a clickable affordance without duplicating it (kept, not moved — `alternatesLine` is still a line of the R8-audited `text`). Built in `web/lib/answer-proof.ts` (`buildAnswerProof`) + `web/components/answer-proof.tsx` (`AnswerProof`). |
| 90 | Product/UX | Same batch: **source chip instead of source line** — a visually separate, expandable element instead of small-print text; combines with the approved drill-through ([#70](open-questions.md)). | **Approved (Stefan, 2026-07-05): build — effort small.** Belongs in the same WP as [#70](open-questions.md)/[#79](open-questions.md) (one source-presentation redesign, not three). **Chip presentation built (2026-07-05, session 22 overnight, WP23):** the attribution renders as a visually separate chip under every answer — ALWAYS showing the full R4 sentence (never collapsed behind a click) + the #86 StatLine link; answers now render from their structural fields (body/staleness/definitie/marking as own elements) with zero loss vs the assembled text, test-pinned. The chip is deliberately the collapsed depth-0 of the #70/#79/#89 drill-through — that expansion stays with the cluster design brief in [08-build-plan.md](08-build-plan.md) |
| 91 | Product/UX | Same batch: **tabular figures + dominant key number** — pure typography (`font-feature-settings: 'tnum'`), numbers set apart from running text FT/NRC-style. | **Approved (Stefan, 2026-07-05): build — effort small.** Pure CSS, no data change; natural companion to [#80](open-questions.md)'s card. **Built (2026-07-05, session 22 overnight, WP23):** `tabular-nums` on the chat message list, cost captions and the balance display (the #68 live number now changes without digit-width jitter); the "dominant key number" half of #91 was already the WP20 stat card |
| 92 | Product/UX | Same batch: **rearranged chart footer** — caveats (voorlopig/verouderd) larger and in amber, source credit smaller/lighter, like a photo credit. | **Approved (Stefan, 2026-07-05): build — effort small.** Rendering-only change in the chart component; the R4 attribution CONTENT stays untouched (one builder, `buildAttributionLine`). **Built (2026-07-05, session 22 overnight, WP23):** provisional/null-cell caveats render amber and a step larger; the attribution line stays smallest/lightest (photo-credit style), caveats above the credit — order and classes test-pinned; strings byte-identical from the one builder |
| 93 | Product | Same batch, explicitly REJECTED by the owner (recorded so future sessions don't re-propose them): (1) a watch-list / subscription on a recurring figure; (2) pattern+shape encoding besides color in charts (accessibility encoding); (3) a comparison card with highlighted difference. | **Decided (Stefan, 2026-07-05): NOT building these three** — deliberately left out of the feature pool; revisit only if the owner reopens them himself |
| 95 | Product/UX | After a successful Stripe purchase, the app redirects to `/credits?purchase=success` — the same "buy more" package list, not back to the main app. Surfaced live (2026-07-05, session 18) during the real Stripe purchase debugging/fix: buying credits is instrumental (a means to keep asking questions), never the destination itself. | **Decided (Stefan, 2026-07-05): redirect to `/` instead** — there is no separate "dashboard" route and none is needed; the main page already is the dashboard (chat + account panel with balance side by side). Change `success_url` in `src/billing/stripe-checkout.ts` from `/credits?purchase=success` to `/?purchase=success`; show a small, dismissible confirmation banner on `/` when that param is present, reusing the existing honest copy ("credits zijn onderweg, duurt meestal een paar seconden" — never a fixed time promise). Pairs naturally with [#68](open-questions.md) (live balance updates): once built, the balance updates on this same page with no reload needed at all. **Built (2026-07-05, session 22 overnight, WP22):** success redirects to `/?purchase=success` — one deliberate deviation from this row's original file pointer: the URL lives in the WEB layer (`web/lib/purchase.ts`, one shared param definition for the checkout-action writer and the page reader), not in `src/billing/stripe-checkout.ts`, which already took the URL as a parameter and stays byte-unchanged (a redirect string is web routing, not billing logic); dismissible banner on the dashboard with the honest webhook/reload nuance (the #68 live updates move on question outcomes, not webhook credits — the banner says "ververs daarna de pagina", never implies auto-update); dismiss strips the query flag so a reload can't resurrect it |
| 98 | Product/UX | From the [UX design brief](10-ux-design-brief.md) (session 21/22): `/` is both "the homepage" and "the dashboard" today (ADR 008's deliberate Phase 0 minimalism — no separate marketing surface exists). Now that [#53](open-questions.md) (anonymous trial) is queued as its own separate route, the site is about to grow a fourth page with no shared shell holding it together unless this is decided first. | **Not yet decided.** Three options in the brief, recommendation **A now, C as a cheap fast-follow, B only if/when #53 is greenlit**: (A) keep `/` merged as the logged-in dashboard, unchanged, #53 lives on its own route, no rework; (B) split into a real public landing page (possibly a live #53-budget teaser chat) + move the dashboard to `/app`/`/dashboard` — real build cost, overlaps #53's own scope; (C) keep `/` merged for logged-in users, but replace today's bare redirect-to-`/login` for logged-out visitors with one minimal pitch screen + login link — cheap, fixes the "stranger sees only a login form" gap without #53's budget/abuse questions. **Note (session 41, ADR [033](decisions/033-chat-workspace-redesign.md) D6): the WP135 workspace builds at the merged `/` — option A's posture — and does not foreclose B or C; #98 itself stays an open owner decision.** | **✅ RESOLVED (session 51, 2026-07-17, owner in-chat): `/` IS the product's public face** — logged-out visitors get the editorial landing (real verified example answer, no chargeable entry point; proxy allowlists `/` EXACT-match, pinned by proxy.test.ts); logged-in keeps the dashboard/workspace. **Amendment (session 52, 2026-07-17): the original "no data reads" phrasing is superseded by the owner-approved #53(c) Ontdek section — the landing's ONLY data reads are the cached, fail-safe, LLM-free discovery charts (ADR [035](decisions/035-homepage-discovery-charts.md)); still no chargeable entry point.** The anonymous-trial chat lands ON this page per the #53 refinement. Built with the papier-en-inkt huisstijl (docs/12-huisstijl.md).
| 113 | Ops/Product | WP16 sub-part 2 — **onboarding trigger cadence, discovered at deploy time (session 27): the Vercel HOBBY plan only allows DAILY cron jobs** — the designed minutes-level schedule (`*/2 * * * *`, ADR [026](decisions/026-on-demand-fetch-job-architecture.md) decision 1) was rejected by `vercel deploy` — deterministically, on every deploy carrying it (an initial "inconsistent validation" reading was the session watching the wrong CI run — see [lessons-learned](lessons-learned.md) session 27). The committed cron is now a daily 06:00 UTC backstop sweep (live-verified registered on the project after commit `ac7b0df` deployed), which alone makes the acknowledgment's *"meestal een kwestie van minuten"* promise false. | **✅ Resolved (Stefan, session 27, 2026-07-06): kick-on-trigger.** After `triggerOnboarding` commits, the app itself fires the cron route (fire-and-forget, `CRON_SECRET`-authed, **fail-soft and strictly post-commit** — a failed kick can never affect money or the acknowledgment, it just degrades to the backstop); the daily 06:00 UTC Vercel cron stays as the crash/retry backstop sweep. GitHub Actions rejected: its own schedule jitter (10–20+ min under load) gives the same latency uncertainty with more moving parts. **Vercel Pro (~€20/mo) is explicitly KEPT as the fallback — owner, verbatim: *"Bewaar die optie als de kick-on-trigger-aanpak ooit tekortschiet."* Revisit trigger: measured kick failures or delivery latency beyond "minuten" in real use → upgrade to Pro (platform-level minutes cron) rather than building more homegrown scheduling.** Build note for the next session: a small mechanical change (mid tier, hermetic-testable), to be built and gate-green BEFORE the RUNBOOK supervised live step flips `ONBOARDING_ENABLED` — this resolution satisfies the gate that row placed on the flag. If latency can still exceed "minuten" in practice, the copy changes too ([#24](open-questions.md)'s no-fixed-time-bound rule). **✅ BUILT (session 28, 2026-07-06, commit `c64e2a2`; Opus build + 3-lens Sonnet review, hermetic gate green — backend 891, web 167, benchmark 14/14+6/6+0 fabricated).** `web/lib/onboarding-kick.ts` fires `/api/onboarding-cron` via Next's `after()` in `maybeTriggerOnboarding` on the `started` AND `duplicate` branches (post-response, so strictly post-commit; never on `insufficient`). Fail-soft by construction — it cannot throw. **Platform-review refinement (the one MEDIUM the review caught): the cron route only responds once the WHOLE job finishes (tens of seconds), but the caller's page action is capped at 30s** ([`web/app/page.tsx`](../web/app/page.tsx) `maxDuration = 30`) — so a naive `await fetch` would be platform-killed mid-wait on any long job, silently, never logging whether the kick fired. Fixed with `AbortSignal.timeout(10s)`: the kick DISPATCHES the job then stops waiting (the job runs on — Vercel request cancellation is **opt-in** via `supportsCancellation`, which this path deliberately does not set, so a client disconnect never cancels the route). The timeout is the expected long-job path, logged as benign dispatch. **Verify the kick fired at the live step via its own log line** (`onboarding kick dispatched …` in `vercel logs`) — RUNBOOK step 3(b), which also closes the review's LOW (step 3 previously only checked the cron route responds, not that the kick itself ran). **✅ VERIFIED LIVE (go-live, session 28, 2026-07-06): the kick fired** — the first two real onboarding jobs finished seconds after their triggers, not at the 06:00 UTC backstop, proving the kick (not the daily cron) started them. RESOLVED + live-proven. |
| 117 | Product / UX | **The app does not auto-update when an onboarding answer lands** (owner, session 28): after the "wordt voorbereid" acknowledgment, the delivered answer only appears on a manual page refresh — no live update or in-app notification. | **Open — ties to [#74](open-questions.md) (live status panel for pending onboarding requests).** The dashboard history is server-rendered at load; a pending→delivered transition needs a poll or push to surface without a refresh. Scope with #74; the honest cheap first step may be a light poll of the pending row's status while any request is in flight. **Session 30 groundwork: PR #14's structured `answerParts` entries are the surface a live-update/poll would refresh — scope the poll with #74 on top of it.** **✅ RESOLVED (2026-08-27, session 66, autonomous): the honest cheap poll, built exactly on that surface. Architecture chosen: client-side `router.refresh()` re-render — NO new status endpoint, no push.** While the server-rendered history contains any pending/running request, `web/components/onboarding-live-status.tsx` (mounted by `QuestionHistory`, so it lives on whichever history surface is active) calls next/navigation's `router.refresh()` every 20s: the route's own server reads re-run, and a pending→delivered transition replaces the amber entry with the REAL delivered answer entry (the answerParts rendering, real ledger cost) in place — no manual refresh. Chosen over a lightweight `/api/onboarding-status` endpoint because a status poll could only flip the label text; the delivered ANSWER would still need the full server re-render this row complains about, and the refresh route adds zero new auth surface (the same userId-scoped `getQuestionHistory` runs again). Accepted trade-off: each tick re-runs all the page's server reads rather than one cheap status query — bounded because the poll only exists while something is in flight (stop condition = the refreshed server-derived count reaching zero, never a client flag), ticks are skipped in hidden tabs (immediate refresh on tab return), and deliveries land in minutes. Same-tab acks: the flag-off Dashboard refreshes once on an `onboarding_pending`/`onboarding_already_pending` outcome so the just-committed row appears and the poll takes over; on the flag-on workspace the ack's answer surface is `/geschiedenis`, which server-renders fresh on navigation and starts the poll there. E-mail delivery notification stays as-is — this poll is the in-app half |
| 119 | Engineering / money | **WP16 job: deliver→finalize is NOT atomic** (session-30 review, verified): `processOneRow` writes the delivery audit row (`answerQuestionAudited`) and then `finalizeDelivered` as separate operations — unlike the refund paths, which run refund+finalize in ONE transaction. A process crash in that window leaves the row 'running' with the answer already delivered; the 20-minute stale reclaim then RE-RUNS delivery: a second audit row + second email, and BOTH audit rows join the same `request_id`'s 100-credit debit in the dashboard (looks like 200 charged; the ledger itself stays correct — no double debit). | **Open — narrow crash window, no money moved, recorded with a fix sketch:** an idempotency check at claim time (does an `onboarding_delivery` audit row for this `request_id` already exist? → finalize instead of re-running). Do as a small reviewed PR; not urgent while the owner is the only user. **✅ FIXED 2026-07-12 (session 39, branch wp119-delivery-idempotency — plain-text ref, merged + deployed session 39; a residual step-8-catch gap in this SAME deliver→finalize area — a delivered answer wrongly refunded when finalizeDelivered threw — was later found + fixed session 43, PR #39 `a395fa8`, see ADR 026 as-built):** the sketch as recorded, HARDENED by the pre-build adversarial review: the recovery query filters `kind = 'answer'` (a bare existence check would have "recovered" a crashed REFUSAL/clarification attempt as delivered — a permanent unrefunded 100-credit charge + a false "Goed nieuws" email; the ⟨F1⟩ finding, two review lenses converged). Check runs at BOTH re-entry points: `processOneRow` claim-time AND the exhausted-attempts loop (without the latter, a crash row hitting MAX_ATTEMPTS was failAndRefund'ed — refunding an answer the user already received = user keeps answer + refund). New store helper `findDeliveredAnswerAuditId` (order by id asc: historical double-rows resolve to the FIRST delivered answer); 4 new PGlite tests incl. the crashed-refusal regression and a ledger byte-identical pin. |
| 121 | Engineering / invariants | **The compose ladder's template rung records but does not ENFORCE its own validation verdict** (session-30 review): `assemble()` attaches `validateAnswerBody(...)` to every answer, but the template fallback is returned regardless of it — safe under the original assumption that the deterministic template is R3-valid by construction, which the negative-cell/long-unit blind spots (fixed in PR #15) proved can silently break for NEW data shapes. | **Open — owner decision:** should a template answer that fails its own validator REFUSE (fail-closed, costs answer-rate on future validator blind spots) instead of serving with `ok:false` recorded? Alternative: an alert/log when a served answer records `ok:false`, so the next blind spot is loud instead of silent. Decide before new data sources (priority #2) multiply the shape surface. **Design written (2026-07-19 overnight design marathon): [session-briefs/2026-07-19-small-designs.md](session-briefs/2026-07-19-small-designs.md) §b — the one-line owner question sharpened (recommendation: serve + admin-alert + R8 marker, because a template ok:false indicates a VALIDATOR blind spot, not a lying answer) + an adjacent finding to fix either way: the template rung can THROW uncaught (non-null assertions outside the try/catch) → wrap + honest internal refusal.** **The unconditional half SHIPPED (2026-07-24, session 55 resumed) with a MEASURED correction to that adjacent finding: the throw is NOT uncaught — `respondToQuestion`/`respondToClarificationReply` already catch every downstream throw (template rung included) and serve the honest 'internal' refusal on every production path (respond.ts fail-closed contract; no external `respondToIntent` caller exists). What WAS true: that failure is SILENT. Shipped: an internal-refusal ADMIN ALERT (`maybeAlertInternalRefusal`, the #144 alerts.ts posture — console.error floor, Resend when configured, fail-soft) on the shared respond-audited site, both turns; send path deduplicated into one `sendAdminAlertEmail` helper; 5 new hermetic tests. The ONE-LINE OWNER QUESTION above (serve+alert vs fail-closed for a template body failing its own VALIDATOR) remains open — that is a different signal (served answer with ok:false) than the throw path closed here.** **✅ RESOLVED (2026-07-24, owner in-chat after a concrete-example read-back — the −39 scenario): OPTION A — serve + admin-alert + R8 marker.** Shipped same day: `maybeAlertTemplateValidationFailure` (fires only on source 'template' + recorded ok:false — the only way a failing verdict can be served; #144 posture, both turns) + the R8 marker in `reconstruct.ts` (a template row stored ok:false re-validates under the distinct label "KNOWN-failing at serve time (#121 serve+alert)", triaged apart from silent corruption; heals when the validator blind spot is fixed — the PR #15 pattern). Proven: 3 new hermetic tests (hook guard, email content, R8 label vs bare-corruption label on a real mutated record) + `audit:verify -- 1 256` against ALL production rows exit 0 (229/231 clean + the 2 known pinned divergences — no new divergence introduced). #121 is now FULLY closed: throw path (alert, 24-07 morning), validator-verdict path (option A, 24-07). |
| 122 | Auth / money | **Assumption (ADR [028](decisions/028-google-sso.md) D2, WP28): Supabase auto-links a verified-e-mail Google sign-in to the EXISTING `auth.users` row with the same e-mail** — one human = one account = one credit balance, and the signup-grant trigger fires no second time. Money-relevant: if the assumption is wrong, a returning magic-link user who clicks Google gets a SECOND account with a fresh 100-credit grant and an empty history. | **Open — verify EMPIRICALLY in WP28's supervised live step (brief §4.3):** magic-link account first, then Google login with the same address → assert one user row (two identities) + exactly one `signup_grant` ledger row; then a fresh-e-mail Google login → new user, one grant. The build must not merge the RUNBOOK claim as fact until this is measured. **Owner CONFIRMED (2026-07-08): 'dat moeten we gewoon doen' — the empirical linking verification in WP28 §4.3 is mandatory, not optional.** **✅ RESOLVED — VERIFIED EMPIRICALLY on live production (2026-07-10, session 34, owner present): (a) same e-mail: Google login with the existing magic-link address linked to the EXISTING `auth.users` row — user count stayed 3, that user's identities became `[email, google]`, signup grants stayed 3 (NO duplicate); (b) fresh e-mail: one NEW user (google-only identity) + EXACTLY ONE +100 grant, written in the same instant as the user row (trigger fired once). ADR 028 D2 is measured fact; both readings taken before AND after each login (read-only SQL).** |
| 129 | Product / UX / architecture | **Owner request (2026-07-11, session 36): source TAGS in the chatbox — a "CBS data" chip with an ✕; deselecting it excludes that database from the search. More databases join later; CBS is PRE-CHECKED by default (owner-decided detail).** | **This is the UI half of the WP30 waist, rendering FROM the source registry (WP30a, live) by construction.** **OWNER-CONFIRMED (2026-07-11, in-chat: "De aanbevelingen: we kunnen alles vastleggen"):** **build WITH the first second source (#130 websearch or WP30c)** — a lone pre-checked CBS chip adds choice-noise without choice. Design notes for the build: deselecting ALL sources → honest refusal ("geen bronnen geselecteerd"), never a guess; the selection is a STRUCTURAL input to the finder/query scoping (a source filter over the catalog — the ADR 030 D5 seam), NOT prompt text (zero prompt bytes achievable); selection state rides the audit record so R8 reconstructs what was searched. ➡ **BUILT + MERGED + DEPLOYED (session 40, 2026-07-12: PR 33 merged `e410ea2` on the owner's in-chat approval, main gate + deploy green; GO-LIVE RUN same day — FULLY LIVE, smoke tests ledger-verified; one WP with #130 per Q10):** chips render FROM the source registry (label "`displayName` data", pre-checked) plus the "Internet" channel chip, all behind `WEBSEARCH_ENABLED` (flag off ⇒ no chips, byte-identical UI); the selection is a server-validated STRUCTURAL input (`SourceSelection`, unknown keys stripped, forced off while dormant) riding the audited envelope as `sourceSelection` with the A1 `?? null` discipline; all-deselected ⇒ client-disabled send + the deterministic `no_sources` refusal belt (full refund by existing gate mechanics). Frozen brief: [session-briefs/2026-07-12-wp129-130-brief.md](session-briefs/2026-07-12-wp129-130-brief.md). |
| 130 | Product / principles / architecture | **Owner request (2026-07-11, session 36): WEB SEARCH as a combinable source — a question can use CBS (pre-checked) and/or websearch; findings from the internet are COMBINED with the answer. Owner explicitly wants this FIRST in the source-expansion lane, and asks to research the best/cheapest technique.** | **Recorded — the single most principle-touching feature ever proposed; needs its OWN pre-build ADR + a short owner interview before any build (the WP16 pattern).** The honest tension, named: web content is live (principle b forbids live fetching in the answer path), unvalidated and unstructured — its numbers can NEVER pass R1/R3/R5 as-is, and the public claim ("every number traceable to an official CBS cell") does not cover it. That does not kill the feature; it constrains the design. **OWNER-CONFIRMED (2026-07-11, in-chat: "De aanbevelingen: we kunnen alles vastleggen"):** **the strictly SEPARATED answer section IS the chosen direction** — the validated CBS body stays exactly as today (validator-backed, attributed), followed by a visually distinct "Van het web (niet door checkdecijfers geverifieerd)" block with its own per-claim source links; web findings NEVER feed the validated body, the validator, the audit numbers or the charts; the separation IS the honesty model, per-section claims. Websearch is NOT an ADR-030-waist source (no tables/cells) — it is a separate augmentation channel; WP30c (structured source #2, #123) stays an independent lane. Opt-in per question via the #129 chip = a natural cost gate; pricing = owner decision at design time. **Technique options to research at the design step ("best en voordeligst"):** (1) Anthropic's native web_search server-side tool — fits the existing Claude API stack, built-in citations, per-search + token pricing, least code — **owner-confirmed PREFERENCE (verify cost at design time)**; (2) a search API (Brave/Tavily/…) + own pipeline — the fallback if (1) disappoints on cost/quality; (3) own scraping — **owner-confirmed REJECTED for v1**. The pre-build ADR + short owner interview still precede the build (exact copy, pricing, section shape) — the DIRECTION is settled, the details are not. ➡ **DESIGN PREP DONE (2026-07-12, session 39, owner-absent): ADR [032](decisions/032-websearch-augmentation.md) drafted (status PROPOSED) + the [interview sheet](session-briefs/2026-07-12-websearch-interview-sheet.md) with per-question recommended defaults. Cost VERIFIED from the official docs: native web_search = $10 per 1,000 searches (~€0,01/search) + result tokens as input; a web-opted question lands ≈ €0,03–€0,08 all-in; Brave ($5/1k) / Tavily ($8/1k basic) save at most half a euro-cent per search but add an owned fetch/extract/citation pipeline — native web_search is confirmed the 'voordeligst' option once engineering cost counts, and citations are built-in (always on, token-free). The ~30-min owner interview is now the ONLY remaining step before the frozen executor brief.** ➡ **INTERVIEW CLOSED (2026-07-12, same session, owner in-chat): all ten points confirmed — the four decision-weight ones (pricing +10 credits, web-only-mode allowed, fail-soft+auto-refund on web error, open web/no allowlist) presented explicitly with tradeoffs; owner replied "Go with your defaults" covering all ten. ADR [032](decisions/032-websearch-augmentation.md) is now ACCEPTED with every decision folded in. #130 is BUILD-READY — the next step is the frozen executor brief (WP27/WP30b pattern: pre-build adversarial review before the build), not another owner decision.** ➡ **BUILT + MERGED + DEPLOYED (session 40, 2026-07-12: PR 33 merged `e410ea2` on the owner's in-chat approval, main gate + deploy green; GO-LIVE RUN same day — FULLY LIVE, smoke tests ledger-verified both modes (CBS+web net 30 / web-only net 10), tool-variant correction to `web_search_20250305` en route (ADR 032 § Go-live correction); one WP with #129):** `src/websearch/` is the new augmentation-channel module (own Anthropic client + the WP's ONE new prompt; `WEBSEARCH_MODEL = 'claude-sonnet-5'` as a named constant, `web_search_20260209`, max_uses 3, NL-localized, fail-soft everywhere); the attach step runs inside BOTH audited entry points so the web section is stored-before-shown (R8) verbatim WITH citations as the additive `webSection` envelope field; money = +10 credits as a `websearch_cost` sibling debit (migration 018 as a FILE only; refund on any non-delivery via the EXISTING `compensate()`; web-only mode nets 10 by existing refusal-refund mechanics — flagged in the PR); benchmark stays web-OFF by construction (verified on the branch head: 14/14 + 6/6 + 0 fabricated GATE PASS). Go-live is a separate owner-supervised RUNBOOK step (apply 018 → `pricing:apply` → `WEBSEARCH_ENABLED=1` → live smoke test ≈ €0,05). |
| 133 | Data integrity / R8 | **Two pre-existing R8 reconstruction bugs found + FIXED live (2026-07-12, session 37-continued) while re-running the owed A1 verification against ALL 240 production audit rows post-WP30b/WP128:** (1) `buildDefinitionLine` crashed on rows stored before session 29 (#115 lever b) — 87/91 live answer rows never serialized an `attribution.definitionText` key at all (`undefined`, not `null`); fixed with the same A1 `?? null` discipline WP30a applied to `attribution.source` (regression: `tests/audit/definition-text-r8.test.ts`). (2) The onboarding-envelope consistency check crashed/false-positived on refusal rows stored before `refusals.ts`'s `input.built.onboarding ?? null` normalization existed — 73/215 refusal rows across nearly every reason lacked the `onboarding` key entirely; same `?? null` fix (regression: `tests/audit/onboarding-envelope-r8.test.ts`). Neither bug ever affected a live answer shown to a user — both only broke RECONSTRUCTION of old stored rows, exactly what this owed verification step exists to catch. Both pushed directly to `main` (owner-supervised in-session fix during the live migration window, #118 house exception), full verification green before each push (typecheck ×2, backend suites, benchmark GATE PASS). Final measured result: **213/215 checkable rows reconstruct clean** (25 GDPR-redacted rows structurally skipped by the verify script pending #133b below). | **Two items remain, NOT fixed tonight (design judgment, not a quick patch):** **(a) Two genuine historical-behavior-versioning anomalies** — row 76 (a 'series' chart spanning 2021MM06→2026MM06, non-contiguous — a LATER session's #64 fix now correctly refuses to chart such a gap, so re-deriving with today's rule legitimately differs from what was shown in 2026-07's row) and row 227 (a "Definitie: X" line that predates the SAME-DAY #115-lever-a circular-title-suppression fix). Both are narrow (2 total), harmless (the stored `final_text`/`response.text` IS what the user actually saw — R8's core promise holds), and raise a real question the codebase hasn't answered: when a deterministic builder's behavior legitimately changes, should reconstruction honor the row's OWN historical rule (needs a versioning mechanism, more machinery) or is "differs from a later, safer rule" an acceptable, documented reconstruction gap? Recommend: a lightweight session decides the policy, not a rushed migration-window patch. **(b) `scripts/verify-audit-rows.ts` now skips GDPR-redacted rows entirely** (25 of 240) rather than crashing on them — `reconstructionReport`/`checkEnvelopeIntegrity` were never taught the `retention.ts` sentinel shape (`redacted: true`, no `.answer`/`.result` at all: `response.answer.schemaVersion` throws on `undefined`). Needs a real decision: should a redacted row reconstruct AT ALL (and if so, to what — verifying it matches the EXACT expected redacted shape, a "redaction integrity" check, not the original content), or is "structurally skip, count, never claim clean" the permanent policy? `reconstructionReport` itself (and the benchmark gate that trusts it) is UNTOUCHED — the skip lives only in the diagnostic script. ➡ **decided 2026-07-12 session 39 — (a) documented-gap policy + known-divergences register, (b) redaction-integrity check; both shipped this PR** (design brief session-briefs/2026-07-12-smalls-133-brief.md §1, pre-build-reviewed): **(a)** no historical-rule versioning machinery — `reconstructionReport` keeps verifying under TODAY's rules, and a legitimately-superseded builder rule (row 76 / #64; row 227 / #115-lever-a) becomes a documented, PINNED entry in the new `src/answer/audit/known-divergences.ts` register (`classifyKnownDivergence`), never a blanket skip — anything not listed still fails, and a listed row whose problems don't match its pinned substring(s) also still fails. **(b)** redacted rows get a real check, not a skip: new `redactionIntegrityReport` (`src/answer/audit/retention.ts`) verifies a redacted row matches the EXACT sentinel shape the redaction wrote (no leftover `answer`/`result`/`chart` key, promoted columns null/empty, `reply_text`/`pending_clarification` paired). `scripts/verify-audit-rows.ts` now routes redacted rows there instead of skipping them. `reconstructionReport` and the benchmark gate stay byte-for-byte untouched; see ADR 016's 2026-07-12 as-built addendum for the full policy record. |
| 137 | Product / UX | **v2 of [#134](open-questions.md)(a) — a RANGE/trend retry chip on `outside_loaded_slice` (deferred from the session-43 build).** v1 ships ONE single-period chip at the boundary ("Wat was {label} in {floor}?"). When the original ask was a RANGE partly below the slice floor, the more faithful retry is the clamped WORKING sub-range ("Hoe ontwikkelde {label} zich van {floor} tot en met {originalTo}?" — the owner's "probeer 2010–2024" shape). Deferred because it needs grain-aware period comparison (floor < to, across JJ/KW/MM) + a second copy template + more surface; single-period is uniform, provably-servable and covers "get unstuck with one click" for v1. | **✅ BUILT + MERGED + LIVE session 43 (2026-07-13), PR #40 (squash `db34700`, main gate + prod deploy + smoke check green).** `buildRefusalSuggestions` now prefers the clamped WORKING sub-range `[floor, originalTo]` as a trend chip for a range-ask `outside_loaded_slice` refusal, falling back to the single-period floor chip when that window isn't fully servable. Simpler than the recorded sketch: NO manual grain/order comparison — the `echoServability` dry-run is the SOLE validity gate (runQuery REFUSES a backwards/mixed-grain/above-ceiling/gappy range, never throws), so an invalid window just falls through; only the degenerate `floor===to` is guarded (avoids "van X tot en met X"). The range attempt is throw-isolated so it can never cost the single-period fallback. Still outside_loaded_slice-only (a range retry is meaningless for "too recent" freshness), canonical + region-less (v1). 6 new tests (2 mutation-verified) + adversarial review SHIP (5 risks CLEAR). |
| 138 | Product / UX | **v2 of [#134](open-questions.md)(a) — a regional period-refusal chip (deferred from the session-43 build).** v1 is region-less only: a refusal carries NO cells, so there is no honest cell-derived region wording, and naming a region from its code is exactly the guess the answer-path generators avoid (drop-never-guess). A region-carrying period refusal (e.g. "werkloosheid in Amsterdam in 2050") therefore gets no chip today. | **✅ RESOLVED — BUILT + LIVE (2026-07-18, session 55 continued, owner-present per #118; commit `f2d015a`, CI run 29639021663 gate+deploy green).** Built exactly per the design ([session-briefs/2026-07-19-small-designs.md](session-briefs/2026-07-19-small-designs.md) §a): `buildRefusalSuggestions` gains an injected `RegionLabeler` closure wired to `regionTermsFor` (context/build.ts — registry/dimension_labels, metadata-only, fail-closed) at the respond.ts call site; chips phrase the registry label ("Wat was … in Amsterdam in 2025?"); the retry candidates carry the ask's own region codes so the echo dry-run proves the REGIONAL cells serve (mutant-pinned); any unlabelable code / no GeoDimension → no chip, byte-identical to the v1 bailout. The KIND_CODE_PREFIX/KIND_BY_PREFIX near-duplicates collapsed into one shared `regionKindForCode`. suggestions.ts confinement now source-scan-pinned (no db import ever). 43/43 suggestions tests incl. 6 new #138 cases + a regional respondToIntent wiring test; benchmark 14/14 + 6/6 + 0. Zero prompt bytes, €0 LLM. |
| 139 | Security | **Open redirect on the magic-link auth callback (`web/app/auth/callback/route.ts`), found by the session-44 authorization/ownership adversarial hunt.** The callback built its post-login redirect as `${origin}${next}` with `next` taken raw from the query string — so `?next=@evil.com` concatenated to `https://ORIGIN@evil.com` (host `evil.com` via userinfo) and `?next=.evil.com` to `https://ORIGIN.evil.com` (an attacker subdomain), both redirecting off-site after a successful login. LOW severity: the off-site redirect only fires on a SUCCESSFUL code exchange, which needs the victim's own valid one-time Supabase `code`; it does NOT read/mutate another user's data or money (the hunt's 5 lenses found ZERO ownership/money/GDPR breaks — this was the only surfaced issue, and the in-scope verifier correctly refuted it as an auth/ownership break). | **✅ FIXED + MERGED + LIVE (session 44, 2026-07-13, PR #42 squash `4e2a2fd`).** New `web/lib/safe-redirect.ts` `safeRedirectUrl(next, origin)` resolves `next` against our origin via the WHATWG URL parser and accepts it ONLY when the resolved origin is still ours (which also normalises `\`→`/` and collapses `//host`/absolute URLs so the origin check catches every off-site case); anything else → `/`. 9 unit tests pin both sides (relative paths survive; `@evil.com`/`.evil.com`/`//evil.com`/absolute/`\`/`javascript:` all collapse to the origin root). Empirically repro'd (the exploit) + verified (the fix) with the Node URL parser before shipping. |
| 142 | Data integrity | **MEDIUM — 'count' exemption can mis-attribute the axis (`validate.ts` count branch), hunt-confirmed.** `countNumbers` includes the distinct-region AND distinct-period counts; with a matching structure noun (`countContext`), a body like "in 4 gemeenten" can ground as 'count' even when the "4" is the number of PERIODS in a single-region result (or vice-versa) — a wrong-axis claim, not a fabricated magnitude. | **✅ FIXED (session 45, 2026-07-16, PR #45, squash `6291dfc`).** Counts are AXIS-BOUND: `countEntries` pairs each count (cells / distinct regions / distinct periods) with its OWN noun set; a wrong-axis noun falls through to unbacked (fail-closed). The compact adversarial review (3 lenses × refute-verify) confirmed one bypass in the first version — defensively-added 'wijken'/'buurten' let a gemeente comparison be described as "4 vergeleken wijken" while the product serves no wijk/buurt granularity — removed + pinned. Right-axis corpus/template forms measured intact (full suite + all 252 stored prod rows re-validate clean). |
| 143 | Data integrity / formatting | **MEDIUM — index-base unit "2025=100" misread as a multiplicative factor (`src/answer/compose/template.ts` displayValueUnit), hunt-confirmed.** For an index measure whose unit is a base declaration like "2015=100"/"2025=100", the display logic can render it as an "×"-style factor next to the value, implying a multiplication relationship that isn't real. | **✅ FIXED (session 45, 2026-07-16, PR #45, squash `6291dfc`) — independently repro'd first** (`displayValueUnit(118.3, 1, '2015=100')` → "118,3 (× 2015=100)"). A digit-bearing unit containing '=' now renders as a plain parenthesized label ("118,3 (2015=100)") — `parseFactorUnit` already excluded '=' units from #125a expansion (now pinned), and the OLD stored "×"-form still re-validates (R8 backward-compat pin). The format.ts NUL residual (task_e718f60d) was fixed separately the same session (PR #46, squash `f909e66`: all four deliberate raw NUL bytes across three source files are now `\u0000` escapes — byte-identical runtime, text-clean source). |
| 144 | Data integrity / core promise | **The bounded RESIDUAL of [#140](open-questions.md) — a fabricated number that exactly equals one of an answer's OWN descriptor-coordinate numbers (an age/income-bracket value, "1 januari"), placed next to that descriptor's own word, can still pass the (v3) validator.** Reason it survives: the legit coordinate echo ("mensen van 45 jaar") and the fabricated reuse ("bestaat al 45 jaar") are word-for-word identical, so no DETERMINISTIC text rule can separate them; the strict variant that would catch it rejects legit stored answers (4 measured R8 regressions, #140). | **✅ BUILT + MERGED, LIVE flag-dormant (session 46, 2026-07-16, ADR [034](decisions/034-semantic-fabrication-check.md); PR #47 squash `94b90e4`, owner in-chat approval per #118(b); main gate+deploy ✓ run 29510755050, prod HTTP 307 — go-live EXECUTED same session, owner present: calibration 9/9 FP=0 FN=0 ×3 (prompt v2), replay leg on the gate, fail-open + admin alert, flip-deploy `deabbfb` run 29513127181 ✓, smoke row 253 `skipped_no_suspects` + audit:verify exit 0 — the checker is LIVE and ACTIVE).** Shape = the brief's recommendation: an additive REJECT-ONLY cheap-tier LLM checker over validated bodies that leaned on a residual-prone exemption (`ClassifiedToken.soft` in validate.ts — corpus-measured scope: 0% of the 18 stored legit benchmark bodies trigger, both residual shapes do; date-form metadata echoes and corpus-screened year continuations are hard). A fabricated verdict takes the same R3 ladder rung (regenerate → template); verdict stored on the envelope (recorded-not-rederived, scope re-derived by R8 with tamper teeth — ADR 016 addendum); checker calls = `llm_calls` role `semantic_check`. **Owner decision (2026-07-16, in-chat, at the go-live with the measured 9/9 eval in hand): FAIL-OPEN + ADMIN ALERT — every fail-open skip e-mails the owner (audit row, user, question, error, meaning; `src/answer/audit/alerts.ts`, `ADMIN_ALERT_EMAIL`); `SEMANTIC_CHECK_FAILMODE` stays unset (ADR 034 §5-resolution).** Go-live = owner-supervised: `npm run semantic-check:record` (small real spend) + `semantic-check:eval --repeat=3` (FP=FN=0 = blocker gate) + env flags (RUNBOOK step). Original problem statement + brief for the record: DESIGN BRIEF: [session-briefs/2026-07-16-144-semantic-check-brief.md](session-briefs/2026-07-16-144-semantic-check-brief.md) (session 45 — recommended shape: an additive reject-only LLM checker, scoped to residual-prone tokens, verdict stored on the audit record for R8; fail-open-vs-closed on checker errors flagged as the owner decision). Do NOT start it at the tail of a long session. Original candidate approaches: a second structured pass that binds each body number to the specific validated cell/coordinate it claims (not just "some metadata word matches"); or an LLM re-read that flags any number not traceable to a shown cell. Low practical probability (the phrasing LLM is instructed to phrase validated results, not inject), bounded blast radius (only the answer's own descriptor numbers), but on a "never a fabricated number" product it is worth closing properly. Was ranked below #141 (period-exemption, HIGH, a broader class) — #141 is since FIXED (session 45, PR #44), which also documents a #144-class residual of its own (temporal marker + un-listed noun), making this semantic pass the shared real close for both. |
| 145 | Security / cost | **MEDIUM — unbounded LLM spend via the untrusted `PendingClarification` on the reply turn (`web/app/actions.ts` `replyToClarification`), found by the session-47 billing-path hunt (reachable TODAY).** `pending` is client-held and sent back verbatim (attacker-controlled like `rawContext`/`rawSelection`; its TS type isn't enforced at runtime); its `question`/`questionNl`/`options` flow verbatim into the clarify LLM prompt (`buildClarifyUserPayload`), but `guardLength` only bounded the top-level `reply`/`question`. So any authenticated user could send a `pending` stuffed to Next's ~1 MB body cap and drive a near-context-window prompt at the flat `simple`/`clarification` price — a cost-asymmetry drain on real Anthropic spend (the ledger stays balanced, so not a credit-conservation bug). No feature flag gates it. | **✅ FIXED + MERGED (session 47, PR #48 squash `7e42656`, owner in-chat "Go" approval per #118(b); main gate PASS + prod deploy).** New `guardPending()` extends the existing `MAX_INPUT_LENGTH` (2000) belt to every prompt-bound pending field + caps the option/axis lists (20); throws BEFORE the gate debits (no charge, no LLM call, no audit row), mirroring `guardLength`. Not the deferred Phase 1–2 rate limiting — just closes a gap in an existing bound. Verified pre-merge: backend 1338/1338, web 323/323 (9 new), benchmark 14/14 + 6/6 + 0 fabricated, real next build. |
| 147 | Money-conservation / hardening | **LOW (defense-in-depth) — no DB constraint bounds a compensation's credited amount to the debit it reverses (`migrations/008` `credit_transactions_validate_compensation`, widened by 013/018), session-47 hunt.** The trigger checks same-user + valid debit-reason but never that the compensation `delta` ≤ the reversed debit's `|delta|`. NOT reachable today: every live caller passes a bounded amount (gate uses `required`/`required−clarifyPrice`; the onboarding job reads the *stored* debit's own delta; web add-on reuses the once-read price). Migration 008 already made the *user* check structural "for a future admin refund tool" — the amount check is the natural third guard. | **Recorded, NOT built.** Next migration: extend the trigger to also assert `new.delta <= -debited.delta`. Defense against a future caller (admin refund tool / hand-run fix / a feature that re-reads a price) that could over-credit with no error. **✅ RESOLVED — MIGRATION FILE + HERMETIC TEST ONLY, LIVE APPLY NOT DONE (2026-08-27, session 66, autonomous, branch `fix/146-150-money-path-hardening`):** `migrations/023_compensation_amount_bound.sql` extends `credit_transactions_validate_compensation` (`CREATE OR REPLACE`, same trigger binding as 008/013/018) to assert exactly `new.delta <= -debited.delta`, as specified. 6 new hermetic PGlite tests in `tests/billing/ledger.test.ts` prove both directions: an exact-boundary full refund and a bounded partial refund still succeed, an over-credit (off-by-one AND wildly oversized) is rejected with `/exceeds the debit it reverses/`, and the bound applies identically across all three debit reasons (`question_cost`/`onboarding_cost`/`websearch_cost`). Numbered 023, not 022 — 022 was independently claimed by a concurrent sibling autonomous session's own migration on a different branch (session 66 Batch 2); caught and renumbered before push to avoid a collision once both land on `main`. **⚠ The LIVE `npm run db:migrate` apply against the real/production database was NOT run this session — that step is explicitly owner-supervised per this task's own brief and stays undone; this row is NOT fully resolved until that step happens.** **✅ PR #101 merged into `main`, session 67 (2026-08-28, `bded817`).** **✅ The live apply HAPPENED 2026-09-02 (session 69, owner present): migrations 022–025 applied to production in one `npm run db:migrate`, verified clean (`schema_migrations` read-only check repeated 2026-09-03, session 71) — nothing left open here.** |
| 148 | Correctness / display | **LOW (display-only) — onboarding `netCost` re-reads the live price instead of the amount actually debited (`web/app/actions.ts` `maybeTriggerOnboarding` 'started' branch), session-47 hunt.** The 'started' branch sets `netCost` via a fresh `onboardingPrice(getDb())` read, a second read distinct from the one `triggerOnboarding` used to debit. `action_class_prices` is a live plain-UPDATE table, so an owner reprice racing a live trigger makes the client's optimistic balance drift from the (correct) ledger by the price delta. Self-heals on page refresh (history.ts computes cost from the ledger); the debit itself is always correct. Exactly the reprice-drift class `refundOnboarding` already fixed for the refund direction. | **Recorded, NOT built.** Thread the actually-charged amount out of `triggerOnboarding` (add `credits`/`amountCharged` to the 'started' result) and use it for `netCost`, eliminating the second read — mirroring `src/ingestion/onboarding.ts`'s "never a fresh price read" fix. **✅ RESOLVED (2026-08-27, session 66, autonomous, branch `fix/146-150-money-path-hardening`):** `TriggerOnboardingResult`'s `'started'` variant (`src/ingestion/onboarding-trigger.ts`) now carries `credits: number` — the SAME `required` value already read once at the top of `triggerOnboarding` and used for the debit itself, never a second read. `web/app/actions.ts`'s `maybeTriggerOnboarding` 'started' branch now sets `netCost: result.credits`; the now-unused `onboardingPrice` import was removed from that file entirely. A new test in `tests/ingestion/onboarding-trigger.test.ts` pins `credits` against a REPRICED heavy class (150, not a hardcoded 100), and the existing `web/app/onboarding-wiring.test.ts` source-scan pin was updated to assert the new `netCost: result.credits` line and that the old fresh-read line is gone. **✅ PR #101 merged into `main`, session 67 (2026-08-28, `bded817`).** |
| 149 | Robustness / input | **LOW — `requestId` is never validated as a UUID before hitting the uuid-typed `credit_transactions.request_id` (`web/app/actions.ts` askQuestion/replyToClarification), session-47 hunt.** A malformed `requestId` throws "invalid input syntax for type uuid" inside `reserveDebit`'s transaction (which cleanly rolls back — fail-safe for money, no debit, no spend) but propagates to a masked 500. Only self-affecting; the real client always sends `crypto.randomUUID()`, and `chat.tsx` catches the rejection and shows a normal retry. Category (f) crash-a-money-path with NO financial consequence. | **Recorded, NOT built.** Validate `requestId` as a UUID at the top of both actions (alongside `guardLength`) and degrade cleanly, mirroring the `validateSelection`/`validateConversationContext` never-throw-on-untrusted-input pattern — mainly for cleaner logs/observability. **✅ RESOLVED (2026-08-27, session 66, autonomous, branch `fix/146-150-money-path-hardening`):** `guardRequestId` (`web/app/actions.ts`) now also calls `isUuid` (`web/lib/trial.ts` — the SAME helper the trial action already used for this exact check, reused rather than duplicated) alongside the existing length/type check, so a well-typed-but-non-UUID `requestId` throws a clean guard-clause error before any DB call, instead of surfacing as a raw Postgres "invalid input syntax for type uuid" error from inside `reserveDebit`'s transaction. Money/user-facing behavior is unchanged either way (fail-safe rollback, `chat.tsx`'s existing retry) — this is the logs/observability improvement the row asked for, via the SAME throw-early pattern `guardLength`/`guardRequestId` already used (closer reading of "mirror the never-throw pattern": the trial action's own analogous fix, `trial-actions.ts`, throws too — the "never-throw" framing describes not gating money spend on garbage input, not literally returning a fallback). Every test call site using the old meaningless `'rid'` placeholder (`web/app/actions.test.ts`, `web/app/actions-threads.test.ts`) now uses a real UUID constant; the malformed-requestId test gained three new format-invalid-but-right-typed cases, and the now-obsolete "accepts at the length boundary" test was removed (a valid UUID is always 36 chars, well under the old 100-char ceiling). **✅ PR #101 merged into `main`, session 67 (2026-08-28, `bded817`).** |
| 150 | Robustness / race | **LOW (latent landmine, not reachable today) — the onboarding stale-row reclaim is time-only, with no structural guard tying `STALE_RUNNING_MS` to the cron's `maxDuration` (`src/ingestion/onboarding.ts` / `onboarding-store.ts`), session-47 hunt.** `reclaimStaleRunning` flips a `running` row back to `pending` purely on `claimed_at` age (20 min), with no claim-generation token. If two invocations ever processed one row concurrently, one could refund while the other delivers (user keeps answer + refund). NOT reachable today: Vercel `maxDuration=300s` (5 min) hard-kills any invocation, a 4× margin under the 20-min threshold, so a still-alive invocation can never be reclaimed. Becomes reachable only if a future deploy raises `maxDuration` toward/past `STALE_RUNNING_MS` without co-updating it. | **Recorded, NOT built.** Cheap footgun guard: a unit test / runtime assertion that `STALE_RUNNING_MS >= N × (cron route maxDuration)`, `N>=3`, so a future `maxDuration` bump can't silently reopen the window. Optional stronger fix: a claim-generation token on `claimOnePending`/`reclaimStaleRunning` + conditional terminal writes. **✅ RESOLVED (2026-08-27, session 66, autonomous, branch `fix/146-150-money-path-hardening`):** a new regression-guard `describe` block in `tests/ingestion/onboarding-job.test.ts` reads `web/app/api/onboarding-cron/route.ts`'s own source (never a hardcoded/duplicated copy that could drift) to extract its declared `maxDuration`, and asserts `STALE_RUNNING_MS >= 3 × maxDuration` (currently 20 min vs. 300s = a measured 4× margin, pinned by its own dedicated test so a change to either constant shows up as a failing test with both values in the diff) — mirroring the existing "SKIP LOCKED source pin" pattern in `tests/ingestion/onboarding-store.test.ts` for a guarantee the hermetic harness cannot otherwise exercise (there is no way to simulate "a cron invocation is still alive at minute 6"). Pure regression guard, zero behavior change — `STALE_RUNNING_MS` and `maxDuration` are both untouched. The row's optional stronger fix (a claim-generation token) was NOT built — out of scope for this batch, left as a future option if the margin is ever eroded. **✅ PR #101 merged into `main`, session 67 (2026-08-28, `bded817`).** |
| 151 | GDPR / data erasure | **HIGH + MEDIUM — GDPR redaction MISSED `pending_table_requests.fit_note` (free-text) and the topic-disclosing CBS table identity (`table_id`/`candidate_ids`/`resolved_table_id`), found by the session-47 GDPR-redaction hunt (2 lenses, dual-confirmed).** Migration 015 (live in prod since 2026-07-10) added these columns AFTER the #120 redaction leg was designed; `retention.ts` cleared only `question_text`/`topic_term`/`failure_summary`. **`fit_note` (HIGH)** = the fit-gate LLM's Dutch sentence (`${measureCode}: ${reading}`, prompt fed the verbatim user question) — it paraphrases the user's topic/period, and survived both self-service delete AND the 2-year purge, on a row keeping the unredacted `user_id`. **table identity (MEDIUM)** = CBS catalog keys that disclose the topic — the SAME class `audit_answers.table_ids` is already cleared for (a session brief wrongly claimed "ids untouched, mirroring the audit-row posture"; the audit posture CLEARS table identity). DB-access-only (RLS blocks anon/authenticated), but survives the user's OWN erasure request — the core invariant. Independently re-verified before fixing (migration 015 columns, the LLM `reading` source, the index scope). | **✅ FIXED + MERGED (session 47, PR #49 squash `af287e1`, owner in-chat approval per #118(b); main gate PASS + prod deploy).** A shared `PENDING_REDACTION_SET` (both legs) now clears `fit_note` on ALL in-scope rows and `table_id`(→`REDACTED` sentinel, it's NOT NULL)/`candidate_ids`(→`[]`)/`resolved_table_id`(→null) on TERMINAL rows only (a `pending`/`running` row's in-flight job still needs its table id — the documented in-flight residual, extended). +5 tests in `tests/audit/retention-onboarding.test.ts`; corrected the wrong "mirrors the audit-row posture" line in [05-data-rules.md](05-data-rules.md) + the smalls-133 brief; `slice_note` verified topic-free (table-slicing mechanics only), correctly left. Verified pre-merge: backend 1343/1343, benchmark 14/14 + 6/6 + 0 fabricated, real next build. **NOTE: this only fixes it going FORWARD — any pre-existing prod rows written before the deploy still carry unredacted `fit_note`/table-identity until a purge or a re-deletion runs over them; a one-off backfill sweep is worth a supervised step (tracked here).** **Measured 2026-09-03 (session 72, autonomous, read-only against production): `pending_table_requests` holds 4 rows, 0 of them redacted — no row was redacted before the forward fix, so there is nothing to backfill; the fixed redaction legs cover these rows when their turn comes (2-year purge or self-service delete). No sweep script needed; nothing left open here.** |
| 154 | Data integrity / honesty | **MEDIUM-HIGH (real, but design-blocked + ≈nil exposure today) — a RETAINED "missing" cell is served with a false "gesynchroniseerd op {vandaag}" date and never trips the staleness warning, found by the session-47 ingestion hunt (CONFIRMED ×2).** Under the documented keep-and-count policy ([#32](open-questions.md)), a cell CBS stops publishing is retained (never deleted) if the total row count stays within ±20% tolerance. But `pipeline.ts:636` bumps `cbs_tables.last_sync_at = now()` unconditionally on every successful sync, and the answer path attaches that TABLE-level timestamp to EVERY served cell as `attribution.syncedAt` (`run.ts:391`), which `format.ts` renders as "Gegevens gesynchroniseerd op {date}" and `staleness.ts` uses to compute age. So a retracted/orphaned cell shows as freshly-reconfirmed and its staleness net never fires. The value itself is still a REAL past CBS cell (principle (a) holds) — it's the DATE honesty + the staleness safety-net that break, on the public "source and date shown" claim. | **Recorded, NOT built — needs a DESIGN decision, not a patch.** The finder's fix sketch (use per-cell `batch_id` → `ingestion_batches.finished_at` for `syncedAt`) is **flawed**: `batch_id` tracks LAST-CHANGE, not last-confirmation (the is-distinct-from upsert guard means an unchanged-but-reconfirmed cell keeps its OLD batch_id), so it would mis-date reconfirmed cells the other way. A correct fix needs a NEW per-cell `last_confirmed_batch_id`/`last_seen_at` written on EVERY sync where the cell is present (even if the value is unchanged), PLUS a decision on how to aggregate a multi-cell result's one displayed "synced on" date (oldest? per-cell?). Exposure ≈ nil today (one user; stable Phase-0 headline tables where cell-level retractions are rare — CBS usually CHANGES a value, caught by the corrections diff, not removes a cell). Owner's call on priority; a proper WP.. **Design written (2026-07-19 overnight design marathon): [session-briefs/2026-07-19-154-design.md](session-briefs/2026-07-19-154-design.md) — NULL-sentinel `last_seen_batch_id` (track absence, not presence: writes ∝ anomalies), mark-on-absence/clear-on-reappearance sync rules, aggregation = oldest cell date (min), staleness fires naturally, migration 021 file-only + flag-free deploy-order-safe rollout, 6 pinning tests incl. the s47 repro. One session, €0 LLM.** **✅ BUILT + LIVE (2026-07-24, session 55 continued, owner-present "ga verder"; €0 LLM, zero prompt bytes):** migration 021 applied to prod in the supervised window (additive nullable column; 0 marked rows = exactly today's behavior) BEFORE the code deploy — **measured design correction: the brief's "code first, apply later" deploy-order claim was inverted by the implementation** (the new `select … last_seen_batch_id` breaks pre-apply; ship order flipped to apply-then-deploy, recorded in lessons). Shipped per design otherwise: pipeline mark-on-absence (prior-batch value with the pre-migration own-batch_id fallback) + clear-on-reappearance OR-term; run.ts min effective date (sideband — the marker stays OFF ResultCell); staleness retained-branch wording *"een deel van deze cijfers is door CBS sinds {date} niet opnieuw bevestigd"*. Proven: the full s47-repro life cycle + transition lower bound in `tests/ingestion/retained-cells.test.ts` (2 scenarios covering the 6 design tests), full verification block green, `audit:verify -- 1 256` unchanged clean. |
| 155 | Data integrity / quarantine | **LOW (clear-cut, cheap, reachable) — the freshness-METADATA helpers bypass the `needs_review` quarantine gate, session-47 hunt (CONFIRMED).** The VALUE path is airtight: `resolveIntent` (`src/query/resolve.ts:332`) refuses a quarantined table with `table_quarantined` before serving any number. But `freshestForCanonical` (`run.ts:203-253`) and siblings (used on forecast/causal/out-of-scope refusal + clarify dry-run paths) SELECT `cbs_tables` without a `status='active'` filter — so a quarantined table's freshest-period METADATA (a period label, never a value) can appear in a refusal/offer message. No fabricated number, but it references a table we flagged as broken. | **✅ FIXED + MERGED (session 47, PR #50 squash `b654010`, bundled with #156, owner "merge" per #118(b); main gate PASS + prod deploy).** `freshestForCanonical` (`run.ts`) now SELECTs `status` and returns null for a non-active table (`freshestForCanonical` was the ONLY ungated `cbs_tables` reader in query/ — the others go through `resolveIntent`'s gate). +2 tests (`tests/query/freshest-quarantine.test.ts`). Verified pre-merge: backend suite green, benchmark 14/14 + 6/6 + 0 fabricated, real next build. |
| 156 | Data integrity / adapter | **MEDIUM (plausible, reachability unproven) — `fetchObservations` re-fetches `/Dimensions` independently of `fetchTableSchema` (two live HTTP calls per sync), session-47 hunt.** `pipeline.ts` gets the dimension list once via `source.fetchTableSchema` (validated by the 5 checks) and again inside `ODataV4Source.fetchObservations` (`odata-v4.ts:170-171`) — a TOCTOU where a coordinate/dimension shifting between the two live fetches could let observations be shaped against a dimension set the checks never validated. User-reachable via the on-demand-onboarding job (`onboarding.ts:173` calls the same `syncTable`). Both verifiers PLAUSIBLE (real double-fetch, but a concrete wrong-number path within one sync window is unproven). | **✅ FIXED + MERGED (session 47, PR #50 squash `b654010`, bundled with #155, owner "merge" per #118(b)).** `CbsSource.fetchObservations` gained an optional `dimensionNames?` param (backward-compatible — the conformance harness + other callers unchanged); `pipeline.ts` now passes the already-validated `schema.dimensions`, so `odata-v4.ts` skips the redundant `/Dimensions` fetch and ONE validated dimension set governs the whole sync (closes the TOCTOU + removes a live HTTP call per sync). +1 adapter test (asserts no `/Dimensions` fetch when names supplied). |
| 162 | Architecture / answer pipeline | **Number-free phrasing ("slot-filling") experiment — make fabrication structurally impossible instead of detected** (spar-session advice #4, 2026-07-17; owner: "klinkt echt uitstekend"). The idea: the phrasing LLM writes the answer body with typed placeholders ("De inflatie daalde in {periode} naar {waarde1}") and deterministic code fills the slots AFTER phrasing from the validated result — numbers never flow through the LLM's *output*, so a fabricated digit becomes structurally impossible rather than something `validate.ts`/the [#144](open-questions.md) semantic checker must catch (both become second-line defenses). Owner's design question, answered for the build session: doesn't the AI need the numbers for conclusions/correlations/charts? **No** — charts are already LLM-free (ADR [007](decisions/007-chart-spec-rendering.md)); conclusions are already registered derivations (R5/R9: code computes, the LLM only words them); correlations are refused by design. What the LLM needs is semantic metadata ABOUT the values (measure, period label, direction, magnitude class, unit) — not the digits. The pattern generalizes to later analyses and the [#158](open-questions.md) Studio suggestions: *code computes a menu of true facts → the AI selects/words them → code fills and verifies.* | **Recorded as a candidate experiment, NOT scheduled (Stefan, 2026-07-17).** Run as an A/B against the current pipeline on the full benchmark; the bar = equal-or-better Dutch phrasing (watch grammar around numbers — "één op de acht", unit/plural agreement — the known hard part of slot-filling). If it wins: an ADR + a new rung in the R3 ladder (the template rung stays). Explicitly NOT a rewrite of the working, tested pipeline — additive experiment first.. **ADR-draft written (2026-07-19 overnight design marathon): [session-briefs/2026-07-19-adr-draft-slot-filling.md](session-briefs/2026-07-19-adr-draft-slot-filling.md) — typed-slot contract, defense-layer table (R3/R10/R11 become structural, #144 unnecessary on the slot path, word-form + R9-direction checks stay), A/B meetopzet (34 questions, blind pairwise judge + owner read-back, ~€1-2), flag rollback. Measured correction: a compose-prompt change does NOT trigger #164 — the answer-fixture set (15 files) is its own hash domain; old+new fixtures coexist.** **✅ RESOLVED — HERMETIC HALF BUILT (2026-08-28, session 66, autonomous; branch `feat/162-slot-filling-hermetic`, PR #113 for owner review per #118(b)).** The §1–§3 mechanism is implemented additively behind `SLOT_PHRASING_ENABLED` — **OFF by default, OFF in production, flag-off behavior proven byte-identical** (request bytes AND envelope, test-pinned in `tests/answer/slot-compose.test.ts`; the 15 legacy answer fixtures are untouched and still replay green). Built: the typed-placeholder payload (`src/answer/compose/slots.ts` — NO value, NO period label, no digits outside slot ids reach the model), the deterministic filler on the template rung's own formatters (R10 unit adjacency + R11 provisional marking filler-owned/structural), the §1 pre-fill rules (zero digits NFKC-normalized outside the closed `{slot}` menu, unknown/malformed placeholder reject, ≥1 value slot, word-form rejection shared verbatim with the legacy validator, placeholder-level R9 binding), the new FIRST rung in the ADR-013 ladder (slot → strict retry → the byte-unchanged template floor; `templateOnly` still wins; #144 checker deliberately not called on slot bodies per §2), the full legacy validator as a belt on every FILLED body, R1/R8 (the envelope's present-only `slotPhrasing` record stores the raw placeholder body + slot map; reconstruction re-derives the map from the stored result and re-fills the raw body byte-identically — `tests/audit/slot-phrasing-r8.test.ts`), and `answer:eval`'s `--slots` mode (the §5 coexisting-fixture mechanism — **slot fixtures NOT yet recorded**, that is live spend). **Explicitly NOT done, stays owner-supervised:** the §6 A/B (blind pairwise judge + owner read-back, ~€1–2) — so NO phrasing-quality claim exists; slot-fixture recording; promotion of the draft to `docs/decisions/`; any flag flip. One documented hermetic-half interpretation: the "filler never starts a sentence with a bare digit" mitigation is prompt-side only (slot rule 8), not a blocking validator rule — the §1 list has exactly 6 rules and enforcement calibration belongs to the A/B (noted in the draft's as-built addendum). **✅ PR #113 merged into `main`, session 67 (2026-08-28, `6fdae6f`).** **Session 79 (2026-09-05) — §6 A/B attempted
3× across two owner-authorized real-spend windows, STOPPED before reaching a phrasing verdict, owner's
explicit call after a full walkthrough of the remaining gap.** Round 1 recording hit 2 hard-gate failures
before the judge could run (18 template-falls across 82 slot calls, 1 unrecovered digit-leak) — 3 root causes
diagnosed from the actual failure data. Round 2 hit an unrelated tooling failure (a worktree-isolation bug,
[RUNBOOK](RUNBOOK.md)) before any fix could be applied — zero code changed, zero fixtures touched. Round 3: all
3 diagnosed bugs fixed + a 4th found live during end-to-end verification (the pre-fill fix for bug 3 was a
no-op until the POST-fill validator belt was also patched) — template-falls dropped 18→3, confirmed via a
git-stash-controlled full-suite run (1806/1807, the 1 failure a pre-existing worktree-environment gap) and an
independent benchmark re-run (GATE PASS, legacy pipeline unaffected throughout, 0 fabricated every round).
**Remaining gap: 2 of 34 cases still fail a DIFFERENT, independent same-sentence check — `checkBinding` in
`validate.ts`'s own R9 pass — a second implementation of nearly the same rule already fixed once elsewhere this
round, fused into the shared validator's logic in a way that isn't safely patchable after the fact** (the
earlier fixes could each filter a known problem-string post-hoc without touching core logic; this one needs
the flagged claim's own derivation data, only available by editing `checkBinding` itself a second time this
round, or duplicating its logic separately with drift risk). **Owner decision: stop here, not a 4th attempt** —
31/34 (91%) clean recorded as the measured result. Judge never run — no phrasing-quality claim exists, win or
loss. Cumulative spend across all 3 rounds: ≈$1.14 (~€1.05). Branch `experiment/162-slot-filling-ab` (worktree
`agent-ad8ec20c3cba7eebd`, head `f26d01e`) kept, not merged, not deleted — a working 91%-fixed foundation for
whenever this is picked up again; next step named: fix or duplicate `checkBinding`'s R9 same-sentence check for
the `verschil*`/derivation case specifically. **Session 80 (2026-09-05): the round-3 "op" Tier-B fix EXTRACTED
and shipped standalone, independent of the rest of this parked experiment** (`main`, `f40a7a9`) — only the
`TEMPORAL_BEFORE` marker addition in `src/answer/compose/validate.ts`, not the `REGION_COUNT_NOUN_WORDS` export
that commit `f26d01e` bundled in for the still-parked slot-filling code (that export has no consumer outside
`slots.ts` and would be dead code on `main`). Fixes a real, live-production correctness bug independent of
slot-filling: a bare "Op &lt;year&gt; ..." opener with no adjacent month name (e.g. "Op 2024 telde Nederland
...") had no way to ground the year as temporal and was wrongly flagged as a possible fabrication. Full
verification (typecheck, backend 118/118 files 1798/1798 tests incl. 3 new, benchmark GATE PASS, web 51/51 files
605/605 tests, real `next build`, `/code-review` low: 0 findings) — see the commit message for the complete
safety argument (reuses the same quantity-noun veto and `SAFE_YEAR_CONTINUATIONS` gate as every other marker,
verified it doesn't reopen the "van"/"met" hole). **The `checkBinding` gap above is now FIXED too (round 4,
2026-09-05, owner-authorized: "go do the checkBinding fix on #162"), on the SAME parked experiment branch**
(`experiment/162-slot-filling-ab`, commit `7dd196a` — not main, not merged, `SLOT_PHRASING_ENABLED` still off).
Threaded an opt-in `relaxDerivationSameSentence` parameter from `validateFilledSlotBody` through
`validateAnswerBody` into `checkBinding` itself: when every one of a derivation's own source periods already
passes the pre-existing body-wide backstop, the same-sentence push is skipped — mirroring the SLOT-R9 pre-fill
relaxation exactly, for the identical B13/A054/A085 shape. No legacy/template pipeline caller passes the new
parameter (default `false`), so the 2026-07-03 adversarial finding this rule protects against (a lone derivation
value with no period anywhere) still fails there exactly as before — proven by a dedicated regression test, not
just asserted. Verified: typecheck clean (root+web), backend 118/118 files 1810/1810 tests (+3, matches the 1807
baseline exactly), benchmark GATE PASS, web 50/50 files 585/585 tests, real `next build`, `/code-review` low: 0
findings, plus a hermetic re-validation of all 34 recorded A/B pairs against the fix (zero LLM calls/spend) —
0 new regressions (one unrelated pre-existing case, A068, a genuine raw-digit leak, incidentally also now clean —
not investigated, out of scope). **Caveat, stated plainly rather than overclaimed:** the fix could not be
replayed against B13's own exact historical repeat-2 failure text — `composeRepeated`'s resume format only
persists attempt-0's body per pair, not every repeat — so the proof is the unit tests reproducing the documented
problem string byte-for-byte against the real derivation shape/values, not a literal replay of that one archived
run. **Still NOT done, still owner-supervised:** the §6 A/B judge (blind pairwise phrasing quality, real spend) —
no phrasing-quality claim exists yet, win or loss, same as every prior round; any flag flip; any merge to `main`.

**§6 A/B JUDGE RUN, same session, owner-authorized ("Yes, run the judge"): FAILS BOTH PHRASING GATES — the
slot-filling approach does NOT clear the bar.** Blind pairwise judge (`experiment/162-slot-filling-ab`, `fc472f7`;
Haiku 4.5, 3 votes/pair, 34 pairs, $0.1719 real spend) against the now-hard-gate-clean recorded pairs: slot wins
6/34, legacy wins 20/34, ties 8/34 — win-or-tie 14/34 = **41.18%** (gate: ≥60%, **FAIL**); at least one vote
flagged a grammar error in the slot text for **9/34 pairs** (gate: 0, **FAIL**). Per the original 2026-07-17
decision ("the bar = equal-or-better Dutch phrasing... Explicitly NOT a rewrite of the working, tested pipeline —
additive experiment first"), a result this far under both gates means: **do not adopt as-is** — no ADR, no flag
flip, no merge. The four rounds of validator fixes (2026-09-05) were not wasted: template-falls were masking the
LLM's actual phrasing quality behind a fallback, so a true measurement was impossible before they landed — this
IS that true measurement, and it says no. Two concrete, judge-cited failure patterns, both diagnostic of WHY
(worth reading if this is ever revisited): (1) the slot template's bare-year opener ("Op {periode}" → "Op 2024
telde Nederland…") is repeatedly flagged as ungrammatical Dutch ("moet 'Op 1 januari 2024' of 'In 2024' zijn") —
the EXACT construction this session's own Tier-B fix (`main` `f40a7a9`) had to rescue from the validator, because
it's a real, honestly-sourced number, not a fabrication; the validator fix was correct, but the underlying
phrasing CHOICE reads as awkward Dutch regardless of its validator legality — a prompt/template quality issue,
not a validator bug, and a candidate fix (prefer "In `<year>`" over "Op `<year>`" for a bare-year opener) if a
round 5 is ever authorized. (2) Restating a derivation's own value again in a later sentence — the exact shape
BOTH the round-3 SLOT-R9 relaxation and the round-4 checkBinding relaxation made validator-legal — reads as
redundant/"houterig" when the repeated sentence adds no new information (A085: "herhaalt onnodig '101.085
inwoners' aan het einde"). Full votes + per-pair verdicts + reasoning: `benchmark/ab-162-judge-votes.json` /
`benchmark/ab-162-judge-report.json` on the experiment branch.

**Round 5 (2026-09-06, session 83, owner-authorized): two targeted prompt-level fixes for the two named patterns above — STILL FAILS both gates, WORSE than round 4.** `buildSlotSystemPrompt` gained rule 10 (prefer "in" over "op" before a bare-year period placeholder — periodKind 'jaar' only, the confirmed pattern) and rule 11 (don't reuse a value/verschil placeholder in a later sentence that adds no new information — the A085 pattern), `SLOT_COMPOSE_PROMPT_VERSION` 1→2. Required a fresh FULL re-recording (the prompt change touches every slot call, not just the failing ones): $0.463 spend, 82 slot compose calls, **0 fabricated** (the structural guarantee held throughout). Hard gate: 4 template-falls (B6, A025 ×2, A068) — all four the SAME pre-existing, out-of-scope residual (the model echoing the literal "1 januari" from the woningvoorraad measure's own `definitionLabel` text, not a value/period placeholder — matching the already-documented A068 case from rounds 3–4), not caused by rules 10/11; 2 of those 4 (B6, A025) happened to template-fall on the specific repeat used for the judge's `slotBody`, so those 2 pairs were effectively judged as template-vs-legacy rather than slot-vs-legacy. **Judge re-run ($0.171, Haiku, 3 votes/pair): win-or-tie 10/34 = 29.41%** (gate ≥60%, FAIL — worse than round 4's 41.18%); **9/34 pairs still grammar-flagged** (gate 0, FAIL — same COUNT as round 4 but a DIFFERENT set of pairs, not the same ones fixed). The two targeted fixes did not net-improve phrasing quality, and fresh sampling surfaced grammar patterns neither rule addressed: quarter-period word order ("in 2025 4e kwartaal" instead of "in het 4e kwartaal van 2025" — periodKind 'kwartaal' was deliberately left out of rule 10's scope, since only the jaar case had judge evidence), a duplicate-unit-mention bug ("mln kWh" stated twice — a pre-existing rule-5 violation, unrelated to this round's edits), and one pair (B13) whose judge complaint names both a "logical inconsistency" and unnatural repetition together, consistent with rule 11 occasionally causing the model to garble period ordering while trying to avoid a repeat rather than actually fixing the redundancy. Full votes/verdicts (same filenames, overwritten): `benchmark/ab-162-judge-votes.json` / `benchmark/ab-162-judge-report.json` on the experiment branch (commit `c31fd91`). **Cumulative spend across all 5 rounds: ≈$1.77.** **This is now two independent judge runs (rounds 4 and 5) both failing both gates, with round 5 measurably worse — the evidence increasingly reads as "slot-filling's phrasing quality has a broader gap than a short list of fixable prompt patterns," not as "two bugs away from parity."** **✅ CLOSED (Stefan, 2026-09-06, session 83, in-chat: "Accept as final on #162").** Round 5 stands as the experiment's final verdict, per the session's recommendation. Per the pre-committed rule: the branch (`experiment/162-slot-filling-ab`, local-only, never pushed) stays parked as documented history, un-merged; `SLOT_PHRASING_ENABLED` stays unset/off permanently — no further rounds; the ADR-draft ([session-briefs/2026-07-19-adr-draft-slot-filling.md](session-briefs/2026-07-19-adr-draft-slot-filling.md)) is NOT promoted to an accepted ADR (the 2026-07-17 bar — "equal-or-better Dutch phrasing" — was not met, twice, with a worsening trend). The number-free-phrasing STRUCTURAL idea itself (code computes a menu of true facts → the AI selects/words them → code fills and verifies) is not refuted by this result — only THIS prompt's phrasing quality is; the pattern stays available to reconsider for a future measure/shape where the LLM has an easier time (e.g. fewer period-grain/unit edge cases), but that would be a fresh experiment, not a round 6. |
| 165 | Registry / product | **Curated 83693NED vs the prod-onboarded uncorrected sibling 83694NED: the everyday term "consumentenvertrouwen" would exist twice in the live intent vocabulary** (the curated key + the WP16 `onboarded:83694NED:*` rows from session 28), risking new clarification rounds or the model picking the uncorrected series for the headline question. | **✅ DONE LIVE (session 49, 2026-07-17; decision per the werkloosheid precedent, docs/05 canonical defaults): the seasonally-adjusted curated key OWNS the everyday term.** Executed go-live step: trimmed the bare term from the `onboarded:83694NED:*` rows' `everyday_terms` in prod (keep the rows + distinct uncorrected-flavored terms so the table stays reachable and audit reconstruction is untouched) — supervised DML alongside register/sync/registry:apply — **executed 2026-07-17: 115/115 rows trimmed, 0 rows still carry the bare term (verified by re-query); curated key answers live (−39, juni 2026, LLM-free runQuery check).** The 83694NED table itself stays ingested and servable. **Measured pre-trim (prod read-only, 2026-07-17 session 49): 115 `onboarded:83694NED:*` rows, EVERY one carrying the bare term "consumentenvertrouwen" as its first everyday term** (the WP16 v1 auto-derivation tags every measure with the topic term) — i.e. the live prompt already carried ~115 near-duplicate vocabulary lines for one table; trim transform = drop the bare term + suffix " (ongecorrigeerd)" on title-terms colliding with the curated keys. The 119-line noise itself is a WP16-vocab-v1 quality observation feeding [#166](open-questions.md)/[#112](open-questions.md). |
| 167 | Ingestion / CBS quirk | **Phantom measures: CBS MeasureCodes metadata can list measures that carry ZERO observations in the ENTIRE table** (85880NED: 17 of 210 codes, each probed individually live 2026-07-17 — real-looking entries incl. units, e.g. `320000` "19 Aardolie-industrie"). The per-measure `row_plausibility` check (and `unit_consistency`'s reverse leg) then quarantines even a fully healthy FULL ingest — which also retro-explains part of the s49 "26/210 zero-row measures" lean-slice refutation (17 of those 26 were phantom table-wide, only 9 were flavor-dependent). The overnight specs' "no adapter changes needed" claim did not survive this. | **✅ RESOLVED (session 50, 2026-07-17): curated seed-level exclusion.** `Phase0Table.excludeMeasures` — measured + per-code documented lists; registration/sync treat those codes as not-published (skipped in units, unit-consistency and per-measure plausibility; fetched rows for them dropped), while the schema FINGERPRINT deliberately stays UNFILTERED so any CBS change to the phantom set still fails the drift check loudly. Validator strictness for unexcluded tables is pinned by a counter-test (`tests/ingestion/ingestion.test.ts` #167). Sprint consequence: the RUNBOOK curated-table procedure gained a phantom-measure probe step; tables #4-#9 should run it before freezing their specs. WP16 self-onboarded tables with phantom measures still quarantine (pre-existing behavior; the fit gate/refund path handles it) — acceptable, tracked here. **Session-53 extension (2026-07-17): the same mechanism covers SLICE-EMPTY measures** — rows table-wide but ZERO within the registered slice (85828NED: the 7 Productie-family measures exist only for industry branches, never retail; probed per measure within the slice). The RUNBOOK probe step + docs/11 quirk #4 now say: probe WITHIN the registered slice first, then table-wide to classify (phantom vs slice-empty). |
| 168 | Coverage sprint / registry | **Tables #4-#9 are BUILT DORMANT (session 53, 2026-07-17, autonomous prep branch): seeds + registry defaults + fixtures + CC11-CC31 frozen (all values v3+v4+fixture-verified), hermetic gate green — but NO vocabulary yet and NOT live.** Per #164 batch discipline + #118(b) (autonomous sessions: branch + PR, zero live-LLM spend, no live syncs), three owner-present steps remain: (1) review + merge the prep PR; (2) apply the STAGED vocab batch ([session-briefs/2026-07-17-coverage-4-9-vocab-batch-staged.md](session-briefs/2026-07-17-coverage-4-9-vocab-batch-staged.md): canonical keys, AVAILABLE_GRAINS, REGIONAL_KEYS, #165 collision analysis) + ONE fixture re-record (Haiku-tier, sub-euro) + re-point CC intents at canonical keys, **plus the `--catalog-add` merge + `tablefinder:record` re-record (measured s53: the catalog-fixture merge alone flips 4 of 11 finder-replay cases to failure-safe `disclose` — shortlist shift, fixture hash-miss; reverted from the prep PR, brief step 4b)**; (3) live prod syncs (`sync <id>` ×6, auto-registers) + `registry:apply` AFTER the deploy is verified live (RUNBOOK step 4 order, #166 finding 8), + LLM-free spot-checks. | **✅ RESOLVED — ALL THREE STEPS EXECUTED (session 54, 2026-07-18, owner-present).** (1) PR #56 squash-merged `5e3a8e2`; (2) the vocab batch (10 canonical keys + prompt v6 + ONE #164 re-record over SIX calibration rounds — final gate intent 72/72 ×3 ZERO flips, followup 23/23, clarify 7/7, tablefinder 11/11; three reasoned relabels; the B16 region_unknown options gap fixed; measured LLM spend ~€10-12, NOT the hoped sub-euro — calibration loops multiply) pushed `49135ef`, gate+deploy green; (3) live syncs ×6 (batches 19-24, 136,511 rows, 0 corrections, 0 quarantines — 85828NED's excludeMeasures proved itself live; no chunk hatch needed) + `registry:apply` (17 tables / 26 keys) AFTER verified deploy + 10 LLM-free spot-checks ALL exact (incl. the regional key: NL01 479.527 / Amsterdam 630.621 & 303.925). The term-collision watchouts held (bare 'werkloosheid'/'huizenprijs' stay with the original keys, measured). Spin-offs: [#172](open-questions.md) (finder-chain regression + reverted Sonnet escalation). |
| 175 | WP26 / trial surface | **The anonymous trial never receives the WP26 flags, so flipping them changes the paid product and NOT the trial — found by the adversarial money-path review, 2026-07-25 (session 57).** `web/app/trial-actions.ts` builds its options without `clickOptionsEnabled`/`answerFirstEnabled` (both are read only in `web/app/actions.ts`). Consequence at go-live: a trial visitor asking a region-less question still gets a clarification they structurally cannot reply to (ADR 036 D5 gives the trial no reply endpoint), and the pot has already paid for that question. Sharper still: `rescue.ts`'s own header cites the trial smoke as its motivating evidence, yet no rescue chip is ever built for a trial visitor. Evidence it is an oversight, not a decision: the comment in `trial-actions.ts` enumerates the deliberately-absent options and these two are not on that list. | **Not fixed autonomously — it is a product decision, not a bug fix.** Passing `answerFirstEnabled` through is the clear half (answer-first needs no reply endpoint, and it is precisely what stops a trial question dying in a clarification). `clickOptionsEnabled` is the open half: a chip the visitor cannot take without spending trial question #2 may be worse than no chip. **Decide this before or with the go-live**, and record the answer in ADR 036 D5 either way so the next session does not re-find it. **✅ DECIDED + BUILT 2026-09-03 (session 71, with the `ANSWER_FIRST_ENABLED` go-live): the clear half — `answerFirstEnabled` is passed through to the trial (`trial-actions.ts`, same env read as `actions.ts`, pinned in `trial-actions.test.ts`); the open half — `clickOptionsEnabled` stays OUT of the trial as the default (a chip the visitor cannot take is worse than no chip), owner veto by exception. Recorded as an as-built note in ADR 036 D5.** |
| 177 | WP26 / R8 fidelity | **A rescue-path fresh parse is recorded in `llm_calls` under role `'clarify'` while it actually issues the standalone question prompt** (`respond-audited.ts` wraps the reply turn as `'clarify'`; the rescue branch then calls `respondToQuestion`). Found twice independently by the 2026-07-25 review. Affects the legitimate rescue path, not only forgeries. | Cosmetic today (no number is affected, no money is affected) but it is exactly the honesty the sibling comment in the same file says the role label protects, and it will mislead any future spend-by-role analysis. Small fix: label the branch `'parse'`. Not done in session 57 to keep the hardening PR to the trust boundary. **✅ FIXED 2026-07-25 (session 58B), with one deviation from the suggestion above: the label is `'intent'`, not a new `'parse'`.** `LlmCallRecord['role']` is a CLOSED union (`'intent' \| 'clarify' \| 'followup' \| 'compose' \| 'semantic_check'`), and `'intent'` is already exactly what a standalone question parse is labelled on the fresh-question path — adding a sixth value would widen the union and change what old rows mean by omission, for no gain. Decided in the AUDIT layer (`respond-audited.ts`) rather than in `respond.ts`, because `isRescuePending` is a pure function of the pending, so the label can be chosen before the call and `respond.ts` stays unaware that roles exist. Pinned by one test that FAILS on revert: the rescue turn records `'intent'` and not `'clarify'` — and it is handed the INTENT fixtures, so a clarify-merge prompt would miss the replay and throw, which means it pins WHICH prompt ran and not merely the label. The opposite over-correction (labelling a REAL clarification reply `'intent'`) is caught by the pre-existing `c-b15-full` test, which already asserts `'clarify'` and not `'intent'` on that flow; it passes on revert by construction, so it is a guard, not a revert-proof. |
| 179 | Testing / measurement | **The fixture-snapshot saving IS measured now, and it is not the 240 s / 35% the PR #61 merge note claimed — it is ~70-145 s on a 430-760 s suite, with the magnitude not resolvable at n=2.** PR #61's "680 s → 440 s" was load-confounded (retracted in [lessons-learned.md](lessons-learned.md)). | **MEASURED 2026-07-25 17:15:57-17:57:08 by the first of the night's two parallel sessions**, four alternating legs on a machine carrying one unrelated background process, `uptime` captured per leg, 98 files / 1509 tests green on every leg: warm **690 s** (load 20.01), cold **762 s** (21.34), warm **432 s** (20.52), cold **577 s** (18.78). Adjacent pairs — the right comparison, since load drifts across 40 minutes — give cold as **+72 s** and **+145 s**. But the *within-arm* spread (warm 690 vs 432 = 258 s) EXCEEDS the between-arm difference, so **the direction is consistent and the magnitude is not resolvable at n=2**; more legs, not a tighter analysis, is what would settle it. **The original confound is now concrete rather than suspected:** 680 ≈ leg 1 (690, loaded) and 440 ≈ leg 3 (432, lighter), i.e. the merge note's pair was two *warm* runs at different loads — it was never a before/after at all. **Why a naive model under-predicts this:** removing ~250 s of cumulative ingest (34 suites × ~7.4 s) at the pool's parallelism would predict only ~35 s of wall clock, and the measurement is 2-4× that. The reason is that the ingest cost is not a constant — the OLD design degraded superlinearly with load (that is what the four `hookTimeout` raises 30→60→120→300 s were), and the second session measured that degradation directly, by accident: its own `globalSetup` build logged `snapshot built in 138470 ms` — **138 s against the 7.9-10.7 s the same build costs idle, a 13-17× penalty** — while a restore stays ~1.2-1.4 s either way. So the saving is small on an idle machine and large on a busy one, which is CI and a real dev laptop. **Standing rule this leaves behind:** report a pair of numbers with their load, never one ratio, and never compare two legs measured 40 minutes apart without interleaving them. |
| 180 | Trial / ops — ✅ **BUILT** | **The anonymous pot has no low-water alert and no automatic refill, so the lead magnet can be silently switched off by an outsider.** Measured shape: the per-IP backstop is 5/day, so ~5 addresses × 5 = 25 covers the whole live pot (23/25 at last reading), in parallel, in minutes, for roughly €0.50 of trial-key spend. `src/answer/audit/alerts.ts` has three `maybeAlert*` seams and none of them watches the pot; `scripts/trial-pot-set.ts` is the only writer and it is manual. Found by the session-58 independent trial hunt (Fable lenses A + B). | **Recorded, not built.** The money loss is negligible and by design (ADR 036 names the pot as the blast-radius cap); the real loss is that the homepage silently degrades to "log in om verder te gaan" and the owner finds out by looking. Smallest fix: fire the existing `sendAdminAlertEmail` seam from `takeTrialQuestion` when `remaining_questions` crosses a threshold. A per-day sub-cap on the pot would bound the burst properly but is a design change. **✅ BUILT 2026-07-26.** `TRIAL_POT_LOW_WATER = 5`; the take now returns `potRemaining` read from the SAME `UPDATE … returning` that decremented it (a second read could see a value that already moved), and `trial-actions.ts` fires `maybeAlertTrialPotLow` **after the answer is produced**. Two deliberate choices: it is fired by the CALLER, never inside `takeTrialQuestion`, because that holds a global advisory lock and an e-mail is a network call — holding the lock across one would serialise every anonymous visitor behind it (the ledger's own "never across an LLM call" rule); and the trigger is `=== 5` / `=== 0`, not `<= 5`, because a take always decrements by exactly 1, so equality gives one warning shot and one "it is empty" instead of a mail per question. **A test of the fail-soft claim found the wiring was not fail-soft**: the alert sat inside the action's outer `try`, so a throw would have refunded and discarded an answer that was already produced — the same shape as the session-52 `attachTrialAudit` finding. Wrapped. Note the interaction with [#186](open-questions.md): a Vercel Firewall rate rule mitigates both — but it must target **`POST /`**, not `/api/`, because Server Actions POST to the page path; the RUNBOOK's suggested rule was pointed at the wrong path and is corrected. |
| 182 | Trial / abuse — ✅ **BUILT** | **The IP backstop HMACs the full address with no prefix truncation, so it bounds nothing over IPv6.** A residential IPv6 customer gets a /64 minimum (often a /56), i.e. 2^64 addresses each yielding a distinct `ip_hash` with its own fresh 5/day budget — one machine, no header tricks, pot-limited only. Measured NOT reachable today: `dig AAAA checkdecijfers.vercel.app` and `checkdecijfers.nl` both return empty, so every client address arriving today is IPv4. | **Recorded with an explicit trigger, not fixed.** It fires the day the edge serves IPv6 or the apex is pointed through an IPv6-capable front — **which the launch plan contemplates (Cloudflare)**, so this is a pre-launch checklist item rather than a someday. Fix is one line plus a test: if the address contains `:`, hash the first four hextets (the /64) instead of the whole address; IPv4 path unchanged. **✅ BUILT 2026-07-25.** `ipBucketKey()` in `web/lib/trial.ts`, exported so the rule is tested DIRECTLY rather than only through the HMAC — where every wrong answer looks like the same opaque hex. It expands `::` before slicing (`2001:db8::1` split on `:` gives `['2001','db8','','1']`, whose first four groups are NOT the /64), normalises hextets so `2001:0db8:` and `2001:db8:` agree, strips brackets/ports/zone indices, and buckets an IPv4-mapped address as the IPv4 it is — otherwise one visitor lands in two buckets depending on which form the platform hands us. IPv4 unchanged. **Zero behavioural change today** (`dig AAAA` still empty for both hostnames), which is why it ships BEFORE the launch trigger rather than after. **No counts restart** — an earlier draft of this row claimed they would, which contradicted its own "zero behavioural change" sentence. `ipBucketKey(x) === x` byte-for-byte for every IPv4 address and for the `'unknown'` sentinel, so the HMAC input is unchanged and every existing `ip_hash` still matches. Only future IPv6 rows would ever be keyed differently, and there are none. |
| 183 | Trial / spend — **product call** | **How long may a trial question be?** The trial Server Action accepts 2000 chars while its own UI caps input at 500, so one pot question can buy ~4× the input tokens the product actually offers (`web/app/trial-actions.ts` `MAX_INPUT_LENGTH = 2000` vs `web/components/trial-chat.tsx` `INPUT_MAX = 500`). Compounding on the same pot unit: the compose ladder can run a second Sonnet call and the semantic checker adds up to one Haiku call per rung — a roughly 3-5× spread in cost per "proefvraag", all requester-influenceable. | **NOT a bug, and deliberately not fixed — the framing this row first carried was wrong and is corrected here.** `MAX_INPUT_LENGTH = 2000` is the *intended* server belt behind a 500-char client cap on the **paid** path too, and `web/app/actions.ts` says so in as many words ("the client input caps at 500 chars; this is the belt behind it"). Server-cap-above-client-cap is defence-in-depth by design, so narrowing only the trial would make the two paths inconsistent for no stated reason. It is therefore a **product decision** — is a 2000-char anonymous question worth one pot question? — not a hardening. Also weighed and rejected: bolting it onto PR #64 would have invalidated a review that PR had already passed, to close a 4× on an already-bounded range when that PR closes a ~200× one. Its own change, with its own reasoning, if the owner wants it. |
| 187 | Trial / abuse — ✅ **ANSWERED from Vercel's docs** | **The IP backstop keys on `x-forwarded-for`'s FIRST entry, which may be client-chosen — and nobody has measured whether it is.** `hashedRequestIp` (`web/lib/trial.ts:46`) reads `forwarded?.split(',')[0]`. That is correct behind a proxy that OVERWRITES the header and wrong behind one that APPENDS, in which case the trustworthy value is the LAST entry and `[0]` is whatever the sender typed. The safety argument is a code comment ("Vercel terminates the connection, so the FIRST entry is the platform-set client address"), not a measurement; Vercel's own non-forgeable `x-vercel-forwarded-for` appears nowhere in the repo; and the existing test (`web/lib/trial.test.ts`) asserts only that a DIFFERENT header value yields a DIFFERENT hash — the attacker's capability, asserted as correct behaviour. Distinct from [#182](open-questions.md), which is about the key being too FINE (IPv6); this is about the INPUT being untrusted. Found by two independent lenses of the 2026-07-25 trial hunt ([dossier](session-briefs/2026-07-25-trial-surface-hunt.md)). | **PLAUSIBLE, not confirmed — and NOT blocked on the owner (re-framed 2026-07-25 on his steer that he wants autonomous work).** Calling this "probing abuse limits" was over-caution: it is TWO ordinary requests, one forged and one not, compared by their stored `ip_hash`. Bounded at 2 pot questions (~€0.04) and reversible with `trialpot:set`. **Expected result: NOT forgeable** — Vercel's request-headers documentation says it overwrites `X-Forwarded-For` and does not forward client-supplied values, so this confirms a fact rather than discovering one. Full procedure, bound and rollback: [session-briefs/2026-07-26-autonomous-followups.md](session-briefs/2026-07-26-autonomous-followups.md) §1 — including the line that stays: one comparison is a config check, repeating it to exhaust the 5/day limit is a load test against our own production and is out of bounds. **✅ ANSWERED 2026-07-25 WITHOUT the live probe, and the answer is NO — not forgeable.** vercel.com/docs/headers/request-headers states it outright: *"Vercel overwrites this header and does not forward external IPs to prevent spoofing, unless a trusted proxy is enabled for Enterprise customers."* We are on Hobby, so no trusted proxy exists and a client-supplied value never reaches us — **the existing `[0]` read is correct**, and the function's comment now says so WITH the citation instead of asserting it. ⚠ **A first draft of this row got the reasoning backwards and a review caught it.** It said `x-vercel-forwarded-for` was "not the documented header" and dismissed it. The page has a dedicated section for it: *"This header is identical to the `x-forwarded-for` header. However, `x-forwarded-for` could be overwritten if you're using a proxy on top of Vercel."* So it is documented, and it is documented as the fix for **exactly** the scenario this product is heading into — put Cloudflare in front (the launch plan) and `x-forwarded-for` carries the PROXY's address, collapsing every visitor into a handful of edge buckets and locking real people out of a 5/day limit they never used. **The code now reads `x-vercel-forwarded-for` first**: a no-op today (the two are identical), correct at the trigger. The false claim is recorded rather than quietly deleted because the lesson is the point — a doc-search index is not the page, and "undocumented" is a claim that needs the page open. The two-request live confirmation stays in the followups brief but is no longer load-bearing. ~~Until measured, **assume the backstop bounds nothing against scripted traffic** and treat the Anthropic hard cap on the trial key as the real spend ceiling rather than a belt-and-braces extra. If forgeable, the fix is to prefer `x-vercel-forwarded-for` / `x-real-ip`; record the measurement in the RUNBOOK either way, so the next session inherits a fact instead of a comment. |
| 189 | GDPR / ops — ✅ **BUILT (dormant)** | **NOTHING SCHEDULES `gdpr:purge`.** The 90-day trial-bookkeeping sweep is real code and is correctly wired into the CLI (`scripts/gdpr-purge.ts` → `purgeExpiredTrialBookkeeping`), but no cron, no CI schedule and no RUNBOOK duty invokes it: `web/vercel.json` carries exactly one cron and it is the onboarding job; `.github/workflows/ci.yml` has no `schedule:` trigger. The first `trial_questions` rows become purgeable ~2026-10-15 and on current wiring nothing will happen on that date. STATUS.md had been claiming "ongoing retention = the monthly maintenance session" while that session's standing agenda never mentioned the purge — corrected in this PR, along with adding the agenda entry. Distinct from [#181](open-questions.md), which is about what the 2-year purge RETAINS; this is about whether anything runs at all. | **Half fixed here (the agenda entry + the contradicted STATUS claim). The rest is NO LONGER parked on the owner** (re-framed 2026-07-25 on his steer): **recommended default is to BUILD the cron**, because the manual route has already failed for the entire life of the feature — nobody ran it and nobody noticed until an adversarial pass went looking, and an agenda line is a promise of exactly that kind. Mirror `/api/onboarding-cron` (Bearer `CRON_SECRET`, fail-closed), call the SAME purge functions the CLI calls, ship it **dry-run by default** behind an env flag, monthly schedule, failures through `sendAdminAlertEmail`. Only the flag flip and watching the first real run stay supervised. Procedure: [session-briefs/2026-07-26-autonomous-followups.md](session-briefs/2026-07-26-autonomous-followups.md) §2. **✅ BUILT 2026-07-25 (session 58B), DORMANT.** `web/app/api/gdpr-purge-cron/route.ts` mirrors the onboarding cron (Bearer `CRON_SECRET`, fail-closed 503 when unset, 401 on mismatch), registered **monthly** (`0 4 1 * *`) in `web/vercel.json`. It calls the SAME code the CLI calls: the orchestration was lifted into `src/answer/audit/retention-job.ts` so the cron and `scripts/gdpr-purge.ts` are two thin adapters over ONE implementation — duplicating it would have been the ⟨F2⟩ drift this project keeps closing. The trial leg is INJECTED, not imported, so `answer/` gains no inward edge to `billing/` (ADR 001's arrow). **It reports only until `GDPR_PURGE_APPLY=1` is set** — a cron that redacts and deletes on its first unattended run is not a change to make blind, and turning the flag off is a complete rollback. Failures and a table-absent report both alert via the existing `sendAdminAlertEmail` seam. **Seven job tests + seven route tests.** To be exact, because an earlier draft of this row overstated it: the purge FUNCTIONS were already well covered; what had NO coverage was the ORCHESTRATION (which legs run, in what order, dry-run vs apply), because it lived only in a script and `scripts/` has no harness — and that is the gap that let "nothing ever runs this" go unnoticed. The pins that matter for an unattended job: a failing trial read **propagates** instead of being reported as an honest skip, and every `GDPR_PURGE_APPLY` value except exactly `'1'` reads as dry-run. **✅ MERGED AND DEPLOYED 2026-07-25 — PR #68, squash `fbffe48`**, gate+deploy green (the deploy is also what validates the `vercel.json` cron entry, so a monthly second cron on Hobby is now proven, not assumed). **Live-verified after deploy:** `/api/gdpr-purge-cron` returns **401** without a valid Bearer — and that 401 is itself the proof the proxy allowlist entry landed, because an unlisted route would have been 307'd to `/login` and never reached the route's own auth at all. That omission was the BLOCKER a review pass over this diff caught: without it the cron would have read as scheduled AND healthy (the 307 returns 200 to the cron dashboard) while never running once — #189 rebuilt with a green tick on top. **✅ FULLY LIVE (2026-09-05, session 78, owner-directed, `vercel env add GDPR_PURGE_APPLY production` = `1` + empty-commit redeploy `490362f`):** triggered one real run via `vercel crons run /api/gdpr-purge-cron` (no `CRON_SECRET` needed — Vercel's own CLI invokes it authenticated); `vercel logs` showed `gdpr-purge-cron: Applied — ... redacted 0 audit_answers row(s) ... 0 trial_questions ... 0 error_log ...`, matching the `npm run gdpr:purge` dry-run baseline taken moments before (0 everywhere) exactly, as RUNBOOK's procedure requires. The monthly cron (`0 4 1 * *`) now actually enforces retention going forward, not just reports. |
| 191 | WP26 / go-live | **The clarification REPLY turn never receives `ANSWER_FIRST_ENABLED`, though its options type declares it and documents it as threaded.** `ClarifyReplyOptions.answerFirstEnabled` exists at `src/answer/intent/clarify.ts:44-47` with the comment *"threaded into the dry-runs below so an option is judged servable under the SAME rules the answer turn will run under"* — but `respondToClarificationReply` builds `clarifyOptions` from `client` / `config` / `extraCanonicalMeasures` only (`src/answer/respond/respond.ts:586-595`) and never sets it. So with the flag ON, a reply turn's `resolveCandidate` and `echoServability` run under **pre-mechanism-B rules** while the first turn runs under B. Found 2026-07-26 (session 60) by the adversarial review of the [#176](open-questions.md) diff — it is the same options-bag-omission shape #176 fixed, one module over, which is why the review went looking for it. | ✅ **FIXED 2026-08-07 (session 61) — and the diagnosis in the left-hand cell was too kind.** The reply turn was not "pre-mechanism-B"; it was **half-B**, a state nobody chose. B has two axes in two layers: **B-region** lives in the QUERY layer (`src/query/resolve.ts:393`) and already reached reply turns via the `{ ...options }` spread into `respondToIntent` (`respond.ts:652`), while **B-period** lives in the INTENT layer (`src/answer/intent/resolve.ts:731`) and is fed by the dropped bag. So flag-on, a reply turn silently defaulted the REGION the user never mentioned and refused over the PERIOD it was allowed to default. **Measured on the hermetic fixture DB, flag ON, pre-fix: first turn → `answer`, reply turn → `refusal (still_ambiguous)` on the same intent; post-fix both `answer`.** The product question ("should a reply turn default?") answers itself once R7 is read: **R7's third branch draws no first-turn/reply-turn distinction** and the safelist is code, not configuration — so half-applying it was an invariant conformance gap, not deliberate conservatism. A reply is also the LAST round by rule (a reply never asks again), making it the turn where refusing costs most: the user answered the exact question we asked and would be refused on an axis nobody asked about, after paying the clarification price — the precise paid dead-end ADR 024 exists to remove. Fix: one line in `respondToClarificationReply`; pinned by `tests/answer/answer-first-reply-turn.test.ts`, whose load-bearing case asserts the two turns AGREE rather than asserting a verdict (a "reply now answers" pin would still pass if a later change made the FIRST turn refuse too). ⚠ Also corrected in the same change: the RUNBOOK's blast-radius cell said `ANSWER_FIRST_ENABLED` affects "First turns" — measurably wrong, since B-region always reached replies. (Original assessment, kept for the record: recorded, NOT fixed — a GO-LIVE item, not a capacity one. Dormant today: `ANSWER_FIRST_ENABLED` is off, so both turns run pre-B and agree. It becomes real the moment that flag is flipped, which is the owner's next WP26 step — the failure shape is a first turn that would have defaulted a period/region and a reply turn that refuses or clarifies instead, i.e. the two halves of one conversation disagreeing about what is servable. Deliberately not fixed in the #176 change: that diff's whole claim is byte-neutrality with the flags off, and threading a second flag into a second module is a behaviour change under flag-ON that deserves its own verification and its own dry-run reasoning (does a reply-turn option *want* B's defaults, or is the user's reply exactly the moment NOT to default?). ⚠ **Answer that question before the WP26 go-live, not after** — see the RUNBOOK's "WP26 answer-first + clickable options" section and [#175](open-questions.md) (the trial receives neither flag either).) |
| 192 | Ingestion / ops | **The documented slow-stream escape hatch cannot complete a RELEASE-day sync — the one job it exists for.** `scripts/sync-from-capture.ts:42` calls `syncTable(db, new FixtureSource(docs), tableId)` with **no options bag**, so `acceptNewCodes` and `rebaseline` are both false and unreachable from the CLI (`process.argv` yields only `tableId` and `dir`, line 22). Every CBS release brings a new period code by definition — `85880NED` gained `2026KW02` on 2026-07-30 — and a new code fails `checkDimensionMapping` (`src/ingestion/validate.ts:311`), which sets the table to `needs_review`. Quarantine is enforced on the value path (`src/query/resolve.ts:332`), so the table then **refuses in production**. The RUNBOOK (step 5) says to expect this hatch "for every 85880NED release-day sync (incl. ~30/7)" — so the documented procedure for the exact case it names would quarantine the table rather than sync it. Not caught before because session 50 used the hatch for FIRST-TIME registration, where registration populates `dimension_labels` and nothing is new. Found 2026-08-07 (session 60 continuation) while running the ~30/7 syncs. | ✅ **FIXED 2026-08-07 (session 61), on its own branch with its own verification, as this cell asked for.** The fix turned out to be slightly more than "thread two flags": `sync-from-capture.ts` had **zero importable surface** (every statement ran as a top-level side effect against real `process.argv`, real `fs`, a real wall-clock date and a real `connectFromEnv`), so a flags-only patch could not have been tested against the real path — and this repo's own session-60 lesson is that a test which cannot see the failing input proves nothing. So it was restructured to mirror `src/ingestion/cli.ts`: exported `parseArgs(argv)` + `runSyncFromCapture(db, source, args)`, with the file I/O, the same-day freshness refusal and `connectFromEnv` left in a thin `import.meta.url`-guarded entry. Pinned by `tests/ingestion/sync-from-capture.test.ts` (5 cases) driving those exports, hermetic on PGlite with a synthetic new dimension code injected into the committed 85224NED fixture — **verified RED before being trusted**: with the options bag removed the recovery case fails exactly as #192 describes. Also added: on a bare `dimension_mapping` failure the script now PRINTS the recovery command, since the operator meeting this is mid-release and the old output named the stage but not the way out. ⚠ Safety property worth recording: `--accept-new-codes` admits new **dimension coordinate** codes only — a brand-new MEASURE code still fails unconditionally (`validate.ts:304-310`), so the flag cannot silently widen the vocabulary. (Original assessment: recorded, not fixed in the same breath — the fix is small but it is a code change on the ingestion path, so it wants its own branch, its own verification and a review. The change: parse `--accept-new-codes` / `--rebaseline` in `sync-from-capture.ts` and thread them into `syncTable`'s options, exactly as `src/ingestion/cli.ts:32-33` already does. Nothing else differs — the script is deliberately "same pipeline, same five validators, only the transport differs", and today it silently diverges from the CLI on precisely the flags a release sync needs. ⚠ **Sequencing note for whoever fixes it:** check whether the direct `ingest sync <id> --accept-new-codes` path succeeds first; if CBS's stream is healthy from the operator's network the hatch is not needed that day, and the 2026-08-07 run is evidence either way (recorded in the session entry). **Related operational lesson, worth more than the bug:** run a release sync WITH `--accept-new-codes` from the start when the code diff has been reviewed read-only beforehand. Running it bare first is what quarantined `85770NED` on 2026-08-07 and briefly took PPI out of service — a plain sync is not the "safe" option when the table is known to have gained a period code; it is the option that trades a clean sync for an outage.) |
| 193 | Data semantics / product honesty | **MEASURED 2026-08-07: CBS revised 1,103 figures that were already marked `Definitief`, going back to 2015KW01.** The `85880NED` sync of the 2026-07-30 flash release (batch 30) carried **2,009 corrections**, and the split is the finding: **1,103 `Definitief → Definitief`** across 45 distinct periods spanning 2015KW01–2026KW01, plus 906 `Voorlopig → Voorlopig`. This is a national-accounts benchmark revision — routine practice at CBS, and the whole reason quarterly national accounts carry a revision policy — but it **empirically disproves the reading that `Definitief` means immutable**, which is the reading a user would naturally take. The pipeline behaved perfectly: every one of the 2,009 changes went through the correction-diff log, values were updated in place, nothing was fabricated, no invariant was touched. The question is not about correctness, it is about what we IMPLY. | ✅ **DECIDED 2026-08-07 (session 61): option (b), the honest-copy pass — and one part of it shipped, because the audit shows the problem is narrower AND sharper than the row assumed.** ⚠ **The sharp part: the product NEVER PRINTS `(definitief cijfer)` at all.** `provisionalDisplay` (`src/sources/registry.ts:73-76`) maps only `Voorlopig` and `NaderVoorlopig`, so a definitive cell renders with **no suffix**. The one place that string existed was `web/components/landing.tsx:68` — inside the example the file's own header comment calls *"the product's real answer shape"*. So the homepage was advertising a shape the pipeline cannot produce. **Fixed this session** (suffix removed from the body; `Status bij CBS: definitief` moved into the source line, which is where the pipeline shows it and what the page's step 3 already promises). ⚠ **Corrected 2026-08-26 (PR #85 review): that "moved" claim was wrong, and the move was the same bug one line over** — `buildAttributionLine` (`src/answer/compose/format.ts`) is the one builder for that sentence on every surface, and it never emits a status clause either, for any status. The relocated text was exactly as fabricated as the suffix it replaced. Fixed by removing it outright instead of relocating it — the source line now matches `buildAttributionLine`'s real, unchanged shape byte-for-byte. Zero audit exposure — the landing page is a pure server component with no LLM and no audit row. **The finality implication is therefore STRUCTURAL, not asserted**: it is the SILENCE where a `(voorlopig cijfer)` marker would be. Verdict on the copy: **not false, but measurably optimistic.** The public-claim rule is NOT breached — the traceability slogan is worded correctly in all four public places. ✅ **THE TWO COPY EDITS SHIPPED — PR #91, `e15c953` (session before 66; doc lagged the code until now, found 2026-08-27 session 66 while reading this row for the session-66 queue's #193 item, which had assumed these were still pending).** Both softening sentences are live in `main`: `buildFreshnessRefusal`'s differs-aside (`src/answer/respond/refusals.ts:326-332`) and the freshness FAQ body (`src/answer/respond/meta.ts:125-130`). **Deliberate implementation choice, explained in the PR's own commit message: APPEND the new sentence rather than REPLACE the old one** — this is exactly how PR #91 dodged the vacuous-test trap named below (the pre-existing `not.toMatch(/laatste definitieve/)` pin stays meaningful only if that string still exists somewhere, gated by the same `differs` condition, rather than being deleted). `tests/answer/respond-refusals.test.ts` correctly asserts BOTH sentences present when `differs` and BOTH absent otherwise (lines ~407, ~425-426) — the trap did NOT bite. **Side effect worth a future look, NOT changed here (a copy/product-polish call, not a bug):** the refusals.ts aside now reads as two back-to-back parentheticals naming the same period in slightly different words — "(Het laatste definitieve cijfer is er voor X.) (De laatste periode met CBS-status 'definitief' is X — ook die cijfers kan CBS later nog bijstellen.)" — technically accurate but repetitive. Consolidating to one sentence would need the SAME care around the test-pin trap PR #91 already solved once; whoever picks this up should re-read PR #91's commit message first. **⚠ STILL OUTSTANDING, unchanged, still owner-supervised — the ONE thing left is the LIVE verification step, not the copy itself (that shipped, see above):** changing a refusal TEMPLATE has an R8 consequence — per [#133](open-questions.md), `reconstructionReport` verifies every stored row against TODAY's builder rules, so live rows written with the old string would start failing reconstruction and each needs a **row-id-pinned** entry in `src/answer/audit/known-divergences.ts` (the register takes exact ids — never a range or pattern, by design). Discovering which ids diverge requires `npm run audit:verify` against the live database, which is owner-supervised — this step was explicitly out of scope for PR #91 (its own commit message says so) and has not been done since either. (For the record, PR #91 successfully avoided the vacuous-test trap this row originally warned about — `tests/answer/respond-refusals.test.ts` still asserts real behavior on both sides of the `differs` condition — by appending rather than rewording, so that specific risk is closed.) ⚠ **Constraint nobody had written down, for the ambitious option (c) / [#88](open-questions.md):** the correction-diff predicate is `where o.value is distinct from s.value` (`src/ingestion/pipeline.ts:582`), so **status-only transitions are invisible to the log** — a "dit cijfer is herzien" feature built on it can only ever claim VALUE changes, never `Voorlopig→Definitief` promotions. (Original assessment: recorded, not acted on — it is a product/copy decision, and the evidence is now unusually strong. Where it bites: `buildFreshnessRefusal` (`src/answer/respond/refusals.ts:306-325`) deliberately distinguishes `freshestAvailable` from `freshestDefinitief` and can offer the definitive period as the safer one, which is good design and stays good — but it teaches the user that `Definitief` is the settled number, and 1,103 counter-examples just landed in one release. Options, in rough order of cost: **(a)** say nothing (the status is already shown verbatim, and CBS's own semantics are what they are — defensible, and it is what we do today); **(b)** soften the copy where we lean on Definitief, so it reads as *CBS's current publication status* rather than *final*; **(c)** the real fix, [#88](open-questions.md) revision awareness — surface "this figure was revised on <date>" from the correction-diff log we already keep, which would turn this from a caveat into a feature no competitor has. ⚠ **Do NOT resolve this by treating Definitief as untrustworthy** — it is still materially more stable than Voorlopig, and the freshness refusal's preference for it is correct. Related: [#71](open-questions.md) (visible "voorlopig" badge), [#81](open-questions.md) (revision-risk gauge — this measurement is the first real input it ever had), and the two frozen reference values that moved in [11-coverage-table-set.md](11-coverage-table-set.md).) **✅ PR #109 merged into `main`, session 67 (2026-08-28, `a4ae327`).** **✅ THE LAST OUTSTANDING PIECE — the live `audit:verify` step — RAN 2026-09-02 (session 69, owner present): zero divergent rows found against production. Nothing needed pinning in `known-divergences.ts`. This row is now fully closed** (the copy shipped in PR #91, the R8 consequence is verified clean; only the [#88](open-questions.md) revision-awareness feature idea remains open, tracked separately, not a blocker). |
| 194 | Ops / security | **Root `nanoid` 3.3.17 has an open HIGH Dependabot alert** ("custom generators can loop indefinitely when size is zero", fixed at 3.3.18) **with no PR covering it, found 2026-08-27 (session 64) via a fresh `gh api .../dependabot/alerts` sweep AFTER the visible 6-PR backlog was already fully merged.** Not in any grouped `npm-all` PR generated so far (session 64's #90 — `@anthropic-ai/sdk`/`pg`/`@types/pg`/`stripe`/`@electric-sql/pglite`/`@types/node` — didn't include it; Dependabot apparently hadn't flagged it yet when #90 was generated). No existing PR exists to `@dependabot recreate` against (unlike [#83→#98](open-questions.md), where recreate worked because #83 already existed). | ✅ **FIXED 2026-08-27 (session 65) — hand-authored on explicit owner instruction, overriding the session-64 default of waiting for Dependabot.** Session 65 opened by asking the owner (present in-chat) what to prioritize; they chose fixing #194 directly over waiting. `nanoid` is a transitive dev-only dependency (`vitest → vite → postcss`, `postcss` pins `^3.3.12`), so the fix is a pure lockfile bump: `npm update nanoid` moved `package-lock.json`'s single `node_modules/nanoid` entry from 3.3.17 to 3.3.18 (`package.json` untouched — nothing declares it directly). `npm audit` now reports 0 vulnerabilities. Full verification block run before push: typecheck ×2 clean, backend 105/105 files · 1579/1579 tests (one `registry.test.ts` timeout under full-suite load, confirmed transient — 14/14 pass in 19s run in isolation), web 42/42 · 453/453, benchmark 14/14+6/6+0 GATE PASS, real `next build`, `/code-review` LOW pass — 0 findings (diff is a version+integrity-hash bump only, no logic to review). Pushed directly to `main` per the owner-present standing authorization (#118 revision) — no PR, since the reviewed-PR-convention concern this row's original reasoning raised was satisfied by the owner's explicit in-chat direction plus the full verification block, not skipped. |
| 195 | Ingestion / ops | **PR #111's disclosed eviction cost ("1 extra DB round-trip per served turn") is understated — an adversarial review (session 67) traced the real call graph and found `touchLastQueriedAt` (`src/query/run.ts:339`, called from inside `runQuery`) fires roughly 4-6 times per served answer, not once: once for the delivered answer plus once per follow-up-suggestion candidate `src/answer/respond/suggestions.ts` dry-runs (adjacentPeriod up to 2, trend up to 2, regionVariant 1, sameTopic 1) plus once per alternate LLM-proposed reading `runQuery`'s use as the internal servability-probe primitive touches during ambiguous-intent parsing (up to 3). `tests/answer/query-count.test.ts`'s pin was bumped to accommodate PR #111 but measures `resolveCandidate`+`runQuery` directly, never the full `src/answer/respond/respond.ts` production path, so the pin does not catch the real multiplier. Separately: because `touchLastQueriedAt` lives inside `runQuery` and `runQuery` is also the servability-probe primitive, a table's `last_queried_at` gets bumped by internal disambiguation/suggestion probes that were never delivered to the user as an answer — undermining the eviction feature's own "unqueried for 30 days" premise (a table can be kept artificially warm by repeated probing that always rejects it). | **Not a merge blocker for PR #111 — the mechanism is dry-run/manual-`--apply`-only, no live cron, so today's connection-pool load ([#173](open-questions.md)) is unaffected.** Must be re-measured (against the real `respond.ts` path, not just `resolveCandidate`+`runQuery` in isolation) and the "unqueried" premise reconsidered (e.g. a separate `last_served_at` that only the delivered-answer path touches, vs. today's `last_queried_at` that every probe touches) BEFORE `tables:evict --apply` is ever run against a live on-demand table, and certainly before any cron/automation is added on top of it. **Measured 2026-09-02 (session 70, #197 step 3, pinned in `tests/answer/query-count.test.ts`): the chip builder alone costs 27 statements / 3 dry-runs on a regional single answer and 39 / 4 on a national one, flag on or off; the `CLARIFY_CLICK_ENABLED` comparison chips add one dry-run only on a national-only measure (12 → 18), worst case 8 vs 6 dry-runs when early generators fail.** **✅ FIXED (2026-09-03, session 72, autonomous; PR #121 MERGED + LIVE 2026-09-03, session 75, owner-approved in chat, squash `527ef2e`):** `echoServability` (dry-run.ts — the servability-probe primitive every follow-up chip, comparison chip and alternate-reading check funnels through) now calls `runQuery` with a new `probe: true` option, and `runQuery` skips the `last_queried_at` bump entirely whenever `probe` is set — no internal disambiguation/suggestion check can warm a table it never delivered as an answer, so the eviction anchor now reads as "last SERVED/DELIVERED read," never "last touched by any internal machinery." Re-measured (`tests/answer/query-count.test.ts`, `#197 step 3`): the chip builder now costs 24 statements / 3 dry-runs on the regional single answer (was 27) and 10 → 15 statements on the national-only measure with `CLARIFY_CLICK_ENABLED` (was 12 → 18) — each row dropped by exactly one statement per dry-run, its former touch; a real served turn still bumps the clock exactly once (newly pinned). ⚠ **Stale as of 2026-09-04 — see [#196](open-questions.md)'s structural-follow-up entry (and its review round 2) for the current numbers**: `resolveIntent` wrapping its read arc in a transaction adds further per-dry-run cost on top of the 24/10→15 figures above. |
| 199 | Product/UX | Session 72's source drill-through cluster ("Bewijs dit cijfer", [#70](open-questions.md)/[#79](open-questions.md)/[#89](open-questions.md)) ships on chat answers only — the live turn AND thread resume — per the design brief's D8: the dashboard's question-history view has no proof panel. `src/billing/history.ts`'s `getQuestionHistory` extracts `answerParts` by SQL JSON path (body/definitionLine/markingLine/attributionLine/staleness) — no `cells`, no `derivations` — so `buildAnswerProof` (`web/lib/answer-proof.ts`) cannot run there without a read-model change; `web/components/trial-chat.tsx` renders `message.text` in its own list, also out. | **✅ BUILT + MERGED + LIVE, session 80 (2026-09-05, `main` `5b8e0fa`).** Full panel, reused as-is. `getQuestionHistory` now reads the full `a.response` column (mirroring `getThreadRows`, `src/threads/index.ts`) and decodes it into a new `answerEnvelope: AnswerResponse \| null` field on `QuestionHistoryEntry` — `buildAnswerProof` itself is called from `question-history.tsx` (a web-layer file), NOT from `history.ts`, since `buildAnswerProof` lives in `web/lib/` and a backend module importing frontend code would cross ADR 001's module boundary (a real correction made during planning, after the design doc's own §2 assumed the opposite). GDPR redaction confirmed safe two ways: the field computation checks both `decoded.kind === 'answer'` AND `'result' in decoded` (a redacted row keeps `kind` but drops `result` — `redactedResponse()`, `src/answer/audit/retention.ts`), and the final whole-branch review caught a second, subtler case — a collapsed clarification round where the clarify row is redacted but the reply row isn't leaves `isDeleted: true` with a non-null `answerEnvelope`; the component's existing `!item.isDeleted` structural check already prevented this from ever rendering, but was undocumented and unpinned — now fixed with a dedicated test and a corrected comment (no behavior change). Built via `subagent-driven-development` in an isolated worktree: 2 tasks, each with an implementer + task-reviewer round (one real fix each — a missed `web:typecheck` check, and a missed full backend-suite/benchmark run), plus the final whole-branch review above. Verified: typecheck (root+web), backend 118/118 files 1801/1801 tests (+3), benchmark GATE PASS, web 51/51 files 608/608 tests (+3), real `next build`, `/code-review` low: 0 findings. Full design: [session-briefs/2026-09-05-question-history-proof-panel-design.md](session-briefs/2026-09-05-question-history-proof-panel-design.md); plan: [superpowers/plans/2026-09-05-question-history-proof-panel.md](superpowers/plans/2026-09-05-question-history-proof-panel.md). |
| 203 | Engineering | **Found 2026-09-05, session 81, during #197 idea 4's final whole-branch review (not a branch regression — confirmed pre-existing on `main`, untouched by that branch): `deriveDirection` (`src/query/derivations.ts`) has no region guard, and `src/query/run.ts` builds cells period-major/region-minor — so on a multi-region, multi-period result (e.g. a 2-region line chart), the registered `direction` derivation's `(firstResultId, lastResultId)` pair can silently span TWO DIFFERENT regions (first period of region A vs last period of region Z), not a real trend for either region.** Reproduced concretely (session 81): Amsterdam 200→190 (falling) + Utrecht 100→150 (rising) on one chart registered `direction: 'down'` from a cross-region diff. Adjacent to [#100](open-questions.md) (the `monotonic` field's own trivial-for-a-sample hazard) but a distinct root cause (no region guard at all, vs. `monotonic` meaning less than the LLM prompt assumes). | **✅ FIXED (session 82, 2026-09-06) — and turned out to be structurally UNREACHABLE today, not just unmeasured.** Before fixing, checked reachability properly rather than assuming: `src/query/resolve.ts` already refuses "several regions AND several periods in one question" at the intent layer (`invalid_intent`, pinned by `tests/query/query.test.ts`'s `'several regions AND several periods at once is out of contract'`) — so `runQuery` can never actually receive a multi-region multi-period `cells` array via the normal chat path. `src/chart/curated.ts` (the other caller of `runQuery`, bypassing the LLM/resolve layer) never constructs a multi-region query either — its 5 canonical charts are all single-national-measure. So this was a **latent, currently-inert defect**: real, and exactly as described, but with no live caller able to trigger it — until the next feature that relaxes either guard (e.g. a future "compare region A vs B over time" question type) would have silently reactivated it with nothing to catch it. **Fix:** `deriveDirection` and `deriveFirstLast` (`src/query/derivations.ts`) now both refuse when their cells span more than one distinct `regionCode` (a new shared `checkSingleRegion` guard, alongside the existing `checkComputable`) — the guarantee now lives in the derivation functions themselves, not in whichever caller happens to be careful. TDD: a new test in `tests/query/query.test.ts` reproduces the exact cross-region cells shape directly against both functions (RED confirmed before the fix), plus a normal-single-region case pinning unaffected behavior. Verified: typecheck clean, backend 119 files/1816 tests (+2), benchmark GATE PASS, web 51 files/610 tests, real `next build`, `/code-review` LOW 0 findings. One unrelated flake surfaced during this session's full web-suite run (`chat-workspace.test.tsx`, confirmed via isolation + re-run to have zero code path to this change) — spun off as its own task rather than bundled in here. |
| 204 | Frontend / architecture | **Owner asked (2026-09-06, session 83) for a sanity check + comprehensive research on which design components fit the project, after an initial exploratory answer said "no component library exists" — true at the code level but incomplete.** [ADR 008](decisions/008-ui-foundation.md) already named shadcn/ui (on Radix) as the UI foundation, 2026-07-02 — it was never built (no `components.json`, no `@radix-ui/*` anywhere, confirmed directly). The session-51 papier-en-inkt huisstijl (`docs/12-huisstijl.md`) shipped as a hand-rolled Tailwind implementation instead, with real, measured duplication (the `bg-accent` button fragment repeated across 8 files, an input fragment across 7) and one house-rule violation (`stat-card.tsx` hardcodes hex colors the huisstijl doc explicitly bans, because SVG-to-canvas export can't resolve CSS custom properties). Full research, product-surface inventory, and a component-fit mapping table: [session-briefs/2026-09-06-frontend-component-research.md](session-briefs/2026-09-06-frontend-component-research.md). | **Recorded as a candidate, NOT scheduled — pending an owner decision.** Session recommendation: finish executing ADR 008 (`shadcn init`), but re-theme it to the EXISTING papier-en-inkt tokens rather than shadcn's own dark-dashboard defaults (the product is the "editorial/light-mode" case the shadcn skill's own guidance names) — new roadmap surfaces (browse layer, Visualisatie Studio, saved-charts gallery) adopt components from day one with zero byte-identity risk; existing surfaces (chat, workspace, charts) migrate opportunistically at most, never as a big-bang rewrite, since [ADR 033](decisions/033-chat-workspace-redesign.md) pins several of them byte-identical as a build-brief gate and [ADR 007](decisions/007-chart-spec-rendering.md)'s chart renderer carries real anti-fabrication data-binding that must never be touched by a reskin. Open for the owner: timing (now vs. alongside the first new roadmap surface) and whether any retrofit of existing surfaces is wanted at all. **✅ RESOLVED (session 87, 2026-09-07, owner in-chat) — the opposite of the recommendation above, by explicit owner choice:** `shadcn init` was executed, but re-themed to shadcn's OWN neutral defaults, not the existing papier-en-inkt tokens — the owner retired papier-en-inkt entirely (`docs/12-huisstijl.md` rewritten) and had every existing chat/chart/workspace surface restyled in one session (a "big-bang rewrite" in exactly the sense this row recommended against), reviewed as 3 mockup options first (`docs/superpowers/specs/2026-09-07-chat-chart-visual-redesign-design.md`). ADR 033's byte-identity gates and ADR 007's anti-fabrication chart-data-binding were both respected — this was a presentation-only pass (verified: `src/` untouched, full backend suite green, chart honesty bindings/PNG export/R11 provisional marking unchanged) — but the SCOPE and SEQUENCING recommendation here was superseded, not followed. |
