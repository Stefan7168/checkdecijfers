# Open questions & assumptions

Every ambiguity found while writing the docs, with the assumption made so work never blocked on it. Assumptions are also marked inline (`**Assumption:**`) where they occur. Owner is Stefan unless noted.

**Current pricing values (action classes, packs, signup grant) live in [09-pricing.md](09-pricing.md) — #3/#4/#58 below keep the decision history, not the reference numbers.**

| # | Area | Question / ambiguity | Assumption made (until answered) |
|---|---|---|---|
| 1 | Data | Which exact CBS tables (IDs) make up the Phase 0 set? Notes cite `85552NED` (solar), `83765NED`, `86103NED`, `70072NED`, `85458NED`, `03759NED` — some may be archived/redesigned by now. | **Resolved** (2026-07-02, implementation session): 8-table set validated against both live catalogs and adversarially re-verified — see [07-phase0-table-set.md](07-phase0-table-set.md). Of the cited IDs only `03759NED` made the set; `85552NED` doesn't exist (notes error). All 8 tables reachable via OData v4 — ADR 003's v3-fallback contingency not needed in Phase 0 |
| 2 | Benchmark | Benchmark tasks reference recent periods (e.g. B1: 1-1-2025, B5: Q4 2025) — availability depends on CBS publication schedules at build time. | Tasks may shift one period back if CBS hasn't published; the *shape* of each task is the requirement, not the year |
| 3 | Pricing | Free allowance: one-time or monthly? Notes contradict (2 one-time credits vs. 2/month vs. 3/day vs. 5/day). | **Resolved** (Stefan, 2026-07-05): 5 free credits at signup, one-time, never refreshed — bounded abuse surface, no subscription-like mechanic, consistent with "credits never expire". Chat requires an account (#47); the separate anonymous-trial experiment stays tracked at [#53](open-questions.md). **Revised (Stefan, 2026-07-04): 100 free credits**, to hold the same value (5 free simple questions) at the new ×20 pricing scale from [#4](open-questions.md)/[#58](open-questions.md) — mechanism unchanged (one-time, never refreshed). Encoded in the WP13 brief ([08-build-plan.md](08-build-plan.md)) and migration [005](../migrations/005_credit_ledger.sql)'s `signup_grant_config` |
| 4 | Pricing | Heavy-analysis credit price: notes say 3, 3–5, and 5 in different places. | **Resolved for launch** (Stefan, 2026-07-05): action classes **1/3/5** credits (simple/analysis/heavy — in Phase 1 nearly everything is simple = 1); launch packs **€5 / 10 credits · €10 / 25 · €30 / 100** (€0.50→€0.30 per credit). **Superseded (Stefan, 2026-07-04):** widened to a **10–100 scale** so future small a-la-carte features (chart edits, exports) can later be priced in whole credits without rescaling everything again — **clarification 10 / simple 20 / analysis 60 / heavy 100** credits (a new fourth class; reasoning in [#58](open-questions.md)); launch packs' credits scaled ×20 at the same three €-price-points so the euro cost per question is unchanged: **€5 / 200 · €10 / 500 · €30 / 2,000** (still €0.50→€0.30 per simple question, depending on pack). **Fourth pack added same day (Stefan, 2026-07-04): €250 / 25,000 credits** (€0.01/credit) — the first three packs' price-per-credit already fell in a clean €0.005 step each tier (€0.025 → €0.020 → €0.015); this tier continues that step rather than picking an arbitrary discount, aimed at power-user/agency volume. Lives in the WP13 pricing-config seam — deliberately easy to change (ADR [006](decisions/006-auth-billing-seams.md)); these are starting values, not frozen law |
| 5 | Legal | "Credits never expire" — confirm consumer-law/accounting wording for the terms at Phase 2. | Promise stands (interview Q3b); legal check before the pricing page ships |
| 6 | Legal | Exact Dutch CC BY 4.0 attribution wording, and whether CBS prescribes preferred wording. | Draft wording in [05-data-rules.md](05-data-rules.md) used until legal/CBS-guideline check |
| 7 | Branding | Final name: keep checkdecijfers.nl or rebrand (notes suggest FeitInCijfers.nl / CijferCheck.nl; also note the name-vs-professional-audience tension)? | checkdecijfers.nl is the working name everywhere (interview Q6); rebrand is a pre-launch (Phase 2) decision. **Owner wish, recorded 2026-07-10 (WP28 Google SSO config): the Google consent screen currently shows the raw Supabase project domain (`vqvohfqapjfdpbojtezx.supabase.co`) — when the domain decision lands, ALSO configure a Supabase custom (auth) domain so the consent screen shows the product's own name (the ADR [028](decisions/028-google-sso.md) trade-off + revisit trigger; owner: "we must remember it").** |
| 8 | Marketing | Slogans "0% hallucinatiegarantie" / "De AI die niet liegt" are absolute claims. | Not used in docs; public claim standardized per interview Q5. Slogans parked pending legal/comms review |
| 9 | Product | Sharing mechanism detail: notes contain both watermarked PNG download and Stefan's landing-page/OpenGraph idea. | Landing-page link is the primary sharing mechanism (Phase 2); watermarked static images serve social exports. Both build on the chart spec (ADR 007) |
| 10 | Product | Interview Q7 (v1 must-haves from the feature pool) wasn't explicitly answered. | **Resolved** (Stefan, 2026-07-02): confirmed — none are v1 must-haves; all features preserved in the roadmap feature pool + architecture seams table |
| 11 | Performance | Response-time promise: notes claim 100ms, 5s, and 10s in different places. | ~10s median for Phase 0 (honest, LLM-bound); faster claims only after caching phases deliver them |
| 12 | Audience | Year-1 target of 250 users and €12.60/user/mo come from the notes' unvalidated P&L. | Used as *indicative* Phase 2 success markers only, labeled as such |
| 13 | Data | Do we need CBS permission/notification for sustained bulk ingestion (fair-use expectations on the Feed)? | CC BY 4.0 + documented bulk channels imply no; confirm CBS fair-use guidance during Phase 0 ingestion setup |
| 14 | GDPR | Retention period for question logs / audit records once accounts exist; anonymization approach. | **Decided (Stefan, 2026-07-04, session 18): 2-year retention, and a user can delete their own question history.** **Built (WP14, 2026-07-05, code-only/hermetic session):** `npm run gdpr:purge` (`scripts/gdpr-purge.ts`, dry-run by default, `--apply` to run for real) redacts `source_tag='user'` `audit_answers` rows older than 2 years; a "Verwijder mijn vraaggeschiedenis" button (account panel) calls a `getClaims()`-verified server action (`deleteMyQuestionHistory`, `web/app/actions.ts`) that redacts the CALLING user's own `source_tag='user'` rows, any age — one click + an inline confirmation step (owner decision, session 23; not a typed-word confirmation). **Load-bearing implementation choice, discovered mid-build:** both paths **redact** (`UPDATE` — overwrite `question`/`final_text`/`response`/`reply_text`/`pending_clarification` with a fixed sentinel) rather than physically `DELETE`, because `credit_transactions.audit_answer_id`'s FK (migration 005, deliberately no `ON DELETE` clause) throws on a hard delete of any clarification/refusal row a compensation entry references — confirmed empirically in `tests/audit/retention.test.ts`. This is also a strictly better match for the owner's UX decision (a placeholder row that keeps its id and ledger join): the dashboard now renders a "verwijderde vraag" placeholder (`web/components/question-history.tsx`) with the credit amount intact, question/answer text gone, row never hidden. No schema change. Hermetic pins (14 tests, `tests/audit/retention.test.ts`) cover: deletion scoped to the calling user only (another user's rows byte-identical survive — the critical security pin), the ledger never written by either path, the purge never touching benchmark/validation-tagged rows, purge idempotency, and the FK-violation proof. Full gate green (714 backend + 135 web tests, benchmark 14/14+6/6+0 fabricated, clean typecheck both sides, real `next build`). **Still open:** (d) a privacy policy, which doesn't exist yet and is a separate, larger piece of Phase 1's GDPR checklist; the live purge has never been run against production (this session was deliberately hermetic-only, per the owner's constraint) — a first live `--apply` run and its measured row count is a follow-up owner-supervised step |
| 15 | Execution | Stefan noted interest in build partner ship.studio. | Docs are written to be executable by any competent builder or AI session; partnership is a business decision outside doc scope |
| 16 | Competitive | Nederland in Beeld self-reports "82+", "90+", and "167" sources in different places — figure unreliable. | Treated as positioning color only; no doc claim depends on the number |
| 17 | Pricing | Scoop alerts: subscription (~€29/mo per the notes) vs. credit-funded — an individual subscription sits in tension with the "no subscription, no fine print" promise. | Slotted Phase 3 with the pricing model explicitly undecided; re-decide at Phase 3 design |
| 18 | Product | Does serving covered historical periods from a cadence-overdue table *with a staleness warning* satisfy principle 3 (c), or should it refuse outright? | **Resolved** (Stefan, 2026-07-02): warn, not refuse — warn-and-serve stands as written in [05-data-rules.md](05-data-rules.md) |
| 19 | Engineering | Intent-confidence threshold values and calibration procedure for R7 (clarify vs. answer). | **Resolved** (2026-07-03, WP6): `answerThreshold 0.9` / `runnerUpThreshold 0.35`, calibrated against a 45-case labelled set ([benchmark/intent-labelled-set.json](../benchmark/intent-labelled-set.json)) — measured: correct parses at 0.92–0.98, the model's shaky readings at 0.75–0.85; final state 45/45 with zero outcome flips over 3 repeats. Procedure + measured basis in ADR [012](decisions/012-intent-parsing-llm-harness.md); re-runnable via `npm run intent:eval` |
| 20 | Roadmap | Trojan-Horse demand-data reports and citation-funding/ads are marked "Recommended reject" — architect judgments, not interview decisions. | **Resolved** (Stefan, 2026-07-02): rejections confirmed; recorded in the business-model section of [01-product-vision.md](01-product-vision.md) |
| 21 | Product | Demand-driven table onboarding (Phase 2–3): may a freshly fetched table answer immediately using auto-generated "provisional" mappings, or only after mapping review? | Review-first assumed (ingest now, answer after mapping review, notify the user) — instant-provisional rejected for now because unreviewed mappings risk confident wrong answers, the failure principle (a) exists to prevent. **Revised (Stefan, 2026-07-04):** the original framing implied a *person* reviews each table — challenged as unnecessary, correctly: what actually catches mapping errors is *independent verification*, not human judgment, and the project's own strongest example (the werkloosheid seasonally-adjusted/yearly mismatch, ADR [019](decisions/019-open-ended-period-ranges.md) §4) was in fact caught by a **structural consistency check** (does this coordinate combination exist with real published data), not by anyone eyeballing StatLine. **New design — fully automated, no manual click:** an onboarding pipeline runs (a) a structural check — does the candidate coordinate combination have real, non-null data across a representative period range; (b) a label-consistency check — does CBS's own published dimension/measure label text match the concept being registered; (c) an independent cross-check against a second, differently-sourced reference figure where one is available. **High confidence on all three → the table auto-graduates from `needs_review`, zero manual click** (satisfies the existing rule in [05-data-rules.md](05-data-rules.md): "mapping review means passing those tasks; nothing else counts" — the tasks' frozen-key values now come from this automated verification, not a person). **Low confidence, or genuinely multiple valid CBS definitions with no clear default → never silently pick one and never refuse: show each candidate's real, correctly-attributed value with its own definition label**, adopting Stefan's proposal directly ("hier zijn de gerelateerde cijfers, zo kun je het van beide kanten bekijken") — related to [#39](open-questions.md) (deferred for *existing* tables; worth revisiting there too). **Residual risk, named not hidden:** an automated check can still miss an error that exists identically in our ingested copy *and* whatever independent source it's checked against (a true shared blind spot) — narrower than "no check at all," and further caught by the disclosure fallback in most remaining cases; accepted as a monitored trade-off, revisit if it ever produces a measured wrong high-confidence answer. **Placeholder queued in [08-build-plan.md](08-build-plan.md) as WP16 (working number)** so this decision surfaces to a session reading only the build plan, not just here |
| 22 | Product | Published answer pages (Phase 2) vs. CBS retroactive corrections: freeze pages as as-of snapshots with a correction banner, or re-validate/re-render on sync? | **Recommendation:** as-of snapshot + visible "gegevens per {sync-datum}" + correction banner when the batch-diff log touches the page's result IDs. Decide at Phase 2 design |
| 23 | Ops | Owner-alerting channel (email / push / other) for batch failures, quarantines, missed syncs, health checks. | Transactional-email provider chosen with Phase 1 magic-link auth (it needs one anyway); channel finalized then |
| 24 | Product | Demand-driven onboarding: review throughput/SLA, and what users are told about wait times for "your table is being prepared". | Decided at Phase 2–3 design; the table onboarding rule ([05-data-rules.md](05-data-rules.md)) already bounds quality per table. **Revised (Stefan, 2026-07-04), per [#21](open-questions.md)'s new automated design:** on the high-confidence path there is no human queue to wait on at all — the bound is ingestion + verification run time (seconds to minutes for a small table; existing ingests already measure 4–6 min even for small tables, [#34](open-questions.md); large tables slower). On the low-confidence path there is no wait either — the honest multi-candidate answer ships immediately instead of the table being held back. "Your table is being prepared" messaging is now only realistically needed for genuinely large first-time ingests (multi-million-row tables) or v3-only tables needing the second adapter (ADR [003](decisions/003-cbs-access-layer.md)) — a narrower case than originally scoped here. **Wording + pricing decided (Stefan, 2026-07-04, session 18):** the message must never promise a specific time bound (it varies too much to be honest) — approved copy: *"Dat onderwerp staat nog niet in onze database. We vragen de cijfers nu automatisch op bij het CBS en controleren ze — meestal een kwestie van minuten. Je krijgt een e-mail zodra je vraag beantwoord kan worden. Heb je ondertussen nog een andere vraag?"* (names no fixed minute count on purpose). **This response costs credits — it is NOT free like an outright refusal** (owner decision, reversing the assumption floated mid-discussion that it should refund like a refusal): triggering real onboarding work is a rendered service, not a non-answer. **Resolved (Stefan, session 26, ADR [026](decisions/026-on-demand-fetch-job-architecture.md)): priced at 100 credits (the existing "heavy" tier — reuse, not a new tier).** Refund-on-verification-failure needs no new policy: `src/billing/gate.ts` already compensates in full on any refusal outcome or thrown exception (verified session 26) — a verification failure just routes through the existing refusal path. The e-mail notification uses the Resend API directly (the account already exists per RUNBOOK for the SMTP relay; this is a new direct send, not a reuse of the SMTP path). |
| 25 | Product | Claim verification ("klopt het dat X?") — the product's namesake action — needs a claim intent type + claim-vs-actual derivations. | **Confirmed** (Stefan, 2026-07-04 — deferred to architect judgment): Phase 0 restates the official figures without a verdict ([02-user-scenarios.md](02-user-scenarios.md)); full verdict support stays slotted Phase 1 ([06-roadmap.md](06-roadmap.md)) |
| 26 | Data | Municipal time series across herindelingen (region reorganizations) carry comparability breaks (trendbreuken) that CBS marks in footnotes the pipeline doesn't ingest. | Out of scope for Phase 0 (benchmark uses the stable G4); herindeling awareness (break flags / region vintages) is a Phase 1 scope decision ([05-data-rules.md](05-data-rules.md), platform-change risk) |
| 27 | Product | Browse-layer page inventory: which topics × regions at which granularity get a pre-rendered page, and in what rollout order? | Driven by the loaded-table registry; designed late Phase 1 with SEO input |
| 28 | Product | Thin-content quality bar for programmatic pages: minimum substance per page (chart, table, comparisons, text), when *not* to generate a page, and rollout pacing to avoid programmatic-spam classification by Google. | Quality over page count assumed; the bar is defined before the first public pages ship |
| 29 | Design | Three UI design sessions **deliberately deferred by Stefan (2026-07-02)**, to be done interactively with him (with visual mockups): (a) the answer-card presentation, (b) the browse-page anatomy (feeds #27/#28), (c) the social-export template — design intent already recorded: gradient background, chart in a white card with small radius and padding, site URL beneath the card in the gradient; per-platform formats with a pre-generated X/Twitter preview. | Nothing blocks on this: Phase 0 builds a plain, functional answer card whose *content* is already bound by the invariants in [05-data-rules.md](05-data-rules.md) (ugly is acceptable per [03-mvp-scope.md](03-mvp-scope.md)); the design pass happens whenever Stefan wants — at the latest before Phase 1 beta (answer card) and before the first public pages ship (browse page). **Update (2026-07-04, WP18):** the CONTENT half of validation-finding F3 (the memo slotted it here) is shipped — meta questions about the product now get dedicated deterministic templates (ADR [022](decisions/022-meta-question-templates.md)); what remains at this design session is visual presentation only |
| 30 | Data | Ingestion: a *fetch/network* failure is not one of the five validation checks — should it quarantine the table like a validation failure does? | Assumed no (2026-07-02, ingestion session): the batch fails loudly (non-zero CLI exit, plain-language summary) but already-validated data is not suspect, so the table stays `active`. The five checks quarantine; infrastructure blips don't |
| 31 | Data | CBS status transitions (voorlopig → definitief) rewrite existing cells on sync. Are those "silent retroactive corrections" for the correction-diff log? | Assumed no (2026-07-02, ingestion session): the correction log names cells whose *value* changed; routine status lifecycle updates are counted in `rows_updated` but not listed as corrections. Revisit if a status flip ever matters editorially |
| 32 | Data | A sync can return *fewer* cells than stored (CBS removed cells — rare, distinct from truncation caught by the ±20% check). Keep serving the stored cells, or quarantine? | Assumed keep-and-count (2026-07-02, ingestion session): missing cells are retained and reported per batch as `rows_missing`, never deleted. Whether a persistent nonzero count should alert/quarantine: decide when it first happens |
| 33 | Ops | Database is at **230 MB of Supabase's 500 MB free tier** (measured 2026-07-03) — 219 MB is the *full* CPI table (611k cells, all spending categories), of which the Phase 0 benchmark uses only the headline series. Slice CPI (like `03759ned`) or budget the paid tier before Phase 1 adds tables? | **Decided** (Stefan, 2026-07-04): slice the CPI table down now rather than pay for a bigger tier. **Not yet buildable as a same-session flip** — [#34](open-questions.md) already flags that the tooling to safely *change* a registered slice doesn't exist yet (needs a reviewed update + rebaseline flow); queued as a small scoped engineering task, not done in this session |
| 34 | Engineering | Ingestion follow-ups observed at first live ingest, deferred to the registry work package: (a) no path exists yet to *change* a registered slice (needs a reviewed update + rebaseline flow); (b) syncs are slow for their size (4–6 min for small tables) because dimension-label writes go row-by-row over the wire — batch them; (c) concurrent syncs of one table aren't locked — add an advisory lock before Phase 1 scheduled syncs. | None of these affect correctness today (single operator, manual CLI); tracked so they're built deliberately, not rediscovered |
| 35 | Product | B6 ("Hoeveel woningen telde Nederland in 2024?") is ambiguous between the 1-januari-2024 stock (Beginstand voorraad) and the 31-december-2024 stock (Eindstand Voorraad) — both exist in `82235NED`, ~70k apart. | **Assumption, now live** (2026-07-03, benchmark-key freeze WP3; encoded in the registry WP4): pinned to Beginstand (1 januari), matching the "op 1 januari" snapshot convention already used for the population table in this same benchmark. Recorded in [benchmark/answer-key.json](../benchmark/answer-key.json) and as `canonical_measures.housing_stock_start_of_year` ([src/registry/defaults.ts](../src/registry/defaults.ts), ADR [010](decisions/010-registry-canonical-measures.md)), Eindstand kept as a visible alternate in both. Registry-internal variant choice (R7) — owner may still override; a one-row change + `npm run registry:apply` |
| 36 | Product | B9 ("Hoeveel faillissementen werden er in 2025 uitgesproken?") is ambiguous between "Totaal rechtsvormen" (4,105, includes personal bankruptcies) and "Bedrijven en instellingen" (3,226, businesses only) in `82242NED`. | **Assumption, now live** (2026-07-03, benchmark-key freeze WP3; encoded in the registry WP4): pinned to "Bedrijven en instellingen" per docs/07's note that press coverage usually cites this reading. Recorded in [benchmark/answer-key.json](../benchmark/answer-key.json) and as `canonical_measures.bankruptcies_businesses` ([src/registry/defaults.ts](../src/registry/defaults.ts), ADR [010](decisions/010-registry-canonical-measures.md)), Totaal kept as a visible alternate in both. Registry-internal variant choice (R7) — owner may still override; a one-row change + `npm run registry:apply` |
| 37 | Engineering | B20's freshness refusal: when the freshest *available* CPI month (currently 2026MM06, Voorlopig) and the freshest *Definitief* month (2026MM04) diverge, should the refusal offer the freshest available period regardless of status, or only the freshest Definitief one? | **Resolved** (2026-07-03, query WP5): freshest-available is the primary offer, carrying its CBS status so the answer layer can mark it per R11; freshest Definitief is exposed alongside as `freshestDefinitief` ([src/query/types.ts](../src/query/types.ts), `FreshnessInfo`). A freshness refusal offers **period + status only, never a value** — a refusal carries no numbers (principle c). Verified by the B20 test in [tests/query/benchmark-intents.test.ts](../tests/query/benchmark-intents.test.ts) |
| 38 | Engineering | The WP5 intent contract supports one varying axis per question: several periods at one place, or several regions at one period — never both (no "compare the G4's growth 2019–2024" in one intent). | **Assumption** (2026-07-03, query WP5; ADR [011](decisions/011-query-contract.md)): B1–B14 need at most one varying axis, so the cross-product shape is out of contract (`invalid_intent`) rather than half-supported. Revisit trigger: a real question shape that needs both, at WP6 calibration or benchmark revision — extend the contract + `INTENT_SCHEMA_VERSION` then |
| 39 | Product | When we resolve a registry-internal variant to its canonical default (R7), should the answer also proactively surface that a *non-chosen* alternate reading exists (e.g. "woningvoorraad per 1 januari — een cijfer per 31 december bestaat ook"), or only state the reading we used? Relates to [#35](open-questions.md) (housing 1-jan vs 31-dec) and [#36](open-questions.md) (bankruptcies businesses vs total). | **Resolved, reopened and reversed** (Stefan, 2026-07-04 — supersedes the 2026-07-03 "maybe later" deferral): **yes, as a standing policy** — never silently pick a definition when a registry-internal alternate exists; state both values with their definition labels. Applies generally (any future `canonical_measures` entry with alternates), not just B6/B9. Matches the transparency stance already taken for future table onboarding ([#21](open-questions.md)). **Not yet built**: the alternates exist in the registry (`canonical_measures[].alternates`, [src/registry/defaults.ts](../src/registry/defaults.ts)) but aren't threaded into `Attribution` in the query layer yet — needs that plumbing + a WP7 copy decision on phrasing. Small, well-scoped follow-up, queued not built |
| 40 | Product | Intent-parser period policy (WP6): a question with an explicit present/recency signal ("Hoe hoog is de werkloosheid nu?") answers with the freshest **published** period — stated explicitly in the answer — instead of asking "welke periode?". Past tense without a named period, and change questions without a baseline, DO clarify. Is answer-with-stated-period the right default, or should present-tense questions also clarify? | **Confirmed** (Stefan, 2026-07-04): keep the current behavior — present-tense questions answer with the freshest period stated explicitly, rather than always clarifying first. No prompt/threshold change |
| 41 | Product | WP7 excludes the user's question text from the answer-phrasing prompt (R2 read literally: only validated results + attribution metadata; also closes the prompt-injection surface). Cost: answers can't mirror the question's wording ("Ja, de bevolking groeide…"). Should WP9's clarification/refusal phrasing — or a later UX pass — reintroduce the question behind a sanitization design? | **Assumption, now live** (2026-07-03, WP7; ADR [013](decisions/013-answer-composition.md)): question excluded. Benchmark scoring is unaffected (it scores numbers/binding/attribution, not conversational tone). **WP9 (2026-07-03, ADR [015](decisions/015-refusal-clarification-composition.md)) kept the exclusion**: refusal/clarification phrasing is deterministic templates that never see free text; the only place the question reaches an LLM is the clarify-reply *parse* (schema-bound output, same confinement as WP6). Next natural revisit: the chat-UI session, with real tone feedback |
| 43 | Engineering | Staleness thresholds (docs/05 failure-table row, implemented WP9): when does "table past its expected update cadence" trip? | **Assumption, now live** (2026-07-03, WP9; ADR [015](decisions/015-refusal-clarification-composition.md)): cadence-string prefix → max age = expected cadence × 1.5 (monthly → 47 days, quarterly → 138, yearly → 549), measured against our `last_sync_at` with the pipeline's injected reference date; unknown cadence strings make no staleness claim. Constants in [src/answer/respond/staleness.ts](../src/answer/respond/staleness.ts), owner-tunable. The margin absorbs CBS's "no fixed publication times"; basis is *our sync age*, not CBS's own publication lag — revisit if a missed-CBS-publication signal is ever ingested |
| 42 | Product | Factor-unit cells (B6 `8.204` × `x 1 000`; B12 `57,6` × `1 000 euro`) are answered with the verbatim value + factor unit — never expanded to `8.204.000`, because expansion is a multiplication no registered derivation backs (R1/R3/R5). Users may find the factor form harder to read. Register a `scale_expansion` derivation so answers can also state the expanded number, traceably? | Open (2026-07-03, WP7; ADR [013](decisions/013-answer-composition.md)). The R10 guard keeps the factor visible either way; expansion would be an additive derivation in the query layer, then a WP7 copy decision |
| 44 | Ops | Benchmark runs write real rows into the live `audit_answers` table (ADR [017](decisions/017-live-benchmark-run.md), decision 3) with `user_id = null` — once real user traffic exists, nothing distinguishes benchmark rows from user rows in reporting or retention tooling. Tag benchmark runs? | **Resolved** (Stefan, 2026-07-05): add a source-tag column (benchmark / validation / user) **in WP13's migration** — that WP already ships a migration and is the moment real users arrive. Existing rows 1–76 (benchmark + validation runs; 74–76 are WP14's live spot-check, 2026-07-04) get back-tagged. In the WP13 brief ([08-build-plan.md](08-build-plan.md)) |
| 45 | Product | Chart embedding in third-party websites (Phase 2+). Should users be able to take a chart beyond our own pages — and if so, how? | Owner's initial position (2026-07-04): embedding is a paid feature (costs extra credits), with two delivery shapes to evaluate: (a) download as a standalone file (SVG for print/vector quality, high-res PNG for platforms that need raster) and (b) a live embed (iframe or whatever mechanism proves best at design time). Trade-off to weigh deliberately: embeds keep visitors on the third party's site, while the already-planned landing-page share links (#9) bring visitors to ours — pricing embeds higher partly compensates for that lost traffic. Technically cheap either way: every renderer draws from the same chart spec (ADR 007), the WP8 SVG renderer is already reserved as the static-export path (ADR 014), and credit pricing plugs into the pricing-config seam (ADR 006). Decide at the Phase 2 sharing/design session, together with #9 and the social-export template (#29). **Cross-reference (owner, 2026-07-04):** if chart toggles (#46) ship before this does, exported/embedded artifacts must not silently carry a visitor's toggled/customized view — see #46(c) for the reasoning and the reputational-risk concern; design both together if their timing overlaps |
| 46 | Product | User-adjustable chart presentation (toggles). Should viewers be able to adjust how a chart presents its data — e.g. hide/show individual series, or highlight one to compare against the rest? | Owner raised 2026-07-04. Three constraints to honor at design time: (a) R6 forbids a *renderer* silently omitting data, but an explicit, visibly-stated user toggle is a different thing — the design must keep that distinction clean (e.g. a visible "2 van 4 reeksen verborgen" indicator so a screenshot of a toggled chart can't circulate looking like the full official picture); (b) presentation state is client-side only — it must never alter the underlying chart spec or the stored audit record (R8); (c) **exporting/embedding a toggled view is a distinct, deliberately deferred risk** (owner, 2026-07-04): a visitor could hide/highlight series, then download or embed that customized view as if it were the official chart — misrepresenting the data under our own attribution, real reputational ("discredit") risk. On-screen toggling is one thing; a static file or a live embed leaves the page context where the on-screen disclosure lives, so "just export whatever's on screen" is not an acceptable default — whatever the eventual answer (reset-to-canonical before export vs. baking the same disclosure into the exported artifact itself), it must be a deliberate design call when export/embedding is actually built ([#45](open-questions.md)), not an accidental side-effect of combining two features. **Explicitly not now:** owner wants the core product live before adding this combination — mostly falls out naturally already, since embedding/export is Phase 2+ while toggles are Phase 1 earliest, but this note stops the interaction being skipped when both eventually exist together. Technically cheap for the chat surface itself: Recharts supports legend-click series toggling natively. Phase 0 explicitly ships "no interactivity" (docs/03); slot the toggle feature itself at the answer-card design session (#29), Phase 1 at the earliest |
| 47 | Ops | Abuse protection for the public chat: no login, no rate limiting, and every question costs real money (~€0.02). The €25/mo spend cap bounds the worst case, but also means one script could exhaust the month's budget in an afternoon and silently stop the product for everyone, including the owner. | **Resolved** (Stefan, 2026-07-04): the integrated chat (going on a real website page) requires an account before any question — no anonymous free-question trial there, closing the scripted-abuse vector structurally. Reaffirms and hardens [#3](open-questions.md)'s existing "chat requires an account" assumption. Prompted a roadmap reprioritization ([06-roadmap.md](06-roadmap.md)): billing/credits (previously Phase 2) is pulled forward alongside auth, since a real public page is going live imminently rather than staying a recruited-only beta. A *separate* unmetered anonymous-trial idea is tracked apart from this at [#53](open-questions.md) — it must not reopen this abuse vector on the main product |
| 53 | Product | Isolated-budget anonymous trial ("lead magnet"): Stefan wants to test offering 2 free questions with no account, as a growth funnel — but on a *separate* page from the main integrated chat, funded from its own capped budget, so anonymous abuse there can't touch the main product's spend or undo the [#47](open-questions.md) account-gating decision. | Raised 2026-07-04, alongside #47. **Sequencing decided 2026-07-04:** built as a fast-follow *after* the paid flow ships, not in the same work package — keeps the first accelerated work package to one access surface instead of two. Not yet designed: the isolated-budget mechanism itself (candidates: a second Anthropic API key with its own smaller spend cap, vs. an app-level counter) and the per-visitor tracking method (cookie/IP — same bypass caveat as #47 applies, but a capped blast radius makes that acceptable here) |
| 54 | Ops/Legal | Business-entity readiness for real payments: [RUNBOOK.md](../RUNBOOK.md) already lists "KvK registration + business bank account... required before Stripe can pay out in the Netherlands" and calls it "the slowest item" — but that checklist entry was filed under Phase 2, and billing just moved into Phase 1 ([#47](open-questions.md)). | **Decided** (Stefan, 2026-07-05): **he will start KvK + business bank account within a few months** — not immediately. Consequence, surfaced and accepted: WP13 ships with Stripe fully working in **test mode**; flipping to live is a small config step once the Stripe account clears. ⚠ Launch-timing awareness: until then, the public page can go live with accounts + the 5 free signup credits (#3), but users who run out have **no way to buy more** — factor into when the page actually launches. Update this entry when registration starts. **Concrete consequence surfaced 2026-07-04:** wiring the live Stripe test-mode account showed its business address is Chiang Mai, Thailand (Stefan's actual residence) — Stripe won't offer iDEAL to any account outside the Netherlands/eligible EU-EEA countries regardless of test/live mode, so this isn't a settings toggle to flip later, it's gated on this same KvK/Dutch-registration step. **Owner decision (2026-07-04): ship card-only for now**, add iDEAL as a fast-follow once KvK clears — cards process fine today regardless of account country, and `src/billing/stripe-checkout.ts` needs no code change either way (payment methods are never hardcoded). **Owner decision (2026-07-04, session 18, supersedes the timing above): KvK starts only when the website is completely finished — NOT "within a few months", not in parallel with the build. Standing instruction to sessions: STOP raising KvK as a next step or blocker in session kickoffs, wrap-ups, and "next up" lists; it is deliberately parked until Stefan declares the site done. Everything it gates (live Stripe mode, iDEAL, real payouts) inherits that timeline** |
| 48 | Product | Y-axis honesty policy for charts: a bar chart whose axis doesn't start at zero can make a small difference look dramatic. Both renderers currently just fit the data range to the values — nobody has decided a policy. | **Resolved + implemented** (policy Stefan 2026-07-04; both renderers verified/implemented 2026-07-04): bar charts must start their value axis at zero (true proportion); line charts may zoom to show real movement, since position rather than area carries the meaning there. ⚠ The problem statement above was inaccurate: the deterministic **SVG renderer already implemented this at WP8** — `makeScale` in [src/chart/render.ts](../src/chart/render.ts) floors bar domains at 0 with the exact rationale ("Bars encode length … a non-zero baseline visually lies about ratios") and lets lines zoom (min−pad/max+pad); only the **Recharts wrapper** (the sole live user-facing path — the SVG renderer is the deferred Phase-2 static-image path, not mounted in the chat UI) lacked it. Now implemented there too: `yAxisDomain(kind)` in [web/components/chart.tsx](../web/components/chart.tsx) returns `[0, 'auto']` for bar / `['auto', 'auto']` for line, wired into the `YAxis` and unit-test-pinned (bar→0, line→zoom). What genuinely remains for the chart **styling** pass (#29) is visual polish (tick presentation, colours, spacing), not the honesty policy, which is done |
| 49 | Product | Corrections vs. exported/embedded charts: #22 already covers our own pages (freeze as an as-of snapshot + correction banner). An iframe embed on someone else's site is live and would silently change under their article if CBS later corrects the data; a downloaded SVG is frozen and would go quietly stale instead. | **Resolved** (Stefan, 2026-07-04): live embeds (iframes) auto-update when CBS corrects the underlying data; downloaded files freeze at export time. Matches how each format naturally behaves. Both must visibly carry their own date/status so neither promise is silent — implement when the embedding feature itself is built ([#45](open-questions.md)) |
| 50 | Legal | Attribution durability on exported artifacts: CBS's license requires attribution, and it's non-droppable on our own pages (R4), but a downloaded/embedded file can be cropped or edited outside our control. The social-export template already bakes attribution in as a watermark. | **Resolved** (Stefan, 2026-07-04): not a blanket rule — decide attribution treatment per export shape at design time, the same way the social-export template already does for its own case. Revisit if any shipped shape turns out to strip attribution in practice |
| 51 | Legal | Accessibility: nothing in the register currently addresses color-only chart encodings (excludes color-blind readers) or screen readers (an SVG has no accessible fallback). The European Accessibility Act may apply to a paid Dutch consumer digital service by Phase 2. | **Resolved** (Stefan, 2026-07-04): deferred — no proactive fix or legal check scheduled; revisit if legally required or a user raises it. Note the EAA risk is more live than when raised, since paid public launch is being pulled forward into the next work package rather than staying a later Phase 2 concern (#47, [06-roadmap.md](06-roadmap.md)) |
| 52 | Product | Numeric data export (CSV/table) for journalists: Phase 1 users will likely want the underlying figures in their own spreadsheets, not just a chart image. The CC BY 4.0 license fully permits redistributing the data this way. | **Resolved** (Stefan, 2026-07-04): targeted at Phase 1 — ship alongside the (now-accelerated) auth/billing work rather than waiting for a later phase. Format/placement decided at that build session. **Built (2026-07-05, session 22 overnight, WP21):** "Download als CSV" under every answer — Dutch-notation CSV (`;` + decimal comma, CBS's own convention), attribution + CC BY marking INSIDE the file via the shared `buildAttributionLine`, values round-trip-pinned to the stored cells exactly (R1/R3), explicit derivations exported with marking + source cell ids (R5). Format reasoning in the [WP21 brief](08-build-plan.md); international-notation/XLSX variants deliberately not v1 (revisit on real user demand) |
| 55 | Engineering | Open-ended period ranges ("sinds 2015", "afgelopen vijf jaar", "nu vergeleken met vijf jaar geleden"): the WP6 `PeriodSpec` cannot express an open end or two disjoint periods, and the prompt is deliberately date-free (ADR [012](decisions/012-intent-parsing-llm-harness.md)) — so the model *cannot* emit a correct end year and deterministically emits a degenerate `fromYear == toYear` range (measured twice at 0.92 confidence, validation pass 2026-07-04, [validation-results-2026-07-05.md](validation-results-2026-07-05.md) F1/E2). These are common journalist phrasings that should ultimately **answer** with a series. | Interim behaviour **implemented 2026-07-05** (no schema change): the degenerate shape exits to a period clarification with a range option clamped to the loaded slice — ask, never guess. **Approved** (Stefan, 2026-07-05): the `PeriodSpec` extension (open-ended range + the two-disjoint-periods comparison shape in the same revision; `INTENT_SCHEMA_VERSION` bump, prompt change, 45 fixtures re-recorded, thresholds re-checked per ADR 012, ~€0.50–1.50) runs as its own small supervised work package — **WP14 in [08-build-plan.md](08-build-plan.md)**, next session, before/alongside WP13. **Resolved** (2026-07-04, WP14 built — ADR [019](decisions/019-open-ended-period-ranges.md)): `since`/`last_n`/`now_vs_ago` in the raw-parse contract (v2, prompt v4; `StructuredIntent` needed NO change — the brief's "INTENT_SCHEMA_VERSION bump" turned out unnecessary, corrected in the ADR); labelled set 45→54, **measured 54/54 + zero flips ×3**; V01 answers (quarterly series — see the ADR's coordinate finding), V02 answers, V28 → honest not_published refusal (owner-confirmed in-session); live spend ≈ €2.40 in-session estimate (above the ~€1.50 estimate — one prompt-rule iteration for a dropped-region wobble, owner-approved before spending; reconcile against the Anthropic Console) |
| 56 | Product | Low-confidence "echo" clarifications (R7 rule 3: single reading below the answer threshold → "Bedoel je {reading}?") use the model's own reading as the sole option — which can name data outside the loaded slice ("alle gemeenten … vanaf 1970", validation pass V22/V23) or silently drop an impossible grain ("per dag", V21), deviating from docs/05's "concrete, *actually available* options". Honest (no numbers, one-round cap ends in an honest refusal) but a soft promise. | **Resolved** (Stefan, 2026-07-05): **check before offering** — a suggestion is only shown if confirming it would actually produce an answer; otherwise fall back to naming what IS available (restores the docs/05 letter). Slotted into **WP15** (conversation memory, [08-build-plan.md](08-build-plan.md)) since both touch the clarification flow. **Implemented** (WP15, 2026-07-04, ADR [021](decisions/021-conversation-memory-structured-context.md) decision 4): `echoServability` dry-runs the suggestion through the real `runQuery` (verdict only — the return type carries no cell values by construction); unservable echoes are replaced by a deterministic clarification naming the gap-free loaded year window or the canonical freshest period. Scope notes recorded in the ADR: rule-4 two-option clarifications are NOT dry-run (→ [#63](open-questions.md)); V21's reading-fidelity gap ("per dag" echoed while the intent carries another grain) is explicitly not solved by servability checking |
| 57 | Product | Multi-turn conversation memory: absent by design in Phase 0 (only the one-round clarification carries state, ADR [015](decisions/015-refusal-clarification-composition.md)). The validation pass measured it as the **single biggest usability gap** — 7 of 38 owner questions were follow-up-shaped and all dead-ended in generic templates ([validation-results-2026-07-05.md](validation-results-2026-07-05.md), signal 1). Not currently in the Phase 1 "Adds" list — scheduling it is a roadmap change only the owner can make. | **Resolved** (Stefan, 2026-07-05): **yes — Phase 1 work package, sequenced directly after WP13** ("WP13 first, then memory"). Design constraint stands as recommended: **structured** context only — the previous turn's resolved intent (the stored query plan, ADR 016) offered as a merge candidate to the parser, the proven WP9 clarify-reply mechanism generalized; never raw chat history into prompts (would reopen the R2/injection surface #41 closed). Brief: **WP15 in [08-build-plan.md](08-build-plan.md)** (includes the #56 suggestion-check). **Implemented** (WP15, 2026-07-04, ADR [021](decisions/021-conversation-memory-structured-context.md)): `ConversationContext` built server-side from the resolved intent (answers + query-refusals), client-held like `PendingClarification`, registry-validated before any use, offered to the parser via a follow-up prompt mode appended to the base prompt (all 54 intent + 7 clarify fixtures stayed byte-identical); audit rows record the offered context (migration 009). v1 limitations recorded in the ADR (explicit targets, quarter/month ranges, dimension-breakdown follow-ups, one referent deep) |
| 62 | Pricing | Standalone HTML/JS chart export (Phase 2, moved from parked/Phase 3 same session): Stefan wants it credit-priced, precisely because — unlike an embed or a shared page — it carries no backlink/traffic value back to the site. How much? | Session first recommended **30 credits** (value-based, not cost-recovery — the render is fully deterministic, ADR [007](decisions/007-chart-spec-rendering.md), zero LLM/API cost; set between `simple`/20 and `analysis`/60 so the free watermarked/backlink tier stayed the obviously better default for most users). **Owner decision (Stefan, 2026-07-04): raised to 300 credits** — a deliberately premium price, well above `heavy`'s 100, not an impulse-buy amount; ≈€3–€7.50 depending on pack ([09-pricing.md](09-pricing.md)'s per-credit rates). **Not yet schema-ready:** `action_class_prices` (migration 006) only models per-question answer classes (simple/analysis/heavy) plus `clarification` — a chart export isn't a question outcome at all, so Phase 2's billing extension needs a broader priced-action concept (export/embed/huisstijl tiers), not a fifth row jammed into today's table. Flagged for whoever scopes that extension, not decided now |
| 61 | Product | Enterprise huisstijl theming (Phase 3): how do we source a company's logo and colors for theming without requiring a manual upload? | **Idea captured, not decided** (Stefan, 2026-07-04) — deliberately left for the build session that actually implements this, not settled now: Brandfetch (or a similar brand-asset API) could fetch logo + colors from a domain/profile; the profile account's photo becomes the square logo, the favicon becomes the icon itself. Stefan's observation: fetching a logo via Google Image search often gives better results than Brandfetch alone — a hybrid/fallback approach is a real option. No technical commitment yet, just a starting point so the idea isn't lost by the time Phase 3 arrives |
| 60 | Product | User dashboard: saved charts + question history — raised by Stefan (2026-07-04, following the WP13 pricing/credits work): "when someone builds a chart in chat, should it save to their account so they can revisit/adjust it later?" and "can we see per-user which questions they asked?" | **Decided (Stefan, 2026-07-04):** two related but distinct pieces. **(a) Question history** — a user seeing their own past Q&A — isn't new: it's the already-named "user-facing audit-trail view" ([03-mvp-scope.md](03-mvp-scope.md): Phase 2+ non-goal until then; seam already exists per [04-architecture.md](04-architecture.md) — "`audit_answers` already stores question→plan→result IDs→numbers; the UI is a view over it"). Nothing new decided here beyond timing (below). **Owner-side lookup already works today, no build needed:** once WP13 ships, `audit_answers.user_id` carries the real user on every question — Stefan can already query which questions a given user asked directly against the database; this was never blocked on a UI. **(b) Saved/pinned charts — new, now decided:** a user can mark a past answer's chart as saved; a saved chart is a **frozen snapshot**, never a live-updating reference — this reuses the fact that an `audit_answers` row's stored chart spec never changes after being written ([05-data-rules.md](05-data-rules.md): "these records live forever"), so "saving" is just a pointer/flag on an existing row, not a new copy of the data. Adjustments on a saved chart are **presentation-only** (series on/off, styling) — the same mechanism as the already-planned chart toggles ([#46](open-questions.md)), applied to a saved item instead of a live one; changing the underlying region/period is explicitly **out of scope here** — that's really a new question, closer to the already-seeded "drill-down buttons" feature ([04-architecture.md](04-architecture.md)) than to chart editing. **Phase 2**, bundled with the audit-trail view and Visualisatie Studio — not pulled into Phase 1 alongside WP13/WP15. **Real dependency, still open:** [#14](open-questions.md) (question-log retention/anonymization) is unresolved and directly blocks both pieces — a dashboard that shows/stores question history for an unbounded time needs that answered first, at the Phase 1 GDPR checklist as already planned |
| 58 | Pricing | WP13's brief originally assumed clarification rounds (doorvragen) cost 0 credits, same as outright refusals. But a free clarification round still costs Stefan real Anthropic API money, and nothing stopped a user fishing across unrelated topics for free (ask about an unloaded topic, get a free doorvraag, try a different topic, repeat) without ever spending a credit. | **Resolved** (Stefan, 2026-07-04): **only outright refusals stay free** (full compensation back to 0 — there's nothing to explore, the system just doesn't have the topic). **A clarification round costs its own flat, small price**: a new `clarification` action class in the pricing config. First drafted same-session at 1 credit (equal to `simple`, so the debit needed no compensation at all); **revised same day** once the whole scale widened ×20 for headroom ([#4](open-questions.md)) — final launch value **10 credits**, deliberately *half* of `simple`'s 20, so a `simple`-assumed debit now genuinely compensates 10 credits back when the outcome turns out to be a clarification (no longer a same-value no-op). Deliberately kept as a small, fixed set of classes (now spanning 10–100) rather than a continuously-computed complexity score: there is no complexity classifier yet (analysis/heavy are still inert placeholder rows), so finer-than-four granularity today would just be arbitrary numbers; the schema is a plain `credits integer` column per class, so a finer scale can be added later without a migration once a real classifier exists. Encoded in the WP13 brief ([08-build-plan.md](08-build-plan.md)) and migrations [005](../migrations/005_credit_ledger.sql)/[006](../migrations/006_pricing_config.sql). Structural invariant: the `clarification` price must never exceed the cheapest answer-class price (10 ≤ 20), or a partial-compensation row would need a non-positive delta, which the ledger's CHECK constraint forbids by design |
| 59 | Legal/Engineering | WP13's `credit_transactions` FK to `auth.users(id)` deliberately has **no `on delete cascade`** (ADR [020](decisions/020-credit-ledger-and-billing-gate.md) decision 5) — cascading a user deletion would silently contradict the append-only, lives-forever design of a financial ledger. But no account-deletion feature exists yet, so the tension is real but theoretical today: the day one is built, a deleted user's `auth.users` row cannot actually be removed while ledger rows reference it (the FK blocks it by default), and simply detaching the ledger from the user (nulling `user_id`) isn't legitimate either — the ledger's whole point is knowing whose balance is whose. | **Not decided — flagged, not resolved.** Candidate approaches for whenever account deletion is designed: (a) anonymize the ledger's link (keep the rows, replace `user_id` with a tombstone value — breaks append-only's "never edit" rule for exactly one column, a real cost); (b) never truly delete the `auth.users` row, only deactivate it (a common pattern that sidesteps the FK entirely); (c) export-then-delete (give the user their ledger history, then genuinely erase, accepting the ledger's completeness invariant is scoped to "while the account exists"). Needs a GDPR-erasure-vs-financial-record-retention legal read, not just an engineering call — same flavor as [#5](open-questions.md)'s "credits never expire" wording check |
| 63 | Product | WP15's #56 fix dry-runs only the R7 **rule-3 echo** suggestion (the owner decision's letter). Rule-4 two-option clarifications ("Bedoel je A of B?") offer readings that passed name→code resolution but were never servability-checked — the same unservable-suggestion class could in principle appear there, and `buildNeedsClarificationAsClarification`'s region presets are guidance text, not checked options. No live occurrence measured yet. | **Open** (raised by WP15, 2026-07-04, ADR [021](decisions/021-conversation-memory-structured-context.md) scope note). Revisit when a validation pass or real usage shows a rule-4 option that cannot be served; the `echoServability` primitive is reusable as-is |
| 64 | Product | Live testing (session 18, 2026-07-04): a question naming two explicit periods/regions in one go ("Hoeveel mensen wonen er in Rotterdam in 2020 en in 2022") gets a clarifying question offering exactly those two readings; replying "Beiden" (both) dead-ends in a `still_ambiguous` refusal (audit rows 79–80) rather than answering with both values. Should the app instead show all explicitly-named values automatically, the way [#39](open-questions.md) already does for registry-internal reading variants? | **Decided (Stefan, 2026-07-04, session 18): yes, build this.** Distinct from #39 — #39 is about an internal technical-reading choice invisible to the user (e.g. housing stock's 1-jan vs 31-dec convention, same regardless of which year is asked); this is about values the **user themselves explicitly named**, which R7 currently classifies as "user-facing ambiguity" requiring clarification. Directly related to [#38](open-questions.md) (the intent contract's one-varying-axis limit — a named multi-period/multi-region question is exactly the cross-product shape #38 scoped out) and ADR [019](decisions/019-open-ended-period-ranges.md)'s explicit revisit trigger ("the general absolute-pair form, '2024 vs 2019', not built — only the narrower relative `now_vs_ago`"). **Not yet scoped into a WP**: needs the intent contract extended to carry N explicit named periods/regions as an answerable multi-value result (not just a two-period comparison), a decision on how "beiden"/"allebei"-style clarify-replies merge, and confirmation the existing comparison/series chart machinery covers N-point rendering without change. **Built (2026-07-05, session 22 overnight; entry in [08-build-plan.md](08-build-plan.md)):** the multi-PERIOD half — a deterministic policy-layer merge (zero prompt bytes; multi-region enumerations already parsed as one candidate, verified). The live reproduction now answers as a two-value series. v1 scope honestly narrow: yearly single-code periods only; "beiden"-style clarify-replies on OTHER ambiguity shapes are unchanged (that reply-merge path is #66's clickable-options territory); the #38 one-varying-axis contract limit stands (a question naming several periods AND several regions still refuses honestly). |
| 65 | Ops/Engineering | Live testing (session 18, 2026-07-04): a real production error left **zero trace** — no `audit_answers` row, no ledger debit (it failed before the billing gate ran), and the server-side stack trace itself was already gone from `vercel logs` by the time the session checked minutes later (short retention on the current Vercel tier). Root cause unrecoverable. Should there be durable, persistent error logging beyond Vercel's short-lived logs? | **Decided (Stefan, 2026-07-04, session 18): yes, build this.** **Execute-ready brief written (2026-07-05 overnight, session 22): WP25 in [08-build-plan.md](08-build-plan.md)** — Postgres `error_log` table (insert-only, fail-OPEN logging), catch-site writes, owner-only access; needs one live migration → next supervised session. Original scoping notes: where errors persist (a new Postgres table alongside `audit_answers`, vs. an external error-tracking service), what triggers a write (at minimum, the catch blocks in `askQuestion`/`replyToClarification`, `web/app/actions.ts`), and access/retention (owner-only). Standing interim practice already recorded in [lessons-learned.md](lessons-learned.md): check `vercel logs` immediately in the same turn a live test error is observed, don't defer |
| 66 | Product | Clarifications currently offer a suggestion as plain text, requiring a free-text reply the clarify-mode LLM re-parses and merges — which can fail (live examples this session: "Beiden" and "Jan modaal inkomen 2024" both dead-ended in `still_ambiguous`, see [#64](open-questions.md) and the modaal-inkomen case in [STATUS.md](STATUS.md)). Since WP15 ([#56](open-questions.md)), every offered suggestion is already dry-run checked for servability before being shown. Could offered suggestions instead be clickable options that resolve directly, skipping a second LLM parse entirely? | **Decided (Stefan, 2026-07-04, session 18): yes, build this.** Because `echoServability` already proves a suggestion is answerable before it's offered, clicking it jumps straight to its known-good structured intent — cheaper (no second LLM call), faster, and immune to clarify-reply-merge failures. **Not yet scoped into a WP**: needs a UI decision (chat.tsx renders clickable option chips alongside/instead of the free-text field when a clarification carries structured options) and a backend decision (a new "resolve this exact offered option" entry point bypassing `replyToClarification`'s LLM merge for clicks specifically, while free-text stays the fallback for when no offered option matches). **Pricing decided (Stefan, 2026-07-04, session 18): yes, cheaper — target ~5 credits** (half the 10-credit free-text clarification-reply price), reflecting that resolving a click needs no second LLM call at all. Exact value to confirm against `docs/09-pricing.md` conventions at build time; needs a new distinct reason/action-class-price row (today's ledger has one flat `clarification` price for the whole reply round, not split by reply mechanism). **Scoped into WP26 as Mechanism A (session 23, 2026-07-05); design in ADR [024](decisions/024-answer-first-defaults-and-clickable-options.md).** Owner deferred WP26 pricing (session 23) → the ~5-credit click price is **not** built into WP26: the click mechanism ships at the normal answer price with no ledger change, and the previously-decided ~5 price lands as a fast-follow config change tracked in [#101](open-questions.md) |
| 67 | Product/UX | WP17's dashboard question history shows a failed clarification round (the clarifying-question audit row and the follow-up refusal row) as **two separate entries sharing the identical original question text** — looks like the same question was answered twice, differently. Confirmed live (Rotterdam 2020/2022 example, audit rows 79–80). | **Decided (Stefan, 2026-07-04, session 18): yes, collapse a clarification round into one folded history item** (question → clarifying question → reply → final outcome shown together), rather than one row per audit record. **Built (2026-07-05, session 20, WP19):** rounds collapse at read time in `getQuestionHistory` (no migration — value-match on the shared original question + the pending's `questionNl`; known two-identical-tabs limitation documented and test-pinned, see the [WP19 brief](08-build-plan.md)); the dashboard renders the exchange inside one fold with the round total labeled "totaal" |
| 68 | Product/UX | WP17's credit-balance panel is server-rendered once per page load — it doesn't reflect a debit until the page reloads, so a user asking several questions in one sitting sees a stale balance. | **Decided (Stefan, 2026-07-04, session 18): yes, update it live.** After each answer, decrement the displayed balance client-side by `GatedResponse.ok.netCost` (already returned, no extra DB read needed). **Built (2026-07-05, session 20, WP19)** as decided, via a new client `Dashboard` boundary + a `Chat.onOutcome` callback. **Plus one session-added extension beyond this decision's letter, flagged for owner veto:** an `insufficient_credits` refusal also syncs the display to the balance it itself reports (same no-extra-read principle — a stale higher number next to a "niet genoeg credits" message would be a visible lie) |
| 69 | Product/UX | Users currently only learn their balance is too low when a question is refused with `insufficient_credits` — no proactive warning beforehand, so running out can surprise a user mid-conversation. | **Decided (Stefan, 2026-07-04, session 18): yes, build a low-balance warning.** Threshold: show it once the balance can cover at most one more `simple` question but not two (today: `20 <= balance < 40`) — the existing `insufficient_credits` refusal already covers the point below that. **Built (2026-07-05, session 20, WP19):** amber banner in the account panel, visible exactly on `simple ≤ balance < 2×simple` with `simple` read live from `action_class_prices` (the threshold tracks a price change automatically) and reacting live to the #68 balance updates; boundary behaviour test-pinned at 19/20/39/40 |
| 70 | Product/UX | "Next-level UX" brainstorm (Stefan's request, 2026-07-04, session 18): source attribution is plain text ("Bron: CBS StatLine, tabel 86141NED...") — could it be a clickable drill-through showing the exact cell, coordinates, and sync date? | **Decided: yes, build.** All the needed data (table id, coordinates, sync date) already exists on every answer — a light first version needs no new backend. Connects to the already-roadmapped "drill-down buttons" (Phase 1) and the Phase 2 user-facing audit-trail view; this can ship as a small standalone piece ahead of either |
| 71 | Product/UX | Same brainstorm: the "voorlopig" (provisional) marking is only in the answer's prose — easy to miss when scanning quickly. A visual badge instead/in addition? | **Decided: yes, build.** Small and visual; the full design pass is deferred ([#29](open-questions.md)), but "ugly is acceptable" pre-launch ([03-mvp-scope.md](03-mvp-scope.md)) means a plain badge can ship now rather than waiting for that session. **Built (2026-07-05, session 22 overnight, WP23):** an amber "voorlopig" pill in every answer's source-chip row whenever any quoted cell is provisional — the message-level generalization of the WP20 stat-card badge (which stays); test-pinned on and off |
| 72 | Product | Same brainstorm: light region/period ambiguity always triggers a clarifying question today (R7), which can then fail to resolve on the reply (live examples: "Beiden", "Jan modaal inkomen 2024", both dead-ended in `still_ambiguous`). Could the app instead answer immediately with the most likely reading plus a lightweight "bedoelde je iets anders?" escape hatch, the way registry-internal variants already resolve to a canonical default? | **Decided: yes, build.** The biggest of this batch — touches R7's clarification policy and the intent parser, not just UI. Addresses the root cause behind [#64](open-questions.md)/[#66](open-questions.md) rather than only the symptoms. Needs real design work (which ambiguity classes are "safe" to default-and-offer vs. still must ask) before a WP brief. **Designed → WP26 (session 23, 2026-07-05); full design + the principle-(c) line in ADR [024](decisions/024-answer-first-defaults-and-clickable-options.md).** The safelist is deliberately narrow (B-region: no region on a geo measure that has a national row → the national total, disclosed via a deterministic `assumptionLine` + a pre-verified correction chip; B-period → the recent TREND, not a single value, owner decision session 23; `region_ambiguous`/below-threshold/rule-4 all stay clarifications — a disclosed pick of a competing entity is still a guess, owner confirmed the safe path). Zero prompt bytes (like #64). **Safelist settled by the owner in session 23; execution now gated only on a supervised build slot + the deferred pricing ([#101](open-questions.md)).** |
| 73 | Product/UX | Same brainstorm: after an answer, there's no quick way to ask an obvious follow-up ("vergelijk met vorig jaar", "en in [andere gemeente]?") without retyping a full new question. | **Decided: yes, build.** Cheap — the underlying derivations (direction/first_last, pre-registered per measure since WP5) and conversation memory (WP15) already exist; this is a UI affordance (suggestion chips under an answer) that sends a ready-made follow-up, not new backend logic. **Deliberately deferred by the overnight session (2026-07-05, session 22, judgment recorded per the brief's own condition):** deterministic templates alone cannot promise a SERVABLE suggestion — an unservable chip invites a paid dead-end round (the #77/#97 cost pattern, and the exact class the WP22 review caught in a template). Belongs with [#66](open-questions.md)'s servability-backed click machinery, where every offered chip is dry-run-proven answerable before it renders **Session 30 (2026-07-08): owner explicitly re-requested this; the deferral condition is now designed away — WP29 (ADR [029](decisions/029-follow-up-suggestion-chips.md)) generates chips deterministically from the answered intent and shows ONLY `echoServability`-passing ones; v1 fills the input (#75 convention), v2 swaps to WP26's Mechanism-A click. Execute-ready.** **✅ BUILT + MERGED (WP29, session 35, 2026-07-11 — PR #24, owner-approved in-chat merge `0c4b324`):** `src/answer/respond/suggestions.ts` (4 generators, every candidate dry-run gated, cap 3, fail-open ×2), `AnswerResponse.suggestions` (structural, R8 `text` byte-pinned by test), chips in `chat.tsx` with the #75 fill-don't-send handler verbatim. Zero LLM, zero prompt bytes, no new charged entry point. **v1 = fill the input; the v2 upgrade seam (WP26 Mechanism-A handler swap, no re-parse) stays open — revisit when WP26 ships.** |
| 74 | Product/UX | Same brainstorm: WP16's "we're fetching this" response (once built) only notifies by e-mail — no way to see progress while still in the app. | **Decided: yes, build.** A small "mijn aanvragen" panel in the dashboard showing pending onboarding requests and their rough status; pairs directly with WP16, should land alongside it rather than as a separate follow-up |
| 75 | Product/UX | Same brainstorm: a brand-new user sees an empty chat with only grey placeholder text as a hint — no worked examples to click. | **Decided: yes, build.** Small: a handful of clickable example-question chips shown only when the chat is empty, lowering the barrier for a first-time, less technical user who might not know how to phrase a good question. **Built (2026-07-05, session 22 overnight, WP23):** three example questions as chips on the empty chat; clicking FILLS the input (never auto-sends — the user sees the #82 cost line first); test-pinned fill-not-send and hidden once a conversation exists. Honest residual (review note): chips 1–2 are verbatim benchmark shapes; chip 3's exact string ("Maak een grafiek … 2020 tot en met 2024") is answerable by mechanism (bare-year ranges force `series` deterministically) but was never itself live-parsed — add it to the labelled set at the next supervised recording window (~$0.02 via `--only`) |
| 76 | Product/UX | Same brainstorm: a new user has no explanation of the credit economy (what 100 signup credits means, roughly what a question costs) until they run out. | **Decided: yes, build. Placement specified by owner (2026-07-04): in the right-hand account panel (`web/components/account-panel.tsx`), directly under the "Credits kopen" button** — not a modal or first-login popup. One or two sentences: signup grant size and roughly what it buys, in credits and in questions. **Built (2026-07-05, session 20, WP19):** two sentences directly under the button, every number read live from `signup_grant_config` + `action_class_prices` (new `getSignupGrantCredits`, test-pinned to track a plain UPDATE); the question count is `floor(grant/simple)` phrased with "zo'n" so it understates, never overpromises |
| 77 | Engineering | Live-reproduced (2026-07-04/05, session 18): "Maak een grafiek van de inflatie van 1 januari 2022 tot en met 31 december 2022" gets a clarifying question asking for the period — **whose own example text offers a bare-year format** ("bijvoorbeeld 2022 tot en met 2025") — and replying with the exact same explicit-date phrasing again still dead-ends in `still_ambiguous` (10 credits then 0, net 10 for nothing). The period here is not actually ambiguous to a human; the parser appears not to recognize spelled-out day-month-year date ranges as period boundaries at all, only bare years. Distinct from [#64](open-questions.md) (multiple named periods) and [#72](open-questions.md) (ambiguity policy) — this is a date-**format** recognition gap on an unambiguous single-year request, not a policy question. | **FIXED (2026-07-05, session 21, ADR [023](decisions/023-explicit-date-range-parsing.md))** — owner-approved pre-WP21 insertion. New `date_range` raw-parse kind (prompt v5, raw-parse v3): the model copies date boundaries verbatim, deterministic code does all calendar arithmetic and picks the finest exact grain. The #77 question now answers as the 12 monthly 2022 CPI cells with the series line chart at the ordinary `simple` price (20 credits), no clarification round — exactly the target behavior. Measured: 62/62 labelled cases at repeat=3 zero flips (+ a review-added 63rd case, stable), zero regressions on all 54 pre-existing cases; full hermetic gate + benchmark GATE PASS after the review fixes. Boundaries that cut a month ("van 15 januari tot 20 maart") clarify honestly rather than widen — v1 offers no suggested aligned range (revisit trigger in the ADR) |
| 78 | Product/UX | Second creative-brainstorm batch (2026-07-05, six expert lenses, owner-filtered — rows 78–93): **"Kopieer als citaat"** — one button under every answer copies a ready-to-paste, editorially formatted sentence with the source baked in (e.g. *"… (CBS StatLine, tabel 82931NED, gesynchroniseerd 3 juli 2026)"*). Pure text template over existing answer + attribution fields — no new data, no computation. Surfaced independently by three of six lenses. | **Approved (Stefan, 2026-07-05): build — top-5 #1, effort small.** The quote must stay honest: built only from the validated `answer.body` + structured `Attribution` fields, plus a "voorlopige cijfers" flag when any quoted cell is provisional and the CC BY bewerking-marking when derivations are present — never a recomposed sentence. **Built (2026-07-05, session 20, WP20):** `web/lib/citation.ts` + "Kopieer als citaat" button under every answer, full-string test pins on all flag variants |
| 79 | Product/UX | Same batch: **"Bewijs dit cijfer"** — a button exposing the existing audit record (WP10/R8) as a clickable, followable step list: which table cell, which coordinates, which sync date, every applied derivation (R5), including an explicit notice when a cell is unpublished/suppressed by CBS. Moves trust from "we say so" to "you can recompute it". Reconstruction already exists internally for CI. | **Approved (Stefan, 2026-07-05): build — top-5 #2, effort medium.** Needs a WP brief first: overlaps heavily with the already-approved drill-through ([#70](open-questions.md)) and the Phase 2 user-facing audit-trail view — one design should serve all three, not three separate buttons |
| 80 | Product/UX | Same batch: **stat card + downloadable image** — for single-number answers, a compact visually distinct card (big number, short label, source baked in) beside the chat text, plus a "Download als afbeelding" button exporting that same card as a clean PNG. Pure rendering layer over the already-validated result (WP8 precedent: dumb renderer over a spec). | **Approved (Stefan, 2026-07-05): build — top-5 #3, effort small; biggest organic-marketing value.** Card and export must render from ONE source (same SVG) so the downloaded image can never drift from what was shown; number formatting via the existing shared `formatValueNl` (R3), provisional cells get a visible badge on the card (partial delivery of [#71](open-questions.md)). **Built (2026-07-05, session 20, WP20):** SVG card + single-source PNG export for single-cell answers; the adversarial review caught the card initially bypassing the shared unit conventions (bare 'aantal', the R10 ×1.000 factor-unit guard) — fixed by deriving the unit suffix from the shared `displayValueUnit` itself, test-pinned per branch. Known limit: a real PNG round-trip cannot run in jsdom — both failure legs are test-pinned, the success path needs a real-browser check on the owner's next login |
| 81 | Product | Same batch: **revision-risk gauge** — instead of the binary "voorlopig" badge ([#71](open-questions.md)), a graded three-level bar: definitief / voorlopig-but-historically-stable / voorlopig-with-frequently-large-revisions, classified from precomputed deterministic statistics over past revisions of the same table/series. | **Approved (Stefan, 2026-07-05): build eventually — top-5 #4, effort LARGE.** Needs new precomputed revision statistics per table/series (new ingestion-side machinery) and a WP brief; [#71](open-questions.md)'s simple badge ships first and is superseded by this later, not blocked on it |
| 82 | Product/UX | Same batch: **cost transparency before sending** — (a) estimated cost next to the send button ("Versturen — kost ~20 credits, je hebt er 100"), (b) a hint that a clarification round may follow, (c) that cost line repeated at the clarification message itself. Complements the built low-balance banner ([#69](open-questions.md)). | **Approved (Stefan, 2026-07-05): build — top-5 #5, effort small.** All numbers from the live pricing tables (ADR 006), never hardcoded. Honest limit on (b): a *confidence-conditional* pre-send hint is impossible before the parse runs — v1 ships a static hint ("een verduidelijkingsvraag kost X, de rest krijg je terug"); a conditional version belongs to the [#72](open-questions.md) design work. **Built (2026-07-05, session 20, WP20):** pre-send cost line with the LIVE balance (#68 state) + static hint under the chat form, and the reply price repeated client-side at every clarification message; all numbers from the live pricing tables |
| 83 | Product | Same batch: **batch question with upfront price** — "Herhaal voor alle provincies" → "12 regio's × 20 credits = 240", fired only after explicit confirmation; the same single-question pipeline invoked N times, no new query capability. | **Approved (Stefan, 2026-07-05): build — effort medium.** Needs a WP brief: price/confirm UX, partial-failure semantics (some regions refuse → per-question refunds already handle it), and rate/spend guardrails per ADR 005 |
| 84 | Product/UX | Same batch: **visually distinct message types** — answer (neutral + source chip) / clarification (orange-yellow + question icon) / refusal (own header "Dit kon ik niet beantwoorden" + a "geen antwoord = geen gok" badge). | **Approved (Stefan, 2026-07-05): build — effort small.** The chat already branches on `kind`; this is styling + two fixed Dutch strings. **Built (2026-07-05, session 22 overnight, WP23):** clarifications render amber with a decorative question marker (kept OUTSIDE the bubble so the pipeline's message text stays byte-exact); refusals carry the fixed header "Dit kon ik niet beantwoorden" + badge "geen antwoord = geen gok"; both strings test-pinned, answers pinned free of them |
| 85 | Product/UX | Same batch: **visible pipeline steps while waiting** — "Vraag geïnterpreteerd → Cijfers opgezocht → Antwoord gecontroleerd" instead of a dead spinner. | **Approved (Stefan, 2026-07-05): build — effort small — BUT honesty constraint:** the current Server Action returns only when everything is done (ADR 018 names the Route Handler swap as the seam for REAL stage streaming). A timed fake progression would be a lie in the one product whose whole claim is honesty — v1 may only show truthful generic activity; real steps wait for the streaming seam |
| 86 | Product/UX | Same batch: **live "Verifieer bij CBS" deep-link** — button linking straight to the exact, pre-filtered CBS StatLine table viewer; pure URL construction over existing registry coordinates, no live CBS call (ADR 003 intact). | **Approved (Stefan, 2026-07-05): build — effort small.** Needs one design check: the StatLine viewer's URL/filter syntax must be pinned by tests against real examples, and the link must land on the TABLE (cell-level deep-linking on StatLine is unreliable) with our coordinates shown alongside ([#70](open-questions.md)/[#79](open-questions.md) show the exact cell). **Built (2026-07-05, session 22 overnight, WP23):** "Bekijk bij CBS StatLine" inside every answer's source chip — pure URL construction to the dataset TABLE view (`web/lib/statline.ts`), pinned against two real Phase-0 table URLs incl. the lowercase-id case; our coordinates stay alongside in the chip/answer (the exact-cell view is the #70/#79 cluster) |
| 87 | Product | Same batch: **historical-range chip** — "laagste sinds 2015 (piek: 7,1% in 2015Q1)" under a single number; an extra deterministic derivation over already-loaded data. | **Approved (Stefan, 2026-07-05): build — effort medium.** This is a REAL new derivation (min/max/rank over a series), so it goes through the R5 pre-registered-derivations regime (registered, tested, reconstructable) — a WP brief, not a UI tweak |
| 88 | Product | Same batch: **revision awareness** — (a) a neutral in-session note when a new answer clashes with an earlier answer because of a CBS revision; (b) markers on later-corrected data points in time-series charts. | **Approved (Stefan, 2026-07-05): build — effort medium.** Needs revision-detection machinery (compare stored audit answers / prior batch values against current cells) — shares groundwork with [#81](open-questions.md); brief them together |
| 89 | Product/UX | Same batch: **"Waarom dit antwoord"** — an info icon showing which canonical default definition was chosen and which alternative existed, clickable instead of only named in the text. | **Approved (Stefan, 2026-07-05): build — effort small.** The data already exists (`Attribution.definitionLabel` + the [#39](open-questions.md) alternate-readings policy, not yet built — combining them in one affordance is the natural shape) |
| 90 | Product/UX | Same batch: **source chip instead of source line** — a visually separate, expandable element instead of small-print text; combines with the approved drill-through ([#70](open-questions.md)). | **Approved (Stefan, 2026-07-05): build — effort small.** Belongs in the same WP as [#70](open-questions.md)/[#79](open-questions.md) (one source-presentation redesign, not three). **Chip presentation built (2026-07-05, session 22 overnight, WP23):** the attribution renders as a visually separate chip under every answer — ALWAYS showing the full R4 sentence (never collapsed behind a click) + the #86 StatLine link; answers now render from their structural fields (body/staleness/definitie/marking as own elements) with zero loss vs the assembled text, test-pinned. The chip is deliberately the collapsed depth-0 of the #70/#79/#89 drill-through — that expansion stays with the cluster design brief in [08-build-plan.md](08-build-plan.md) |
| 91 | Product/UX | Same batch: **tabular figures + dominant key number** — pure typography (`font-feature-settings: 'tnum'`), numbers set apart from running text FT/NRC-style. | **Approved (Stefan, 2026-07-05): build — effort small.** Pure CSS, no data change; natural companion to [#80](open-questions.md)'s card. **Built (2026-07-05, session 22 overnight, WP23):** `tabular-nums` on the chat message list, cost captions and the balance display (the #68 live number now changes without digit-width jitter); the "dominant key number" half of #91 was already the WP20 stat card |
| 92 | Product/UX | Same batch: **rearranged chart footer** — caveats (voorlopig/verouderd) larger and in amber, source credit smaller/lighter, like a photo credit. | **Approved (Stefan, 2026-07-05): build — effort small.** Rendering-only change in the chart component; the R4 attribution CONTENT stays untouched (one builder, `buildAttributionLine`). **Built (2026-07-05, session 22 overnight, WP23):** provisional/null-cell caveats render amber and a step larger; the attribution line stays smallest/lightest (photo-credit style), caveats above the credit — order and classes test-pinned; strings byte-identical from the one builder |
| 93 | Product | Same batch, explicitly REJECTED by the owner (recorded so future sessions don't re-propose them): (1) a watch-list / subscription on a recurring figure; (2) pattern+shape encoding besides color in charts (accessibility encoding); (3) a comparison card with highlighted difference. | **Decided (Stefan, 2026-07-05): NOT building these three** — deliberately left out of the feature pool; revisit only if the owner reopens them himself |
| 94 | Product | Session-18 chart-format deep dive (a separate strand from the #78–#93 UX brainstorm): new chart formats (bevolkingspiramide, ranglijst, dumbbell, "sinds [jaar]"-indexlijn, lollipop dot-plot, diverging bar, isotype pictograms, trendbreuk-marker, sparkline-KPI card) plus chart-triggering refinements (min-3-real-points before a line chart, >15-category → table fallback, long-series label thinning, a manual "show as chart anyway" override) and two genuinely new result shapes (distribution/breakdown; two-measures-side-by-side, owner-clarified as display-not-causal). | **Idea bank, NOT queued (Stefan, 2026-07-04/05).** Captured in full with a deterministic honesty guardrail per format in [idea-bank.md](idea-bank.md); pointer in the [06-roadmap.md](06-roadmap.md) feature pool. Approved *directions*, downstream of the near-term queue ([STATUS.md](STATUS.md)); re-prioritize with the owner and check [03-mvp-scope.md](03-mvp-scope.md) before building any of it. Two items carry a hard prerequisite: the bevolkingspiramide needs the ingest slice widened to Geslacht×Leeftijd cells; the trendbreuk-marker is blocked on [#26](open-questions.md) (trend-break registry) |
| 95 | Product/UX | After a successful Stripe purchase, the app redirects to `/credits?purchase=success` — the same "buy more" package list, not back to the main app. Surfaced live (2026-07-05, session 18) during the real Stripe purchase debugging/fix: buying credits is instrumental (a means to keep asking questions), never the destination itself. | **Decided (Stefan, 2026-07-05): redirect to `/` instead** — there is no separate "dashboard" route and none is needed; the main page already is the dashboard (chat + account panel with balance side by side). Change `success_url` in `src/billing/stripe-checkout.ts` from `/credits?purchase=success` to `/?purchase=success`; show a small, dismissible confirmation banner on `/` when that param is present, reusing the existing honest copy ("credits zijn onderweg, duurt meestal een paar seconden" — never a fixed time promise). Pairs naturally with [#68](open-questions.md) (live balance updates): once built, the balance updates on this same page with no reload needed at all. **Built (2026-07-05, session 22 overnight, WP22):** success redirects to `/?purchase=success` — one deliberate deviation from this row's original file pointer: the URL lives in the WEB layer (`web/lib/purchase.ts`, one shared param definition for the checkout-action writer and the page reader), not in `src/billing/stripe-checkout.ts`, which already took the URL as a parameter and stays byte-unchanged (a redirect string is web routing, not billing logic); dismissible banner on the dashboard with the honest webhook/reload nuance (the #68 live updates move on question outcomes, not webhook credits — the banner says "ververs daarna de pagina", never implies auto-update); dismiss strips the query flag so a reload can't resurrect it |
| 96 | Engineering | Live-observed (2026-07-05, session 21, owner's first real look at the #77 fix): one question hit the generic "Er ging iets mis bij het ophalen van het antwoord" — Vercel logs (pulled immediately, per the lesson) show the cause exactly: `POST / 404 — Failed to find Server Action "709ed9…"` at 04:24. **A fresh deploy invalidates Server Action IDs in already-open tabs** (Next.js hashes them per build; the owner's tab predated the review-fix deploy minutes earlier), so the FIRST action from a stale tab 404s with the catch-all error copy. No charge was made (the action never ran — the debit lives inside it) and every later question in the same tab succeeded. This will hit real users on every deploy while tabs are open. | **Leg (a) built (2026-07-05, session 22 overnight, WP22):** chat.tsx's catch now branches on Next 16's own `unstable_isUnrecognizedActionError` detector — honest copy ("De site is net bijgewerkt, waardoor deze vraag niet is verstuurd (er zijn geen credits afgeschreven)") + a "Ververs de pagina" button; the no-charge claim is structurally true (the debit lives inside the action that never ran); test-pinned with the real Next error class. **Leg (b) still an owner decision:** Vercel deployment skew protection (serves old clients their own build for a window) — plan availability/cost check; (a) makes the failure honest, (b) would make it disappear |
| 97 | Engineering | Live-observed (2026-07-05, session 21): "Wat was de maand in 2024 waar de inflatie het meest steeg" — a **max-over-PERIODS** question — got the region clarification "Welke gemeente of provincie bedoel je precies…?", which is wrong on two axes: the user asked about months (not places), and inflation has no regional split at all. Mechanism: the raw contract's `max` derivation is defined ONLY over named regions (prompt: "welke … de meeste/hoogste over named regions"); the model tagged the question `max`, the resolver's max-guard fired its canned region_unknown text, and the "Nederland" reply then dead-ended in still_ambiguous — net 10 credits for a misleading round (the same cost pattern #77 complained about). Pre-existing WP6 shape, surfaced by first real usage; NOT a regression of the #77 fix (verified: the guard and its text predate it). | **Leg (a) built (2026-07-05, session 22 overnight, WP22):** the resolver's max guard split into two dedicated failure reasons — `max_needs_regions` (geo table, <2 named regions: asks which regions to compare) and `max_on_national_measure` (no regional split: says regional comparison is impossible here AND that period-peak lookup is not built yet — the (b) gap named, not hidden — then offers the series alternative "per maand of per jaar"); the shared `region_unknown` template stays byte-identical for genuinely unknown place names (test-pinned). The #97 reproduction now gets an honest clarification instead of gemeente-of-provincie. **Leg (b) still an owner phase-gate call:** argmax over the periods of a series ("welke maand piekte") — deterministically computable from cells already fetched; needs a derivation-contract extension (ADR 011/016 seam), its own WP + benchmark/labelled cases |
| 98 | Product/UX | From the [UX design brief](10-ux-design-brief.md) (session 21/22): `/` is both "the homepage" and "the dashboard" today (ADR 008's deliberate Phase 0 minimalism — no separate marketing surface exists). Now that [#53](open-questions.md) (anonymous trial) is queued as its own separate route, the site is about to grow a fourth page with no shared shell holding it together unless this is decided first. | **Not yet decided.** Three options in the brief, recommendation **A now, C as a cheap fast-follow, B only if/when #53 is greenlit**: (A) keep `/` merged as the logged-in dashboard, unchanged, #53 lives on its own route, no rework; (B) split into a real public landing page (possibly a live #53-budget teaser chat) + move the dashboard to `/app`/`/dashboard` — real build cost, overlaps #53's own scope; (C) keep `/` merged for logged-in users, but replace today's bare redirect-to-`/login` for logged-out visitors with one minimal pitch screen + login link — cheap, fixes the "stranger sees only a login form" gap without #53's budget/abuse questions. **Note (session 41, ADR [033](decisions/033-chat-workspace-redesign.md) D6): the WP135 workspace builds at the merged `/` — option A's posture — and does not foreclose B or C; #98 itself stays an open owner decision.** |
| 99 | Product/UX | From the [UX design brief](10-ux-design-brief.md): the site has no shell at all (no header, no footer, no way to log out anywhere — verified by grep, zero hits) and no site-wide CBS/CC BY 4.0 attribution or privacy link, only the per-answer inline attribution (correct per R4, but no standing site-level echo of it). | **Draft footer copy needs sign-off, everything else in the brief's WP24 is mechanical:** "Cijfers: CBS StatLine (CC BY 4.0) · Elk getal herleidbaar tot een officiële CBS-tabel · [Privacybeleid] · [Over dit project]" — `[Privacybeleid]` has no target yet (waits on [#14](open-questions.md)'s own build, urgent-flagged; omit or placeholder until then), `[Over dit project]` can lean on existing [01-product-vision.md](01-product-vision.md) language. Approve, edit, or reject the wording; the header (balance chip, "Credits kopen" link, working "Log uit") has no open product question, just needs building. **➡ STRUCTURE SIGNED OFF (owner interview, session 41, 2026-07-12, ADR [033](decisions/033-chat-workspace-redesign.md) D6): the shell ships inside WP135; footer = "Cijfers: CBS StatLine (CC BY 4.0) · Elk getal herleidbaar tot een officiële CBS-tabel · Over dit project" with the privacy link DEFERRED until #14(d)'s policy exists (no dead links). Byte-level copy gets a final owner look at the WP135 PR review.** |
| 100 | Engineering | `DerivationRecord.direction.monotonic` describes the SAMPLE (any 2-point sequence is trivially monotone), but the compose prompt's rule 6 maps it onto "de reeks" — for gap-free series those coincide; for DISJOINT samples (now_vs_ago since WP14/V02, and #64 enumerations since session 22) they diverge, so monotonic=true could license straight-line wording over periods nobody sampled. Surfaced by the #64 adversarial review; pre-existing seam, not a #64 regression (V02 has produced this exact record shape since WP14). The session-22 mitigation closed the CHART channel (a non-contiguous series draws no line — `contiguousPeriodCodes` gate in chart/build.ts, test-pinned) and kept endpoint direction prose (true, derivation-bound, the B13/V02 convention). | **Open — queued for the next prompt-touching WP** (rule 6's wording change is prompt bytes = a supervised fixture re-record): make monotonic speak only for gap-free samples, e.g. "bij een reeks met overgeslagen periodes: noem alleen het netto verschil tussen de genoemde periodes, nooit een doorlopende trend". Until then the exposure is bounded: the record's netChange/direction stay true, R9 binding holds, and the chart can no longer draw the hole |
| 101 | Product/Pricing | Two WP26 pricing sweeteners the owner deferred (session 23, 2026-07-05, "decide later") so the WP ships with zero cent-logic change. **(i)** Should a *defaulted* answer ([#72](open-questions.md)/Mechanism B — e.g. national total served because no region was named) cost less than a full `simple` answer, since the user didn't get exactly what they asked? Today it flows through `gate.ts` as a normal answer (20). **(ii)** Should a *clicked, LLM-free* clarification resolution ([#66](open-questions.md)/Mechanism A) cost ~5 (the previously-decided price, session 18) instead of the normal answer price WP26 ships it at? Needs a new distinct reason/action-class-price row and must respect the ledger's `clarification ≤ simple` CHECK constraint. | **Open — deferred by owner (session 23).** Both are independent config/ledger changes decidable anytime before launch; neither blocks WP26. Note the net effect is already a win without either: an ambiguous-region question that today costs *clarify 10 + answer 20 = 30 over two rounds* (or 10-for-nothing on a dead-end) becomes one 20-credit answer under WP26. Decision surface: whether the trust/goodwill value of a discount outweighs the reporting/CHECK-constraint complexity of a second answer-price tier |
| 102 | Product/Priority | Owner priority stack (Stefan, session 23, 2026-07-05), overriding the earlier decision-gated framing: **(1)** on-demand CBS fetch on missing data (WP16, [#21](open-questions.md)/[#24](open-questions.md)) — top priority, Fable-authorized on the hard parts; **(2)** new data sources beyond CBS (likely API-based — the roadmap's enrichment sources: PDOK/Kadaster, RIVM, UWV, etc.); **(3)** re-optimize answer/question quality (incl. WP26) on the widened data base. | **Decided (owner).** Grounded in the session-23 experience audit (measured, live, 110 questions): 40 answer / 32 clarification / 38 refusal; **20 of 56 answerable questions did not just answer**, and **all 14 out-of-coverage questions hit the wall** — the coverage wall (1/2) is a bigger lever than the clarify-policy (3) alone. Two implications to settle for #2 before building it: adding non-CBS sources **broadens the public claim** from "every number traceable to an official CBS cell" to "official sources" ([CLAUDE.md](../CLAUDE.md) public-claim rule needs a matching update), and non-CBS sources **likely trigger the ADR [001](decisions/001-single-app-vs-split.md) Python split** (a real architecture step). WP16 = the next build; WP26 re-sequenced to tier 3. **Update (same session): owner moved GDPR ([#14](open-questions.md)) to the FRONT of the plan (do-now, urgent). For #2, sources are ALWAYS cited; owner also raised multi-source answers as a paid add-on — tracked at [#103](open-questions.md).** |
| 103 | Product | Multi-source answers / cross-source insight (owner idea, session 23, 2026-07-05): once #2 brings in non-CBS sources, for a SINGLE question combine MULTIPLE sources — either to reach an answer one source can't, or to surface a novel cross-source insight ("een leuk nieuw inzicht"). | **Idea captured (owner).** Owner: this should cost **extra user credits** (more work / more value), and every source used is always cited (the broadened "official sources" claim). Design questions for later: which questions warrant multi-source vs single; how the deterministic pipeline combines cells from different sources while keeping principle (a) (every number still a verbatim cell from one validated source, never an LLM-synthesized cross-source figure) and principle (c); the pricing tier. Sits under priorities #2/#3 ([#102](open-questions.md)); not built. |
| 104 | Engineering | WP16 sub-part 1 (table finder, session 24): `DEFAULT_FIND_TABLE_CONFIG.highConfidence` was a **conservative uncalibrated placeholder (0.8)**. The rerank replay fixtures (`tests/fixtures/llm/tablefinder`) were **not yet recorded at build time** (needs live Haiku spend), so no end-to-end rerank test was on the CI gate yet — only Stage-1 recall, the allowlist validator, and routing (with a stub rerank) were hermetically pinned. | **✅ Mostly DONE (session 25, supervised, owner present):** migration 011 applied to prod + the live grants/RLS check passed; real `catalog:refresh` (4,858 rows); `tablefinder:record` run live (Haiku); `highConfidence` **calibrated to 0.8** — confident FLOOR 0.85 measured and stable across two runs, 0.8 sits 0.05 below it, failure-safe (a below-0.8 pick DISCLOSES, never a wrong table); an **end-to-end replay test is now on the gate** (`tests/catalog/find-replay.test.ts`, 8/8 hermetic). The seed set also grew a mislabel fix (zonnepanelen→85004NED) + two recall-gap fixes (see [#105](open-questions.md)). **RESIDUAL (still open, sub-part 2):** the labelled set has **NO disclose-expected case**, so the confident/disclose boundary is *not directly measured* — 0.8 is a confident-floor value, not a measured midpoint. Add a genuinely-ambiguous disclose case (so the boundary is pinned) and re-check the threshold on the live 4,858-row mirror (recall there is far noisier than the 61-row fixture). **Session 31 (2026-07-08, WP27 stage A): the disclose-expected case now EXISTS in the labelled set** (`inkomen-vaag` — topic "inkomen", vague question, expect disclose) and the replay test will pin it on the gate; **the boundary is now DIRECTLY MEASURED (same day, supervised record): prompt v2 gained a vague-question honesty rule after the first measurement showed Haiku at 0.85 on a six-way-ambiguous income question; `inkomen-vaag` now discloses, stable across two runs, while all confident cases hold a 0.85 floor over the 0.8 threshold (11/11 twice).** Residual for stage D: the ADR-025 live-mirror re-check of the threshold (recall on the 4,858-row mirror is noisier than the 74-row fixture). **Stage D (session 33, 2026-07-10) added ONE live-mirror data point, not the full re-check: the acceptance-test finder ran against the real 4,858-row mirror and picked 37789ksz confidently at 0.85 — exactly the measured fixture floor, no drift observed (n=1). The systematic live-mirror eval (tablefinder:eval pointed at the live mirror instead of the fixture catalog) remains open — monthly-maintenance material, no longer tied to any WP.** |
| 105 | Engineering | WP16 sub-part 1: recall-quality knobs deliberately kept minimal for v1. **(a)** The alias-hint seed (`src/catalog/aliases.ts`) is a small starter set — a measured recall miss (`zonnepanelen`→`zonnestroom` was already one) is how it grows. **(b)** Stage-1 FTS covers title+summary only, NOT dimension names (they aren't in the Datasets listing — a per-table enrichment pass would be needed). **(c)** pgvector stays deferred (ADR [002](decisions/002-postgres-system-of-record.md)). | **Open — expand only on measured evidence** (ADR 025 revisit triggers). The order is: grow alias hints (cheap) → add dimension-name enrichment if title+summary proves insufficient → add pgvector as an *additive* recall signal into the same Stage-2 shortlist only once keyword recall is measured to miss real tables. Don't build the semantic-search project before the cheap version is proven insufficient (phase-gate + cost discipline). **Update (session 25):** two more measured recall misses fixed via alias hints during live calibration — `inwoners/bevolking`→`bevolking` (surfaces 03759ned) and `aantal woningen`→`voorraad woningen` (surfaces 82235NED, the STOCK table, distinct from the koopwoningen PRICE tables); seed now 10 entries. Both are Stage-1 precision fixes, not rerank changes; each is mutation-pinned (removing the alias fails its recall test). |
| 106 | Engineering | WP16 sub-part 1: two catalog-mirror operational items. **(a) Refresh cadence** — `catalog:refresh` (`src/catalog/cli.ts`) bulk-mirrors the ~4,858-row v4 catalog into `cbs_catalog`; CBS publishes no explicit "catalog changed" signal, so the cadence is a judgment (weekly? monthly, in the maintenance session?). **(b) Coverage gap** — the v4 catalog (4,858 tables) and the legacy `opendata.cbs.nl` catalog (5,943) diverge by ~1,000; a table only in the legacy set would read as "we don't have that" (a principle-(c)-adjacent false-negative — a refusal, never a wrong number). | **Open.** (a) Default to the monthly maintenance session unless a faster cadence is wanted; the mirror is metadata-only and cheap to refresh. (b) Accept v4-only for v1 (it's the platform the rest of the adapter uses and CBS is steering to); reconsider a legacy-catalog merge only if real "we don't have that" refusals surface for tables CBS actually publishes (ADR 025 revisit trigger). **Addendum (session-26 review, LOW):** when the row-by-row upsert gets batched (the recorded ~19-min lesson), also add a statement/idle-in-transaction timeout backstop — today the refresh holds one pooled connection (of 4) open with no server-side cutoff if a round-trip stalls mid-transaction; bounded because the op is manual and off the request path. |
| 107 | Product | WP16 sub-part 2 — **onboarding slice greediness**: fetch the WHOLE table or a slice when a table is onboarded on demand? Owner inclination (session-26 notes): greedy — whole table — with a slice safety net for gigantic ones. | **Resolved (Stefan, session 26, ADR [026](decisions/026-on-demand-fetch-job-architecture.md)): greedy by default, auto-slice only above an estimated ~150,000 cells** (computed pre-fetch from the catalog metadata's dimension cardinalities, never "gigantic by feel" — roughly a quarter of the CPI table that forced the [#33](open-questions.md) DB-budget discussion), sliced to the question's topic + sensible defaults (national totals, recent years) above the cap. The number is a config constant, cheap to retune once real onboarded-table sizes are measured; [#34](open-questions.md)'s change-a-slice tooling gap still applies to any later widening. Interacts with [#110](open-questions.md): greedy is cheaper to accept once unqueried tables can be evicted. |
| 108 | Engineering | WP16 — **successor/staleness re-discovery**: CBS republishes tables under a NEW id and flips the old one to `Gediscontinueerd`/`Vervallen`, with NO machine-readable successor link (live-measured: the mirror is discontinued-dominant, 3,559 of 4,858 rows). A registered table going discontinued should trigger re-discovery and a re-onboard. | **Deferred out of WP16 sub-part 2 v1 (Stefan, session 26, ADR 026) — not blocking, own follow-on work.** Design sketch stands for later: detection is cheap and half-built already (the mirror stores `status` + CBS's modified timestamp, so each `catalog:refresh` can diff the status of REGISTERED tables and flag flips for the maintenance session, complementing [#43](open-questions.md)'s cadence-based staleness warnings); successor SEARCH would reuse the sub-part-1 finder on the discontinued table's own title/topic — owner-reviewed re-onboard, never automatic. Covered periods stay warn-and-serve ([#18](open-questions.md)/[#43](open-questions.md)); the successor matters for FRESH periods only. |
| 109 | Product | WP16 × WP26 — the **"answerable-but-requires-onboarding" suggestion chip**: when a question (or a follow-up chip per [#66](open-questions.md); [#73](open-questions.md) stays rejected) could be answered by a table we haven't onboarded, show an explicit "ik haal het op — ~X credits, ~Y min" chip (with [#82](open-questions.md)-style pre-send transparency), or suppress it? | **Deferred out of WP16 sub-part 2 v1 (Stefan, session 26, ADR 026) — not blocking, own follow-on work (WP26-adjacent UX layered on the core loop).** Recommendation stands for when it's picked up: SHOW it — the coverage wall is the measured #1 lever ([#102](open-questions.md)) — with three honesty guards: (i) only on a `confident` finder verdict (≥ 0.8), never `disclose`; (ii) the chip's copy promises the FETCH, not the answer; (iii) reuse the now-resolved [#24](open-questions.md) refund mechanism (the billing gate's existing full-refund-on-refusal) for "onboarded fine but this specific question still isn't servable" rather than inventing new billing logic. |
| 110 | Engineering | WP16 sub-part 2 — **on-demand data lifecycle** (on-demand tables = a TTL cache ABOVE the permanent catalog mirror): (a) the sync refresh must be registry-driven so onboarded tables participate; (b) evict cells + registration after N days unqueried (new `last_queried_at` column, bumped by every answer/follow-up/chart read of the table); (c) PIN the 8 seed tables (eviction-exempt); (d) pure TTL vs LRU-with-frequency. | **Split by scope (Stefan, session 26, ADR 026). Item (a) ✅ FIXED (session 27, sub-part 2 build):** `sync --all` now enumerates the DB's registered `cbs_tables` set (seed auto-registration and explicit-id args unchanged, test-pinned: a DB-registered non-seed table IS synced by `--all`). The gap as found: the target list was the hardcoded `PHASE0_TABLES` seed ([src/ingestion/cli.ts](../src/ingestion/cli.ts)), so an on-demand-onboarded table would never have refreshed. **Items (b)/(c)/(d) — full eviction/TTL — are DEFERRED, own follow-on work, not blocking.** Design stands for later: debounce the `last_queried_at` bump (write only when > ~1 day stale); GC follows the `gdpr:purge` CLI pattern (dry-run default, `--apply`, config window); pin via a flag on `cbs_tables`, seeds pinned by default; recommend pure 30-day-since-last-query TTL over frequency-weighting until churn is measured. **Eviction is SAFE for the audit trail — verified: R8 reconstruction re-derives from the stored record alone ([src/answer/audit/reconstruct.ts](../src/answer/audit/reconstruct.ts) header: "no database"), so past answers stay reconstructable after cells are evicted.** The labelled-set disclose-case gap stays tracked at [#104](open-questions.md), not duplicated here. |
| 111 | Product | WP16 sub-part 2 — **onboarded-table delivery coverage**: v1's clean answerable case is a national, single-dimension table. An onboarded table needing a GEO/regional split or a non-'totaal' sub-coordinate currently ends `unanswerable` → **refuse-and-refund** (the delivery-must-answer gate + R1 hold by construction — never a wrong number, but the user waited minutes and got a refund instead of an answer). | **Open — follow-on after the supervised live step.** The auto-generated registry vocabulary (`src/ingestion/onboarding-vocab.ts`) registers the safe subset; widening it (regional flags, sub-coordinate default_coordinates rows) is the quality lever. Measure first: how often real onboarding requests land on such tables (the pending rows + `failure_summary` record it) — widen on evidence, per the ADR 025 pattern. **⬆ ELEVATED to a real priority — owner-flagged at the go-live (session 28, 2026-07-06):** the very FIRST live onboarding — *"Hoeveel mensen zaten er in 2023 in de bijstand?"* — hit this: finder confidently picked `85615NED`, delivery returned `unanswerable` ("geen maat die we onder deze vraag konden aanbieden"), 100 credits refunded. The money-safety worked perfectly, BUT the owner's verdict (verbatim, binding): *"dat is er eentje die echt wel beantwoord moet kunnen worden … we kunnen niet, als iemand zo'n simpele vraag stelt, zeggen dat het ons niet lukt … dit kan echt niet."* Bijstand is a common, simple, national question and MUST answer. **Diagnosis (CBS metadata checked, session 28): `85615NED` is a FINDER MIS-PICK, not primarily a delivery gap.** `85615NED` = *"Personen met bijstand; (Her)instromers en uitstromers"* — it measures INFLOWS/OUTFLOWS (entry/exit moments), broken down by gender/age/origin, quarterly. It has no stock "how many people ARE in bijstand" total — so for *"hoeveel mensen zaten er in de bijstand"* (a stock/count question) delivery correctly found no fitting measure and refused. **The real fix lever is (a) FINDER PRECISION:** "hoeveel mensen in de bijstand" must map to a STOCK/count table (e.g. a *"Bijstandsuitkeringen; kerncijfers"* / *"Personen met (algemene) bijstand"* national-total table), NOT the flows table. The rerank matched the topic word "bijstand" but ignored stock-vs-flow intent. **⚠ OWNER STEER (session 28, binding): do NOT solve this statically per-topic — "we kunnen niet voor elke vraag ditzelfde probleem statisch gaan oplossen; het moet gewoon slimmer worden aangepakt."** So the fix is NOT per-topic aliases (bijstand→stock table, then repeat for every subject — that doesn't scale to the ~4,858-table catalog). It is a GENERAL capability: **the rerank must reason about each candidate table's actual SHAPE — its measure names + dimensions (stock vs flow, total vs breakdown, level vs change) — against the question's intent, not just topic-word overlap.** Today the rerank sees mostly title/Description; it should also see the table's measures/dimensions so "meet in-/uitstroom" vs "meet aantal personen" is visible and scorable. **Plus a fit-check BEFORE spending 100 credits:** the stock-vs-flow mismatch is currently only discovered at DELIVERY (after fetch + debit → refund); a pre-fetch "does this table actually contain a measure that answers this question?" gate could reject/try-next-candidate before charging. This is a design-heavy core-product question (intent↔table-shape matching + a pre-fetch fit gate) — a legitimate **Fable** candidate (owner authorized Fable on WP16's hard sub-parts). Secondary (b): the general #111 delivery-coverage widening still applies for genuinely region/characteristic-split tables. **Session 30 (2026-07-07): the pre-design PROBLEM DOSSIER is written ([session-briefs/2026-07-07-111-problem-dossier.md](session-briefs/2026-07-07-111-problem-dossier.md)) — key verified fact: `findTable` receives only the parser's `unmatchedMeasureTerm` ("bijstand"), so the stock-vs-flow intent is discarded BEFORE the finder runs; a rerank-prompt-only fix cannot work. The design step is deliberately NOT started — the owner's model-tier question is pending (dossier's final section).** **Session 31 (2026-07-08): WP27 stage A BUILT on branch `wp27-stage-a-question-aware-rerank` (per #118) with three owner-approved amendments A1–A3 (full measured evidence in ADR [027](decisions/027-finder-shape-fit-gate.md)'s Amendments section): A1 the bijstand-stock target is `37789ksz` ("Sociale zekerheid; kerncijfers" — the ONLY v1-deliverable table for the year-explicit national question; counts uitkeringen, owner-accepted); A2 Regulier-first recall quotas (20+4, shortlist 24 — measured: the raw top-20 held 14 discontinued tables and 37789ksz sat at position 51); A3 deterministic deliverability pre-checks in the stage-C fit gate (time-only dims; bare-year ⇒ JJ codes). Supervised record done same session (11/11 twice).** **Session 32 (2026-07-10): stage A MERGED + DEPLOYED (PR #17, merge `478a852` — the question-aware rerank is LIVE); stage B (candidate chain through the trigger + migration 015 as a FILE — not applied to production until stage D — deploy-order-safe via a store-side column probe) MERGED + DEPLOYED same day (PR #18, merge `d8e02b5`); stage C (the fit gate: Haiku measure-fit + amendment A3's deterministic pre-checks in the job, mechanically dormant until migration 015 lands) BUILT + adversarially reviewed + #124-measured, MERGED + DEPLOYED same day (PR #21, merge `dab47fb`). Remaining: ONLY supervised stage D (migration 015 to prod, fit-fixture record + threshold calibration, the live bijstand acceptance test) — then this item closes on the owner's live acceptance.** **✅ CLOSED (session 33, 2026-07-10, owner-supervised stage D, PR #22): migration 015 live on prod (grants/RLS clean, dedupe index untouched); fit threshold calibrated (kept 0.8 — live record 6/6, every verdict 0.95, see ADR [027](decisions/027-finder-shape-fit-gate.md) as-built stage D); and the owner's LIVE ACCEPTANCE TEST PASSED: the exact dead-end question ANSWERED — "Het totaal aantal bijstandsuitkeringen kwam in 2023 uit op 390,2 x 1000" (audit 240, measure D000203_2 verbatim per A1, chain recorded, fit gate accepted candidate 1, kick in 2s, delivered in 88s, 100 credits kept). The secondary (b) leg — delivery-coverage widening for genuinely region/characteristic-split tables — stays a measure-first follow-on lever (the pending rows' failure summaries are the evidence base), no longer blocking anything.** |
| 112 | Engineering | WP16 sub-part 2 — **extended-vocabulary prompt variant is fixture-unmeasured**: the intent parser's system prompt is byte-identical to the calibrated one while no onboarded vocab exists (`extraCanonicalMeasures` default-empty — fixtures/benchmark untouched, proven), but a LIVE turn after an onboarding (and every delivery re-run) renders EXTRA vocabulary entries — a prompt variant the 63-case labelled set has never measured (confidence thresholds, R7 routing were calibrated on the Phase-0 vocabulary). | **Open — bounded today, measure at first live use.** Containment: the delivery re-run's output must be a full-pipeline-validated ANSWER or it refunds (never a wrong number), and a live chat turn still passes every downstream validator. Residual risk is calibration drift (confidence values on the extended prompt), not fabrication. When the first real onboardings land: record a small labelled set against the extended prompt (the [#104](open-questions.md) pattern) before trusting its confidence values as calibrated. **Now LIVE-relevant (go-live session 28): the delivered consumentenvertrouwen answer (CBS 83694NED) exercised the extended-vocab prompt in production — the first real use of this variant. It answered correctly (−24, full attribution), but its confidence values are still uncalibrated; record the labelled set as more onboardings accumulate.** **Session 30: the re-onboarding MONEY BUG leg is FIXED in PR #13 (awaiting owner review): live turns load the onboarded vocabulary (`loadOnboardedVocabulary`, read-only, pre-gate, fail-soft, gated on `ONBOARDING_ENABLED`), the clarify-reply merge accepts onboarded keys, and re-onboarding under a new synonym ADDS terms instead of erasing the old one. The original calibration residual above still stands (record a labelled set against the extended prompt).** |
| 113 | Ops/Product | WP16 sub-part 2 — **onboarding trigger cadence, discovered at deploy time (session 27): the Vercel HOBBY plan only allows DAILY cron jobs** — the designed minutes-level schedule (`*/2 * * * *`, ADR [026](decisions/026-on-demand-fetch-job-architecture.md) decision 1) was rejected by `vercel deploy` — deterministically, on every deploy carrying it (an initial "inconsistent validation" reading was the session watching the wrong CI run — see [lessons-learned](lessons-learned.md) session 27). The committed cron is now a daily 06:00 UTC backstop sweep (live-verified registered on the project after commit `ac7b0df` deployed), which alone makes the acknowledgment's *"meestal een kwestie van minuten"* promise false. | **✅ Resolved (Stefan, session 27, 2026-07-06): kick-on-trigger.** After `triggerOnboarding` commits, the app itself fires the cron route (fire-and-forget, `CRON_SECRET`-authed, **fail-soft and strictly post-commit** — a failed kick can never affect money or the acknowledgment, it just degrades to the backstop); the daily 06:00 UTC Vercel cron stays as the crash/retry backstop sweep. GitHub Actions rejected: its own schedule jitter (10–20+ min under load) gives the same latency uncertainty with more moving parts. **Vercel Pro (~€20/mo) is explicitly KEPT as the fallback — owner, verbatim: *"Bewaar die optie als de kick-on-trigger-aanpak ooit tekortschiet."* Revisit trigger: measured kick failures or delivery latency beyond "minuten" in real use → upgrade to Pro (platform-level minutes cron) rather than building more homegrown scheduling.** Build note for the next session: a small mechanical change (mid tier, hermetic-testable), to be built and gate-green BEFORE the RUNBOOK supervised live step flips `ONBOARDING_ENABLED` — this resolution satisfies the gate that row placed on the flag. If latency can still exceed "minuten" in practice, the copy changes too ([#24](open-questions.md)'s no-fixed-time-bound rule). **✅ BUILT (session 28, 2026-07-06, commit `c64e2a2`; Opus build + 3-lens Sonnet review, hermetic gate green — backend 891, web 167, benchmark 14/14+6/6+0 fabricated).** `web/lib/onboarding-kick.ts` fires `/api/onboarding-cron` via Next's `after()` in `maybeTriggerOnboarding` on the `started` AND `duplicate` branches (post-response, so strictly post-commit; never on `insufficient`). Fail-soft by construction — it cannot throw. **Platform-review refinement (the one MEDIUM the review caught): the cron route only responds once the WHOLE job finishes (tens of seconds), but the caller's page action is capped at 30s** ([`web/app/page.tsx`](../web/app/page.tsx) `maxDuration = 30`) — so a naive `await fetch` would be platform-killed mid-wait on any long job, silently, never logging whether the kick fired. Fixed with `AbortSignal.timeout(10s)`: the kick DISPATCHES the job then stops waiting (the job runs on — Vercel request cancellation is **opt-in** via `supportsCancellation`, which this path deliberately does not set, so a client disconnect never cancels the route). The timeout is the expected long-job path, logged as benign dispatch. **Verify the kick fired at the live step via its own log line** (`onboarding kick dispatched …` in `vercel logs`) — RUNBOOK step 3(b), which also closes the review's LOW (step 3 previously only checked the cron route responds, not that the kick itself ran). **✅ VERIFIED LIVE (go-live, session 28, 2026-07-06): the kick fired** — the first two real onboarding jobs finished seconds after their triggers, not at the 06:00 UTC backstop, proving the kick (not the daily cron) started them. RESOLVED + live-proven. |
| 114 | Ops/Engineering | **The CI post-deploy smoke check only covers `/login` — an authenticated-only failure keeps CI fully green** (proven by the session-27 incident: the logged-in dashboard `GET /` 500'd on a missing-table query while gate AND deploy showed success; `/` redirects anonymous visitors to `/login`, so the one URL the smoke hits can never see a logged-in regression). The smoke check was itself added after the session-16/17 outage for exactly the "deploy green, app broken" class — this is that class's remaining blind spot. | **Open.** Options, roughly in effort order: (a) extend the smoke to an authenticated request using a long-lived test-user session token stored as a CI secret (real coverage, but a credential to manage + rotate); (b) a synthetic health route that exercises the dashboard's real reads (incl. every table the history/balance queries touch) WITHOUT auth, called by the smoke — no credential, catches missing-relation/SQL regressions, misses auth-layer breakage; (c) accept + rely on the owner's own use as the detector (status quo — found this incident within the hour, but only because the owner happened to look). Leans (b) as the honest cheap layer; decide alongside [#65](open-questions.md) (durable error logging), which the same incident also argues for — the 500s were only diagnosable because `vercel logs` was pulled within retention. **Reinforced at the WP16 go-live (session 28): the proxy 307-redirect bug (`/api/onboarding-cron` → /login) would NOT have been caught by the CI smoke check — it was found only by a manual pre-flight `curl` of the real endpoint before the first paid use.** Interim mitigation adopted this session: `web/proxy.test.ts` now pins the cron route (and the webhook) as public, so *that* specific regression fails in CI; but the general blind spot (an authenticated-only or middleware-layer break passing CI) stands — this is now the second concrete instance arguing for option (b), a synthetic health route the smoke exercises. |
| 115 | Product / answer-quality | **Onboarded-table answers are too weak — owner verdict at the go-live (session 28): "veel te matig om als antwoord terug te geven."** The first delivered answer read: *"Consumentenvertrouwen was in 2024 −24 gemiddelde saldo van de deelvragen. Definitie: Consumentenvertrouwen."* Three concrete faults: (1) **circular definition** — "Definitie: Consumentenvertrouwen" just repeats the term (the onboarded table's measure had no real definition text, so the composer fell back to the label — it should show a real definition or omit the line); (2) **jargon without scale** — "−24 gemiddelde saldo van de deelvragen" is CBS-speak and meaningless without the −100..+100 range; (3) **no plain phrasing** — cf. a clearer "gemiddeld −24 (het gemiddelde van de twaalf maandwaarden)". | **⏳ PARTIALLY DONE (session 28) — ties to WP26 (answer quality) and #112.** **Lever (a) ✅ SHIPPED (commit `7de00b0`): the circular "Definitie: X" line is now suppressed on onboarded answers** (`src/answer/compose/compose.ts` — drop the line when `definitionLabel` is merely the measure's own title; case-SENSITIVE so the seed `population` definition, which differs from its title only in case, is untouched; benchmark-proven zero seed regression, 2 pins in `compose-template.test.ts`). Root structural cause (still driving b/c): on-demand-onboarded tables get a WEAKER answer than the 8 hand-curated seed tables, which carry curated definitions/units/canonical-measure phrasing in the registry; the auto-generated vocab (`extraCanonicalMeasures`) has none of that. **Tension to respect: principle (a)** — the LLM must not interpret/guess; the fix is *clearer deterministic phrasing + real definitions*, NOT LLM interpretation. Levers: **(a) ✅ DONE**; **(b) ✅ DONE + LIVE-VERIFIED on production (session 29)**; **(c) ✅ DONE + LIVE-VERIFIED (session 29)**. **Live proof (read-only compose against prod, session 29):** *"Consumentenvertrouwen was in 2024 -24 (gemiddelde saldo van de deelvragen)."* + **Definitie:** CBS's full real definition ending *"…De indicator kan een waarde aannemen van -100 (iedereen antwoordt negatief) tot +100 (iedereen antwoordt positief). Bij een waarde van 0 is het aandeel pessimisten gelijk aan het aandeel optimisten."* — the exact scale the owner wanted, in CBS's own words. **What shipped (levers b + c, session 29, LIVE):** CBS's `MeasureCodes` carry a rich per-measure **`Description`** (for consumentenvertrouwen: the meaning AND the explicit *"−100 (iedereen negatief) tot +100 (iedereen positief)"* scale) which we **fetched and discarded** — verified live against CBS OData. Now **captured verbatim** (`CbsMeasure.description` → `cbs_tables.units` → a **new nullable `canonical_measures.definition_text`**, migration 014) and rendered as the answer's "Definitie:" line for onboarded measures (`buildDefinitionLine` in `compose/format.ts`, now the SINGLE source of truth shared by compose + audit-reconstruct). **Architecture note (chosen over the "make `definition_label` nullable" sketch): `definition_label` doubles as the sentence SUBJECT, so a paragraph can't live there — hence a DEDICATED column, which keeps every seed answer byte-identical (`definition_text` NULL for the curated set → benchmark unchanged by construction; no `prompt.ts`/intent-vocab change needed after all).** `cleanCbsDefinition` distils the blurb to its first block + strips a leading title-echo line (punctuation-tolerant), **never rewording** (principle a; NULL when CBS gives nothing). **(c):** `displayValueUnit` sets a 3+-word descriptive unit ("gemiddelde saldo van de deelvragen") in parentheses (template path; no Phase-0 measure has such a unit, so seed unchanged). **Adversarial multi-lens review (5 lenses, each finding verified): 3 real issues caught + fixed** — (i) `resolve.ts` read `definition_text` on the HOT canonical path → deploy-before-migration would degrade EVERY canonical question to a refusal+refund (the #114 class); fixed by gating the read on the `onboarded:` key prefix (an onboarded key can only exist AFTER 014, so the hot seed path never touches the new column — **deploy-order-safe**); (ii) `audit/reconstruct.ts` re-derived the definition line with the OLD logic → onboarded answers would fail R8 reconstruction; fixed by the shared `buildDefinitionLine`; (iii) the title-echo match missed a trailing period. Number-honesty + test-honesty lenses clean. **Measured: full backend suite green, benchmark 14/14 + 6/6 + 0 fabricated GATE PASS, clean typecheck.** **✅ SUPERVISED LIVE STEP DONE (session 29, owner-present):** migration 014 applied to prod → code pushed + deployed (CI caught a web-side `Attribution` typecheck break I'd missed — deploy correctly blocked until fixed, `dc406fb`) → `83694NED` re-synced **with `--rebaseline`** (a NORMAL sync does NOT rewrite `units`, only a rebaseline does — the metadata never updated on the first plain sync; #34a) → `scripts/onboarding-reregister.ts` (new, committed) backfilled `definition_text` (8 of 115 measures have a real CBS blurb; 107 have none → correctly NULL) → live-verified by a read-only compose. **A REAL bug caught in live verification (fixed `776e645`): `cleanCbsDefinition` kept only the FIRST block, but consumentenvertrouwen's Description is a short preamble in block 1 and the real definition + scale in block 2 — the "first block only" cut dropped exactly the scale. Fixed to keep the whole verbatim blurb (the review's clean-edge lens had flagged this risk; it was wrongly dismissed as by-design).** **Residual (ties to the deferred expander + #117): the full CBS definition is LONG (a ~180-word paragraph) and renders inline in the dashboard `finalText` blob until the expander lands — complete + correct but verbose; owner chose to review the expander later.** Note: a fresh live chat turn does NOT load the onboarded vocab (`web/app/actions.ts` injects the finder but no `extraCanonicalMeasures`), so re-asking an already-onboarded topic RE-triggers onboarding (100 credits) instead of answering directly (1 credit) — a separate gap belonging to #112, not #115. **UX note discovered this session: an onboarded answer is delivered ASYNC to the DASHBOARD history (`question-history.tsx` renders the full `finalText` blob), not the live chat — so the "clean bubble via expander" the owner leaned toward is a real dashboard refactor (expose structured fields; pairs with [#117](open-questions.md)/[#74](open-questions.md)), not a thin layer; the substance (real definition + scale) now appears in the answer body everywhere (dashboard/email/audit) regardless.** Ties to WP26 (answer quality) and #112. **Session 30: the deferred expander is BUILT in PR #14 (awaiting owner review): answer entries expose structured `answerParts` from the stored envelope; long definitions fold behind 'Meer over deze meting' with the verbatim scale sentence kept visible; attribution always visible (#90); seed display unchanged.** |
| 116 | Product / UX | **The delivery email has no link back to the chat** (owner, session 28): the "je cijfers staan klaar" / "kon niet" emails tell the user the answer is in the chat but give no link. | **Open — small, concrete.** Add a deep-link to the app (ideally to the specific answer) in `buildEmail` (`src/ingestion/onboarding-notify.ts`), using the app origin already available to the deploy. Deep-link-to-specific-answer may need an anchor/route; a link to the dashboard is the cheap first step. **Built 2026-07-12, session 39 (this change; plain text, no live PR links):** dashboard-root deep link added to all three delivery emails via a `NEXT_PUBLIC_APP_URL` read inside `productionNotifier` (the cron route stays byte-untouched); `buildEmail` gained an optional, fail-soft `appOrigin: string \| null = null` parameter appending "Bekijk je antwoord: {origin}/" (delivered) or "Bekijk de status in je dashboard: {origin}/" (unanswerable/failed), trailing slashes normalized. Per-answer anchor stays future work (the "cheap first step" scope named above). |
| 117 | Product / UX | **The app does not auto-update when an onboarding answer lands** (owner, session 28): after the "wordt voorbereid" acknowledgment, the delivered answer only appears on a manual page refresh — no live update or in-app notification. | **Open — ties to [#74](open-questions.md) (live status panel for pending onboarding requests).** The dashboard history is server-rendered at load; a pending→delivered transition needs a poll or push to surface without a refresh. Scope with #74; the honest cheap first step may be a light poll of the pending row's status while any request is in flight. **Session 30 groundwork: PR #14's structured `answerParts` entries are the surface a live-update/poll would refresh — scope the poll with #74 on top of it.** |
| 118 | Governance / process | **The CLAUDE.md build-phase git workflow ("commit + push directly to `main`, no branches/PRs/review") carries a revisit trigger — "first real deploy / external users / a second human contributor" — and the WP16 go-live (session 28) arguably HIT the "first real deploy": the product now has a live, money-handling feature in production.** | **✅ DECIDED (owner delegated the call to the session, 2026-07-06; now a working agreement in [CLAUDE.md](../CLAUDE.md) git-workflow): owner-supervised in-session work keeps push-to-`main`; autonomous sessions touching core/money-path code require branch + PR + owner review. Rationale below.** The original friction reasons partly still hold (single contributor = AI sessions, single reviewer = owner, and **no EXTERNAL users yet** — only the owner uses the app). But two things changed: (1) it is live + on the money path (argues for review-before-merge); (2) the concrete new risk this session surfaced — an **autonomous** chip/overnight session could push UNREVIEWED core-product/money-path changes straight to a live product. **Session recommendation (split the trigger): keep push-to-`main` for OWNER-SUPERVISED in-session work (like today's go-live); but require branch + PR + owner review for AUTONOMOUS sessions (task chips, overnight runs) that touch core-product/money-path code now that it is live.** Decide before the next autonomous run. A full switch to branch+PR-for-everything can wait for actual external users. |
| 119 | Engineering / money | **WP16 job: deliver→finalize is NOT atomic** (session-30 review, verified): `processOneRow` writes the delivery audit row (`answerQuestionAudited`) and then `finalizeDelivered` as separate operations — unlike the refund paths, which run refund+finalize in ONE transaction. A process crash in that window leaves the row 'running' with the answer already delivered; the 20-minute stale reclaim then RE-RUNS delivery: a second audit row + second email, and BOTH audit rows join the same `request_id`'s 100-credit debit in the dashboard (looks like 200 charged; the ledger itself stays correct — no double debit). | **Open — narrow crash window, no money moved, recorded with a fix sketch:** an idempotency check at claim time (does an `onboarding_delivery` audit row for this `request_id` already exist? → finalize instead of re-running). Do as a small reviewed PR; not urgent while the owner is the only user. **✅ FIXED 2026-07-12 (session 39, branch wp119-delivery-idempotency — plain-text ref, awaiting owner review per #118):** the sketch as recorded, HARDENED by the pre-build adversarial review: the recovery query filters `kind = 'answer'` (a bare existence check would have "recovered" a crashed REFUSAL/clarification attempt as delivered — a permanent unrefunded 100-credit charge + a false "Goed nieuws" email; the ⟨F1⟩ finding, two review lenses converged). Check runs at BOTH re-entry points: `processOneRow` claim-time AND the exhausted-attempts loop (without the latter, a crash row hitting MAX_ATTEMPTS was failAndRefund'ed — refunding an answer the user already received = user keeps answer + refund). New store helper `findDeliveredAnswerAuditId` (order by id asc: historical double-rows resolve to the FIRST delivered answer); 4 new PGlite tests incl. the crashed-refusal regression and a ledger byte-identical pin. |
| 120 | GDPR / retention | **The GDPR retention seam misses ALL onboarding data** (session-30 review, double-confirmed): `deleteUserQuestionHistory` and the 2-year purge scope to `source_tag='user'` on `audit_answers` only — so (a) the delivery answer row (`source_tag='onboarding_delivery'`, carrying the verbatim question + intent + answer) and (b) `pending_table_requests.question_text`/`topic_term` are NEVER redacted by the self-service button or the purge, contradicting docs/05's retention promise and migration 005's "question text lives in exactly ONE place" invariant. | **Open — fix as its own reviewed PR (task chip spawned, session 30):** widen the retention scope to include `onboarding_delivery` rows and add a `pending_table_requests` redaction leg to BOTH paths, with the same cross-user-scoping pins as #14. Exposure today is nil (the owner is the only user) but this must land before external users. **✅ BUILT 2026-07-12 (session 39; own branch, PR pending owner review per #118).** `src/answer/audit/retention.ts`: the audit scope is now a single shared `AUDIT_SCOPE = source_tag in ('user','onboarding_delivery')` fragment used by both paths' WHERE, the purge's feedback subselect, and the new `countPurgeableQuestionHistory` (⟨F2⟩ — the dry run reuses the purge's own fragments so preview===apply, pinned by an equivalence test). Both paths add an UNguarded (⟨F3⟩) `pending_table_requests` leg redacting `question_text`/`topic_term`/`failure_summary` (all statuses) in the SAME transaction — self-service by bound `user_id`, purge by `created_at < cutoff`. Belt (⟨F4⟩): `registerOnboardingVocabulary` returns the empty-onboarded result without writing when `topicTerm === REDACTED_QUESTION_TEXT`, so a redacted-then-claimed row refuses+refunds instead of polluting `canonical_measures`. Pins: `tests/audit/retention-onboarding.test.ts` + the ⟨F4⟩ test in `tests/ingestion/onboarding-vocab.test.ts`. **Accepted residuals (exposure nil today):** (a) in-flight race — a redaction racing a `running` job doesn't stop it; its in-memory question copy still delivers, and the post-redaction delivery audit row is caught by the NEXT deletion/purge run; (b) ⟨F5⟩ the terminal notify email racing a deletion can quote the pre-redaction question text, but only to the user's OWN address and it is not stored by us; (c) the dashboard's non-delivered onboarding branch renders the sentinel verbatim (cosmetic — acceptable copy for a user who deleted their own history; no component change in v1). |
| 121 | Engineering / invariants | **The compose ladder's template rung records but does not ENFORCE its own validation verdict** (session-30 review): `assemble()` attaches `validateAnswerBody(...)` to every answer, but the template fallback is returned regardless of it — safe under the original assumption that the deterministic template is R3-valid by construction, which the negative-cell/long-unit blind spots (fixed in PR #15) proved can silently break for NEW data shapes. | **Open — owner decision:** should a template answer that fails its own validator REFUSE (fail-closed, costs answer-rate on future validator blind spots) instead of serving with `ok:false` recorded? Alternative: an alert/log when a served answer records `ok:false`, so the next blind spot is loud instead of silent. Decide before new data sources (priority #2) multiply the shape surface. |
| 122 | Auth / money | **Assumption (ADR [028](decisions/028-google-sso.md) D2, WP28): Supabase auto-links a verified-e-mail Google sign-in to the EXISTING `auth.users` row with the same e-mail** — one human = one account = one credit balance, and the signup-grant trigger fires no second time. Money-relevant: if the assumption is wrong, a returning magic-link user who clicks Google gets a SECOND account with a fresh 100-credit grant and an empty history. | **Open — verify EMPIRICALLY in WP28's supervised live step (brief §4.3):** magic-link account first, then Google login with the same address → assert one user row (two identities) + exactly one `signup_grant` ledger row; then a fresh-e-mail Google login → new user, one grant. The build must not merge the RUNBOOK claim as fact until this is measured. **Owner CONFIRMED (2026-07-08): 'dat moeten we gewoon doen' — the empirical linking verification in WP28 §4.3 is mandatory, not optional.** **✅ RESOLVED — VERIFIED EMPIRICALLY on live production (2026-07-10, session 34, owner present): (a) same e-mail: Google login with the existing magic-link address linked to the EXISTING `auth.users` row — user count stayed 3, that user's identities became `[email, google]`, signup grants stayed 3 (NO duplicate); (b) fresh e-mail: one NEW user (google-only identity) + EXACTLY ONE +100 grant, written in the same instant as the user row (trigger fired once). ADR 028 D2 is measured fact; both readings taken before AND after each login (read-only SQL).** |
| 123 | Product / data | **Which organization becomes the FIRST second source (WP30c)?** Owner proposed data.overheid.nl (2026-07-08). **Session-30 assessment (web-verified):** data.overheid.nl is a METADATA CATALOG (CKAN/DCAT) — it indexes ~tens of thousands of datasets but hosts none; the data lives with hundreds of publishing organizations in heterogeneous formats (CSV/XML/APIs/PDF), so it is the wrong shape for ONE conformance-passing adapter (ADR 030 D1/D6) — but the right instrument for CHOOSING sources, and a plausible LATER cross-source discovery layer for the finder (feature-pool idea, not WP30). | **Open — owner decides. Recommendation: a 'StatLine voor derden' portal, concretely data.politie.nl (police crime/nuisance figures per gemeente/buurt, open data, OData API on CBS's own StatLine-for-partners infrastructure):** a genuinely different official organization (satisfies the public-claim widening) with high question-value topics that fit our regional pipeline, Nederland-scope, and the SMALLEST possible adapter delta — same StatLine family as today's adapter. ⚠ Verify at WP30c start: derden portals likely serve OData v3 (not v4) — ADR 003's recorded v3-fallback contingency would finally apply; bounded, anticipated work. Runner-up candidates via data.overheid.nl's own catalog once the owner browses it (DUO/RDW/RIVM etc.). **OWNER DECISION (2026-07-08): the first-source CHOICE is DEFERRED — 'eerst doorgaan met CBS', but the structure must be demonstrably open ('niet dichtgemetseld'). Candidates under consideration: Eurostat, LISA (⚠ verify licensing — LISA has historically NOT been freely open data, which would strain the 'official open sources' claim), data.politie.nl (the standing session-30 recommendation), others. Consequence: WP30a+WP30b (waist formalization + adapter conformance harness) proceed as designed — they ARE the openness guarantee and need no source choice; WP30c waits for this row.** ➡ **Both LIVE: WP30a (session 36, PR #26) and WP30b (session 37, PR #27 merged `f6bcf26`) — the openness guarantee is now EXECUTABLE (conformance harness + [how-to-add-a-source guide](how-to-add-a-source.md), incl. the owner preconditions and the verified WP30c wiring points). WP30c start = this row's decision + that guide.** |
| 124 | Engineering / product | **Onboarded multi-measure vocabulary tags EVERY registered measure with the user's topic term** (`registerOnboardingVocabulary`: `everydayTerms: [topicTerm, title]` per measure). Discovered in WP27 stage-A measurement (session 31): `37789ksz` registers 18 measures — AOW, Anw, WAO, … — ALL carrying everyday term "bijstand", three of them bijstand-titled (totaal / tot AOW-leeftijd / vanaf AOW-leeftijd). Risk: the delivery parse sees several plausible readings and CLARIFIES (rule 4) instead of answering → unanswerable → refund, even after a perfect finder pick + fit accept. | **✅ MEASURED — the risk does NOT materialize (session 32, 2026-07-10, WP27 stage C, owner-approved ~cent record):** the REAL Haiku delivery parse of *"Hoeveel mensen zaten er in 2023 in de bijstand?"* against all 18 tagged 37789ksz measures resolved **"Totaal bijstandsuitkeringen" directly, confident, NO rule-4 clarification** — delivered *"Totaal bijstandsuitkeringen was in 2023 390,2 (x 1000)"* with CBS's real definition + source line. Pinned as a hermetic replay e2e on the CI gate (recorded via `scripts/onboarding-delivery-record.ts`, committed for re-records). Residual: this measures ONE question/table family; stage D's live acceptance test re-verifies end-to-end, and a future multi-measure onboarding that DOES clarify would surface via the pending rows' failure summaries — revisit only on such evidence. **Stage D LIVE-CONFIRMED (session 33, 2026-07-10): the production delivery answered "Het totaal aantal bijstandsuitkeringen kwam in 2023 uit op 390,2 x 1000" (audit 240) — the hermetic measurement predicted the live behavior exactly, no rule-4 clarification. Revisit trigger unchanged.** |
| 125 | Product / UX | **Owner flag (2026-07-11, from the live #111 acceptance answer): "390,2 x 1000" reads strangely.** Two distinct readability gaps in one answer: **(a) factor units render verbatim** — CBS publishes 37789ksz in thousands (unit `x 1 000`), and R10's misreading guard deliberately keeps the factor string verbatim (rewording units is forbidden; inventing 390.200 would be an unregistered derived number, R1/R5), so the honest-but-clunky "390,2 x 1000" is what principle (a) currently allows; **(b) the headline answers in *uitkeringen* while the question asked *mensen*** — disclosed honestly in the Definitie line (uitkeringen ≠ personen: partners share one uitkering), but the headline sentence never bridges the gap. | **Open — owner decides the display convention; fix is a SMALL standalone WP after WP29 merges (hermetic, no DDL, no spend — autonomous-safe per #118 branch+PR).** Recommendation for (a): a **registered unit-expansion derivation** for pure numeric factor units (`x 1 000` → also state `= 390.200`, exact multiplication, carried as a DerivationRecord with the derived-data marking and validator whitelist) — every number stays traceable, the reader gets the figure they actually want; the cheaper alternative (display-alias "duizend" for `x 1 000`) rewords a unit and therefore needs an explicit ADR overriding R10's verbatim rule. (b) is answer-quality/WP26 territory (phrasing bridge "gemeten als aantal uitkeringen"), not a WP29-adjacent fix — fold it into the WP26/experience-audit lane rather than patching compose ad hoc. Neither goes into WP29 (its brief freezes compose/validate untouched). **OWNER DECISION (2026-07-11, in-chat, session 35): part (a) = "uitgerekend erbij" — the expanded figure ALONGSIDE CBS's verbatim notation (the "390,2 × 1.000 (= 390.200)" shape), via the recommended registered unit-expansion derivation; the "390,2 duizend" rewording was explicitly NOT chosen (no R10 override). Execute-ready as task chip `task_e58effda` (decision baked into its prompt; small ADR first, then build; branch+PR per #118). Part (b) stays in the WP26 lane.** **✅ Part (a) MERGED + DEPLOYED — LIVE (session 36, 2026-07-11, merged PR #25 `cb8f580`, main gate + deploy green). The WIP branch was RESUMED (not redone) and finished.** As designed in ADR [031](decisions/031-unit-expansion-derivation.md): runQuery pre-registers a `unit_expansion` DerivationRecord per eligible non-null pure-factor-unit cell (exact integer-scaled arithmetic — never float multiplication); `applyUnitExpansions` (compose-level, one site for LLM + template paths) splices the display in post-validation (`390,2 x 1000 (= 390.200)` bare / `8.204 (x 1 000 = 8.204.000)` in parens), fail-open on every doubt and re-validated after splicing; the LLM payload filters the record out so every recorded fixture replays byte-identically (zero re-record, zero spend — measured: full gate + benchmark green). The interrupted session's adversarial design review WAS re-run to completion (26 agents, 6 lenses × dual skeptics): 0 findings confirmed; two killed-but-real tail risks were hardened anyway as cheap belts (misattribution belt, double-render belt — ADR 031 D4 as-built) and the marking-line trade-off is now explicit in the ADR. Verification: typecheck ×2, backend suite green (29 new tests incl. R8 old-row byte-stability), web suite green, benchmark 14/14 + 6/6 + 0 fabricated GATE PASS. Residuals recorded in ADR 031 (derived values over factor units don't expand; `1 000 euro` family = revisit trigger). **Part (b) stays in the WP26 lane.** |
| 126 | Ops / infra | **Would making the private repo public make GitHub Actions free?** Surfaced by the #125a session (2026-07-11) while the Actions account sat at ~90% of its 3,000 included min/month (the recurring billing-block risk, RUNBOOK). | **Answer: yes — GitHub Actions is unlimited + free on standard runners for PUBLIC repos; the 3,000-min cap applies only to private repos.** **OWNER ACTIVATED this option (2026-07-11, session 36, at 2,709/3,000 min) → the recommended SECRET-HISTORY SCAN was RUN and came back CLEAN (measured, all 239 revisions, every blob):** no `.env`/`.env.local` ever committed (only the empty-placeholder `.env.example`); `web/.env.production` has always held ONLY the three `NEXT_PUBLIC_*` public-by-design values (in-file header documents why); `config/supabase-prod-ca-2021.pem` is Supabase's PUBLIC root CA certificate, not a key; ZERO hits on Anthropic/Stripe/GitHub/AWS key patterns, private-key blocks, passworded connection strings, JWTs (the Supabase key in the repo is the new `sb_publishable_` client format — public by design; no service_role anything), or env assignments with real values; commit messages name env-var NAMES only; workflows use `${{ secrets.* }}` exclusively. **Residual honesty note: a fork made WHILE public keeps the code visible even after flipping back to private** — low risk in a short window, but real; pricing/strategy docs (09-pricing) also become world-readable. GitHub repo secrets (VERCEL_TOKEN etc.) are NOT exposed by going public, and public repos gain free secret-scanning/push-protection. **✅ FLIP EXECUTED BY THE OWNER (2026-07-11, session 36, after the clean scan): the repo is PUBLIC** — Actions minutes are now unlimited/free on standard runners (verified: the WP30a PR-#26 gates ran post-flip). Flip back the same way (Settings → Danger Zone → Change visibility) when the build phase ends; remember the residual: forks made while public persist. Related: the monthly-maintenance minute budget + the historical billing block (STATUS CI quirk, RUNBOOK). |
| 127 | Ops / cost | **Should we enable Anthropic prompt caching (`cache_control`) on our LLM calls?** Owner spotted the console's "Prompt caching — Not enabled" nudge (2026-07-11, session 36). | **Answered: technically possible, DEFERRED — not useful at today's scale.** Four measured reasons: (1) our static prompt prefixes (few hundred tokens) sit BELOW the minimum cacheable prefix (~1,024 tokens), so most calls cannot cache at all; (2) the cache TTL is ~5 min and the owner is the only user — mostly-miss traffic pays the +25% cache-WRITE premium and saves nothing; (3) adding `cache_control` changes request bytes → every recorded LLM fixture re-keys → full re-record with live spend (against the zero-prompt-byte/hermetic discipline) unless the fixture hasher is first taught to ignore it; (4) current live LLM spend is cents — 50–90% of ~nothing is ~nothing. **Revisit trigger (matches the existing [04-architecture](04-architecture.md) scale table, which already names prompt caching at ~1,000 active users):** real question volume or a much larger static prefix; then FIRST make the fixture hasher ignore `cache_control`, then enable. |
| 128 | Product / UX | **Owner request (2026-07-11, session 36): thumbs up/down on every answer; a thumbs-DOWN opens a dialog asking for more feedback.** | **OWNER-CONFIRMED (2026-07-11): build as a SMALL standalone WP BEFORE WP26.** ➡ **BUILT (session 37-continued, 2026-07-12, owner present) per the frozen brief (session-briefs/2026-07-12-wp128-brief.md — pre-build review 35 agents, 11 killed findings folded per the killed-pile discipline): migration 017 (FILE; applies with 016 in the supervised window), the guarded write-only store (ownership+kind+source_tag IN the insert statement), fail-soft server action (free — no billing call), FeedbackButtons chat UI (inline 👎-panel, aria-pressed, WP29 chip idiom), GDPR hard-delete interplay inside the redaction transaction. MERGED + DEPLOYED — LIVE (2026-07-12, merge f13fc39, main gate + deploy green); migrations 016+017 APPLIED to production the same session (supervised, owner present) — see #133 for two bugs the A1 re-verification caught + fixed live during that window. Residuals: dashboard/onboarded-answer feedback = the #117/#74 lane; the 👎-shortlist read path = WP26 lane; no rate limit (review-accepted — the unique constraint bounds cardinality).** Shape: two buttons on the answer chip + a free-text dialog on 👎; one additive `answer_feedback` table (audit-row id, verdict, optional text, user, timestamp — migration as a FILE, applying = supervised per the house rule); zero pipeline/LLM/prompt impact (display + write-only store; the R8 audit row is untouched — feedback POINTS at it, never modifies it). Feeds the answer-quality lane: 👎-rows become the experience-audit shortlist. Autonomous-safe per #118 (branch+PR) except the DDL apply. |
| 129 | Product / UX / architecture | **Owner request (2026-07-11, session 36): source TAGS in the chatbox — a "CBS data" chip with an ✕; deselecting it excludes that database from the search. More databases join later; CBS is PRE-CHECKED by default (owner-decided detail).** | **This is the UI half of the WP30 waist, rendering FROM the source registry (WP30a, live) by construction.** **OWNER-CONFIRMED (2026-07-11, in-chat: "De aanbevelingen: we kunnen alles vastleggen"):** **build WITH the first second source (#130 websearch or WP30c)** — a lone pre-checked CBS chip adds choice-noise without choice. Design notes for the build: deselecting ALL sources → honest refusal ("geen bronnen geselecteerd"), never a guess; the selection is a STRUCTURAL input to the finder/query scoping (a source filter over the catalog — the ADR 030 D5 seam), NOT prompt text (zero prompt bytes achievable); selection state rides the audit record so R8 reconstructs what was searched. ➡ **BUILT + MERGED + DEPLOYED (session 40, 2026-07-12: PR 33 merged `e410ea2` on the owner's in-chat approval, main gate + deploy green; GO-LIVE RUN same day — FULLY LIVE, smoke tests ledger-verified; one WP with #130 per Q10):** chips render FROM the source registry (label "`displayName` data", pre-checked) plus the "Internet" channel chip, all behind `WEBSEARCH_ENABLED` (flag off ⇒ no chips, byte-identical UI); the selection is a server-validated STRUCTURAL input (`SourceSelection`, unknown keys stripped, forced off while dormant) riding the audited envelope as `sourceSelection` with the A1 `?? null` discipline; all-deselected ⇒ client-disabled send + the deterministic `no_sources` refusal belt (full refund by existing gate mechanics). Frozen brief: [session-briefs/2026-07-12-wp129-130-brief.md](session-briefs/2026-07-12-wp129-130-brief.md). |
| 130 | Product / principles / architecture | **Owner request (2026-07-11, session 36): WEB SEARCH as a combinable source — a question can use CBS (pre-checked) and/or websearch; findings from the internet are COMBINED with the answer. Owner explicitly wants this FIRST in the source-expansion lane, and asks to research the best/cheapest technique.** | **Recorded — the single most principle-touching feature ever proposed; needs its OWN pre-build ADR + a short owner interview before any build (the WP16 pattern).** The honest tension, named: web content is live (principle b forbids live fetching in the answer path), unvalidated and unstructured — its numbers can NEVER pass R1/R3/R5 as-is, and the public claim ("every number traceable to an official CBS cell") does not cover it. That does not kill the feature; it constrains the design. **OWNER-CONFIRMED (2026-07-11, in-chat: "De aanbevelingen: we kunnen alles vastleggen"):** **the strictly SEPARATED answer section IS the chosen direction** — the validated CBS body stays exactly as today (validator-backed, attributed), followed by a visually distinct "Van het web (niet door checkdecijfers geverifieerd)" block with its own per-claim source links; web findings NEVER feed the validated body, the validator, the audit numbers or the charts; the separation IS the honesty model, per-section claims. Websearch is NOT an ADR-030-waist source (no tables/cells) — it is a separate augmentation channel; WP30c (structured source #2, #123) stays an independent lane. Opt-in per question via the #129 chip = a natural cost gate; pricing = owner decision at design time. **Technique options to research at the design step ("best en voordeligst"):** (1) Anthropic's native web_search server-side tool — fits the existing Claude API stack, built-in citations, per-search + token pricing, least code — **owner-confirmed PREFERENCE (verify cost at design time)**; (2) a search API (Brave/Tavily/…) + own pipeline — the fallback if (1) disappoints on cost/quality; (3) own scraping — **owner-confirmed REJECTED for v1**. The pre-build ADR + short owner interview still precede the build (exact copy, pricing, section shape) — the DIRECTION is settled, the details are not. ➡ **DESIGN PREP DONE (2026-07-12, session 39, owner-absent): ADR [032](decisions/032-websearch-augmentation.md) drafted (status PROPOSED) + the [interview sheet](session-briefs/2026-07-12-websearch-interview-sheet.md) with per-question recommended defaults. Cost VERIFIED from the official docs: native web_search = $10 per 1,000 searches (~€0,01/search) + result tokens as input; a web-opted question lands ≈ €0,03–€0,08 all-in; Brave ($5/1k) / Tavily ($8/1k basic) save at most half a euro-cent per search but add an owned fetch/extract/citation pipeline — native web_search is confirmed the 'voordeligst' option once engineering cost counts, and citations are built-in (always on, token-free). The ~30-min owner interview is now the ONLY remaining step before the frozen executor brief.** ➡ **INTERVIEW CLOSED (2026-07-12, same session, owner in-chat): all ten points confirmed — the four decision-weight ones (pricing +10 credits, web-only-mode allowed, fail-soft+auto-refund on web error, open web/no allowlist) presented explicitly with tradeoffs; owner replied "Go with your defaults" covering all ten. ADR [032](decisions/032-websearch-augmentation.md) is now ACCEPTED with every decision folded in. #130 is BUILD-READY — the next step is the frozen executor brief (WP27/WP30b pattern: pre-build adversarial review before the build), not another owner decision.** ➡ **BUILT + MERGED + DEPLOYED (session 40, 2026-07-12: PR 33 merged `e410ea2` on the owner's in-chat approval, main gate + deploy green; GO-LIVE RUN same day — FULLY LIVE, smoke tests ledger-verified both modes (CBS+web net 30 / web-only net 10), tool-variant correction to `web_search_20250305` en route (ADR 032 § Go-live correction); one WP with #129):** `src/websearch/` is the new augmentation-channel module (own Anthropic client + the WP's ONE new prompt; `WEBSEARCH_MODEL = 'claude-sonnet-5'` as a named constant, `web_search_20260209`, max_uses 3, NL-localized, fail-soft everywhere); the attach step runs inside BOTH audited entry points so the web section is stored-before-shown (R8) verbatim WITH citations as the additive `webSection` envelope field; money = +10 credits as a `websearch_cost` sibling debit (migration 018 as a FILE only; refund on any non-delivery via the EXISTING `compensate()`; web-only mode nets 10 by existing refusal-refund mechanics — flagged in the PR); benchmark stays web-OFF by construction (verified on the branch head: 14/14 + 6/6 + 0 fabricated GATE PASS). Go-live is a separate owner-supervised RUNBOOK step (apply 018 → `pricing:apply` → `WEBSEARCH_ENABLED=1` → live smoke test ≈ €0,05). |
| 131 | Product / i18n | **Owner request (2026-07-11, session 36): the platform and ALL pages become MULTILINGUAL — many languages: English, German, Spanish, Mandarin, Hindi (besides Dutch).** | **Recorded — phase it in three layers of very different depth, because "multilingual" cuts through the honesty machinery unevenly:** **(L1) Site/UI chrome** (pages, buttons, dashboard, login, e-mails): standard i18n (e.g. next-intl), bounded and safe — no pipeline impact; the natural first slice. **(L2) Question INPUT in any language:** the intent parser may already cope (LLM-based) but this is UNMEASURED — needs a labelled non-Dutch set before claiming it; refusal/clarify replies would still come back in Dutch until L3. **(L3) Localized ANSWERS — the deep layer:** the R3/R9/R10 validator vocabularies are DUTCH (direction words steeg/daalde, quantity word-forms duizend/miljoen, voorlopig/procentpunt conventions), the compose prompt + templates + refusal copy are Dutch product copy, `formatValueNl` is Dutch-locale number formatting, and the benchmark phrasing is Dutch — EVERY answer language needs its own validator vocabulary + prompt variant (fixture re-records) + benchmark extension, or the no-fabrication guarantee silently weakens for that language. Plus a principled decision: CBS-verbatim strings (measure titles, units, definities) are Dutch DATA — translating them collides with the verbatim rule (principle a/R10); candidate: keep data-borne strings verbatim + add a translated gloss. **OWNER-CONFIRMED (2026-07-11, in-chat: "De aanbevelingen: we kunnen alles vastleggen"):** **L1 first (riding the site-shell WP24 lane), EN as the L3 pilot language before fanning out; each further language is a real per-language cost, not a toggle.** Sequencing against the #128–#130 lane still open. |
| 132 | Privacy / ops | **Owner question (2026-07-12, session 37): is the now-PUBLIC repo (#126) traceable to the owner personally (name, former employer)?** Measured audit (full git history + tree): **YES, on several layers** — (1) the owner's personal e-mail is the git AUTHOR on ~234 of ~254 commits (visible in every clone; the address contains the full name); (2) the 2026-07-03 work-identity history-fix REMOVED the employer identity from commit metadata (verified: zero hits) but the fix's own documentation still spells the work e-mail out (lessons-learned session-3 entry, RUNBOOK GitHub row + fresh-clone step); (3) the RUNBOOK provider-logins table + a STATUS session-34 line carry the personal e-mail addresses (written when the repo was private); (4) `KICKOFF_PROMPT.md` carries old `/Users/<fullname>` machine paths; (5) one session brief carries the current machine username in sandbox paths. The repo-owner ACCOUNT (first name + number) is inherently public. NB tree-only redaction does not clear the git HISTORY. | **Options presented to the owner (session 37): A** stop new leakage (repo-local noreply commit identity — ✅ DONE session 37 — + GitHub "keep e-mail private" settings, owner action); **B** redact the current tree (RUNBOOK/STATUS/lessons/KICKOFF; provider table keeps redacted hints — collides with the "everything in the repo" convention, so this IS the recorded policy: operational docs stay, personal identifiers get redacted now the repo is public); **C** history rewrite (git filter-repo: author → noreply everywhere + PII strings scrubbed from old file versions; force-push; Dependabot PRs re-based after; the ONLY way the e-mail leaves commit metadata — best done while the repo is young: no forks/external users yet); **D** back to private (loses the free CI minutes). Session recommendation: A+B+C now, not D. ➡ **OWNER APPROVED A+B+C in-chat (2026-07-12) — B and C EXECUTED same session, measured:** B = tree redaction (5 files, commit on main); C = git-filter-repo (mailmap → noreply + 9 replace-text rules) on a fresh mirror, verified pre-push (authors: ONLY noreply; PII in all historical blobs: 0; HEAD tree hash byte-identical `594a8ac…`), force-pushed at 0 forks/0 stars; stray branch `wp27-stage-d-fit-calibration` deleted; Dependabot PRs #5/#6/#19/#20 told `@dependabot recreate`; final proof = a fresh clone FROM GITHUB: sole author e-mail is the noreply address, 0 PII hits across every historical version — **⚠ CORRECTED by the independent post-rewrite audit: that proof covers `refs/heads/*` ONLY. GitHub permanently retains `refs/pull/N/head` for all 27 (closed) PRs, and those still serve the ENTIRE pre-rewrite history (287 commits, zero overlap with the rewritten main) with every identifier intact — publicly fetchable by anyone via `git ls-remote 'refs/pull/*'` or the PR Commits tabs. The refs/pull namespace is server-owned: not deletable or overwritable by the repo owner; a git rewrite structurally cannot reach it.** A pre-rewrite safety bundle sits at `~/checkdecijfers-pre-rewrite-2026-07-12.bundle` on the build machine (contains the OLD history by design — owner deletes it when satisfied). **Post-rewrite adversarial audit (same session, "must be flawless" owner steer) found + fixed ONE more leak layer: GitHub Actions run METADATA (`head_commit.author.email` on every pre-rewrite run, publicly readable via the API) survives a git rewrite — all 330 pre-rewrite runs DELETED (the 3 post-rewrite runs, all green incl. deploy, remain); the runs API now serves only the noreply address. MEASURED RESIDUAL: orphaned pre-rewrite commits that a merged PR references (e.g. the PR #27 commits) are still served by GitHub's API/PR-commits tab WITH the old author e-mail — only GitHub Support can de-reference those.** **RESIDUAL — owner actions (upgraded after the independent audit's refs/pull finding):** (1) ✅ DONE by the owner (2026-07-12): GitHub → Settings → Emails → "Keep my email addresses private" + "Block command line pushes that expose my email" both ON (GitHub's own caveat confirms the residual: previously authored commits with a public e-mail remain public — the refs/pull route decision below still stands); (2) check github.com/Stefan7168 profile fields (name/company); (3) **REQUIRED for the scrub to be real — OWNER CHOSE ROUTE (a) in-chat (2026-07-12):** **route (a) GitHub Support ticket** (keeps the repo, PR links and history intact; takes days) — support.github.com → "Sensitive data removal", text: *"I rewrote the history of my public repository Stefan7168/checkdecijfers on 2026-07-12 to remove personal e-mail addresses and identifiers from commit metadata and file contents. Please purge ALL cached pre-rewrite commits, INCLUDING the refs/pull/N/head references for all 27 pull requests, which still serve the entire pre-rewrite history publicly. Please also run garbage collection so the orphaned commits stop being fetchable by SHA."*; **route (b) delete + recreate the repo — ✅ FULLY STAGED (2026-07-12): all 89 doc PR-links pre-neutralized to plain text, re-setup drill in the RUNBOOK ('Route B drill'), measured blast radius: 3 recoverable secrets / 0 webhooks / 0 issues / 0 forks; execution = ~20 min owner-present, production untouched; UPGRADED to a TWO-PHASE reversible drill after the owner's pre-mortem request (rename+private first — PII instantly non-public, full rollback in ~2 min; the irreversible delete moves to a later maintenance session, weeks after the new repo is proven; pre-mortem findings + research links in the RUNBOOK drill); awaits only the owner's explicit in-chat GO — OWNER DECISION 2026-07-12: DEFERRED ~a week ('repo openhouden tijdens het bouwproces, volgende week sluiten'); building continues meanwhile. Interim rules until route B runs: (i) sessions reference PRs as PLAIN TEXT in docs, never live links (avoids a re-neutralization round); (ii) new commits already carry the noreply identity; (iii) the drill's forks==0 check at T-0 is the go/no-go — a fork appearing during the deferral flips the fix to the Support-ticket route**; (4) ⚠ ANY OLD CLONE (the pre-switch machine!) must be RE-CLONED, never pushed from (RUNBOOK note). Realism note: the addresses were public for ~9 days (repo public since #126) + in Actions metadata — treat both gmails and the Vercel team slug as potentially harvested regardless of route; they are contact identifiers, not credentials (the audit reconfirmed: no secret has ever been in the repo). Dependabot PRs re-create themselves weekly. First name via the account handle stays visible by choice. |
| 133 | Data integrity / R8 | **Two pre-existing R8 reconstruction bugs found + FIXED live (2026-07-12, session 37-continued) while re-running the owed A1 verification against ALL 240 production audit rows post-WP30b/WP128:** (1) `buildDefinitionLine` crashed on rows stored before session 29 (#115 lever b) — 87/91 live answer rows never serialized an `attribution.definitionText` key at all (`undefined`, not `null`); fixed with the same A1 `?? null` discipline WP30a applied to `attribution.source` (regression: `tests/audit/definition-text-r8.test.ts`). (2) The onboarding-envelope consistency check crashed/false-positived on refusal rows stored before `refusals.ts`'s `input.built.onboarding ?? null` normalization existed — 73/215 refusal rows across nearly every reason lacked the `onboarding` key entirely; same `?? null` fix (regression: `tests/audit/onboarding-envelope-r8.test.ts`). Neither bug ever affected a live answer shown to a user — both only broke RECONSTRUCTION of old stored rows, exactly what this owed verification step exists to catch. Both pushed directly to `main` (owner-supervised in-session fix during the live migration window, #118 house exception), full verification green before each push (typecheck ×2, backend suites, benchmark GATE PASS). Final measured result: **213/215 checkable rows reconstruct clean** (25 GDPR-redacted rows structurally skipped by the verify script pending #133b below). | **Two items remain, NOT fixed tonight (design judgment, not a quick patch):** **(a) Two genuine historical-behavior-versioning anomalies** — row 76 (a 'series' chart spanning 2021MM06→2026MM06, non-contiguous — a LATER session's #64 fix now correctly refuses to chart such a gap, so re-deriving with today's rule legitimately differs from what was shown in 2026-07's row) and row 227 (a "Definitie: X" line that predates the SAME-DAY #115-lever-a circular-title-suppression fix). Both are narrow (2 total), harmless (the stored `final_text`/`response.text` IS what the user actually saw — R8's core promise holds), and raise a real question the codebase hasn't answered: when a deterministic builder's behavior legitimately changes, should reconstruction honor the row's OWN historical rule (needs a versioning mechanism, more machinery) or is "differs from a later, safer rule" an acceptable, documented reconstruction gap? Recommend: a lightweight session decides the policy, not a rushed migration-window patch. **(b) `scripts/verify-audit-rows.ts` now skips GDPR-redacted rows entirely** (25 of 240) rather than crashing on them — `reconstructionReport`/`checkEnvelopeIntegrity` were never taught the `retention.ts` sentinel shape (`redacted: true`, no `.answer`/`.result` at all: `response.answer.schemaVersion` throws on `undefined`). Needs a real decision: should a redacted row reconstruct AT ALL (and if so, to what — verifying it matches the EXACT expected redacted shape, a "redaction integrity" check, not the original content), or is "structurally skip, count, never claim clean" the permanent policy? `reconstructionReport` itself (and the benchmark gate that trusts it) is UNTOUCHED — the skip lives only in the diagnostic script. ➡ **decided 2026-07-12 session 39 — (a) documented-gap policy + known-divergences register, (b) redaction-integrity check; both shipped this PR** (design brief session-briefs/2026-07-12-smalls-133-brief.md §1, pre-build-reviewed): **(a)** no historical-rule versioning machinery — `reconstructionReport` keeps verifying under TODAY's rules, and a legitimately-superseded builder rule (row 76 / #64; row 227 / #115-lever-a) becomes a documented, PINNED entry in the new `src/answer/audit/known-divergences.ts` register (`classifyKnownDivergence`), never a blanket skip — anything not listed still fails, and a listed row whose problems don't match its pinned substring(s) also still fails. **(b)** redacted rows get a real check, not a skip: new `redactionIntegrityReport` (`src/answer/audit/retention.ts`) verifies a redacted row matches the EXACT sentinel shape the redaction wrote (no leftover `answer`/`result`/`chart` key, promoted columns null/empty, `reply_text`/`pending_clarification` paired). `scripts/verify-audit-rows.ts` now routes redacted rows there instead of skipping them. `reconstructionReport` and the benchmark gate stay byte-for-byte untouched; see ADR 016's 2026-07-12 as-built addendum for the full policy record. |
| 134 | Product / UX | **Owner idea (2026-07-12, session 39): when a refusal is specifically a period-coverage refusal ("we don't have this data for that range"), the answer should come back with a follow-up suggestion for a period that DOES work** — e.g. "inflatie tussen 2001 en 2024" refuses; a chip like "Probeer 2010–2024" would let the user retry with one click instead of guessing a working range themselves. | **Recorded, scoped, NOT built.** Grounded in the real refusal code (2026-07-12): the mechanism is PARTIALLY ALREADY THERE. Two of the three period-coverage refusal reasons ALREADY compute and surface the real boundary as PROSE inside the refusal text — `outside_loaded_slice` (`buildOutsideSliceRefusal`, `src/answer/respond/refusals.ts:362-378`, using `refusal.refusal.nearestAlternative` = `slice.periodFloor` computed in `src/query/resolve.ts:399`) says *"Ik kan wel cijfers laten zien vanaf {nearest}."*; `freshness` (`buildFreshnessRefusal`, refusals.ts:306-336) says *"Ik kan het cijfer voor {available} direct geven, vraag daar gerust naar."* Both already carry the real, deterministic boundary — zero guessing, principle (c) is already satisfied for these two. **The gap is narrower than the raw idea:** (1) turn that existing prose into an actual clickable chip — reuse the WP29/#73 mechanism (`src/answer/respond/suggestions.ts`, `ADR 029`: deterministic generators, `echoServability`-gated, fill-don't-send per #75) rather than build a new one, since `buildSuggestions` already exists and is proven; today it only fires on the ANSWER path (`respond.ts:172`, needs a `ValidatedResult`) so wiring a refusal-side variant is new surface, not a copy-paste. (2) The THIRD reason, `not_published` (`buildNotPublishedRefusal`, refusals.ts:341-359 — **the exact reason in the owner's own example**, "CBS heeft voor inflatie … (nog) geen cijfer over deze periode gepubliceerd") has `offer: null` today — no boundary is computed or known for this case at all, and it may genuinely be unknowable (CBS never having published the measure that far back doesn't imply a nearest-working start year exists to suggest). Scoping this properly needs: (a) a small WP wiring suggestion chips onto `outside_loaded_slice`/`freshness` refusals (the two reasons that already know the answer — cheap, high-confidence first slice), (b) a separate, harder investigation into whether `not_published` can ever offer a real alternative (may end in "no, refusal stays prose-only for this reason" — an honest answer is fine). **Assumption marked:** the owner's example ("probeer 2010–2024") would fall under `not_published` today given 86141NED's actual coverage, i.e. the owner's own example is the HARD case, not the easy one — worth confirming with a real query before scoping the WP.)
| 135 | Product / UX / architecture | **Owner request (2026-07-12, session 40, with reference screenshots of an external AI-assistant product as inspiration): redesign the chat WORKSPACE layout.** (1) The chat conversation fills the full width UNTIL a chart or table appears; from then on charts/tables dock in a RIGHT-hand pane while the conversation stays on the LEFT — so users can keep asking while the visual stays in view. (2) Multiple charts/tables in one conversation → a TAB system in the right pane. (3) A conversation-history SIDEBAR on the left (grouped by day: vandaag/gisteren/afgelopen 7 dagen) with a "nieuwe chat" button to start a fresh conversation. (4) Credits move to the TOP-RIGHT navigation. (5) The follow-up suggestion chips (WP29) MUST stay. | **Recorded, NOT built — a UI-architecture WP that needs its own design round + brief (own fresh session; recorded at the WP129+130 go-live, context-window end).** Honest design notes for that round: **(a) conversation persistence is the deep half** — today's chat thread is ephemeral page state (history lives in `audit_answers` + the dashboard list); a sidebar of resumable conversations means persisted THREADS (a new grouping entity over audit rows, or a new store), which is **GDPR-scoped personal data** — the #14/#120 retention + self-service-deletion machinery must cover it from day one, and "nieuwe chat" = an explicit ADR-021 context reset (per-thread conversation context already exists structurally). **(b)** The right-pane dock is a rendering-architecture change only — charts/tables are already structural per-answer fields (`chart`, `card`, `csv`), so docking = collecting them per thread + tab state; the WP129+130 web section is NOT a chart and stays in the conversation flow (the separation IS the honesty model). **(c)** Credits-in-nav + the workspace shell overlap WP24 (site shell) and #117/#74 (async delivery surface) — sequence together. **(d)** Suggestion chips, feedback buttons, source chips and the pre-send cost line are envelope-driven and survive a layout change untouched — pin byte-identity of the MESSAGE rendering in the redesign brief. **➡ DESIGN ROUND DONE (session 41, 2026-07-12): ADR [033](decisions/033-chat-workspace-redesign.md) ACCEPTED; owner interview locked all four open choices — (1) retention = the same 2-year #14 term (`chat_threads` stores NO text columns by design, titles derived read-time from the first audit row, so purge + self-service deletion cover threads with zero new machinery and a fully-redacted thread disappears from the sidebar); (2) resume = read AND continue asking (deterministic envelope replay + server-side context rebuild, ADR-021 revalidation as the staleness guard); (3) WP24 shell absorbed (see #98/#99); (4) per-thread delete DEFERRED — residual: a per-conversation delete button is a fast-follow once wanted, the centralized redaction machinery takes a thread-scoped leg cheaply but needs its own cross-user pins. Build = WP135 in [08-build-plan.md](08-build-plan.md): branch + PR per #118(b), dormant behind `WORKSPACE_ENABLED`, migration 019 live DDL only in a supervised step. **➡ BUILT + MERGED TO MAIN session 41 (2026-07-12, owner's explicit in-chat instruction, owner present): full pattern ran (frozen brief, 62-agent pre-review → A1–A7, staged build gate PASS, 22-agent post-review → 2 blockers fixed). DORMANT in prod; NEXT: the owner-supervised go-live (RUNBOOK § "WP135 chat workspace").** |
