# ADR 039 — Chart presentation panel: a pure resolver over the ADR 038 reducer

**Status:** accepted, 2026-09-09 (session 91, autonomous, branch `wp218-chart-styling`) — phases 0 and 1 of WP218 as built; later phases are appended as dated addenda at the end of this file.

## Context

Open-questions [#218](../open-questions.md): the owner asked (session 89) for a Recharts-style chart *configuration* tool — pick a chart type, adjust presentation options such as line thickness, with the tool pre-filled with what the chart currently shows. Session 90 ran a four-angle Fable architecture panel + synthesis ([session-briefs/2026-09-09-session-90-chart-config-tool-synthesis.md](../session-briefs/2026-09-09-session-90-chart-config-tool-synthesis.md)) and the owner answered its eight open decisions A–H in chat: A an English/Dutch switch for the whole app (WP218 phase 4, [#219](../open-questions.md)); B a Colours tab with swatch + hex + picker per series, default the stock Recharts palette, plus brand colours and fonts (phase 3); C styles saved in the user's account (phase 2); D more chart types after a best-practice research piece (phase 5); E each chart starts fresh; F an anonymous usage counter (phase 6, [#220](../open-questions.md)); G the panel everywhere, homepage and trial included; H four named thickness steps.

The binding invariants for anything that touches a chart: every number on a chart is a spec string bound to its cell (R1/R6); the provisional marking — hollow marker / hatched bar — can never be switched off (R11); bars keep a zero baseline ([#48](../open-questions.md)); the export shows what is on screen; the session-87 stock look ([12-huisstijl](../12-huisstijl.md) → Charts) stays the default. One constraint the panel surfaced and this session verified: the web-side honesty tests walk every text node of the whole chart card for digit tokens (`web/components/chart.test.tsx`), so any control copy inside the card must be digit-free (ADR [038](038-chart-view-state-editing.md)'s session-90 correction).

## Decision

**A presentation is a set of plain user overrides on top of a test-pinned stock look, resolved per render into effective values plus honesty locks; both the Recharts render and the panel read only those effective values.**

- `web/lib/chart-presentation.ts` (pure, no React/Recharts import) holds the `ChartPresentation` type — `lineWidth` (thin / normal / thick / extraThick = 1 / 2 / 3 / 4 px), `markers` (all / provisionalOnly), `grid` (both / horizontal / none), `xLabels` (flat / tilted), `axisLines`, `valueLabels`, `zeroBaseline` (auto / zero), `seriesColors` (series index → `#rrggbb`), `fontFamily` — the constant `STOCK_PRESENTATION` (today's literals, deep-equal pinned by a test so the session-87 look cannot drift), the Recharts palette (moved here from `chart.tsx`, re-exported there), and `resolvePresentation(ctx, overrides, base = STOCK)` → `{ values, locks, applicable, pristine }`. Overrides pass an allow-list parse (`sanitizeOverrides`, zod) on every call: unknown keys, wrong enum values, malformed colours and non-numeric indexes are dropped, never thrown on.
- **Locks re-run on every render**, so a stale override can never apply to a newly-unsafe spec: in bar form value labels are forced shown (without them a bar chart has no scale — the axis deliberately shows no numbers of its own) and the baseline forced to zero; line width, markers and baseline are not applicable to bars; **the panel is not offered in table form, so nothing applies there** (final-review fix, session 91: this line used to say "in table form only the font applies", which contradicted the panel-placement line below and was unreachable dead code in the resolver — corrected in both places the same session). Lock reasons are fixed, digit-free Dutch strings.
- **The ADR 038 reducer gains a `presentation` slice** (`setPresentation(patch)`, `resetPresentation`); its existing `reset` action — fired when the visual dock or the Ontdek toggle swaps the spec on the same mounted `ChartView` — clears the slice (**owner decision E: each chart starts fresh**).
- **R11 by geometry:** `dotGeometry(lineWidth)` grows the marker with the line (`r = max(4, px + 2)`, ring 2) so a 4 px stroke cannot swallow the hollow ring; under `provisionalOnly` the non-provisional dots become transparent but stay in the DOM with their `data-point`, role, tab stop and click handler, so the point count, keyboard walking and click-to-annotate are unchanged and the hollow markers always render. `chart-small-multiples.tsx`, which drew `dot={false}`, now draws the hollow marker for provisional points (the R11 gap the panel found).
- **Colours (owner B) with a guard:** per series a swatch, a hex text box and a native colour picker, pre-filled with the effective colour; `judgeColor(hex)` refuses a colour whose WCAG contrast against EITHER theme's card (`#ffffff` light, `#171717` dark — `--card` in `globals.css`) is below 1.25 — the hollow ring would vanish — with a digit-free reason, and warns (still applies) below 3.0 per theme. Every stock palette colour passes (the stock `#ffc658` on white is ≈ 1.55:1: a warning, never a refusal); an untouched palette default never warns — that palette is the owner's accepted session-87 trade-off. Hex codes live only in `<input>` values, never in text nodes, which keeps the whole-card digit scans exemption-free.
- **Fonts:** a curated list (Roboto, Open Sans, Lato, Merriweather, Playfair Display from Google Fonts; Georgia, Arial system), loaded on demand by one `<link>` per family (`web/lib/font-loader.ts`) rather than bundled through `next/font`; the family is applied as CSS on the chart container so SVG text inherits it, and the export's `inlineComputedPaint` writes the computed family onto every text node. Known limit: the PNG export rasterises through an `<img>`, which cannot load web fonts, so a PNG falls back to a system font; the SVG keeps the family name.
- **Tilted labels** reserve axis height (`xAxisHeight`) and left margin (`xLabelOverhang`) so nothing is clipped on screen or in the export.
- **The panel** (`web/components/chart-config-panel.tsx`) is a dumb component over the resolved values: an `Opmaak` trigger at the right of the Weergave tablist, a `role="region"` with hand-rolled tabs Grafiek / Kleuren / Lettertype (house rule 4 — the tested ARIA patterns of `chart.tsx`/`chart-toggle.tsx`), radiogroups for exclusive choices, `aria-pressed` toggles, locked controls disabled with an `aria-describedby` reason, Escape closes and refocuses, `Standaard` resets (disabled while pristine). It mounts inside the card but outside the `chartContainerRef` export container, on every `ChartView` (chat, dock, Ontdek, trial — **owner G**), **not offered in Tabel form** (nothing applies there — see the resolver line above). Copy is Dutch, matching the card's own controls, with an `en` variant in `PANEL_COPY` so phase 4 only passes a language.
- **Nothing in `src/` changes; `chart-download.tsx` is untouched** — `stroke-width`, `r`, `transform` and grid presence travel verbatim into the export (test-pinned: the SVG export carries `stroke-width="3"` after Dik), and `Standaard` restores a byte-identical stock svg.

## Alternatives considered

1. **Presets only** (Standaard / Minimaal / Presentatie) as the first slice — fewer states, but it does not deliver the owner's literal example ("a thicker line, nothing else") and turns "pre-filled with current values" into "pre-selected"; presets remain possible later as named `Partial<ChartPresentation>` objects over the same overrides.
2. **A second reducer / component state beside the ADR 038 reducer** — would duplicate the atomic `reset` that session 89 introduced to stop state leaking across a dock-tab spec swap; presentation must ride that same action or the leak returns.
3. **Presentation in the chart spec (schema v2, ADR 007's seam)** — audit rows are immutable except for GDPR redaction (R8), so a reader's override could never live there; the spec would carry constants forever. The seam is honoured by a promotion trigger instead (below).
4. **A free pixel input for thickness** — breaks the exemption-free digit scan and is fiddly on a phone (owner H: named steps).
5. **A Popover/Collapsible primitive for the panel** — the dock `<aside>` is `overflow-hidden`, and hand-rolled ARIA is the tested house pattern.
6. **Curated palettes only, no picker** (the panel's own recommendation) — overruled by the owner (B); the per-pick contrast judgement above is the compromise that keeps the R11 guard.

## Consequences

- Zero change to `src/chart/*`, `buildChartSpec`, stored specs, prompts, migrations; the R1–R11 backend suites are untouched by phases 0–1.
- `ChartView` grows by the resolver call and the panel mount; `chart.tsx` (already large) gains ~60 lines — the grid/axis JSX is now near-identical in three places (line, bar, small multiples), an extraction candidate noted for the final review.
- New surface for the honesty scans: the panel is inside the card, so its copy must stay digit-free; the panel's own tests walk every text node in both languages.
- The account default (phase 2) is a second layer under the per-chart overrides: `resolvePresentation(ctx, overrides, base)` already takes the base.

## Revisit triggers

- **A second production renderer** (OG image, PDF export #215, embeds): move the vocabulary to `src/chart/presentation.ts` so `renderChartSvg(spec, { presentation })` reads the same object (ADR 007's seam).
- **Usage counter data** (phase 6) showing which options are used decides whether phase 5's extra chart types and any later chat-routed presentation edits (ADR 038's deferred Phase 4) are worth building.
- **Brand fonts** (phase 3) may bring families outside the curated list — `fontStack` already accepts a regex-validated family name.

---

## Addendum 2026-09-09 — phase 2 (account default) and phase 6 (usage counter), as built

- **Store:** `src/chart/user-styles.ts` over migration `028_user_chart_styles.sql` (FILE-ONLY until the owner's supervised apply — RUNBOOK): `user_chart_styles(user_id pk, style jsonb, brand jsonb, created_at, updated_at)` with the guarded `auth.users` FK, and `chart_style_usage(event, day, count)`. Every function degrades on an absent table (reads → null/[], writes → `unavailable`, counter → no-op), so the code ships before the DDL without an error page.
- **Composition:** the workspace page reads the row server-side and hands it to `ChartStyleProvider`; `ChartView` builds `base = withAccountDefault(accountStyle)` and calls `resolvePresentation(ctx, state.presentation, base)` — per-chart tweaks sit on top and are still cleared per chart (E); `Standaard` returns to the account default. "Bewaar als mijn standaard" saves the effective values **minus the keys the resolver locked for the current form** (a bar-forced zero baseline must never become a line chart's default — found in review). The two actions live in `web/app/chart-style-actions.ts` (small import graph).
- **Personal data:** the row is user-keyed; it joins the retention job as an injected leg (`retention-job.ts` `chartStyles`, two-year `updated_at` window, both composition roots inject the same functions), the account-level "Verwijder mijn geschiedenis" also deletes it, and the panel's "Vergeet mijn standaard" is the self-service delete. **Assumption** (open-questions #218): two years since the last save is the retention window for a preference row.
- **Counter (#220):** `panel_open`, `option_changed`, `default_saved`, `default_forgotten`, `brand_applied` — event × UTC day × count, no user id or IP; a `'use server'` action validates the enum; the client tracks through an injectable sink (`web/lib/chart-usage-client.ts`, registered by `ChartUsageTracker` in the root layout) so tests and server renders never touch it. Anonymous visitors count too (G).

## Addendum 2026-09-09 — phase 3 (brand colours and fonts via Brandfetch), as built

- `src/chart/brandfetch.ts`: domain normalisation (plain hostnames only), a free-mail list, a tolerant zod parser for the Brand API payload, `pickBrandColours` (brand → accent → dark → light, max eight) and `pickBrandFont` (Google or system fonts only — a custom font cannot be loaded and is skipped), and `fetchBrand` that never throws (404 → not found, 401/403 → unauthorised, 429 → rate limited, else unavailable). The endpoint path form is a single constant, UNCONFIRMED against a live key ([session-briefs/2026-09-09-session-91-brandfetch-research.md](../session-briefs/2026-09-09-session-91-brandfetch-research.md)).
- **Spend control:** `brand_cache` (migration `029_brand_cache.sql`, file-only; public brand facts by domain, kept ≤ 30 days per Brandfetch's terms) is checked BEFORE the per-user cap (five lookups a day, stored in the user row's `brand.lookups`); no `BRANDFETCH_API_KEY` → the panel says lookup is not possible; signed-in users only. Cost facts for the owner: 100 free lookups in total, then a paid plan (≈ $99/month, re-check before paying) — superseded later the same day by the owner's hard cap of 100 real calls per calendar month (addendum below).
- **Guard unchanged:** brand colours pass `judgeColor`; refused ones keep the palette colour; the font is applied only when one was found. The Kleuren tab's "Pas merkkleuren toe" asks for a website when the login domain is a free-mail provider (not an error), applies colours + font as ordinary overrides in one patch, and shows an attribution line; a later "Bewaar als mijn standaard" records the applied brand in the row.

## Addendum 2026-09-09 — phase 5 (more chart types), as built (owner D, after the research read-back)

- Research ([session-briefs/2026-09-09-session-91-chart-type-picker-research.md](../session-briefs/2026-09-09-session-91-chart-type-picker-research.md)): users expect ONE visible control, a sensible default already chosen, unsuited types visibly greyed with a one-line reason (Tableau's Show Me), and their other settings to carry over. So the Weergave switch grew in place: `ChartForm = line | area | bar | hbar | table` (`Lijn · Vlak · Staaf · Liggend · Tabel`), pure guards `areaFormAllowed` (single-series time series only), `hbarFormAllowed` (comparisons only) beside `lineFormAllowed`, and `fallbackForm` for a same-instance spec swap.
- **Area:** filled single series, Y axis forced to zero (the fill encodes magnitude — #48's own logic; `zeroBaseline` locked with a reason), gaps kept, the same hollow markers. **Horizontal bar:** one bar per region in the spec's order (R6 — never sorted), region labels on the category axis (`RegionAxisTick`, verbatim), the number axis from zero with no invented ticks, the value label at the bar's end bound via `data-label-for`, hatch for provisional; hide/highlight per region as in the legend. The membership scans pass in both forms; the export is unchanged.
- **Never offered:** pie/donut, stacked, 100 %, scatter, sorted/ranked, smoothed/step — explained once in the panel's collapsed note "Waarom geen taart- of gestapelde grafiek?".

## Addendum 2026-09-09 (later the same day, owner present) — layout chosen from a design canvas

- The owner reviewed two Claude Design canvases the session drew from the real components (the chart card with its panel; the chat screen) and chose: **the panel's compact two-column layout ("option A") with the chart on top and the settings under it**, and for answers **the answer card ("option B")** with the like/dislike buttons first. As built: the `Opmaak` trigger stays in the Weergave tab row (a plain button whose open state lives in `ChartView`, reset per chart with the rest of the presentation state); the region renders directly after the chart's export container; the Grafiek tab is a label/pills grid with one `Tonen` row for the three toggles; the chart-language select sits in the region's header; the "why no pie chart" note and `Standaard` form a footer on the Grafiek tab only (the Kleuren tab keeps its own `Standaardkleuren`). A first build moved the trigger with a React portal into a placeholder in the tab row — it never worked in a real browser (the button stayed in place, clicks did nothing) and was replaced by the plain controlled button in the same session.
- **Answer card (chat):** every answered question renders in a shadcn `Card` whose footer carries the full R4 source sentence and the source badge on the left and, on the right, first the like/dislike buttons (outline, white background, grey border, light shadow, lucide icons + labels), then proof / citation / CSV as quiet buttons with icons, then the cost line. The proof panel and the not-helpful panel take the footer's full width when open. Refusals, clarifications and info messages are unchanged.

## Addendum 2026-09-09 — global monthly Brandfetch cap, as built (owner decision: "max it at 100 per month, and then disable it until the new cycle, so we avoid paying")

On top of the phase-3 per-user daily cap, `lookupBrand` (`web/app/chart-style-actions.ts`) now checks a GLOBAL monthly cap — `BRAND_FETCHES_PER_MONTH = 100` real Brandfetch calls across all users, summed by `sumChartStyleEventsInMonth` (`src/chart/user-styles.ts`) over the `chart_style_usage` counter's `brand_fetch` event for the current UTC calendar month — checked after the cache but before the daily cap and any fetch, failing closed (`monthly_cap`) both once the cap is hit and whenever the counter table itself is absent (never a paid call that cannot be counted). The panel shows a new digit-free reason (`chart.panel.brandMonthlyCap`); the cap resets automatically at the next UTC month. Cache hits never count and stay allowed even past the cap.

## Addendum 2026-09-09 (session 92, owner present) — Frame styling and the floating Style panel, as built

**Owner ask:** "when people download the graph, it looks too boring" — frame the chart with a background colour, gradient or uploaded image, plus padding, rounded corners, shadow, inset and aspect ratio, all visible on screen and in the export. **Three owner decisions (same session):** (1) the frame shows ON SCREEN and in the download — what you see is what you get; (2) an uploaded image lives only in the reader's browser for this chart, never saved to our servers; (3) the Style button becomes a floating panel beside the chart, not a centred modal.

- **The six frame keys** (added to `ChartPresentation` via `web/lib/chart-presentation.ts`): `frameBackground` (None / Colour / Gradient / Own image — each with its own controls), `framePadding` (None / Small / Medium / Large), `frameCorners` (Square / Rounded / Very rounded), `frameShadow` (None / Soft / Strong), `frameInset` (None / Small / Large — the chart sits on a white card inside the background), `frameAspect` (Auto / 16:9 / 4:5 / 1:1 / 1.91:1). Gradient has six named presets (Dawn, Ocean, Forest, Berry, Slate, Sand) and custom hex From/To; the angle is fixed at 135°. All keys are in Dutch and English via `web/lib/i18n/messages.ts`. **Final-review correction:** the frame keys were originally added to `applicable` unconditionally, including table form ("a table gets the same frame as any other chart form") — the final review reversed this: **Tabel form has no frame and no Style panel (as before) — a framed table would need its own export path.** The six frame keys are applicable in every OTHER chart form (line, area, bar, hbar), never in table.
- **On screen:** `ChartFrame` (new `web/components/chart-frame.tsx`, used in `chart.tsx`) wraps the export container with the frame's background, padding, border-radius, box-shadow, optional inset card, and aspect-ratio sizing. A pristine frame renders no style (no wrapper element). Chart scales to fit inside the aspect ratio via `ResponsiveContainer height="100%"`.
- **Export:** `attributedSvgMarkup` in `chart-download.tsx` gains a `frame` argument and wraps the chart SVG in an outer `<svg>`: a `<rect>` for solid background, `<linearGradient>` + `<rect>` for gradient (endpoints computed from the CSS angle `135deg` via `gradientEndpoints(angleDeg, width, height)`, in `userSpaceOnUse` px — final-review fix: this replaced an earlier `objectBoundingBox` approximation that only matched the on-screen CSS gradient exactly for a square box), or an `<image>` with data URL for the uploaded image (both `href` and `xlink:href` set for compatibility, per test-pinned export). Corners via `rx`, shadow via `<feDropShadow>` (on an image with rounded corners, the shadow filter sits on a wrapping `<g>` rather than the clipped `<image>` itself, so it is not clipped away with it — final-review fix), inset card as a nested `<rect>` with padding. Unique clip-path id per export (`frame-clip-<counter>`). Attribution text and badge sit inside the frame. PNG rasterises the outer SVG (data URL inlined); SVG keeps the image as base64. The chart clone's own unconditional white background rect is skipped whenever a non-pristine frame (or an image background) is active, so the frame's own background shows through the chart area exactly as on screen (final-review fix). **Shadow without a background:** a `frameShadow` with no background at all (`'none'`, or `'image'` with nothing chosen) used to have nothing to attach the shadow filter to; the export now applies the filter to the inset card when inset is on, or otherwise emits a dedicated white ground rect just to carry the shadow — an intentional screen/export asymmetry (the chart itself stays transparent on screen in this case), noted in `docs/open-questions.md`.
- **The uploaded image:** lives ONLY in browser-side `ChartView` state as `frameImage: string | null` (a data URL from `FileReader` on PNG/JPEG/WebP ≤ 2 MB — final-review fix, lowered from 5 MB; rejected types and oversized files show a plain refusal message). Never reaches the server, never saved to the account default (when "Save as my default" is clicked, `{ kind: 'image' }` is dropped to `'none'` with a status line saying "the picture is not saved"). If the background is set to "Own image" but no image is uploaded, the resolver degrades it to `'none'`.
- **Contrast guard — final-review correction:** the original design re-judged colours AFTER the fact (`judgeColor` re-run once a frame background was already applied, silently dropping/keeping the reader's per-series colour override). The final review replaced this with an UP-FRONT refusal: before committing a solid background, a gradient (either stop), or turning the inset card off, `ChartConfigPanel` checks every effective series colour (`seriesMeta[i].color`) against every candidate backdrop (`frameBackdrops` of the would-be values) using `contrastRatio` and `COLOR_REFUSE_BELOW`. If any series would become illegible, the change is refused outright — `onChange` is never called — and a `role="alert"` line (`chart.panel.frameBgRefused`) explains why. Series colours are never changed by the frame feature; `chart.tsx`'s former silent-drop guard was removed as redundant. Both directions are guarded: a background that would make any series unreadable is refused with a message, and a series colour (or a brand palette) is judged against the frame's current backdrops, not only the card colours; gradient presets that would refuse are shown disabled with the reason.
- A frame with only padding/corners/aspect keeps the white chart ground in the export; the ground is dropped only when a background or the inset card paints behind the chart.
- **Counter:** `frame_changed` event added to `CHART_STYLE_EVENTS`, fired once per frame control change via `trackChartStyleEvent` in `chart.tsx`.
- **The floating Style panel:** `ChartConfigPanel` becomes a non-modal dialog (`role="dialog"`, no `aria-modal`) portaled to `document.body` via `createPortal` (portal for POSITION only — state stays in `ChartView`). Positioned fixed at the right edge (`lg:`+up: `right: 1rem; top: 5rem; width: 22rem; max-height: calc(100vh - 6rem)`, scrollable); below `lg` (under `1024px`) it becomes a bottom sheet (`fixed inset-x-0 bottom-0 max-h-[60vh] rounded-t-lg`). Opens/closes via the existing `openPanel: 'style' | 'story' | null` state; Escape and a Close button close and refocus the trigger; outside-click does NOT close (the reader is adjusting the chart behind it). One panel per page via `StylePanelOwnerProvider` context in `web/app/layout.tsx`: only the owning chart's panel is shown; opening Style on chart B closes it on chart A. The Story panel keeps its own under-chart slot.
- **The Frame tab (fourth tab):** renders in the panel alongside Grafiek/Kleuren/Lettertype. Controls: radio buttons for background kind (switches between solid-hex, gradient-presets + From/To hexes, or file-input), five more radiogroups for padding/corners/shadow/inset/aspect-ratio (all plain enums, never locked), a Reset button disabled when the frame is pristine and no image is uploaded. Image upload resets the file input after reading, so re-selecting the same file fires the change event.
- **Rollback:** remove the Frame tab render and the `ChartFrame` wrap from `chart.tsx` (roughly 15 lines). Stored frame keys in old charts are ignored: either remove the six keys from `chart-presentation.ts`'s `overridesSchema` (old values drop harmlessly on `sanitizeOverrides`), or leave them in the schema and the resolver will silently ignore any override value that arrives with no `ChartPresentation` field to hold it.

## Addendum 2026-09-09 (session 92, owner present) — Story mode, as built

**Superseded 2026-09-10 (session 94, owner ask) — the SELECTION mechanism only:** `buildStorySteps`
below (chronological overview/start/highest/lowest/latest/explore) is no longer called by `chart.tsx` —
replaced by `chart-insights.ts`'s `buildFindings` (real outlier/jump ranking, optionally AI-phrased). The
UI/chart-reaction mechanism described below (the trigger, the panel, the point ring, snapshot/restore,
locked controls) is UNCHANGED and still accurate — only the CONTENT feeding it changed. Full detail: ADR
[041](041-chart-insights.md). The trigger's shown text also changed ("Verhaal"/"Story mode" →
"Inzichten"/"Insights"), referenced below by its OLD text — historical, not current.

The owner asked for a "Story mode" button with a colourful border and a magic-wand icon, "to show something exciting will happen", and chose (from three options) a scroll story IN the chat: the chart stays, short steps scroll past it. Design: [superpowers/specs/2026-09-09-story-mode-and-embed-design.md](../superpowers/specs/2026-09-09-story-mode-and-embed-design.md) Part A.

- **Mechanism:** `web/lib/chart-story.ts` — `buildStorySteps(displayedSpec, lang)`, pure — turns the chart's own spec into steps (time series: overview · start · highest · lowest · latest · explore, duplicates dropped; several series: overview · one step per series up to five · explore; comparison: overview · highest · lowest · explore). A caption is a message template filled ONLY with spec strings (`periodLabel`, `formattedValue`, `unit`, a series label, `trendHeadline`); the whole-card digit scans run with the story open in both languages. "Highest/lowest" is a selection over the spec's values (the caption repeats that point's own `formattedValue`), the same class as highlight — **Assumption**, open-questions #221. Ties → the earliest period; null points skipped; a provisional point says so in words. A comparison whose bars are all equal, or a series with fewer than two plotted values, has no story and shows no trigger.
- **UI:** `ChartStoryTrigger` (the app's one gradient: a 2 px ring, lucide `WandSparkles`, "Verhaal" / "Story mode") as a row-mate of the Weergave tablist after Opmaak, offered only for chart forms with at least three steps and never with small multiples on; `ChartStoryPanel` (`chart-story.tsx`) in the Opmaak slot under the chart, outside the export container, one open panel at a time (`openPanel: 'style' | 'story' | null` in `ChartView`). Cards in a fixed-height scroll area, the card with the largest visible share of the scroll area (at least half) is the step (IntersectionObserver; buttons, dots and arrow keys drive the same index); Escape closes and refocuses. The ring is dashed so it can never be read as the hollow provisional marker, and in bar form a dashed outline around the bar plays the same role; a single-series time series in bar form therefore reacts too.
- **Chart reaction:** per step the reducer's `setHighlight`; a point step draws one ring outside the point's marker (`r + 5`, digit-free, no `data-point`) — R11's hollow ring stays visible inside it. Opening snapshots the reader's hidden/highlight/zoom state and shows the full chart; closing restores it in one new reducer action `setView`. A spec swap closes the story (owner E). While the story is open the legend, the Vanaf/Tot selectors and the small-multiples toggle are disabled with a readable reason ('Sluit het verhaal om dit te wijzigen.'), so the chart can never contradict the narration; choosing another chart form closes the story first and restores the reader's view. **Zoom is not used** (the spec's "zoom to the step's period window" was dropped: a one-period window is a degenerate chart, a wider window would be an invented range) — recorded as a follow-up.
- **Counted, not stored:** `story_open`, `story_step` on the anonymous counter (migration 028, file-only until applied). Nothing persisted; a download taken mid-story bakes the dashed story ring and the dimmed series (both are on screen — #46(c)) and never the panel text; decided: emphasis (a ring, dimming) needs no disclosure line because every value stays on the chart, the same call the session-89 highlight already made.
- **Rollback:** remove the trigger render in `chart.tsx`; nothing else depends on it.

**Battle-test correction (2026-09-10, Playwright on production):** the on-screen aspect ratio is NOT CSS `aspect-ratio` — that shrank the chart, overflowed the card and widened a phone's page in turn. `ChartFrame` measures its own width with a ResizeObserver and sets an explicit height = max(the chart's normal height + padding + inset, width ÷ ratio); the width is always the card's. When the ratio cannot be met at the card's width the screen shows the taller-than-ratio box and the export still honours the exact ratio by widening its canvas.

**Superseded 2026-09-10 (owner UI feedback, session 94) — the floating Style panel (this addendum's own "The floating Style panel" bullet above) is gone.** `ChartConfigPanel` no longer calls `createPortal`; it returns its content directly, rendered inline exactly where `<ChartConfigPanel>` is mounted in `chart.tsx` — the same "chart first, panel under it" slot the Story panel already used. `role` changed from `"dialog"` to `"region"` (there was never an outside-click close or a focus trap, so "dialog" semantics no longer fit an element that isn't an overlay). The `lg:`-gated floating-box-vs-bottom-sheet split is gone — one plain card (`rounded-lg border border-border bg-card shadow-sm`) renders identically at every viewport width. `aria-labelledby` still points at `copy.regionLabel`, so no accessible-name assumption elsewhere breaks. **Known gap carried over, not introduced by this change:** `copy.regionLabel` is one fixed string, not unique per chart instance — a page with several charts open at once (no `StylePanelOwnerProvider`) can show multiple regions with the identical accessible name in a screen reader's region list; a per-instance name is a future fix if it turns out to matter. Same session: the chat polish batch's "Chart in panel →" pill (top of the answer card, `float-right`) also moved, into the `CardFooter` action row as a plain `Button`, positioned immediately left of Copy — see the chat-polish plan/STATUS for that half.
