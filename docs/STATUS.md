# STATUS

> **Tracker, not a source of truth.** Scope and the gate are defined in [03-mvp-scope.md](03-mvp-scope.md), the benchmark in [02-user-scenarios.md](02-user-scenarios.md). This file only records progress against them. Update it whenever project state changes (see the definition of done in [CLAUDE.md](../CLAUDE.md)) — with **measured results only, never aspirational ones**.

**▶ TOP PRIORITY STACK — owner decision, session 23 (2026-07-05); this ORDER overrides the "decision-gated" framing below.** The owner set an explicit sequence; everything else queues behind it:
0. **✅ DONE + live-verified on production (2026-07-05): GDPR retention purge + self-service deletion (#14).** `npm run gdpr:purge` + a "Verwijder mijn vraaggeschiedenis" button, both **redact** the content (question + answer + the topic columns gone; the financial skeleton stays — a `credit_transactions` FK blocks a real row-DELETE). Reviewed + committed (`6aafb40`); 715 backend + 135 web tests green. The purge ran clean on prod (0 rows — nothing is 2 years old yet); ongoing retention = the monthly maintenance session. Owner confirmed the redact-and-retain posture. *Full detail: the "Last updated" entry below + the #14 section in [08-build-plan.md](08-build-plan.md). ([#59](open-questions.md) — the separate account-deletion FK tension — stays open; #14 does not touch it.)*
1. **On-demand CBS fetch when data is missing — WP16.** If a question needs data not in our DB, fetch the CBS table via API → verify → store → answer, with *"je tabel wordt voorbereid"* wait-messaging (email + dashboard "wordt aan gewerkt"). Was Phase 2-3, now **#1, the biggest build. Execute-ready brief in [08-build-plan.md](08-build-plan.md); Fable-authorized on the hard sub-parts (owner).** **Sub-part 1 (table discovery) — ✅ HERMETIC FOUNDATION BUILT (session 24, 2026-07-05), full gate green; design + the Fable judgment in ADR [025](decisions/025-cbs-catalog-table-discovery.md).** Shipped hermetically (€0 spend, no live DDL): a `cbs_catalog` mirror (migration 011) + Dutch FTS (verified on PGlite), `CbsSource.fetchCatalog()`, and `src/catalog/` = ingest + Stage-1 FTS recall + Stage-2 rerank (hard allowlist, `TABLE_RERANK_MODEL='claude-haiku-4-5'`) + the `findTable` router (confident/disclose/none). **The Fable answer (owner asked): topic→table does NOT earn Fable in v1** — a closed shortlist multiple-choice with a hard allowlist, safer structurally than by model size; one named constant with a recorded Haiku→Sonnet→Fable escalation ladder gated on a measured miss. **Sub-part 1 supervised live step — ✅ DONE (session 25, owner present):** migration 011 applied to prod (grants/RLS live-confirmed locked by inheritance — 0 anon/authenticated grants, RLS on, 0 policies); real `catalog:refresh` mirrored 4,858 rows; `tablefinder:record` run live (Haiku) → `DEFAULT_FIND_TABLE_CONFIG.highConfidence` **calibrated to 0.8** (confident floor 0.85 measured/stable, failure-safe = disclose) + an end-to-end replay test now on the gate (`tests/catalog/find-replay.test.ts`, 8/8 hermetic); 3 finder misses fixed (1 mislabel zonnepanelen→85004NED + 2 alias recall gaps) → 8/8. **Residual — ✅ CLOSED session 31 (WP27 stage A, [PR #17](https://github.com/Stefan7168/checkdecijfers/pull/17)):** the labelled set now has a disclose-expected case (`inkomen-vaag`) and the confident/disclose boundary is directly measured ([#104](open-questions.md)). **WP16 sub-part 2 — ✅ LIVE IN PRODUCTION (go-live session 28, 2026-07-06, owner-supervised).** On-demand CBS fetch works end to end; both paths verified live (delivered: consumentenvertrouwen→CBS `83694NED`, −24, full CC BY attribution, 100 credits kept; unanswerable+refund: bijstand → `85615NED`, ledger compensation +100). A go-live proxy bug was caught pre-flight + fixed (`42b275b`). **➡ Both go-live follow-ups are DONE: #115 shipped sessions 28–29; #111 CLOSED via WP27 (sessions 31–33, ADR [027](decisions/027-finder-shape-fit-gate.md)) — the bijstand question now ANSWERS live (stage-D acceptance test, session 33). See the "Last updated" entries for the records.** (Built session 27: 888 backend tests, benchmark 14/14 + 6/6 + 0 fabricated, 154 web tests.) Full build detail in the WP16 section of [08-build-plan.md](08-build-plan.md); residuals [#111](open-questions.md) (delivery coverage: national single-dimension tables answer, geo/sub-coordinate refuse-and-refund) + [#112](open-questions.md) (extended-vocab prompt variant unmeasured). The original seam notes, for the record: **Seam precision (session-26 review): that seam is TWO structurally different exits, not one** — the `unmatchedMeasureTerm` parse exit carries free text and fits `findTable(topic)` directly (the live seam); the `runQuery`→`buildQueryRefusal`/`table_not_registered` branch has no free-text phrase left and needs its own adapter (and is effectively unreachable with the current hand-curated registry) — exact shapes in the WP16 brief ([08-build-plan.md](08-build-plan.md)). Sub-part-2 design inputs logged as [#107](open-questions.md)–[#110](open-questions.md) (slice greediness, successor re-discovery, onboarding chip, data lifecycle/eviction — incl. the verified gap that `sync --all` is seed-bound, not registry-driven). **All four blocking decisions now LOCKED (session 26, ADR [026](decisions/026-on-demand-fetch-job-architecture.md)): Vercel Cron job engine, 100-credit pricing on the existing "heavy" tier, internal-consistency-only verification for v1, core-loop-only scope** (successor/chip/eviction deferred, except the one verified #110(a) registry-driven-refresh bug). **Ready for an execute build session — no further owner decisions needed to start the hermetic foundation** (mirrors sub-part 1: build + gate green first, live DDL + real spend in a separate supervised step after).
2. **New data sources beyond CBS** (likely API-based). **▶ ARCHITECTURE DESIGNED (session 30, 2026-07-08, owner-steered source-neutral/Nederland-scope): ADR [030](decisions/030-multi-source-architecture.md) + [audit dossier](session-briefs/2026-07-08-multi-source-dossier.md); build = WP30 in [08-build-plan.md](08-build-plan.md), after WP27; the concrete first source is an OPEN owner decision (WP30c).** Broadens the public claim from "official CBS cell" to "official sources" (CLAUDE.md needs a matching update) and likely triggers the ADR 001 Python split.
3. **Answer/question-quality optimization on the widened data base** — re-run the experience audit + ship the clarify-policy fix (WP26, now tier-3, ready but after the data work).

*Grounded in the session-23 experience audit (110 questions, live, measured): 40 answer / 32 clarification / 38 refusal; **20 of 56 answerable questions did not just answer**, and **all 14 out-of-coverage questions hit the wall** — the coverage wall (1/2) is a bigger lever than the clarify-policy (3) alone.*

**Current phase:** Phase 0 complete; **WP21 (CSV export #52) + WP22 (live-feedback smalls #95/#96a/#97a) + WP23 (display smalls #84/#86/#90/#91/#92/#71/#75) all shipped 2026-07-05, session 22 overnight**, **#14 GDPR retention + self-service deletion shipped 2026-07-05 (code-only/hermetic session, item 0 above)** on top of the #77 fix (session 21, ADR [023](decisions/023-explicit-date-range-parsing.md)), WP19+WP20 (session 20), WP18/WP17 and the live-verified end-user flow — **next follows the TOP PRIORITY STACK above (GDPR #14 done → WP16 → new sources → WP26), NOT the old "decision-gated" framing:** the session-22 wrap-up items (#98/#99 site shell, #96b, #97b, #53) and the **#65 error logging** brief ([08-build-plan.md](08-build-plan.md)) are real open items but no longer the front of the queue; anonymous-trial [#53] has its full brief (the session-19 owner-delegated order is complete), **plus a large decided-but-unbuilt backlog — everything below is owner-confirmed, no priority order implied:**
  - **Bug-shaped, from live testing:** explicit multi-period/multi-region auto-display ([#64](open-questions.md)); durable error logging beyond Vercel's short retention ([#65](open-questions.md))
  - **WP16, demand-driven table onboarding:** now owner-confirmed wanted, with its user-facing copy and "costs credits" pricing decided ([08-build-plan.md](08-build-plan.md), [#24](open-questions.md))
  - **Clarification UX — now designed as one WP (WP26), session 23, ADR [024](decisions/024-answer-first-defaults-and-clickable-options.md); awaits owner read-back of the safelist + a supervised build:** clickable pre-verified suggestion buttons ([#66](open-questions.md)/Mechanism A) + smart-default-with-escape-hatch on the narrow safe set instead of always clarifying ([#72](open-questions.md)/Mechanism B) — the two root causes of the "paid dead-end" (net 10 credits for nothing), zero prompt bytes, pricing deferred ([#101](open-questions.md))
  - **Dashboard polish — ✅ all four built in WP19 (session 20, entry below):** collapse a clarification round into one history item ([#67](open-questions.md)); live balance updates instead of only-on-reload ([#68](open-questions.md)); low-balance warning banner ([#69](open-questions.md)); a brief credits-economy explainer under the "Credits kopen" button ([#76](open-questions.md))
  - **"Next-level" UX ideas, all owner-approved:** clickable source-attribution drill-through ([#70](open-questions.md)); a visual "voorlopig" badge ([#71](open-questions.md)); follow-up suggestion chips under an answer ([#73](open-questions.md)); a live status panel for pending WP16 onboarding requests ([#74](open-questions.md)); example-question chips on an empty chat ([#75](open-questions.md))
  - **GDPR:** [#14](open-questions.md) question-log retention — **✅ built** (2026-07-05, code-only/hermetic session): 2-year purge CLI + self-service deletion, both via redaction (see item 0 above). Live purge run against production is still outstanding, owner-supervised, whenever a maintenance window opens.
  - **Second creative-brainstorm batch (2026-07-05, owner-filtered, rows [#78–#93](open-questions.md)):** top-5 = citation-copy button (#78), "bewijs dit cijfer" audit exposure (#79, brief first — merge with #70/#90), stat card + PNG download (#80), revision-risk gauge (#81, LARGE — needs revision statistics, brief with #88), pre-send cost transparency (#82); plus batch questions (#83), message-type styling (#84), honest waiting-steps (#85, real steps blocked on the ADR 018 streaming seam), CBS deep-link (#86), historical-range chip (#87, real R5 derivation), revision awareness (#88), "waarom dit antwoord" (#89), source chip (#90), number typography (#91), chart-footer rearrangement (#92); **three ideas explicitly REJECTED by the owner (#93: watch-list, pattern-encoding, comparison card)**. Owner authorized immediate execution alongside recording — **the three small top-5 items (#78/#80/#82) were built the same day as WP20 (session 20, entry below)**; CSV export [#52] stays next in the standing order after that
  
  **KvK is deliberately parked until the website is finished (owner decision 2026-07-04, [#54](open-questions.md)) — do not raise it as a next step.**
**⚡ NEXT-MODEL HANDOFF (updated 2026-07-11, session 36 close — read this first, then the block below):**

- **Repo state (3 sentences):** Production is LIVE (`ONBOARDING_ENABLED=1`, real credits move; the owner is the only user). **WP27 IS COMPLETE — all four stages: A+B+C merged+deployed session 32 ([PR #17](https://github.com/Stefan7168/checkdecijfers/pull/17), [PR #18](https://github.com/Stefan7168/checkdecijfers/pull/18), [PR #21](https://github.com/Stefan7168/checkdecijfers/pull/21)), and stage D EXECUTED owner-supervised session 33 ([PR #22](https://github.com/Stefan7168/checkdecijfers/pull/22)): migration 015 is LIVE on production (grants/RLS clean), the fit threshold is CALIBRATED (kept 0.8 — live record 6/6, every verdict 0.95), and the owner's live acceptance test PASSED — the #111 bijstand question ANSWERS ("… 390,2 x 1000", delivered in 88s, kick in 2s, credits kept); #111 is CLOSED (ADR [027](decisions/027-finder-shape-fit-gate.md) as-built stage D has every measurement).** **WP28 (Google SSO) is FULLY COMPLETE (session 34: [PR #23](https://github.com/Stefan7168/checkdecijfers/pull/23) merged + deployed `e8b09be`; all owner config done; #122 VERIFIED live, both halves — Google SSO works in production; RUNBOOK WP28 section carries the as-executed record incl. the two-Supabase-accounts quirk and Google's secret-shown-once gotcha)**; **WP29 (follow-up chips #73) is MERGED + DEPLOYED — LIVE (session 35, 2026-07-11: [PR #24](https://github.com/Stefan7168/checkdecijfers/pull/24), owner-approved in-chat merge `0c4b324`, gate + deploy green on main)**; `main` carries the remaining FROZEN design **WP30a/b** (multi-source waist; **WP30c first-source choice DEFERRED by the owner — CBS-first, see [#123](open-questions.md)**); the owner-flagged "390,2 x 1000" display ([#125](open-questions.md) part (a), decided "uitgerekend erbij") is **✅ MERGED + DEPLOYED — LIVE (session 36, 2026-07-11, [PR #25](https://github.com/Stefan7168/checkdecijfers/pull/25), merge `cb8f580`, main gate + deploy green): the interrupted WIP branch was RESUMED and finished per ADR [031](decisions/031-unit-expansion-derivation.md)**; Dependabot PRs [#19](https://github.com/Stefan7168/checkdecijfers/pull/19)/[#20](https://github.com/Stefan7168/checkdecijfers/pull/20) are OPEN, untouched — monthly-maintenance material; **the Vercel git-disconnect is VERIFIED DONE (PR #22 carried zero "Vercel" statuses — the ADR 018 d7 drift is healed)**; nothing is blocked on an owner decision except WP30c (the WP26 safelist was already read back + settled in session 23 — ADR [024](decisions/024-answer-first-defaults-and-clickable-options.md) status line; WP26 awaits only its supervised build, not a decision).
- **Build queue** (each = own branch + PR per #118; never push code to `main`):
  - [x] **WP27 — ✅ COMPLETE (sessions 31–33; PRs #17, #18, #21, #22).** The finder→chain→fit-gate machinery is live and PROVEN: question-aware rerank (stage A), candidate chain (stage B), Haiku measure-fit gate + A3 deterministic pre-checks (stage C), migration 015 + calibration + the passed live acceptance test (stage D). Record: ADR [027](decisions/027-finder-shape-fit-gate.md) (as-built A–D), RUNBOOK stage-D section, [#111](open-questions.md) CLOSED.
  - [x] **WP28 — ✅ MERGED + DEPLOYED (session 34, 2026-07-10, [PR #23](https://github.com/Stefan7168/checkdecijfers/pull/23) `e8b09be`, gate + deploy green, owner-present merge).** Google SSO per the brief (three files + 11 tests incl. the #113 cross-pin; callback/proxy/`src/` byte-untouched). **ALL owner steps ✅ DONE same day: Google Cloud config, Supabase provider config, and the [#122](open-questions.md) verification — BOTH halves PASSED live (same e-mail → linked to the existing user, no duplicate grant; fresh e-mail → one new user, one grant). WP28 IS FULLY COMPLETE** (as-executed record + gotchas: RUNBOOK WP28 section). Brief: [session-briefs/2026-07-08-google-sso-brief.md](session-briefs/2026-07-08-google-sso-brief.md)
  - [x] **WP29 — ✅ MERGED + DEPLOYED (session 35, 2026-07-11: [PR #24](https://github.com/Stefan7168/checkdecijfers/pull/24), owner-approved in-chat merge `0c4b324`, gate + deploy green on main — LIVE).** Follow-up chips (#73) per the frozen brief: `src/answer/respond/suggestions.ts` (4 deterministic generators, every chip `echoServability`-gated in-request, cap 3, zero LLM), `AnswerResponse.suggestions` (structural; R8 `text` byte-pinned), chat.tsx chips with the #75 fill-don't-send handler. Record: ADR [029](decisions/029-follow-up-suggestion-chips.md) as-built, [#73](open-questions.md) → built. Brief: [session-briefs/2026-07-08-follow-up-chips-brief.md](session-briefs/2026-07-08-follow-up-chips-brief.md)
  - [x] **#125a — ✅ BUILT (session 36, 2026-07-11) → PR from `wp125a-unit-expansion`; merged on the owner's in-chat approval after a green gate — [PR #25](https://github.com/Stefan7168/checkdecijfers/pull/25), merge `cb8f580`, main gate + deploy green — LIVE.** Registered unit-expansion derivation + one compose-level display splice (ADR [031](decisions/031-unit-expansion-derivation.md) as-built): factor-unit answers read `390,2 x 1000 (= 390.200)`. Zero prompt bytes, zero fixture re-records, no DDL; the 26-agent design review completed (0 confirmed), 2 extra fail-open belts hardened.
  - [ ] **WP30a/b** — THE NEXT BUILD; pre-build adversarial design review first (the WP27 pattern). ADR: [030](decisions/030-multi-source-architecture.md)
  - [ ] Smalls: #119 (job claim idempotency), #120 (GDPR onboarding data; chip `task_16f26281`), #116 (email chat-link) — briefs in [open-questions.md](open-questions.md)
- **Executor prompts: ALL executed (WP28 → session 34, WP29 → session 35 / [PR #24](https://github.com/Stefan7168/checkdecijfers/pull/24)).** No ready-made prompt for WP30a/b on purpose: it starts with a pre-build adversarial design review (the WP27 pattern) over ADR [030](decisions/030-multi-source-architecture.md) + the [multi-source dossier](session-briefs/2026-07-08-multi-source-dossier.md) — write the executor brief in that session, don't improvise one here. The [#125](open-questions.md) display fix's task chip (`task_e58effda`) is EXECUTED — session 36 carried its decision through the machine-switch kickoff and finished the WIP branch.

- **Verification block (run for EVERY change, from repo root; ALL must pass before a PR):**

  ```bash
  npm ci && (cd web && npm ci)
  npm run typecheck && (cd web && npm run typecheck)
  npx vitest run                                     # full backend suite
  (cd web && npx vitest run)                         # web suite
  npm run benchmark:run && npm run benchmark:score   # must print 14/14, 6/6, 0 fabricated, GATE VERDICT: PASS
  git push -u origin <branch> && gh pr create        # then gh pr checks <PR#> --watch → gate must pass
  ```
  Repo-access quirk: if git says "Repository not found", run `gh auth switch -u Stefan7168` (the repo-local credential helper is already configured).
  CI quirk (since 2026-07-08): a gate job that FAILS in seconds with **0 steps** and the annotation *"recent account payments have failed or your spending limit needs to be increased"* is the **GitHub Actions billing block**, not a code failure — owner fixes it under GitHub Settings → Billing & plans, then `gh run rerun <run-id>` (see RUNBOOK).

**▶ NEXT SESSION STARTS HERE (2026-07-11, session 36, owner present at close — first session back on the original machine after the switch): #125a (FACTOR-UNIT EXPANSION DISPLAY, "uitgerekend erbij") IS BUILT; the owner approved the merge in-chat and the session EXECUTED it on a green gate: **[PR #25](https://github.com/Stefan7168/checkdecijfers/pull/25) MERGED `cb8f580`, main gate + deploy GREEN — #125a IS LIVE IN PRODUCTION.** Local gate fully green pre-PR: typecheck ×2, backend 1029/1029, web 195/195, benchmark 14/14 + 6/6 + 0 fabricated GATE PASS. The completed 26-agent adversarial design review (the piece the interrupted WIP session never finished) returned 0 confirmed findings; its two strongest killed findings were hardened into extra splice belts anyway (misattribution + double-render, ADR 031 D4 as-built). Factor-unit answers now read "390,2 x 1000 (= 390.200)" / "8.204 (x 1 000 = 8.204.000)" — every expanded figure a registered derivation, zero prompt bytes, zero fixture re-records, no DDL. **WP30a WAS THEN ALSO BUILT AND MERGED the same session (the owner's "door met bouwen", then his in-chat "Groen licht" on the green gate): [PR #26](https://github.com/Stefan7168/checkdecijfers/pull/26), merge `7864271`, main gate + deploy GREEN — WP30a IS LIVE (display-only, byte-identical by design and by pin).** ALSO: **the owner FLIPPED THE REPO PUBLIC (2026-07-11, per [#126](open-questions.md)) — GitHub Actions minutes are now unlimited/free on standard runners; the 3,000-min budget pressure is structurally gone.** The pre-build design review had completed first (62 agents → ADR 030 amendments A1–A7); the post-build diff review (11 agents) found 1 confirmed blocker — a stale strict-toEqual test — fixed in-session. Local gate fully green pre-PR: typecheck ×2, backend 1044/1044, web 198/198, benchmark 14/14 + 6/6 + 0 fabricated GATE PASS (every fixture replays — zero prompt bytes). Byte-identity is the WP's hard claim: every display string identical for source=cbs AND absent source (A1), pinned by golden tests + the source-stripped R8 regression. NEXT: **WP30b (conformance harness + guide) is its own later session (A5); WP30c stays owner-deferred ([#123](open-questions.md)); migration 016 applying = owner-supervised with the `scripts/verify-audit-rows.ts` re-run (A1); #125 part (b) waits in the WP26 lane; smalls #119/#120/#116; Dependabot #19/#20 monthly.** **NEW OWNER REQUESTS (2026-07-11, end of session 36, all recorded + roadmap-slotted): [#128](open-questions.md) thumbs up/down feedback (+dialog on 👎 — small WP, best BEFORE WP26), [#129](open-questions.md) source-selection chips in the chat (the WP30-waist UI half — build WITH the first second source), and [#130](open-questions.md) WEB SEARCH as a combinable opt-in source — owner wants this FIRST in the source-expansion lane; it touches principles (a)/(b)/(c) at once, so it starts with its own pre-build ADR + owner interview (the WP16 pattern), never a direct build. PLUS [#131](open-questions.md): a MULTILINGUAL platform (EN/DE/ES/Mandarin/Hindi …) — recorded in three layers (UI i18n → non-Dutch input, measured → localized answers, the deep validator-vocabulary layer; EN pilot first). **ALL FOUR recommendations OWNER-CONFIRMED in-chat the same session ("De aanbevelingen: we kunnen alles vastleggen"): #128 before WP26; #129 with the first second source; #130 separated-web-section direction + native-web_search preference (scraping rejected; the ADR/interview still precedes the build); #131 layered, EN pilot.** #125 part (b) (mensen-vs-uitkeringen headline) waits in the WP26 lane; residual smalls #119/#120/#116 chip-able; Dependabot #19/#20 = monthly maintenance; NEW: [#127](open-questions.md) (prompt caching — owner asked twice, answered + deferred with a revisit trigger).** — **Older (2026-07-11, session 35 FINAL close + machine-switch handoff, owner present): WP29 (FOLLOW-UP CHIPS, #73) IS MERGED + DEPLOYED — LIVE IN PRODUCTION ([PR #24](https://github.com/Stefan7168/checkdecijfers/pull/24): merge `0c4b324`, main gate + deploy GREEN, merged branch deleted). THE NEXT BUILD IS #125 part (a) — the "390,2 x 1000" factor-unit display ("uitgerekend erbij": expanded figure alongside CBS's verbatim notation, `390,2 × 1.000 (= 390.200)`, via a registered unit-expansion derivation) — AND A WIP BUILD ALREADY EXISTS: a separate session started it, then stopped for a machine + Claude-account switch, leaving branch `wp125a-unit-expansion` (pushed: WIP `dcb544b` INCOMPLETE + untested + unreviewed, handoff `ec4e6a1`). ⚠ THIS IS NOT GREENFIELD — a fresh session on the new machine must `git fetch`, then **`git checkout wp125a-unit-expansion`** (⚠ the handoff brief AND ADR 031 exist ONLY on that branch, not on `main` — the links below resolve only once you're on the branch, or via `git show wp125a-unit-expansion:<path>`) and read [session-briefs/2026-07-11-125a-wip-handoff.md](session-briefs/2026-07-11-125a-wip-handoff.md) FIRST (it lists exactly what's done: ADR [031](decisions/031-unit-expansion-derivation.md) = the complete spec + types/derivation/run/prompt-filter/validator edits; and the SIX remaining pieces: `expand.ts` splice, `compose.ts` wiring, exhaustiveness/typecheck, all tests, benchmark (B6 + the #124 replay text shift), docs-to-built). ADR 031 explains the two load-bearing traps: EXACT integer arithmetic (`390.2*1000` is inexact in float) and the prompt-payload filter that keeps LLM fixtures byte-identical (no re-record, no spend). Resume-vs-redo is the next session's call — deleting `wp125a-unit-expansion` (local+remote) is the reversible undo, since ADR 031 + [#125](open-questions.md) hold all the context. Either way: finish under a proper branch + PR + owner review per #118 (it touches compose/validate — the most invariant-dense area), and re-run the pre-build adversarial design review (the WP27/WP29 pattern; the WIP session's review workflow never completed). AFTER #125: WP30a/b (pre-build adversarial design review first; WP30c owner-deferred, [#123](open-questions.md)); part (b) of #125 (mensen-vs-uitkeringen headline) → WP26 lane; residual smalls #119/#120/#116 chip-able; Dependabot #19/#20 = monthly maintenance; the public-repo-CI option is now [#126](open-questions.md).** — **Older (2026-07-11, session 35 mid-close, superseded the same day): WP29 was BUILT AND ON [PR #24](https://github.com/Stefan7168/checkdecijfers/pull/24) — NOT MERGED (the #118 rule: autonomous core-product change → branch + PR + owner review; no session merges it). ONE OWNER ACTION: review + merge PR #24 (full local gate green: typecheck ×2, backend 1000/1000, web 195/195, benchmark 14/14 + 6/6 + 0 fabricated GATE PASS; verify the CI gate on the PR is green before merging — the adversarial review's 3 heavyweight lenses came back clean and its one confirmed test-gap was closed + mutation-verified in-session). ALSO NEW: the owner flagged the live "390,2 x 1000" display (2026-07-11) → recorded as [#125](open-questions.md) with a design recommendation (registered unit-expansion derivation) + task chip `task_42577827` — a SMALL standalone WP after the PR-#24 merge; part (b) of #125 (mensen-vs-uitkeringen headline bridging) belongs to the WP26 answer-quality lane. AFTER THAT the next build is WP30a/b (multi-source waist — pre-build adversarial design review first, the WP27 pattern; WP30c first-source choice stays owner-deferred, [#123](open-questions.md)). Residual smalls #119/#120/#116 remain chip-able; Dependabot #19/#20 = monthly maintenance.** — **Older (2026-07-10, session 34 FINAL close, owner present): WP28 (GOOGLE SSO) IS FULLY COMPLETE — code merged + deployed ([PR #23](https://github.com/Stefan7168/checkdecijfers/pull/23), merge `e8b09be`, gate + deploy green), ALL owner config done the same day (Google Cloud + Supabase provider), and the #122 verification PASSED live on BOTH halves (same-e-mail → linked to the existing user, identities `[email, google]`, NO duplicate grant; fresh e-mail → exactly one new user + one +100 grant). "Doorgaan met Google" WORKS IN PRODUCTION. THE NEXT BUILD IS WP29 (follow-up chips — executor prompt above); WP30a/b after. Operational knowledge from this session lives in the RUNBOOK WP28 section: the TWO-SUPABASE-ACCOUNTS quirk (checkdecijfers = org "stefan"; the browser is often on the glaibaan account — silent wrong-org bounce; use incognito), Google's secret-shown-once gotcha + the "missing OAuth secret" 400 signature, and the consent-screen domain wish recorded on [#7](open-questions.md).** Built exactly per the frozen brief: the `signInWithGoogle` Server Action (the brief's literal code), the "Doorgaan met Google" button below a divider (shared `busy` state; Next's documented `unstable_rethrow` pattern so a NEXT_REDIRECT throw never renders as an inline error), and 11 new web tests incl. the #113 cross-pin (action `redirectTo` path ↔ `isPublicPath('/auth/callback')` proven consistent in one test). `web/app/auth/callback/route.ts`, `web/proxy.ts`, `web/lib/current-user.ts`, all of `src/` and every migration are **byte-untouched** (the brief's must-NOT-touch list held). Full verification block green locally before the PR. **Owner-present continuation (same day): PR #23 merged on the owner's in-chat "execute the next step" (gates 10m14s/9m32s pass, zero Vercel statuses), main gate + deploy green; then the Google Cloud config was driven in-browser (Chrome profile 'Personal', account verified as stefanpeek00@gmail.com on-page first): project `checkdecijfers`, Google Auth Platform wizard (External, owner ticked-approved the API-services policy), OAuth client `checkdecijfers-supabase` with redirect URI `https://vqvohfqapjfdpbojtezx.supabase.co/auth/v1/callback`, published to production (testing mode only admits listed test users). Remaining: RUNBOOK WP28 steps 2–3 — Supabase Google provider (Client ID in the RUNBOOK; secret read from the Google console, dashboards only) + the MANDATORY [#122](open-questions.md) linking verification (same-e-mail Google login → ONE user, ONE `signup_grant`; fresh e-mail → new user, one grant).** **One measured refinement (ADR 028 as-built): pre-configuration the button silently bounces back to `/login?error=auth`** — server-side `signInWithOAuth` only builds the authorize URL and cannot see a disabled provider; the inline Dutch error covers server-side failures only. **THE NEXT BUILD IS WP29 (follow-up chips — executor prompt above); WP30a/b after; Dependabot #19/#20 = monthly maintenance.** — **Older (2026-07-10, session 33 close, owner present): WP27 IS COMPLETE — stage D executed this session ([PR #22](https://github.com/Stefan7168/checkdecijfers/pull/22)): migration 015 live on prod (owner-confirmed, grants/RLS clean, dedupe index untouched), fit threshold CALIBRATED (kept 0.8 — live Haiku record 6/6, every verdict at 0.95, "calibrated not moved" per the finder precedent; hermetic fit-replay e2e now on the gate), and the OWNER'S LIVE ACCEPTANCE TEST PASSED: "Hoeveel mensen zaten er in 2023 in de bijstand?" ANSWERED in production — "Het totaal aantal bijstandsuitkeringen kwam in 2023 uit op 390,2 x 1000" (audit 240; chain recorded, fit gate accepted 37789ksz/D000203_2, kick in 2s, delivered in 88s, 100 credits kept, ledger-verified). #111 CLOSED. Vercel git-disconnect VERIFIED (zero "Vercel" statuses on PR #22). THE NEXT BUILD IS WP28 (Google SSO) or WP29 (follow-up chips) — both execute-ready, autonomous-safe, executor prompts above; WP30a/b after. Dependabot #19/#20 = monthly maintenance.** — **Older (2026-07-10, session 32 FINAL close, owner present): WP27 stages A + B + C are ALL MERGED + DEPLOYED ([PR #17](https://github.com/Stefan7168/checkdecijfers/pull/17) `478a852`, [PR #18](https://github.com/Stefan7168/checkdecijfers/pull/18) `d8e02b5`, [PR #21](https://github.com/Stefan7168/checkdecijfers/pull/21) `dab47fb` — every merge owner-approved in-chat, gate + deploy green per merge; CI on the #21 merge verified at close). THE NEXT SESSION IS WP27 STAGE D — the owner-present LIVE step (executor prompt above): migration 015 to prod, fit-fixture record + threshold calibration, then the live bijstand acceptance test. Its precondition (PR #21 merged, main green) is SATISFIED.** Stage C — the measure-fit gate — was BUILT + adversarially reviewed + #124-MEASURED this session. Stage C per the brief's pseudocode: `src/ingestion/onboarding-fit.ts` (Haiku closed-choice over the table's OWN measure codes + 'geen', hard allowlist, threshold 0.8 = documented pre-calibration placeholder), amendment A3's deterministic deliverability pre-checks BEFORE any LLM call (time-only dims; JJ codes on bare-year questions — the checks that route past the live #111 mis-pick shape), processOneRow routing resolved→resume / []→legacy / candidate loop (D2a/b/c), `setResolvedTable` (DB + in-memory; `fit_note` diagnostics only), `fitClient` wired in the cron route. **The #124 measurement (owner-approved, one Haiku call ≈ €0.01, `scripts/onboarding-delivery-record.ts`): the REAL delivery parse ANSWERS across 37789ksz's 18 same-tagged measures — "Totaal bijstandsuitkeringen was in 2023 390,2 (x 1000)", NO rule-4 clarification — pinned as a hermetic replay e2e on the CI gate.** Everything stays mechanically DORMANT in production until stage D applies migration 015 (pre-015 every row's chain reads back [] → legacy path → no fit calls, no spend). Verification on a clean tree: typecheck ×2, 982/982 backend, 182/182 web, benchmark 14/14 + 6/6 + 0 fabricated GATE PASS (an earlier concurrent run showed 6 skips + 1 web fail — reviewer-agent mutation testing sharing the working tree; clean rerun authoritative, CI re-proves). The 5-lens adversarial review returned 4 lenses CLEAN, 2 raw findings refuted, 1 CONFIRMED (the >= acceptance boundary at exactly the threshold was unpinned — mutation-verified by both skeptics) — fixed same session with a boundary test referencing the constant. **➡ NEXT: owner reviews + merges [PR #21](https://github.com/Stefan7168/checkdecijfers/pull/21) (verify the CI gate on it is green first), then a fresh OWNER-PRESENT session runs WP27 stage D via the stage-D executor prompt above (the LIVE step: migration 015 → fit-fixture record + calibration → the live bijstand acceptance test). Dependabot PRs [#19](https://github.com/Stefan7168/checkdecijfers/pull/19)/[#20](https://github.com/Stefan7168/checkdecijfers/pull/20) are open, untouched — monthly maintenance.** — **Older (2026-07-10, session 32 first half): stage B detail — the candidate chain (pick + sanitized alternatives, cap 3) through every carrier link incl. the constructing `onboarding-finder.ts`, migration 015 as a FILE ONLY (incl. `fit_note` per the stage-C "same migration" pin), deploy-order safety via a store-side column probe so the pre-015 production window cannot abort the money tx; its 5-lens review found 1 confirmed test-adequacy gap (the live respond.ts construction site had zero pipeline-level coverage) — fixed with a pipeline test that drives B15 through respondToQuestion with an injected finder.** — **Older (2026-07-09, session 31 continued-close): WP27 stage A (question-aware rerank, #111) BUILT + supervised-RECORDED + REVIEW-HARDENED as [PR #17](https://github.com/Stefan7168/checkdecijfers/pull/17), head `82633df`. The labelled set passed 11/11 on two consecutive record runs (threshold 0.8 held, confident floor 0.85; the #104 disclose boundary is now directly measured); the full local gate was re-verified at wrap-up (typecheck ×2, 945/945 backend, benchmark 14/14 + 6/6 + 0 fabricated GATE PASS, 180/180 web), and a 16-agent adversarial review of the PR (6 lenses × dual skeptics, 2026-07-09) found the THREE HEAVYWEIGHT LENSES CLEAN (money invariants, prompt/fixture byte-discipline, recall-quota math) — its 4 confirmed + 1 split findings were all doc/comment/test-only and are folded in as `82633df` (biggest catch: the stage-B brief omitted the one file that CONSTRUCTS candidateIds, onboarding-finder.ts — now named in the brief). ✅ THE BILLING BLOCK IS RESOLVED (2026-07-09, later the same day): the owner purchased a GitHub Actions plan (3,000 included min/month — the practical root cause was the included minutes being exhausted with the overage payment failing; the annotation's "payments/spending limit" wording obscures that) and **both PR-#17 gate runs then passed for real (7m42s + 9m34s) — the PR is FULLY CI-GREEN at head `82633df`**. ONE OWNER ACTION LEFT BEFORE THE NEXT BUILD: review + merge PR #17 (CI is green; the merge also lands the review-corrected stage-B brief on `main`, which the next build needs). Owner steer (2026-07-09, recorded): do NOT stack stage-B/C branches on the unmerged PR — build resumes only after the merge. THEN the next build = WP27 stage B (candidate chain + migration 015 as a file, hermetic — the stage-B executor prompt above is ready), then stage C (fit gate incl. amendment A3's deterministic deliverability pre-checks), then supervised stage D (the live bijstand acceptance test). The three owner-approved amendments A1–A3 + all measured evidence live in ADR [027](decisions/027-finder-shape-fit-gate.md) § Amendments; note the labelled bijstand-stock case pins CHAIN semantics (`chainContains: 37789ksz`, `notPick: 85615NED`) — measured: Haiku picks the undeliverable person-table first with 37789ksz in its alternatives, which is deliberately accepted because stage C's A3 pre-checks route past it (an information gap, not a model-tier gap — do NOT "fix" this by escalating the tier). WP28 (Google SSO) and WP29 (follow-up chips) remain independent, execute-ready alternatives; residual smalls #119/#120/#116 remain chip-able.** — **Older (2026-07-08, session-30 close):** PRs #13–#16 are MERGED + DEPLOYED (CI gate+deploy green on every merge commit — all session-30 fixes are LIVE, incl. the R8-trail heal). Owner answered the #111 tier question: Fable — the WP27 design is FROZEN (ADR [027](decisions/027-finder-shape-fit-gate.md) + [execute brief](session-briefs/2026-07-07-111-design.md), adversarially reviewed). Owner also requested Google SSO (2026-07-08) — designed same session as WP28 (ADR [028](decisions/028-google-sso.md) + a literal [execute brief](session-briefs/2026-07-08-google-sso-brief.md) written for a smaller executor; independent of WP27, autonomous-safe, per-#118 branch+PR). — Session-30 first half (2026-07-07, autonomous, Fable) reviewed the sessions-27–29 Opus work and shipped every fix as branch+PR per the [#118](open-questions.md) rule: **[PR #13](https://github.com/Stefan7168/checkdecijfers/pull/13)** (#112 — live turns load the onboarded vocab: a repeat question on an onboarded topic answers at 1 credit instead of re-triggering the 100-credit flow), **[PR #14](https://github.com/Stefan7168/checkdecijfers/pull/14)** (#115 residual — the definition expander + the duplicate-dashboard-row dedupe), **[PR #15](https://github.com/Stefan7168/checkdecijfers/pull/15)** (validator blind spots: negative cell values + long units — **merging this heals the live −24 answer's currently-FAILING R8 audit re-validation**), **[PR #16](https://github.com/Stefan7168/checkdecijfers/pull/16)** (exact-delta refund, queue-wedge isolation, kick↔route auth cross-pin, doc-truth fixes). Each PR is gate-green (backend suite + benchmark 14/14 + 6/6 + 0 fabricated + web + both typechecks); CI re-proves per PR. **Then: answer the #111 model-tier question** — the problem dossier is written ([session-briefs/2026-07-07-111-problem-dossier.md](session-briefs/2026-07-07-111-problem-dossier.md)); the design step deliberately NOT started per the owner's coordinate-first instruction. Full session detail in the newest "Last updated" entry below. — **Older (2026-07-06, after the go-live):** WP16 sub-part 2 is **LIVE in production** — `ONBOARDING_ENABLED=1` is set, on-demand CBS fetch works end to end, both paths verified (see the "Last updated" entry below; the session-27 dashboard incident stays fixed). **[#113](open-questions.md) was DECIDED (Stefan, session 27) + BUILT + verified live (session 28): kick-on-trigger, with Vercel Pro as the owner's kept fallback if it ever falls short.** Both staged steps are now DONE: **(1) build the kick-on-trigger — ✅ DONE (session 28, commit `c64e2a2`)**; **(2) the RUNBOOK supervised live step — ✅ DONE (session 28, owner present) — on-demand CBS fetch is LIVE in production**, both paths verified (delivered + refund), see the "Last updated" entry below. **➡ NEXT PRIORITY (owner-flagged at the go-live): the [#111](open-questions.md) bijstand finder stock-vs-flow fix** — the first real live question ("hoeveel mensen in de bijstand") dead-ended because the finder picked a flow table over a stock/count table, and the owner's binding verdict is that such simple questions MUST answer. A task is spawned (`task_7c181a8a`); concrete plan is in #111. **Alongside it, three more go-live follow-ups the owner raised:** [#115](open-questions.md) weak onboarded-answer quality (circular "Definitie:", jargon "gemiddelde saldo van de deelvragen" — visible on EVERY onboarded answer; task `task_eead9a19`), [#116](open-questions.md) the delivery email has no chat-link, [#117](open-questions.md) the app doesn't live-update when an answer lands. **✅ SEQUENCING DONE (session 29): [#115](open-questions.md) was built + live-verified FIRST (ALL levers a/b/c); the [#111](open-questions.md) finder stock-vs-flow design is now THE active next priority (a genuine design step, possibly Fable — coordinate the model tier with the owner).** **#115 lever (a) — the circular "Definitie: X" line — ✅ SHIPPED (session 28, commit `7de00b0`).** **#115 levers (b) real definitions + (c) plainer phrasing — ✅ DONE + LIVE-VERIFIED ON PROD (session 29):** CBS's real per-measure `Description` (its meaning + the −100..+100 scale) now renders as the "Definitie:" line on onboarded answers, via a dedicated nullable `canonical_measures.definition_text` (migration 014, applied to prod) with `buildDefinitionLine` as the single builder shared by compose + audit-reconstruct; the supervised live step ran end-to-end (migrate → deploy → rebaseline sync → `scripts/onboarding-reregister.ts` backfill → read-only compose verify), and a live-caught bug (the definition cleanup had dropped the scale by keeping only CBS's first block) was fixed (`776e645`). The verbose-inline-definition tidy-up (an "expander") is deferred to [#117](open-questions.md)/[#74](open-questions.md) — owner will review it later. Per the [#118](open-questions.md) decision, this in-session (owner-present) work went to `main`; autonomous chip runs on core/money-path now require branch+PR+review. Then the broader stack (new sources #2, WP26 answer-quality #3). — Older, still true: session 22 (the autonomous overnight run) is **complete** — queue fully executed, four green deploys, €0 live spend; the brief ([session-briefs/2026-07-06-overnight.md](session-briefs/2026-07-06-overnight.md)) is ticked and historical. **#14 GDPR is built, reviewed and committed (session 23, commit `6aafb40`); the one outstanding step is a live `--apply` purge on production, owner-supervised.** A fresh session follows the TOP PRIORITY STACK at the top (GDPR #14 done → WP16 → new sources → WP26); the site-shell decisions (#98/#99, WP24) and the #65 brief are open but NOT next. No autonomous queue is pending. **Session 23 (2026-07-05, owner present) added NO code** — after a full state review the owner chose to invest in answer quality, and this session **designed the #72+#66 "paid dead-end" fix as WP26** (ADR [024](decisions/024-answer-first-defaults-and-clickable-options.md); owner chose to build both mechanisms as one WP and to defer WP26 pricing → [#101](open-questions.md)). **Next on WP26:** owner read-back of the safelist (ADR 024 decision 3), then a supervised build — it touches the chat UI + a new `resolveClarificationOption` server action and may need one additive audit column (live DDL), so it is not an autonomous-safe slot.

**Last updated:** 2026-07-11 (session 36; autonomous start, owner joined mid-session — the first session on the original machine after the switch-back; the machine-switch kickoff prompt carried the #125a handoff) — **#125a FACTOR-UNIT EXPANSION DISPLAY MERGED + DEPLOYED — LIVE ([PR #25](https://github.com/Stefan7168/checkdecijfers/pull/25): both branch gate runs green, deploy correctly skipping on the branch; owner confirmed the merge in-chat after an in-flight-scope check; merge `cb8f580`; main run green — gate: success, deploy: success, 9m25s).** Machine notes: this machine's working tree had been emptied by the owner for the switch (every tracked file deleted, `.git` intact) — restored via `git stash push` (reversible; the RUNBOOK "Moving to a new machine" section now carries the switch-back recipe); local `main` fast-forwarded; the WIP branch REBASED onto origin/main (both touch STATUS — an unrebased PR would conflict). **The handoff's open resume-vs-redo call: RESUMED** — the WIP's derivation layer survived the completed design review intact, and the feared switch-exhaustiveness typecheck break did not exist (both typechecks were green on the raw WIP). Finished per ADR [031](decisions/031-unit-expansion-derivation.md): **(1)** `src/answer/compose/expand.ts` — the D4 post-validation splice (anchors on scanBody's cell-backed tokens; unit phrase via the validator's own `unitMaskPhrases` variants within the R10 window; bare form `390,2 x 1000 (= 390.200)` + in-parens form `8.204 (x 1 000 = 8.204.000)`; every belt fails OPEN and the spliced body is re-validated — any problem discards the splice whole); **(2)** wired ONCE in compose.ts `assemble()` so the LLM and template paths share the mechanism and the SPLICED body is what R8 stores; **(3)** the WIP's types/derivations/run/prompt-filter/validate layer kept as-is after review. **The re-run adversarial design review COMPLETED (26 agents: 6 mid-tier lenses × dual heavy-tier skeptics): 0 confirmed findings; the fixture-bytes lens AFFIRMED D3's byte-identity claim (every recorded LLM fixture replays — zero re-record, zero spend). Two killed-but-real tail risks were hardened anyway as ~5-line belts (+2 tests): misattribution (an anchor never claims a factor phrase with another numeric token in between — R10's shared window would otherwise let a record-less neighbour's phrase carry the WRONG expansion) and double-render (skip when the body already shows the expanded figure, which now VALIDATES since the record backs it). One factual error in the ADR corrected: its IEEE example was false (390.2×1000 IS exact in V8; measured sweep: 96/9,999 one-decimal values ×1000 are inexact, e.g. 16,1 → 16100.000000000002) — the exact-arithmetic design stands, now with a true example. The marking-line trade-off (CC BY line renders even when a belt skips the visible expansion — same pre-existing pattern as direction/first_last) is now EXPLICIT in the ADR.** Verification (measured, clean tree): `npm ci` ×2, typecheck ×2, backend **1029/1029** (29 new: exactness incl. the IEEE trap, eligibility refusals, runQuery pre-registration incl. per-cell series records, splice shapes + belts + occurrence claiming, R3 wrong-figure rejection, R8 old-row byte-stability incl. a simulated pre-#125a row; one contention flake en route — benchmark-charts + cli.test hit the local 30s parallel-load ceiling twice, solo-green in ~1.5s → ceiling raised to 60s per vitest.config's own documented precedent, lessons s36), web **195/195** (no web changes), benchmark **14/14 + 6/6 + 0 fabricated GATE VERDICT: PASS** (B6 legitimately gains the in-parens expansion + the CC BY marking line; the frozen key pins facts, not text — and the #124 delivery-replay assertions still hold). Also this session: the owner asked (twice) about the Anthropic console's prompt-caching nudge → **answered + deferred as [#127](open-questions.md)** (static prefixes below the ~1,024-token cacheable minimum; 5-min TTL vs owner-only traffic pays the +25% write premium for nothing; `cache_control` would re-key every recorded fixture; live spend is cents — revisit at real volume, matching 04-architecture's existing ~1,000-user scale trigger). Stale-doc fix in passing: 04-architecture's WP16 row still called #111 "active next priority" (closed via WP27 since session 33). **➡ EXECUTED: [PR #25](https://github.com/Stefan7168/checkdecijfers/pull/25) merged `cb8f580`, main gate + deploy GREEN — #125a IS LIVE. THEN: the WP30a pre-build design review ran to completion the same session (an internet interruption killed the first run silently — journal timestamp was the tell; the workflow RESUME mechanism recovered it with the finished agents replayed from cache) → ADR 030 Amendments A1–A7 + the frozen [WP30a executor brief](session-briefs/2026-07-11-wp30a-brief.md). THEN WP30a WAS BUILT from that brief (owner's "door met bouwen"), branch `wp30a-source-registry`: src/sources/registry.ts (pure leaf, A1 absent→cbs fallback), Attribution.source (runQuery writes cbs), EIGHT display sites consolidated byte-identically (attribution line, template null-reasons + A2 two-tier provisional map, chart null-note, citation, statline URL+label, stat-card footer + StatCardData.sourceLabel, refusals statusSuffixNl — the third copy the diff review surfaced), migration 016 as a FILE (source columns + prefix CHECK + widened platform check; code reads nothing — deploy-order-safe), benchmark key +source (15 clean insertions, zero reformat), tests: registry golden pins (cbs AND absent byte-equality), migration-016 (incl. proof the old platform check is really dropped), source-r8 A1 regression (source-stripped stored rows reconstruct, B6+B4). As-built deviations in ADR 030 (no adapterFactory in the client-bundled registry; chart spec carries no source field; license reads the stored field). Post-build diff review (11 agents, 3 lenses × dual skeptics): 1 confirmed blocker (stale strict-toEqual in web/lib/stat-card-data.test.ts missing sourceLabel — the targeted test round had missed exactly that file) fixed in-session + an A1 pin added. Verification (measured): npm ci ×2, typecheck ×2, backend 1044/1044, web 198/198, benchmark 14/14 + 6/6 + 0 fabricated GATE VERDICT: PASS. [PR #26](https://github.com/Stefan7168/checkdecijfers/pull/26) merged `7864271` on the owner's in-chat "Groen licht" after both branch gates passed; main gate + deploy GREEN — WP30a LIVE. The owner also flipped the repo PUBLIC this session (#126 executed) — Actions minutes now free/unlimited; ALSO OPEN: the owner activated [#126](open-questions.md) (temporarily public repo for free Actions minutes — the account sat at 2,709/3,000 min this cycle); the secret-history scan started (no .env ever committed; the .pem is Supabase's PUBLIC root CA cert) and is PARKED until the in-flight work completes, per the owner.** Lessons in [lessons-learned.md](lessons-learned.md) (session 36).

**Last updated:** 2026-07-11 (session 35 wrap-up continuation — cross-session handoff for the machine/account switch) — **WP29 is LIVE (see the entry below); this continuation only WIRES `main` to the interrupted #125a WIP so a fresh machine finds it.** A separate session (the #125a build) started the "uitgerekend erbij" factor-unit display, then stopped for the owner's machine + Claude-account switch and left a clean, pushed WIP: branch `wp125a-unit-expansion` (`dcb544b` WIP INCOMPLETE `[skip ci]` → `ec4e6a1` handoff `[skip ci]`), with **ADR [031](decisions/031-unit-expansion-derivation.md)** (complete authoritative spec) and a full handoff at **[session-briefs/2026-07-11-125a-wip-handoff.md](session-briefs/2026-07-11-125a-wip-handoff.md)**. That session deliberately did NOT touch STATUS/open-questions/build-plan ("another session does the prep"); this wrap-up does that prep — the NEXT-SESSION block + [#125](open-questions.md) now point at the WIP + brief so #125 is not redone greenfield, and the public-repo-CI side finding from that session is filed as [#126](open-questions.md). No code was changed here (this session owns only `src/answer/respond/*` from WP29, all merged); the `wp125a` branch was NOT touched (read-only inspection only). This docs-pointer commit is docs-only + `[skip ci]` (the Actions account is at ~90% of its 3,000 included minutes; nothing to prove — WP29's code state on `main` is already gate+deploy-green). **Two sessions shared one working directory this switch — lesson recorded.** — Earlier this session — **WP29 FOLLOW-UP CHIPS BUILT → [PR #24](https://github.com/Stefan7168/checkdecijfers/pull/24) (branch `wp29-follow-up-chips`, head `d589379` + this docs commit), awaiting owner review per #118.** Built literally per the frozen brief: **(1)** `src/answer/respond/suggestions.ts` — four deterministic generators in fixed priority (adjacent period → trend window → region variant → same-table topic); every candidate's `StructuredIntent` is dry-run through `echoServability` IN the same request before its chip may surface (the exact #73 deferral condition, now the R7-options pin); cap 3; fail-open to `[]` twice (module + call site — a chips hiccup can never cost the paid answer); the module needs zero db access because the dry-run IS the loadedness check (next-then-previous neighbor probe; 5-then-3-period trend window, gap-free by runQuery's completeness pass; trend skipped when the answer already IS a series). **(2)** `AnswerResponse.suggestions: string[]` — structural, assembled post-compose in `respondToIntent` (the ONE construction site both entry points share, so first-turn and clarification-reply answers get chips identically); the R8-audited `text` is byte-untouched, pinned by a golden-text test (sync date spliced from the structural attribution field — the one run-dependent token); additive for old audit rows (reconstruct checks only named fields). **(3)** chips in `web/components/chat.tsx` with the #75 styling and handler verbatim — click FILLS the input, never sends; no new charged entry point; `billing/`, prompts, schemas, fixtures, migrations all byte-untouched (PR diff proves). Two recorded micro-refinements inside the design (PR + ADR as-built): ranges say "tot en met" (the #75/policy.ts convention — kills the exclusive-"tot" re-parse ambiguity D3 accepts as residual), and chip copy always names the answered region(s) explicitly. Generator 4 never fires on the Phase-0 seed (every seeded table has exactly one canonical measure) — pinned by test with an injected sibling registry. **Verification (measured): npm ci ×2, typecheck ×2, backend 1000/1000 (12 new in tests/answer/suggestions.test.ts — real fixture db + real dry-run), web 195/195 (2 new chip tests), benchmark 14/14 + 6/6 + 0 fabricated GATE VERDICT: PASS. Adversarial review (5 lenses on the mid tier × dual refuting skeptics on the session model, 9 agents): R7-gating, R8/audit and money/entry-point lenses CLEAN; one generator finding refuted by both skeptics (sameTopic plural wording — unreachable on the seed); one CONFIRMED test-adequacy gap — no pin proved suggestions also ride the warn-and-serve STALE answer branch — closed same session with a mutation-verified test (the exact mutant `if (!staleness.stale)` now fails exactly that pin).** Browser-pane verification deliberately skipped: the chip surface sits behind authenticated chat (login a session cannot perform); DOM behavior (render, click-fills, never-submits) is pinned in jsdom instead. v2 seam recorded (#73, ADR 029): when WP26 Mechanism A ships, chips swap their click handler to the pre-resolved-intent path — generation + gating are already identical. **Owner joined mid-session and flagged the live bijstand answer's "390,2 x 1000" display → [#125](open-questions.md) (analysis + recommendation recorded: registered unit-expansion derivation for pure factor units; display-alias "duizend" would need an ADR overriding R10's verbatim rule; part (b) mensen-vs-uitkeringen bridging → WP26 lane) + task chip `task_42577827` — deliberately NOT folded into WP29 (its brief freezes compose/validate).** **FINAL continuation (owner present, same day): PR #24 MERGED + DEPLOYED — WP29 IS LIVE.** Both branch gate runs passed full-length (9m26s / 9m40s — real runs, not the billing-block signature; deploy jobs correctly "skipping" on the branch); the owner approved the merge in-chat ("merge jij maar") → merge `0c4b324` (merge-commit style, matching #21–#23), **main gate + deploy both GREEN**, merged local+remote branch deleted. Also this continuation: the owner chose the [#125](open-questions.md) display convention from the presented options — **"uitgerekend erbij"** (expanded figure alongside CBS's verbatim notation via a registered unit-expansion derivation; "390,2 duizend" rewording explicitly rejected, so NO R10 override needed) — recorded on #125 and baked into the refreshed task chip `task_e58effda` (supersedes `task_42577827`; design hint included: hang the display line off the STORED DerivationRecord so old audit rows stay byte-stable in reconstruct). **➡ NEXT (fresh chat, owner-announced): build #125 via the chip or its prompt; then WP30a/b.** Lessons in [lessons-learned.md](lessons-learned.md) (session 35).

**Last updated:** 2026-07-10 (session 34; built autonomous, finished owner-present) — **WP28 GOOGLE SSO BUILT → [PR #23](https://github.com/Stefan7168/checkdecijfers/pull/23) (branch `wp28-google-sso`); merged + config + verification all landed later the SAME session — read through to the FINAL continuation at the end of this entry.** The brief was followed literally: **(1)** `web/app/login/actions.ts` gained `signInWithGoogle` (the brief's verbatim code — Supabase `signInWithOAuth` for the provider URL, `redirect()` to it; any error returns the inline Dutch copy); **(2)** `web/app/login/login-form.tsx` gained the divider + secondary "Doorgaan met Google" button — one shared `busy` state across both doors, and the catch block starts with `unstable_rethrow(err)` (the Next-16 documented pattern, read from `node_modules/next/dist/docs` per web/AGENTS.md) so framework control-flow throws reach Next, not the error slot; **(3)** two new test files (11 tests): action tests stub the Supabase client at the module seam but use the REAL `redirect()` (asserting the thrown NEXT_REDIRECT digest carries exactly the provider URL), the Google-error→Dutch-copy path, an independence pin (broken Google leaves the magic link working), form tests (both buttons, inline Google error, magic-link flow untouched, shared-busy pin), and the **#113 cross-pin** (the action's `redirectTo` resolves to `/auth/callback` AND `isPublicPath('/auth/callback')` is true — in ONE test). Must-NOT-touch held: callback route, proxy, current-user, `src/`, migrations all byte-identical. **Dev-server round trip verified in the browser** (login page renders both doors; clicking Google POSTs the action → 303 to the Supabase authorize URL), which surfaced the one measured refinement: **server-side `signInWithOAuth` is a URL builder — a disabled provider does NOT produce the inline error; pre-configuration the round trip bounces silently to `/login?error=auth`** (callback's existing no-code path). Fail-soft holds; documented in ADR 028 as-built + the new RUNBOOK WP28 owner-steps section (Google Cloud + Supabase config, then the mandatory #122 one-account/one-grant live check). Docs updated same PR: ADR 028, RUNBOOK, 04-architecture (new Supabase-Auth row), 06-roadmap, 08-build-plan, open-questions #122, this file. Verification block before the PR: `npm ci` ×2, typecheck ×2, backend suite, web suite (193/193, 11 new), benchmark 14/14 + 6/6 + 0 fabricated GATE PASS. **CONTINUED same session (owner present, "let's execute the next step"): PR #23 MERGED (both gate runs full-length green 10m14s/9m32s, zero "Vercel" statuses; merge `e8b09be`; main gate + deploy green — WP28 is LIVE, button inert until config) and RUNBOOK §4 STEP 1 EXECUTED in-browser (Chrome profile 'Personal', signed-in account verified on-page as stefanpeek00@gmail.com before any click): Google Cloud project `checkdecijfers` → Google Auth Platform wizard (app checkdecijfers, External, support/contact gmail; the API-services user-data policy tick was owner-approved in-chat) → OAuth client `checkdecijfers-supabase` (Web application, redirect URI `https://vqvohfqapjfdpbojtezx.supabase.co/auth/v1/callback`) → PUBLISHED to production (required: testing mode only admits listed test users; basic scopes need no verification review). Client ID recorded in the RUNBOOK; the secret stays in the Google console. STEPS 2–3 PAUSED on a discovered account quirk, now recorded in the RUNBOOK: the owner has TWO Supabase accounts — checkdecijfers lives in the org-"stefan" account (the MCP's auth), but the browser session belonged to the glaibaan-org account (parallel GlaiBaan work; logging out would disturb that session). The wrong-account dashboard silently bounces to the glaibaan org — check the breadcrumb org name first; use an incognito window to avoid touching the parallel session.** **FINAL continuation (owner present): steps 2–3 EXECUTED same day → WP28 FULLY COMPLETE.** The Supabase step ran in an incognito window on the org-"stefan" account (the two-accounts quirk held exactly as recorded). First attempt hit `{"code":400,...,"msg":"Unsupported provider: missing OAuth secret"}` — root cause: Google's console shows a client secret ONLY at creation (never again), and the original was never captured; fixed with the console's own rotation ("+ Add secret" → copy icon → clipboard-only paste into Supabase; old secret disabled after verification, per the console's one-secret warning). **The #122 verification then PASSED on both halves, measured read-only before and after each login: (a) Google login with the existing magic-link e-mail → user count unchanged (3), identities `[email]` → `[email, google]`, grants unchanged (3) — NO duplicate; (b) fresh-e-mail Google login → exactly ONE new user (google-only) + exactly ONE +100 grant, same-instant trigger. ADR 028 D2 is measured fact; #122 RESOLVED.** Owner wish recorded on [#7](open-questions.md): the Google consent screen shows the raw Supabase project domain — configure a Supabase custom auth domain when the product domain lands. **➡ NEXT: the next build is WP29 (executor prompt above); WP30a/b after; residual smalls #119/#120/#116 chip-able.** Lessons in [lessons-learned.md](lessons-learned.md) (session 34).

**Last updated:** 2026-07-10 (session 33, owner present) — **WP27 STAGE D EXECUTED — WP27 COMPLETE, #111 CLOSED ([PR #22](https://github.com/Stefan7168/checkdecijfers/pull/22)).** The owner-supervised live step ran start to finish, every live action owner-confirmed first: **(1)** migration 015 applied to prod (`npm run db:migrate`), per-migration check clean (0 anon/authenticated grants, RLS on, three columns correct, every index incl. the dedupe partial-unique untouched; the pre-015 legacy row reads back `[]` — the D2c path live-confirmed); **(2)** `benchmark/measurefit-labelled-set.json` seeded per the brief (6 cases, 3 accept / 3 'geen': both bijstand tables + 4 new schema-only fixtures 80416ned/85554NED/84826NED/83163NED, labels verified from live CBS measure lists) + `scripts/measurefit-eval.ts` (live/record/replay, mirrors tablefinder-eval) + `tests/ingestion/fit-replay.test.ts` on the gate; **live record 6/6 correct, EVERY verdict at 0.95** — threshold KEPT at 0.8, now CALIBRATED (floor 0.95 uniform, margin 0.15, wrong-code ceiling unmeasured because zero wrong picks — raising would be a guess; the session-25 "calibrated, not moved" precedent); **(3)** pre-flight before the paid step (cron route → 401 fail-closed; dedupe state clean; balance sufficient — a mid-check surprise turned out to be the owner buying a 500-credit pack minutes earlier: verified from the ledger instead of asserted); **(4) THE ACCEPTANCE TEST PASSED LIVE**: ack + 100-credit debit → kick claimed in 2s → chain `["37789ksz","85585NED","85692NED"]` recorded → fit gate accepted candidate 1 (`fit_note` = D000203_2 + sound stock-vs-flow reading, exactly the calibration measurement) → ingest → **delivered in 88s**: *"Het totaal aantal bijstandsuitkeringen kwam in 2023 uit op 390,2 x 1000"* (audit 240, definition + CC BY attribution, measure named verbatim per A1), credits kept (ledger-verified, no refund); **(5)** docs: ADR 027 as-built stage D, RUNBOOK stage-D section (+ the committed `scripts/force-ipv4.mjs` standing rule — the queued lessons-learned promotion landed), #111 closed / #104 n=1 live-mirror data point / #124 live-confirmed, this entry. **Also verified: the Vercel git-disconnect worked — PR #22 carries zero "Vercel" statuses.** Gate on the PR: typecheck ×2, 962/962 backend (incl. 6 new fit-replay), benchmark 14/14 + 6/6 + 0 fabricated GATE PASS, 182/182 web; CI gate green on the branch. **✅ PR #22 MERGED same session on the owner's delegated in-chat approval ("ik volg jouw wijsheid") after both gates passed — merge `509b093`, gate + deploy green on main verified at close, merged local branch deleted. ➡ NEXT: the next build is WP28 (Google SSO) or WP29 (follow-up chips) — both autonomous-safe, executor prompts in the handoff block; WP30a/b after.** Lessons in [lessons-learned.md](lessons-learned.md) (session 33).

**Last updated:** 2026-07-10 (close) — **SESSION 32 FINAL CLOSE (owner present): PR #21 (stage C) MERGED + DEPLOYED on the owner's in-chat approval (merge `dab47fb`) — WP27 stages A+B+C are ALL LIVE (dormant until 015); only the owner-present stage D remains, its executor prompt is ready in the handoff block and its precondition is satisfied.** Also this close: the **Vercel GitHub integration drift** (against ADR 018 d7 — the integration appeared on the repo and posted failing "Vercel" statuses on every PR/commit, incl. green-deployed main commits; diagnosed as integration-wide, NOT change-related, by checking sibling Dependabot PRs + main's commit status) was flagged to the owner, who initiated the dashboard disconnect at close ("Remove Git Connection" — project settings/env preserved; the CI deploy job uses VERCEL_TOKEN+CLI and is unaffected). Verify on the next PR: no "Vercel" statuses. Wrap-up ran the full ritual (lessons ×3 entries this session, memory, full doc set, stale-doc sweep, clean state, CI green per merge). **➡ NEXT: a fresh OWNER-PRESENT session runs WP27 stage D via the executor prompt in the handoff block.** Lessons in [lessons-learned.md](lessons-learned.md) (session 32 + continued).

**Last updated:** 2026-07-10 (later) — **SESSION 32 CONTINUED (owner present): PR #18 (stage B) MERGED + DEPLOYED on the owner's in-chat approval (merge `d8e02b5`, gate + deploy green); WP27 stage C BUILT + #124-MEASURED the same session → [PR #21](https://github.com/Stefan7168/checkdecijfers/pull/21), awaiting owner review; stage D executor prompt ready in the handoff block.** Stage C = the fit gate per the brief's pseudocode (see the NEXT block above for the full inventory). Two measured facts this half-session: **(1) #124 does NOT materialize** — the real recorded Haiku delivery parse answers "Hoeveel mensen zaten er in 2023 in de bijstand?" across all 18 topic-tagged 37789ksz measures with "Totaal bijstandsuitkeringen … 390,2 (x 1000)", no rule-4 clarification (owner-approved ~cent record via the committed `scripts/onboarding-delivery-record.ts`; pinned as a hermetic replay e2e); **(2) the adversarial review's one confirmed finding** (dual-skeptic mutation-verified): the inclusive `>=` acceptance boundary was unpinned at exactly the threshold — closed with a boundary test that references `DEFAULT_MEASURE_FIT_CONFIG.acceptThreshold` so stage D's recalibration keeps the pin. Process notes: the first verification run overlapped the review agents' mutation testing in the shared working tree (6 phantom skips + 1 phantom web fail — clean rerun 982/982 + 182/182 authoritative; lesson recorded: isolate mutation-testing reviewers in worktrees); production dormancy of the whole chain until stage D is mechanical (pre-015 chains read back []). **➡ NEXT: owner reviews + merges [PR #21](https://github.com/Stefan7168/checkdecijfers/pull/21), then a fresh owner-present session runs stage D (the live step) via the executor prompt above.** Lessons in [lessons-learned.md](lessons-learned.md) (session 32).

**Last updated:** 2026-07-10 — **SESSION 32 (owner present): PR #17 MERGED + DEPLOYED on the owner's in-chat approval (merge-commit method matching #13–#16, merge `478a852`, gate + deploy verified green on main — stage A is LIVE); WP27 stage B BUILT the same session → [PR #18](https://github.com/Stefan7168/checkdecijfers/pull/18), awaiting owner review.** Stage B per the brief's letter: `candidateIds` threaded through the full carrier chain (the constructing link `onboarding-finder.ts` first — the PR-#17 review catch — then routing → parse outcome → envelope, both respond.ts sites → web action → trigger → store `createPendingRequest` + `fromRow`; the field is REQUIRED on every carrier so the compiler enforces each hop), migration 015 as a FILE ONLY (⚠ never run against production — stage D applies it; includes `fit_note text` now because the brief's stage C pins it to the "same migration", avoiding a later edit of a merged migration). **One necessary addition beyond the letter, flagged on the PR for owner review: deploy-order safety.** Merging stage B deploys code while production still runs the pre-015 schema (015 waits for stage D); an INSERT naming the missing column would abort the whole money transaction. The store now probes `pg_attribute` for `candidate_ids` before naming it (a SELECT cannot abort the tx) and falls back to the legacy INSERT — rows created in that window read back as `[]` = exactly the legacy no-fit-gate path (ADR 027 D2c). This delivers the brief's own "either deploy order is safe" claim (false without it) and is pinned by drop-the-columns tests at store and trigger level. Invariants diff-proven: `src/billing/` untouched (ledger primitives byte-identical), zero prompt/fixture bytes, `table_id` never mutated. Verification: full STATUS block green (npm ci ×2, typecheck ×2, 954/954 backend, 181/181 web, benchmark 14/14 + 6/6 + 0 fabricated GATE PASS). Pre-PR **5-lens adversarial review** (money / carrier-chain / deploy-order / byte-discipline / test-adequacy; mid-tier legwork per the cost rule, dual refute-by-default skeptics, 7 agents): 4 lenses ZERO findings; 1 CONFIRMED (both skeptics independently mutation-verified it): the live respond.ts construction site had no pipeline-level coverage — corrupting its `candidateIds` to `[]` passed all 954 tests + typecheck. Fixed in-session: a pipeline test drives B15 (*"Hoeveel mensen zitten in de bijstand?"* — literally the #111 question) through `respondToQuestion` with an injected finder and asserts the envelope verbatim; the mutation now fails it (verified, then reverted). One process slip caught before CI could redden: the first commit used `commit -am`, which silently missed the two UNTRACKED files (migration 015 + its test) — fixed as its own commit; lesson recorded. **➡ NEXT: owner reviews + merges [PR #18](https://github.com/Stefan7168/checkdecijfers/pull/18) (verify the CI gate on it is green first), then WP27 stage C via the executor prompt above.** Lessons in [lessons-learned.md](lessons-learned.md) (session 32).

**Last updated:** 2026-07-09 (later) — **SESSION 31 FINAL CLOSE (owner present): billing RESOLVED via Actions-plan purchase → PR #17 FULLY CI-GREEN (both gates passed: 7m42s + 9m34s, head `82633df`); only the owner's review + merge remains.** Practical root cause of the multi-day block: the included Actions minutes were exhausted on this private repo while the overage payment kept failing — the job annotation's "payments have failed or spending limit" wording never names the minutes-exhaustion case, so the RUNBOOK entry now does. The duration-signal verification protocol (real gate = minutes, blocked = seconds/0 steps) confirmed the fix on the first rerun. Also clarified for the owner (worried about e-mail/credit burn): the "failed run" e-mails were GitHub's own notifications from manual check reruns — no recurring process exists, nothing auto-retries, and CI reruns consume only Actions minutes (blocked runs ≈ 0 min), never Anthropic credits or app credits; usage stood at ~2,000/3,000 min on purchase day, a full gate run costs ~8–10 min, so the monthly maintenance session should watch the minutes meter. **➡ NEXT: owner reviews + merges [PR #17](https://github.com/Stefan7168/checkdecijfers/pull/17) (a review summary is posted ON the PR), then a fresh session builds WP27 stage B via the executor prompt above.**

**Last updated:** 2026-07-09 — **SESSION 31 CONTINUED (owner present): billing-fix attempt did NOT land; owner steer: no stacked builds on dead CI; PR #17 adversarially REVIEWED (clean on the heavy lenses) and review-hardened to head `82633df`.** The owner reported the GitHub billing fixed, but reruns of PR #17's gate (attempts 2+3) failed identically in seconds — 0 steps, same billing annotation — so the fix has not landed (candidates: wrong account — repo owner is Stefan7168; failed payment not explicitly retried; Actions spending limit $0; RUNBOOK entry updated with this verification protocol). The owner explicitly REJECTED stacking stage-B/C branches on the unmerged PR ("this will mess up the project") — recorded as a steer: build resumes only after #17 merges. The remaining Fable budget went to a **16-agent adversarial review of PR #17** (6 lenses, mid-tier legwork per the cost rule, dual refute-by-default skeptics per finding, 0 dead agents): the money-invariant, prompt/fixture-byte-discipline and recall-quota lenses returned ZERO findings — the merge-critical surfaces held. 4 confirmed + 1 split findings, ALL doc/comment/test-only, folded in as `82633df` (typecheck + 192/192 affected tests re-verified; no product-code behavior change, so the recorded fixtures and the gate verdict stay valid): **(HIGH)** the stage-B brief's carrier chain omitted `src/ingestion/onboarding-finder.ts` — the ONE file that CONSTRUCTS `candidateIds`; an executor following it literally would ship an always-empty candidate list that typechecks (the brief now names the link + the construction rule); **(MEDIUM)** the schema-only `85615NED` fixture reports the real live $count (181,440) with zero observation pages — fine for the metadata-only fit gate, but ingesting it dies with a misleading "sync fetched 0 rows" refusal (now documented at fixture-source consumer, capture script and brief); **(LOW ×2)** brief stage-C pseudocode uses DB snake_case vs the TS camelCase accessors (executor note added), and the `--catalog-add` comment over-claimed "byte-identical" where the merge normalizes file formatting (comment now tells reviewers to verify semantically); **(split)** the replay test's hardcoded 0.8 floor now references `DEFAULT_FIND_TABLE_CONFIG.highConfidence` so a future recalibration can't desync test and routing. **➡ NEXT: owner lands the billing fix (RUNBOOK protocol) → rerun checks → review/merge PR #17 (`82633df`) → WP27 stage B via the executor prompt above.** Lessons in [lessons-learned.md](lessons-learned.md) (session 31 continued).

**Last updated:** 2026-07-08 — **SESSION 31 (owner present): WP27 stage A BUILT + supervised record DONE → [PR #17](https://github.com/Stefan7168/checkdecijfers/pull/17); three measured brief amendments owner-approved; CI red from GitHub billing, NOT code.** Pre-build verification of the frozen brief against live CBS (free metadata reads + a hermetic PGlite experiment ingesting the real 4,858-row catalog with the repo's own code) overturned three brief assumptions; each was put to the owner with evidence and approved in-session (full record: ADR [027](decisions/027-finder-shape-fit-gate.md) § Amendments): **A1** — the bijstand-stock target is `37789ksz` ("Sociale zekerheid; kerncijfers"): every person-level stock table is v1-undeliverable (characteristic dims ⇒ the vocabulary registers zero measures — the ACTUAL cause of the live bijstand dead-end — and month/quarter-only ⇒ `requireGrain('JJ')` honestly refuses a year question); `37789ksz` counts UITKERINGEN, not persons (owner accepted; the answer names CBS's measure verbatim). **A2** — Regulier-first recall quotas (20 current + 4 historic, `RECALL_LIMIT` 24): measured, the raw "bijstand" top-20 held 14 discontinued tables and `37789ksz` sat at position 51 — Stage 2 can never rank what Stage 1 never shows. **A3** (lands in stage C) — deterministic deliverability pre-checks before the Haiku measure-fit (time-only dims; bare-year ⇒ JJ codes), because a measure-honest fit gate would ACCEPT the undeliverable person-tables and refund without trying candidate 2. **Stage A shipped ([PR #17](https://github.com/Stefan7168/checkdecijfers/pull/17)):** question-aware rerank (prompt v2 — full question + topic; `FindTableQuery` threaded through `findTable`/`RerankFn`/`TableFinder`; intent prompt byte-untouched), confident outcome now carries allowlist-sanitized `alternativeIds` (stage B's chain source), Regulier-first recall, bijstand fixture family captured (`37789ksz` full observations; `85615NED` schema + real $count only; Regulier distractor rows so the hermetic shortlist mirrors live competition), labelled set 8→11 cases. **Supervised record (owner present, ~cents): 11/11, stable across two runs; 0.8 threshold held (confident floor 0.85).** Two measured findings folded in mid-record: a **vague-question honesty rule** (prompt v2 addition: a bare topic-word question scored 0.85 on a six-way-ambiguous income shortlist → now discloses; the disclose case closes #104) and **chain semantics** for bijstand-stock (`chainContains: 37789ksz` + `notPick: 85615NED` — Haiku correctly reads the stock shape but picks person-table `85585NED` first because deliverability is invisible in title/summary; stage C's A3 pre-checks route past it — an information gap, no tier escalation would help). **Wrap-up re-verified the full local gate: typecheck ×2, 945/945 backend, benchmark GATE PASS, 180/180 web. CI is red SOLELY from the GitHub Actions account billing block (0-step jobs + billing annotation; account-wide — also hit the owner's other repo). ➡ NEXT: owner fixes billing + reruns checks + reviews/merges PR #17; then WP27 stage B → C (hermetic, own branch+PR each) → supervised stage D.** Lessons in [lessons-learned.md](lessons-learned.md) (session 31).

**Last updated:** 2026-07-08 — **SESSION 30 CONTINUED (owner present): PRs #13–#16 MERGED + DEPLOYED; #111 tier answered (Fable) → WP27 design FROZEN; Google SSO requested → WP28 designed.** All four session-30 fix PRs merged with the owner present (CI gate + deploy green on each merge commit — every fix is LIVE, including PR #15's retroactive heal of the live −24 answer's R8 re-validation). **WP27 (#111):** the owner's fresh Fable authorization was used for the design step — question-aware rerank (prompt v2) + an in-job, metadata-only measure-fit gate (closed choice over the candidate table's own measure list + 'geen', hard allowlist, Haiku with the ADR-025 escalation ladder) + try-next-candidate via `candidate_ids` (migration 015 sketch), measurement-first (bijstand stock/flow + disclose labelled cases specified before code). Decisions + rejected alternatives in ADR 027; literal staged brief in session-briefs/2026-07-07-111-design.md; 3-lens adversarial design review run (mid tier) with findings triaged into the frozen docs. **WP28 (Google SSO, owner request 2026-07-08):** Supabase built-in provider, server-initiated flow reusing the EXISTING provider-agnostic `/auth/callback` (zero changes there, zero proxy changes, zero `src/` changes; signup-grant trigger already idempotent per user); one-verified-e-mail-one-account linking policy recorded as ASSUMPTION #122 — must be verified empirically in the supervised step, it is money-relevant (a wrong assumption = duplicate 100-credit grants); config lives in Google/Supabase dashboards (owner RUNBOOK steps in the brief §4), fail-soft until configured. Docs: ADR 028 + an execute brief deliberately written for a smaller executor model + WP27/WP28 rows in 08-build-plan + roadmap Phase-1 line + open-questions #122. **Also designed same session (owner request): WP29 follow-up suggestion chips (#73) — ADR 029 + execute brief; deterministic, servability-gated, zero prompt bytes, v1 fill-don’t-send.** **➡ NEXT: build WP27, WP28 and/or WP29 (all independent, all execute-ready; branch+PR per #118); WP30 (multi-source waist) is designed and queued AFTER WP27 — its WP30c stage needs the owner to pick the first source.**

**Last updated:** 2026-07-07 — **SESSION 30 (autonomous, Fable): adversarial review of the sessions-27–29 Opus work + four fix PRs AWAITING OWNER REVIEW (the first #118-rule branch+PR session — nothing merged to main by this session except docs).** The owner asked for a review of the recent Opus WPs and the #112/#115-residual fixes. **A 34-agent adversarial review (6 lenses, dual-skeptic verification) of `a4e03a5..d20a148` produced 10 confirmed findings + 2 more I verified by hand after the review's verify agents died mid-run** (a session-limit interruption; "refuted" from a dead verifier is a null result — both turned out real). **Four PRs, each gate-green (backend suite + benchmark 14/14 + 6/6 + 0 fabricated + web + both typechecks), all branch+PR per #118:**
- **PR #13 — fix #112 (the re-onboarding money bug):** live turns now load the onboarded vocabulary (`loadOnboardedVocabulary`, read-only, pre-gate, fail-soft, same `ONBOARDING_ENABLED` switch), so re-asking an onboarded topic answers at 1× instead of re-triggering the 100-credit flow; the clarify-reply merge accepts onboarded keys; re-onboarding under a new synonym now ADDS terms instead of erasing the old one. Empty vocab ⇒ prompt/schema bytes identical (pinned). *Residual: extended-vocab confidence calibration still owed (#112's original row).*
- **PR #14 — #115 residual (definition expander) + dashboard dedupe:** answer entries expose structured `answerParts` read verbatim from the stored envelope; long CBS definitions fold behind "Meer over deze meting" with the verbatim scale sentence kept visible (selection, never rewording); attribution always fully visible (#90); short/seed definitions unchanged. Plus the review's confirmed duplicate-row finding: the ack refusal row is excluded when its queue row (via `ack_audit_answer_id`) represents the request — one question, one entry.
- **PR #15 — validator blind spots (the review's most important catch, verified by probe):** a NEGATIVE cell value could never validate (unsigned tokenizer; only derivations had an abs-match) and a 34-char descriptive unit could never fit R10's 24-char window — so the LIVE −24 consumentenvertrouwen answer ships from the template rung with `validation.ok:false` recorded and **fails its R8 audit re-validation as stored**. Fixed strictly-narrowing (sign captured only when standalone; window extends by the unit's own length); ranges/compounds pinned byte-identical; **merging heals the live row's R8 trail retroactively** (reconstruct re-runs the current validator).
- **PR #16 — money/job review follow-ups:** refund now reverses the STORED debit delta (a mid-flight `heavy` reprice minted credits: debit 100, refund 150 — confirmed by 2 verifiers); the exhausted-refund loop got per-row isolation (one poisoned row wedged the whole queue into repeating 500s — refund+finalize are atomic, so the poisoned row stays retryable); a kick↔route Bearer **cross-pin** test (the 401 tests never proved the CORRECT header is accepted); + doc-truth fixes (RUNBOOK's `{"claimed":N}` shape never existed; `onboarding-reregister.ts` told operators a plain sync suffices — it must be `--rebaseline` (#34a); migration 014's "first block" comment contradicted the 776e645 fix; the wrap-up hook missed CLAUDE.md's own "let's close this" example, now empirically pinned).
**Recorded, verified, NOT fixed (owner call / next session):** (i) deliver→finalize is not atomic — a crash in that window + the 20-min reclaim re-runs delivery (second audit row + email; both rows would show the 100-credit cost) — fix sketch: an idempotency check at claim; (ii) GDPR retention/self-service deletion misses ALL onboarding data (`source_tag='onboarding_delivery'` rows + `pending_table_requests.question_text/topic_term` are never redacted — contradicts docs/05's retention promise; task chip spawned); (iii) the compose template rung records but does not ENFORCE its own validation verdict (safe-by-construction assumption; now an explicit open question). **#111: problem dossier written (docs/session-briefs/2026-07-07-111-problem-dossier.md), design NOT started per the owner's tier-coordination instruction.** Key verified fact: `findTable` receives only `unmatchedMeasureTerm` ("bijstand") — the stock-vs-flow intent is discarded before the finder runs, so a rerank-prompt-only fix cannot work; the dossier lays out the metadata-source/fit-gate/question-signal design space and ends with the tier question. **➡ NEXT: owner reviews+merges PRs #13–#16 (order-independent; #15 heals the live R8 trail), answers the #111 tier question, then the #111 design step.** Lessons in [lessons-learned.md](lessons-learned.md) (session 30).

**Last updated:** 2026-07-06 — **#115 (b)+(c) ANSWER-QUALITY DONE + LIVE-VERIFIED ON PRODUCTION (session 29, owner-present, ultracode) — the real CBS definition + scale now render on onboarded answers.** Live proof (read-only compose against prod): *"Consumentenvertrouwen was in 2024 -24 (gemiddelde saldo van de deelvragen)."* + a **Definitie:** line carrying CBS's full real definition ending *"…-100 (iedereen negatief) tot +100 (iedereen positief)…"* — the exact scale, in CBS's own words, replacing the go-live's circular *"Definitie: Consumentenvertrouwen."* The go-live's "veel te matig" onboarded answer ("−24 gemiddelde saldo van de deelvragen. Definitie: Consumentenvertrouwen") is fixed at the root. **Lever (b):** CBS's per-measure `Description` — which we fetched and DISCARDED; for consumentenvertrouwen it carries the real definition AND the explicit *"−100 tot +100"* scale (verified live against CBS OData) — is now captured verbatim (`CbsMeasure.description` → `cbs_tables.units` → a new **nullable `canonical_measures.definition_text`**, migration 014) and rendered as the "Definitie:" line for onboarded measures. **Lever (c):** a 3+-word descriptive unit ("gemiddelde saldo van de deelvragen") is set in parentheses. **Architecture (my call, differs from the kickoff's "make definition_label nullable" sketch): a DEDICATED `definition_text` column**, because `definition_label` doubles as the sentence subject — this keeps all 8 seed answers **byte-identical** (seed `definition_text` is NULL → benchmark unchanged by construction; no `prompt.ts`/intent-vocab change needed). `cleanCbsDefinition` distils CBS's blurb to its first block, never rewording (principle a). **`buildDefinitionLine` (in `compose/format.ts`) is now the SINGLE source of truth shared by compose + audit-reconstruct.** **Adversarial 5-lens review (each finding verified) caught + fixed 3 real issues:** (i) the hot canonical SELECT read the new column → a deploy-before-migration would degrade EVERY canonical question to a refund-refusal (the #114 class) — fixed by gating the read on the `onboarded:` key prefix (**deploy-order-safe**: an onboarded key can't exist before 014, and the hot seed path never touches the new column); (ii) `reconstruct.ts` used the OLD definition-line logic → onboarded answers would fail R8 reconstruction — fixed by the shared builder (also closes a latent lever-(a) drift); (iii) a punctuated title-echo slipped the strip. Number-honesty + test-honesty (mutation-probe) lenses clean. **Measured: full backend suite green (~910, +new pins), benchmark 14/14 + 6/6 + 0 fabricated GATE PASS, clean typecheck; NO prompt bytes changed (definitionText is assembled post-LLM → fixtures byte-identical).** **✅ THE SUPERVISED LIVE STEP IS DONE (session 29, owner present, all 5 steps executed):** (1) owner ran `npm run db:migrate` → 014 applied to prod; (2) code pushed + deployed — **CI caught a web-side `Attribution` typecheck break I'd missed and correctly BLOCKED the deploy until fixed (`dc406fb`); prod was never on broken code** (lesson: a change to a type shared by `src/` + `web/` must run BOTH `npm run typecheck` AND `npm run web:typecheck`); (3) re-synced `83694NED` **with `--rebaseline`** — a plain sync does NOT rewrite `units` (only a rebaseline does), so the metadata never updated on the first attempt (#34a); (4) `scripts/onboarding-reregister.ts` (new, committed) backfilled `definition_text`; (5) live-verified by a read-only compose against prod. **A REAL bug was caught in live verification + fixed (`776e645`): `cleanCbsDefinition` kept only CBS's FIRST block, but consumentenvertrouwen's Description is a preamble in block 1 + the real definition/scale in block 2 — the cut dropped the scale. Fixed to keep the whole verbatim blurb.** **Residual: the full CBS definition is a ~180-word paragraph rendered inline in the dashboard `finalText` blob — complete + correct but verbose; the "expander" tidy-up (a real dashboard-rendering refactor pairing with [#117](open-questions.md)/[#74](open-questions.md), since onboarded answers deliver ASYNC to the dashboard, not live chat) is deferred — owner chose to review it later.** Also surfaced: a fresh chat turn doesn't load the onboarded vocab, so re-asking an already-onboarded topic re-onboards (100 credits) instead of answering directly — a [#112](open-questions.md) gap, not #115. **➡ NEXT: the [#111](open-questions.md) finder stock-vs-flow precision fix (design-heavy, a Fable candidate — coordinate the tier with the owner).** Full detail: #115 in [open-questions.md](open-questions.md). Lessons in [lessons-learned.md](lessons-learned.md) (session 29).

**Last updated:** 2026-07-06 — **✅ WP16 SUB-PART 2 IS LIVE — on-demand CBS fetch went to production, owner-supervised (session 28). The supervised live step in the RUNBOOK is DONE.** Executed end to end with the owner present, all measured live: migrations 012+013 applied to prod (`npm run db:migrate`, owner ran it — the harness correctly blocked autonomous prod DB access) + the `pending_table_requests` lockdown live-verified (0 anon/authenticated grants, RLS on, 0 policies); `CRON_SECRET` + `RESEND_API_KEY` + `ONBOARDING_ENABLED=1` set in Vercel (Production) + fresh deploy (a `--prebuilt` deploy can't be dashboard-"redeployed" to pick up new env vars — triggered via an empty-commit push instead). **A real go-live BUG was caught pre-flight before spending a credit:** a curl of the deployed `/api/onboarding-cron` returned 307→/login — the session-refresh proxy (`web/proxy.ts`) redirected the route before its own `CRON_SECRET` auth ran, so the kick + daily cron would have silently never executed, while every hermetic test stayed green (they call the handler + job directly, past the proxy — the #114 middleware blind spot, made concrete). Fixed (commit `42b275b`): added `/api/onboarding-cron` to `PUBLIC_PATH_PREFIXES` (like the Stripe webhook — both self-authenticate), extracted a pure `isPublicPath()` and added `web/proxy.test.ts` pinning it. Then **both paths verified LIVE:** (1) **DELIVERED** — *"Wat was het consumentenvertrouwen in 2024?"* → on-demand fetched CBS table **83694NED**, synced 2026-07-06, answered **−24** with full CC BY attribution, 100 credits kept; (2) **UNANSWERABLE + REFUND** — *"Hoeveel mensen zaten er in 2023 in de bijstand?"* → fetched `85615NED`, delivery honestly refused ("geen passende maat"), **100 credits fully refunded** (ledger `#39 compensation +100 reverses #38` — migration 013's widened trigger working; would have thrown before it). The **kick is proven** by timing (jobs finished seconds after the trigger, not at the 06:00 UTC backstop) and the email path by two real Resend emails. **Owner-flagged priority out of the go-live: the bijstand question MUST answer** — it dead-ended because the finder mis-picked a FLOW table (`85615NED` = in/uitstroom) over a STOCK/count table; this is a finder-precision gap (stock-vs-flow), recorded + elevated in [#111](open-questions.md) with a concrete fix plan and a spawned task. **Next priority stack: (1) the #111 bijstand finder stock-vs-flow fix (owner-flagged, common questions must answer), then the rest — new sources (#2), answer-quality/WP26 (#3).** — **#113 KICK-ON-TRIGGER BUILT — session 28 (Opus build + 3-lens Sonnet review, hermetic).** The last code step before the supervised live step: after `triggerOnboarding` commits, the app fires `/api/onboarding-cron` itself so the delivery re-run starts within minutes instead of waiting for the daily 06:00 UTC backstop cron — the cadence that makes the acknowledgment's *"meestal een kwestie van minuten"* promise true ([#113](open-questions.md), decided session 27). **New `web/lib/onboarding-kick.ts`** (framework-free, structurally cannot throw — fail-soft per #113) wired via Next's `after()` in `maybeTriggerOnboarding` on the `started` AND `duplicate` branches (post-response ⇒ strictly post-commit; never on `insufficient`). The `Bearer <CRON_SECRET>` header byte-matches the route's check (and what Vercel Cron itself sends), so one route serves both callers. **Review: money-path/fail-soft and executing test-honesty (mutation probes) both clean; the platform-idiom lens caught one MEDIUM — the cron route only responds when the WHOLE job finishes (tens of seconds), but the caller's page action is capped at `maxDuration = 30`, so a naïve `await fetch` would be silently platform-killed mid-wait on any long job and never log whether the kick fired.** Fixed (session-28 refinement, folded into the commit) with `AbortSignal.timeout(10s)`: the kick DISPATCHES the job then stops waiting — safe because **Vercel request cancellation is opt-in** (`supportsCancellation`, which the onboarding-cron path deliberately does not set), verified against the Vercel changelog, so a client disconnect never cancels the running route. The signal-honoring timeout test caught a real guard bug in the fix itself (a `DOMException` is not `instanceof Error` in Node, so the first `TimeoutError` guard never matched — every long job would have logged a false error; fixed to match on `.name`). RUNBOOK step 3 now tells the live-step operator to confirm the kick fired via its own `onboarding kick dispatched …` log line (closing the review's LOW). **Measured: root+web typecheck green; backend 891 (byte-identical — no `src/` change); web 167 (was 154; +10 #113 wiring/behavior pins, +3 refinement); benchmark 14/14 + 6/6 + 0 fabricated GATE PASS.** No `src/` byte changed, no new dependency, no DDL. **Dormant in production until the supervised live step** (the kick is only reachable via `onboarding_pending`, which needs `ONBOARDING_ENABLED=1`). Lessons in [lessons-learned.md](lessons-learned.md) (session 28). **Next — the single remaining step: the RUNBOOK "WP16 sub-part 2 — the supervised live step" checklist, owner present.** — **WP16 SUB-PART 2 BUILT + MERGED — session 27 (ultracode build, owner-directed).** The on-demand-fetch core loop, built hermetically from the frozen [canonical design](session-briefs/2026-07-06-wp16-sub2-design.md) by an orchestrated 17-agent Opus/Sonnet workflow (the owner's remaining Fable budget was reserved for the design + this final review; the first workflow attempt died with a process exit and was relaunched Fable-free — the continuity mechanism worked as designed). **Four build stages** (scaffold / trigger seam / cron job / dashboard), each committed on branch `wp16-sub2`, then a gate loop (2 fix rounds) and a **4-lens adversarial review** (money-path, R1/R8/principle-b, design-conformance, EXECUTING test-honesty with 9 mutation probes): zero HIGH/MEDIUM defects; the single double-confirmed LOW (the `FOR UPDATE SKIP LOCKED` guard is unexercisable under single-connection PGlite — its describe block over-claimed) closed with a mutation-proven source pin; the live concurrency check is explicitly part of the RUNBOOK supervised step. **Measured at merge: 888 backend tests (768→888), benchmark 14/14 + 6/6 + 0 fabricated GATE PASS, 154 web tests (135→154), clean typechecks, `git status` clean.** Key shapes (full detail: [08-build-plan.md](08-build-plan.md) WP16 + the [handoff log](session-briefs/2026-07-06-wp16-sub2-handoff-log.md)): finder = injected optional dep (B15/benchmark byte-identical by construction, `toStrictEqual`-pinned); acknowledgment = refusal-reason `onboarding_pending` (`gate.ts` **byte-untouched**, diff-verified); atomic 100-credit debit + pending row (`triggerOnboarding`); cron job with claim/reclaim/attempt-cap, 150k-cell slice cap, runtime registry vocabulary (`extraCanonicalMeasures`, default-empty → prompt bytes identical, all fixtures untouched), delivery through the full normal audited pipeline (the e2e's answer-LLM stub THROWS, so the delivered number can only come from the fixture-ingested cell), full refund on every failure path (migration 013 widened the compensation trigger — a gap the scaffold surfaced by asserting the throw). Session-26's #110(a) gap (`sync --all` seed-bound) is fixed and pinned. **Residuals recorded:** [#111](open-questions.md) (geo/sub-coordinate onboarded tables refuse-and-refund — v1 answers national single-dimension tables), [#112](open-questions.md) (extended-vocab prompt variant fixture-unmeasured). **Production impact today: NONE — dormancy is MECHANICAL (post-merge review catch, same session): the finder is only constructed when `ONBOARDING_ENABLED=1`, unset in prod, so the deployed app behaves byte-identically pre-WP16 until the RUNBOOK supervised live step (migrations 012+013, `CRON_SECRET`, `RESEND_API_KEY`, `ONBOARDING_ENABLED=1`, cron + first-fetch verification, owner present) flips it; the finder's own catch was also widened to match its "ANY failure degrades to the plain clarification" contract, both test-pinned.** **Deploy-time discovery (same session): the Vercel HOBBY plan rejects sub-daily crons — the committed cron is now a DAILY backstop sweep and the real trigger cadence is [#113](open-questions.md) (kick-on-trigger recommended), which GATES flipping `ONBOARDING_ENABLED` (the "kwestie van minuten" copy must stay true).** **CORRECTION — the "production impact: NONE" claim above was WRONG (owner-reported incident, same session, fixed within the hour):** the `ONBOARDING_ENABLED` gate covered the finder (the write path) but NOT the dashboard's new history read — `getQuestionHistory` unconditionally queried the not-yet-migrated `pending_table_requests` table, so **`GET /` 500'd for every logged-in user** from the `ac7b0df` deploy until the fix (Vercel logs: `relation "pending_table_requests" does not exist`; the CI smoke check stayed green because it only covers `/login`, and `/` redirects anonymous visitors there). No data touched, no money moved, only the owner uses the app — but the dashboard was down and the claim was false. Fixed by threading the SAME master switch through the read path (`includeOnboarding`, default OFF), with two mechanical incident pins (a throwing-db test proving the default never touches the table + a page-wiring source pin) — the smoke-check blind spot is now [#114](open-questions.md). — **WP16 SUB-PART 2 DECISIONS LOCKED — session 26 (owner present), ADR [026](decisions/026-on-demand-fetch-job-architecture.md).** After the sanity review below found the codebase clean, the owner asked to resolve as many open decisions as possible so a longer, less-supervised build session could follow. Four decisions, all confirmed as the session's recommendation: **(1) job engine — Vercel Cron + a new `pending_table_requests` status table** (not Workflow DevKit/Queues — new paradigms rejected for v1 on the same "boring beats powerful" grounds as ADR 001; first cron entry in this repo); **(2) pricing — 100 credits, reusing the existing "heavy" tier** ([#24](open-questions.md) resolved; refund-on-verification-failure needs no new logic — `src/billing/gate.ts` already compensates in full on any refusal/thrown error, verified this session); **(3) verification scope — internal consistency only for v1** (reuses existing ingestion validators; no genuine second source exists yet to cross-check against, that's priority-#2 territory); **(4) v1 scope — the core discover→fetch→verify→answer loop only**, deferring [#108](open-questions.md) (successor re-discovery), [#109](open-questions.md) (onboarding chip), and [#110](open-questions.md)'s full eviction/TTL as separate follow-on work — except the one verified #110(a) bug (`ingest sync --all` is hardcoded to the 8 seed tables, not the registered set), which rides this build. The WP16 brief in [08-build-plan.md](08-build-plan.md) and the four open-questions rows are updated to match. **Next: the sub-part-2 hermetic build itself** (migration authored but not applied, cron config, async job, slicing, verification, billing wiring, tests — mirroring how session 24 built sub-part 1's foundation before session 25's supervised live step). No code changed yet this entry; docs + ADR only.

**Last updated (same day, earlier):** 2026-07-05 — **FULL-REPO SANITY REVIEW + owner-notes triage — session 26 (autonomous; the owner asked for an expert check of the last two days' Opus work).** All measured: (1) **local full gate re-run GREEN** — 768 backend tests, benchmark 14/14 + 6/6 + 0 fabricated GATE PASS, clean typecheck both sides, 135 web tests; CI independently green on all three session-25 commits. (2) **Production re-verified read-only** (repo db client, SELECTs only): migrations 1–11 applied, `cbs_catalog` 4,858 rows with the status distribution byte-identical to the session-25 claim (Gediscontinueerd 3,559 / Regulier 1,277 / Vervallen 22), 8 registered tables, 0 anon/authenticated grants, RLS on. (3) **Multi-agent adversarial review (6 lenses on the mid tier, every finding verified by 2 independent skeptics, + a completeness critic; 21 agents): ZERO code defects in the WP16 sub-part-1 work** — the hard allowlist is enforced in two independent layers, every failure path routes to disclose/none, the FTS recall is fully parameterized (no injection), the ingest transaction+prune logic is safe against partial failures, the replay test is genuinely end-to-end (the fixture hash covers the full shortlist, so recall drift fails loudly), and the calibration artifacts match every doc claim number-for-number. What the review DID catch was **doc staleness, all fixed this session:** ADR 001/CLAUDE.md's module list predated `registry//billing//db//catalog/` and named a `validation/` module that never existed (ADR 001 now carries the as-built list); ADR 025's "refreshed on a schedule" wording contradicted the open #106 cadence question; 04-architecture had no catalog row and a pre-WP16 onboarding seam row; the CI header comment undercounted its own suites (the catalog suite runs but was unlisted); migration 011 gained a lockdown-inheritance pointer comment. (4) **The review's best catch (completeness critic): the sub-part-2 wiring seam is TWO structurally different exits, not one** — recorded precisely in the WP16 brief so the next session doesn't wire `table_not_registered` (no free text, generic copy, unreachable today) as if it were the `unmatchedMeasureTerm` fit. (5) **Owner's session-26 notes triaged against reality:** the reconcile/land/docs/AGENTS.md items were verified ALREADY LANDED in session 25 (direction A — aliases restored + fixtures re-recorded — confirmed in-tree); the four genuinely new items became **[#107](open-questions.md) slice greediness / [#108](open-questions.md) successor re-discovery / [#109](open-questions.md) onboarding suggestion chip / [#110](open-questions.md) on-demand data lifecycle**, each with a session-26 assessment — including one VERIFIED real gap: `ingest sync --all` targets the hardcoded seed, not the registered set, so an on-demand table would never refresh (#110a); and one verified non-issue: R8 audit reconstruction is self-contained, so eviction can't break the audit trail. No code changed this session (docs + comments only); zero live LLM spend on the product path; no DDL. **Next: WP16 sub-part 2 (supervised — live DDL + real spend, coordinate with the owner).** — **WP16 sub-part 1 — SUPERVISED LIVE CALIBRATION — session 25 (owner present).** Executed session 24's deferred supervised step end to end on production, all measured: (1) **migration 011 applied to prod**, and the RUNBOOK per-migration grants/RLS check **live-confirmed** `cbs_catalog` is locked by inheritance from migration 003 (0 `anon`/`authenticated` grants, RLS on, 0 policies, owner+service_role only — verified, not assumed); (2) real **`catalog:refresh` mirrored 4,858 rows** (Numeric 4,473 / Mixed 309 / Text 76; Regulier 1,277 / **Gediscontinueerd 3,559** / Vervallen 22 — discontinued-dominant, exactly why the "prefer Regulier" rerank is load-bearing), production Dutch FTS live-exercised (all 8 registered tables present; "inflatie"/"huizenprijzen"/"faillissementen" raw top-5 is all-discontinued — Stage 2 does the real work); (3) **`tablefinder:record` run live (Haiku): first pass 5/8.** The 3 misses were three different things — one **mislabel** (zonnepanelen: the rerank's `85004NED` "zonnestroom-productie" beats the seed's general-renewables `82610NED`, verified from CBS metadata + independently in review) and two **Stage-1 recall gaps** (bevolking, woningvoorraad — the rerank behaved correctly, disclosing at 0.25 on a bad shortlist), fixed with **2 alias hints**; re-record → **8/8**. `highConfidence` **calibrated to 0.8** (confident floor 0.85 measured/stable; failure-safe — below-0.8 discloses, never a wrong table). Added an **end-to-end replay test on the gate** (`tests/catalog/find-replay.test.ts`, hermetic, €0). **Adversarial review (3 lenses, mid tier) caught a real HONESTY defect — corrected same-session:** my first calibration note cited a "0.25 disclose, wide gap" data point that was from the PRE-fix run (superseded — that case is now confident), so the final set has **no disclose case** and the confident/disclose boundary is under-measured; the docs now state only what was measured (confident floor), and a disclose case + live-mirror re-check are owed in sub-part 2 ([#104](open-questions.md)). The test-honesty lens's HIGH "changes don't exist" was a false alarm from `isolation: worktree` branching off HEAD while the diff was uncommitted (lesson recorded); the mutation proof was done in-tree instead (removing the 2 aliases fails exactly the 3 new tests, restored byte-exact). **Measured gate: 768 backend tests green (760 + 8 replay), benchmark 14/14 + 6/6 + 0 fabricated — GATE PASS, clean typecheck; web untouched.** Recorded quirks: node/undici can't reach CBS from a v6-black-holed host (IPv4-force `--import` preload let the unmodified CLI run — the deploy host is unaffected); `catalog:refresh` is slow (~19 min, row-by-row upserts over the pooler). A stray untracked `AGENTS.md` (a 63-line CLAUDE.md duplicate, not created this session) was converted, on the owner's call, into a thin one-line pointer to CLAUDE.md (keeps the cross-tool filename convention, kills the drift risk) and committed. **Next: WP16 sub-part 2.** — **WP16 sub-part 1 (table finder), HERMETIC FOUNDATION — session 24 (owner present, kicked off "start on Opus; flag if topic→table earns Fable").** Built the whole table-discovery foundation with zero live LLM spend and no live DDL: (1) `migrations/011_cbs_catalog.sql` — a `cbs_catalog` metadata mirror kept structurally separate from `cbs_tables`, with a Dutch `setweight` `tsvector` + GIN index (FTS first use in the repo; verified working on PGlite before writing the migration); (2) `CbsSource.fetchCatalog()` on the ADR-003 seam (real in `odata-v4.ts` at `…/CBS/Datasets`; replayed from a REAL captured `tests/fixtures/cbs/_catalog.json`, 61 topical rows incl. all 8 registered ids), closing ADR 003 revisit trigger #3; (3) `src/catalog/` — `ingestCatalog` (idempotent mirror refresh + timestamp prune), Stage-1 `recallCandidates` (Dutch FTS + curated alias hints, Text/non-nl excluded — principle c), Stage-2 rerank (`rerank-schema.ts`/`rerank-prompt.ts`/`rerank.ts` with the **hard allowlist validator** — the picked id MUST be in the shortlist or it throws → disclose), and the `findTable` orchestrator (confidence routing confident/disclose/none, rerank *injected* so routing is unit-tested without LLM fixtures); (4) `scripts/tablefinder-eval.ts` + `benchmark/tablefinder-labelled-set.json` (seed) + `src/catalog/cli.ts` (`catalog:refresh`) + a `--catalog` capture routine, all off the CI gate. **Three empirical CBS corrections to the brief's research (measured this session):** the summary field is `Description` and it's IN the listing (no per-table call); the catalog `Identifier` IS verbatim the id the data endpoints need (`/85773NED`→200, `/85773`→404); `Status`/`DatasetType` give free principle-(c) signals (Text excluded; `Regulier` preferred in rerank — discontinued tables outrank the live one on raw keyword match, which is *why* Stage 2 exists). **The Fable judgment (ADR [025](decisions/025-cbs-catalog-table-discovery.md) decision 3): topic→table does NOT earn Fable in v1** — a closed multiple-choice over a supplied shortlist with a hard allowlist is an *easier* shape than intent parsing (Haiku); the principle-(c) risk is contained structurally, so Fable would be a cost bug. `TABLE_RERANK_MODEL='claude-haiku-4-5'`, one constant, recorded escalation ladder. **Measured (hermetic): 757 backend tests green (715 → +42 new catalog tests across 5 files), benchmark 14/14 + 6/6 + 0 fabricated — GATE PASS, clean typecheck; web untouched.** **Deferred to a SUPERVISED session (owner-authorized live spend + DDL):** apply migration 011 to prod (+ the RUNBOOK grants/RLS live-check on `cbs_catalog`), real `catalog:refresh`, record rerank fixtures + calibrate `highConfidence`, then sub-part 2 wiring. — **GDPR retention + self-service deletion (#14), code-only/hermetic session:** built `npm run gdpr:purge` (`scripts/gdpr-purge.ts`) and a "Verwijder mijn vraaggeschiedenis" self-service button/server action (`deleteMyQuestionHistory`, `web/app/actions.ts`), both via `src/answer/audit/retention.ts`. **Real finding mid-build:** the brief's literal `DELETE FROM audit_answers` would throw a foreign-key violation on any clarification/refusal row a `credit_transactions.audit_answer_id` compensation entry references (migration 005's FK has no `ON DELETE` clause, by design) — confirmed empirically before writing product code. Fixed by **redacting (UPDATE) instead of deleting**: question/answer text is overwritten with a fixed sentinel, the row/id/ledger-join all survive — which is also a better structural match for the owner's "verwijderde vraag" placeholder-row decision than a delete would have been. No schema change. Measured: 715 backend tests (700 pre-existing + 15 new — incl. the reviewer's topic-scrub pin), 135 web tests (9 new), benchmark 14/14+6/6+0 fabricated unchanged, clean typecheck both sides, real `next build` green. **Reviewed + committed session 23 (commit `6aafb40`);** the reviewer extended the redaction to also clear the topic columns (`intent`/`table_ids`/`conversation_context`) per owner *"wis de inhoud volledig"*. Live-verified on production (dry-run + `--apply`, 2026-07-05: 0 rows — nothing is 2 years old yet); the #14 redact-and-retain posture confirmed by the owner ([#59](open-questions.md), the separate account-deletion FK tension, stays open). — **Session 23 (owner present, design only — no code, no benchmark run):** wrote ADR [024](decisions/024-answer-first-defaults-and-clickable-options.md) + the **WP26 brief** ([08-build-plan.md](08-build-plan.md)) for the #72+#66 "paid dead-end" fix (clickable pre-verified options + answer-first structural defaults), after a full state review of the project; owner chose to build both as one WP and deferred WP26 pricing ([#101](open-questions.md)). — Session 22 (overnight, autonomous): **WP21 + WP22 + WP23 + #64 shipped** (CSV export #52; #95/#96a/#97a live-feedback fixes; #84/#86/#90/#91/#92/#71/#75 display smalls); execute-ready briefs for #65 (WP25), #14, the #70/#79/#89/#90 drill-through cluster, #53 and #87 in [08-build-plan.md](08-build-plan.md).

**Also written this session boundary, owner-requested — a [UX design brief](10-ux-design-brief.md)** for the site shell (navigation/footer), the dashboard, and the homepage/chatbox: documents the current gaps (no header, no footer, **no sign-out anywhere in the app — verified by grep**, `/credits` is a dead end with no way back, no site-wide CBS/CC BY attribution or privacy link, `/` doubles as both homepage and dashboard with no separate surface for a logged-out visitor). Proposes a minimal header+footer shell and lays out three options for the homepage-vs-dashboard question, recommending the cheapest one now and deferring the expensive one until [#53](open-questions.md) (anonymous trial) is actually greenlit. Two decisions recorded for the owner: [#98](open-questions.md) (which IA option) and [#99](open-questions.md) (footer copy sign-off) — the header/logout build itself has no open product question and is written up as **WP24 (placeholder)** in [08-build-plan.md](08-build-plan.md), buildable as soon as #99 is answered. No code changed.

**Session 22 (continued) — #64, explicit period enumeration (all measured):** "Hoeveel mensen wonen er in Rotterdam in 2020 en in 2022" now answers with BOTH values (the live reproduction dead-ended in a paid still_ambiguous round, audit rows 79–80). Multi-REGION enumerations already parsed as one candidate — the gap was multi-PERIOD only, and the fix is a **deterministic policy-layer merge, zero prompt bytes**: plausible readings differing only on single-code yearly periods, every named year literally present in the question, merge into one multi-code intent the query contract has served since WP5 (contract-pinned). R7's carve-out is the owner decision verbatim: an enumeration the user typed is not interpretation ambiguity — Utrecht-gemeente-vs-provincie differs on the REGION axis and still clarifies (pinned). Merged confidence = the weakest source reading, so a shaky enumeration still confirms first (pinned). **Measured: 700 backend tests (answer 407, query 71, chart 40), benchmark 14/14 + 6/6 + 0 fabricated — GATE PASS, 126 web tests, both typechecks, zero live spend. Review verdict FIX-FIRST, fully resolved pre-push:** the line-chart channel closed (a series with a by-construction hole draws NO chart — `contiguousPeriodCodes`, which also retro-fixes the V02/now_vs_ago line WP14 could draw); the derivation-prose channel judged with recorded reasoning (endpoint "gestegen van A naar B" is true, bound and the B13/V02 convention; the pre-existing monotonic-sample seam recorded as [#100](open-questions.md) for the next prompt-touching WP); all four executing-lens coverage gaps closed and the HIGH one mutation-proven (a widened grain regex collapsing quarters into fabricated year codes now fails its pin).

**Session 22 (continued) — WP23, approved display smalls #84/#86/#90/#91/#92/#71/#75 (+#73 judged deferral), all measured:** the owner-approved brainstorm smalls, batch-built per the overnight queue's item 3. The core is a display restructure with a zero-loss guarantee: an answer now renders from its STRUCTURAL envelope fields — body in the bubble, staleness/definitie/bewerking lines as their own elements, and the attribution as a **source chip** (#90) that ALWAYS shows the full R4 sentence (never behind a click) plus the **"Bekijk bij CBS StatLine" deep-link** (#86 — pure URL construction to the dataset table view, pinned against two real Phase-0 URLs incl. the lowercase-id case; ADR 003 intact: the USER goes to CBS, our request path never does). Compose.ts assembles `text` from exactly those fields, so nothing the assembled string carried can drop (test-pinned); `text` itself — the R8 audit string — is untouched server-side. **#84:** clarifications amber with a decorative marker kept OUTSIDE the bubble (the pipeline's message text stays byte-exact for tests and readers); refusals announce "Dit kon ik niet beantwoorden" + the "geen antwoord = geen gok" badge [META/SMALLTALK NOTE]. **#71:** an amber voorlopig pill at message level whenever any quoted cell is provisional (the stat-card badge's generalization). **#75:** three benchmark-proven example questions as chips on the empty chat — clicking FILLS the input, never sends (the user sees the #82 cost line first). **#91:** tabular figures on messages, cost captions and the balance. **#92:** chart-footer caveats amber and larger, the source credit smallest (photo-credit style), caveats-above-credit pinned; attribution strings byte-identical from the one builder. **#73 (follow-up chips) deliberately NOT built** — judgment recorded in its row: deterministic templates cannot promise a servable suggestion, and an unservable chip invites a paid dead-end (the #77/#97 pattern); belongs with #66's servability-backed clicks.

**WP22+WP23 adversarial review, second round (2 lenses on the WP23 diff + the WP22 fix): every finding closed and mutation-proven before the closure push.** The display-honesty lens caught the round's HIGH: meta answers ride the refusal envelope by DESIGN (ADR 022 — the text answers), so the new #84 refusal header ("Dit kon ik niet beantwoorden") would have sat on top of a true answer about our sources — the exact misleading-UI class the product exists to prevent; fixed by reason-aware presentation (meta + smalltalk render as plain info; genuine refusal reasons keep the header) with reason-pinned tests, plus its MEDIUM (the zero-loss claim said "test-pinned" while the test pinned fixture-field presence — the test now mirrors the REAL production text assembly and walks every line of it) and two LOWs recorded (the deliberate staleness-position change now documented; example-chip 3's exact string is answerable by mechanism but never live-parsed — queued for the next recording window at ~$0.02). The executing worktree lens ran 14 mutation probes: 11 caught (both #92 legs, both pill directions, all three structural-line drops, chips-fill-not-send with an honest note about a stale-closure false-negative it corrected itself, both #95 wiring pins proven, the reload closure proven); its 3 survivors were closed same-session and each re-proven to fail (production-realistic text fixture, the StatLine link bound via a second table id, the refusal strings bound to header-vs-badge positions).

**Session 22 (continued) — WP22, live-feedback smalls #95/#96(a)/#97(a), all measured:** the three fixes from the owner's first real production sessions, exactly the overnight queue's item 2. **#95:** Stripe success now redirects to `/?purchase=success` (the dashboard IS the app) with a dismissible banner whose copy keeps the webhook honesty ("meestal een paar seconden", reload shows the new balance); one shared param definition (`web/lib/purchase.ts`) for the action-writer and page-reader; dismiss strips the query flag via replaceState. One deliberate deviation recorded in the row: the URL lives in the web layer, not `src/billing/stripe-checkout.ts` (which already took it as a parameter and stays byte-unchanged). **#96(a):** the stale-tab deploy failure (live-diagnosed: Server-Action ids die on deploy; Next throws `UnrecognizedActionError`) now gets honest copy + a "Ververs de pagina" button via Next 16's own `unstable_isUnrecognizedActionError` detector — the no-charge claim verified structurally (the debit is the action's first step, and the action never ran); tests reject with the REAL Next error class so the `instanceof` detection is exercised, never mocked. Leg (b) (platform skew protection) stays an owner decision. **#97(a):** the resolver's max guard split into `max_needs_regions` (geo table: asks which regions to COMPARE) and `max_on_national_measure` (no regional split: names BOTH real gaps — regions impossible here, period-peak not built (#97b) — and offers the measure's own gap-free loaded year window as a CHECKED range option); the shared `region_unknown` template pinned byte-identical for genuinely unknown place names. **Measured (the WP22+WP23 combined gate — one push, see the concurrency note below): 679 backend tests green (answer 391→395: three #97a cases + the empty-window leg), benchmark 14/14 + 6/6 + 0 fabricated — GATE PASS, 126 web tests, clean typecheck both sides, real `next build` green, zero prompt bytes (all 92 fixtures replay byte-identically; the review verified the mechanism — resolution is strictly post-LLM), zero live LLM spend, zero DDL.**

**WP22 adversarial multi-lens review (house rule, mid tier; the executing lens in an ISOLATED WORKTREE — the WP21 lesson applied same-night): 4 lenses.** The refusal-honesty lens caught the round's real defect pre-push: the first `max_on_national_measure` template hardcoded "per maand of per jaar" — grains 4 of the 7 national-only measures don't have (KW-only unemployment, JJ-only housing/income/solar), the exact #56 unservable-suggestion sin, provable without live traffic; fixed same-hour by offering the measure's own gap-free loaded JJ window through the SAME checked builder the degenerate-range guard uses (`openEndedRangeOptions`, now accepting a no-floor call), template options-aware with both variants full-string-pinned and the fixture-DB-derived window asserted in the resolver test. The same lens traced the reply-merge risk honestly: a bare "per maand" reply would still dead-end in the pre-existing single-period-series guard (the #77 cost shape) — the checked range option is exactly the reply-shape that provably resolves (the B4 range path), and the deeper fix is #66's clickable options (this clarification is its prime candidate — noted in the row). Pipeline-stability lens: NO DEFECT — prompt-template diffs empty, the template-vs-payload distinction made precise (clarification questionNl reaches the clarify-reply payload at runtime as EVERY clarification always has; zero recorded fixtures involve the max path), audit reconstruction structural (no enum whitelist to break), boundaries clean. Web-correctness lens: NO DEFECT across seven items — searchParams-as-Promise verified against the INSTALLED Next 16 docs, the computed-key type proven with a real tsc probe, `/` was already dynamic (cookies) so no caching change, the detector export confirmed in the client bundle, replaceState desyncs nothing (no router-state hooks in the tree). Test-honesty lens (EXECUTING, isolated worktree): 8 of 10 probe families caught cleanly — including proving the <2-regions threshold genuinely guards a live wrong-shaped intent (weakening it let a one-region max slip to the query layer) and that the template pins are independent of the sweep lists (sweeps blinded, the pin still fired). Its 2 real gaps were closed same-session and re-proven by mutation: the refresh button's reload is now BOUND (jsdom's non-configurable location needs full-object replacement — the lens proved the idiom), and the #95 redirect wiring got source-pins (a recorded judgment: a server-action/Server-Component behavioral harness was judged not worth its cost for two lines, silence on a money-path worse — both pins proven to fail their reverts).

**Concurrency + deploy (measured):** while this session's review round was still closing, a concurrent owner-side session pushed its UX-brief docs commit (19db59b) — carrying this session's three local WP commits (f57ff4a, f7fe91e, a9bee3b) to origin, where **CI went green on the full gate (run 28723250006)** and deployed; the review-closure commit below then followed as its own push. The session-19 precedent exactly: verified with `git log` before assuming anything, nothing lost, both sessions' docs edits merged cleanly in the shared tree.

**Session 22 (overnight, autonomous — the owner-authorized brief executed queue-order) — WP21, CSV export (#52), all measured:** "Download als CSV" under every answer, beside WP20's citation button. Format decisions (explicitly delegated to this session; reasoning + rejected alternatives in the [WP21 brief](08-build-plan.md), no ADR — a reversible product-surface convention): Dutch-notation CSV matching CBS StatLine's own downloads (`;` separator + decimal comma, **no thousands grouping** — deliberately not `formatValueNl`, which grouped values would break outside Dutch Excel; UTF-8 BOM for Excel's encoding sniff; CRLF; RFC 4180 quoting). Honesty structure: the preamble's source row is the **verbatim `buildAttributionLine` output** (the one builder shared with answer text and chart specs — the export cannot drift, R4/CC BY *inside* the file); `waarde` round-trips to the stored cell value EXACTLY (padding only when lossless — the guard's own trigger case is pinned: value 3.35/decimals 1 exports as `3,35`, never `3,4`); `eenheid` is the raw CBS unit verbatim (R10); per-cell `status` + `bijzonderheid` for null-with-reason cells (R11); ONLY explicit derivations export, in a marked section with source cell ids resolvable against the `cel-id` column (R5) — the implicit binding derivations (direction/first_last, and the real non-explicit `max` run.ts registers on every comparison) are proven non-exported. Client-side Blob download, no new endpoint, zero backend files changed. **Measured: 675 backend tests green (byte-untouched backend), benchmark 14/14 + 6/6 + 0 fabricated — GATE PASS, 101 web tests (76 → 94 built → 101 after review fixes), clean typecheck both sides, real `next build` green both before and after the review fixes, zero live LLM spend.**

**WP21 adversarial multi-lens review (house rule, mid tier): 4 lenses (export-honesty, dialect+injection, test-honesty EXECUTING with punch-a-hole grants, docs+boundaries), zero HIGH product defects, every finding actioned same-session.** The test-honesty lens proved three green-surviving mutations (the `explicit` filter dropped — run.ts's real implicit `max` would export; the difference's minuend-precision swapped — unobservable while fixtures shared decimals; the round-trip guard's trigger case uncovered) plus a latent quoted-field trap in the test helper and the unpinned empty-result shape — **all closed with 7 new pins, each re-verified to fail its mutation (1 failed/21 passed per probe, restored file 22/22)**. The export-honesty lens executed one MEDIUM defense-in-depth gap (a derivation whose reference cell is missing would leak raw float noise — unreachable today by run.ts construction; fixed with a source-cell fallback chain, both legs pinned, and the final fallback stays the exact raw value: an unregistered rounding would be a value change) and one LOW judged no-change (1e21 exponent serialization — a magnitude no CBS cell can reach, round-trips exactly). The dialect lens returned NO DEFECT with executed proof (RFC 4180 quoting 17/17 edge cases incl. Python-csv round-trips; formula-injection provenance traced field-by-field to FK/refusal-gated CBS metadata — **considered decision, recorded: no OWASP cell-mangling; it would un-verbatim the data against a threat requiring CBS itself to publish hostile metadata**; BOM/revoke-timing verified against spec with the real-browser limit stated honestly). The docs lens verified every brief claim against code; its scope-table finding was fixed same-day (docs/03's Phase-0 "no exports" row now points at the #52/WP21 supersession) — and its "intermittent web-suite flake" MEDIUM was **re-measured after the lenses finished: 6/6 consecutive full-suite runs green; judged an artifact of the review's own orchestration (an executing lens mutating `csv.ts` in the shared working tree while another lens ran the full suite concurrently), not a code defect — lesson recorded: executing skeptics get isolated worktrees, the established house pattern this session deviated from and reaffirms.**

**Session 21 — #77 fix, explicit date ranges (all measured):** the live-reproduced dead end ("Maak een grafiek van de inflatie van 1 januari 2022 tot en met 31 december 2022" → clarification loop → `still_ambiguous`, net 10 credits for nothing) is fixed at the root: a new `date_range` raw-parse period kind (the model copies day/month/year boundaries VERBATIM plus a "tot en met"-vs-"tot" flag; `PROMPT_VERSION` 5, `RAW_PARSE_VERSION` 3) resolved by new deterministic code owning ALL calendar arithmetic — leap-year validation, exclusive ends ("tot 1 januari 2023" = t/m december 2022), whole-month alignment (boundaries cutting a month clarify honestly, never silently widened), and finest-exact-grain selection (inflation gets its 12 monthly 2022 cells as the existing series line chart at the ordinary `simple` price; a KW-only measure serves quarter-aligned date ranges, a JJ-only measure whole calendar years). Query contract, chart, billing and R8 reconstruction verified untouched. **Measured: first live probe 61/62 with zero regressions on all 54 pre-existing cases; record run 62/62 at `--repeat=3` with zero outcome flips; the review-added 63rd case recorded stable at `--repeat=3`; confidence min 0.92 / median 0.95 (R7 thresholds hold unchanged); clarify 7/7 and follow-up 22/22 re-recorded first-try; two label decisions per the ADR 019 §7 label-follows-policy precedent (the single-month oddity answering at 0.85 → the R7 echo clarification is correct behavior; the bare-"tot" owner decision in the review block below). Hermetic gate after the review fixes AND the skeptic-catch fix below: 675 backend tests green (answer 353→391), benchmark 14/14 + 6/6 + 0 fabricated — GATE PASS, 76 web tests, clean typecheck both sides, real `next build` green.** All 92 prompt-dependent fixtures (re)recorded (63 intent + 7 clarify + 22 follow-up), orphans deleted so the gate itself proves the set complete. **Live spend, measured: 1,867,581 in / 32,379 out Haiku tokens ≈ $2.03 at list pricing (≈ €1.75–1.90; every summand named in ADR 023)** — above the informal "well under €1" estimate given before scoping; the why (any base-prompt change = full fixture re-record + repeat=3 stability, priced from the measured ~6.6K tokens/call) is a lessons-learned entry so future prompt-touching fixes get priced honestly up front.

**#77 adversarial multi-lens review (house rule, mid tier): 6 lenses → 7 findings (4 MEDIUM, 3 LOW); the fixture-integrity lens ran a full hash-reachability closure and reported zero defects (every fixture byte-matches the live prompt, orphan-free, 1:1 with the case list).** Procedure note, recorded honestly: every finding arrived with the finder's own executed proof (the calendar lens live-probed the fixture DB; the docs lens recomputed every number from the committed artifacts), so the session judged all seven directly instead of running a second skeptic pair per finding — the FIX diff then got its own two-skeptic verification (one executing, one judging) before push — **and the executing skeptic caught a real defect in the fix itself, proven with a before/after probe via git stash:** the demotion re-ran AFTER the max-region guard had already checked the FORCED value, so a demoted 'max' hint without its comparison regions slipped past the resolver's specific "name the regions" clarification and only died later in the query layer as a generic invalid_intent (never a wrong number — a refusal-quality regression); fixed by re-running the guard on the final derivation (shared failure builder so the two sites can't drift), pinned both ways (guard fires without regions; a legitimate demoted max over Amsterdam/Rotterdam executes). The judging skeptic returned SOUND across design, docs arithmetic ($2.03 recomputed from the five summands), fixture counts, and the disclosed prompt-wording wart. All seven addressed same-day: (1 **MEDIUM, the round's catch, executed live by the finder**) a whole-calendar-year date range on a yearly-only measure (or whole quarter on quarterly-only) collapsed to ONE period code while the forced `series` derivation drove it into the degenerate-range guard — a spurious clarification for a question with one honest direct answer, the very #77 symptom class (safe: a refusal, never a wrong number); fixed by demoting a moot forcing back to the model's own hint (a plain lookup now answers; an explicit "maak een grafiek" still clarifies — one point is not a series), with pins on both sides plus a no-demotion guard on uncollapsed ranges; (2 MEDIUM) the prompt's own month-only bare-"tot" example was never live-scored — closed with a new labelled case recorded at `--repeat=3` via a new `--only` eval flag (targeted recording now costs ~$0.02 instead of a full-set rerun): **the model read "van maart tot september" as everyday-INCLUSIVE at 0.95, stable, while correctly keeping day-precise "tot 1 januari" exclusive — owner decision (Stefan, in-session): accept the inclusive reading** (ADR 023 alternative 4 reversed on measurement; the prompt rule's wording fix deliberately rides the next prompt-changing WP, wart recorded beside `PROMPT_VERSION`); (3 MEDIUM) the KW quarter arithmetic was test-exercised on months 1 and 6 only — Q4-edge and cross-year-span pins added; (4 MEDIUM) the lessons entry's own cost-estimation formula undercounted the measured per-call tokens by ~30% — corrected to the report-derived ~6.6K/call; (5 LOW) the "WP14 precedent" citation sharpened to the actual precedent (ADR 019 §7's reviewed label correction); (6 LOW) spend restated in USD list terms with every summand named; (7 LOW) ADR 023 now documents that conversation memory (ADR 021) deliberately does not carry month/quarter-grain ranges — a follow-up after the #77 answer re-parses standalone, fail-closed, revisit trigger recorded.

**CI + deploy (measured):** the pre-review #77 commit (4805116) rode a concurrent session's docs push and went green on CI run 28719100587 — the full gate including the 92 re-recorded fixtures; the review-fix push (commit ec61ab3) then went green on **CI run 28719732857** (`gate` and `deploy` both success, post-deploy smoke check included; independently re-verified: production `/login` → 200) — so **checkdecijfers.vercel.app now runs the reviewed #77 fix: explicit date ranges answer end to end.** **Live-verified by the owner the same night (2026-07-05, ~04:25):** the #77 question verbatim → the 12-month 2022 inflation series with chart, full attribution and the ordinary "20 credits" caption, zero clarification rounds — the bug's exact reproduction now answers end to end in production; the WP20 stat card rendered for "inflatie in 2024" (3,3%, source line), the #82 cost lines showed on a clarification round ("10 credits · antwoorden op de wedervraag kost ~20 credits") and the still_ambiguous refund displayed "0 credits". **Owner also confirmed (same session): "Download als afbeelding" saves the PNG and "Kopieer als citaat" fills the clipboard — the two success paths jsdom can't reach, closing WP20's last honest limit.** Two NEW live observations recorded the same hour: **[#96](open-questions.md)** — one question hit the generic error because a fresh deploy invalidates Server Action IDs in already-open tabs (diagnosed from Vercel logs pulled immediately: `POST / 404, Failed to find Server Action`; no charge made, later questions fine); **[#97](open-questions.md)** — a max-over-PERIODS question ("de maand waar de inflatie het meest steeg") got the misleading gemeente-of-provincie clarification because the `max` derivation is regions-only (pre-existing WP6 shape, not a #77 regression; net 10 credits for a misleading round — the #77 cost pattern on a new path).

**Session 18/20 (concurrent, owner present) — first real Stripe purchase debugged and fixed live:** the owner's first real test purchase completed at Stripe but credits never landed. Root cause, found through direct production diagnosis (not guessed): `STRIPE_SECRET_KEY` creates checkout sessions in a separate Stripe **Sandbox** ("GlaiBaan sandbox", account `acct_1TpJfCATKgdSn8Uc`) nested under the same login, distinct from the "Check de Cijfers" Test-mode account (`acct_1TpJevAS07S6LQh2`) where the webhook was actually configured — that account showed zero API activity ever, while the sandbox where payments genuinely succeeded had no webhook destination at all. Fixed: a new webhook destination created inside GlaiBaan sandbox itself, `STRIPE_WEBHOOK_SECRET` replaced in Vercel (marked Sensitive) and a fresh production deploy triggered to pick it up (env var edits don't apply to a running deployment). **Verified end-to-end, live:** a real test purchase produced ledger row `id=22`, `reason='purchase'`, `delta=+200`, `stripe_checkout_session_id` matching the completed session exactly — the first successful purchase this ledger has ever recorded. Full account-mixup story + the fix in [RUNBOOK.md](RUNBOOK.md) and [lessons-learned.md](lessons-learned.md).

**Session 20 (second half) — WP20, brainstorm smalls #78/#80/#82 (all measured):** the owner delivered a second, pre-filtered creative-brainstorm batch mid-session and authorized immediate execution alongside recording; all sixteen items were first recorded as open-questions rows 78–93 (including the three explicit rejections, #93) and pushed as their own docs commit before any code. Then the three small top-5 items were built as WP20 — **#78** "Kopieer als citaat" (validated `answer.body` verbatim + structured attribution, automatic "voorlopige cijfers" and CC BY bewerking flags — full-string test pins); **#80** the stat card (compact SVG for single-cell answers, shared-formatter number, voorlopig badge, source line) with "Download als afbeelding" exporting **the same SVG node** to PNG; **#82** pre-send cost transparency (cost line + saldo under the chat form tracking the LIVE #68 balance, static clarification-cost hint, and the reply price repeated at each clarification message — all figures from the live pricing tables). Zero backend files changed; the client's only backend value-imports are import-traced pure chains. Also fixed for real: the web vitest include was `**/*.test.tsx` only, silently skipping the new pure-lib `.test.ts` files — broadened, lesson recorded. **Measured (hermetic, after the review fixes): 637 backend tests green (unchanged — no backend file touched), benchmark gate PASS (14/14, 6/6, 0 fabricated), 76 web tests (24 → 44 → 76 across this session), clean typecheck both sides, real `next build` green (the client now bundles two backend pure-leaf chains — verified by the build, not assumed), dev-server boot + `/login` clean.** Zero LLM spend again. **Honest limits:** the logged-in chat surfaces (citation button, card, cost lines) are not live-verified — owner's next login is the first real look; the PNG *success* path cannot run in jsdom (both failure legs are test-pinned; the success path needs one real-browser download). *[Both closed 2026-07-05, session 21: owner verified all surfaces live incl. a real PNG download and clipboard copy — see the session-21 entry.]*

**WP20 adversarial multi-lens review (house rule, mid tier): 5 lenses → 5 raw → 5 deduped findings → 2 skeptics each, 16 agents; one finder (the quote-and-export lens) died at the structured-output retry cap — recorded as missing coverage, its checklist re-swept by the session directly (citation is body-verbatim + structured fields by construction; PNG content is serialization-identical to the shown SVG, only CSS decoration like the border doesn't carry into the export), never counted as a clean pass.** **3 double-confirmed, all fixed same day:** (1 **HIGH, the review's catch of the day**) the card's unit rendering bypassed the shared body conventions — a population count would read "18.044.027 aantal" and a factor-unit table would show "8.204 x 1 000" WITHOUT the parenthesized R10 ×1.000-misreading guard, on exactly the artifact meant to be shared outside the product; fixed by deriving the card's unit suffix from the shared `displayValueUnit` itself (zero backend changes — the function was already exported), pinned per branch incl. both real benchmark unit shapes; (2 MEDIUM) label truncation had zero boundary coverage — pinned with hardcoded-literal expectations at 45/46/47 chars; (3 MEDIUM) the entire PNG-download body incl. the "Downloaden lukte niet" message was unexercised — both reachable failure legs now click-tested under stubbed Image/URL APIs. **1 split verdict judged by the session:** the brief's "leaf modules only" sentence omitted the pre-existing type-only `respond/types.ts` import path — technically-true docs imprecision, sentence corrected (the judge's REFUTED reasoning — pre-WP12 pattern, type-erased, no runtime risk — is why it's a wording fix, not a code change). **1 finding was the test-honesty lens reporting "no defect" on three suspected gaps (clipboard full-string binding, getByText ambiguity, fixture-masked crash paths) — verified sound by both skeptics.** Review worktrees (5) removed before these numbers were recorded.

**CI + deploy (measured):** the brainstorm-recording push went green (run 28715389112), and the reviewed WP20 push (commit 2d495f2, carrying pre-review ac9cdcb) went green on **CI run 28716207303** — `gate` and `deploy` both success, post-deploy smoke check included (`gh run view` conclusions, plus an independent `curl`: production `/login` → 200) — so **checkdecijfers.vercel.app now runs the reviewed WP19 + WP20 dashboard.**

**Session 20 — WP19, dashboard polish (all measured):** the four owner-decided dashboard items built as one WP — **#67** a clarification round now collapses into ONE folded history entry, grouped at read time in `getQuestionHistory` (no migration: the reply row already carries the answered pending's `questionNl` plus the shared original question; round cost = the SUM of both turns' reconstructed net costs, nulled whole if either side is unattributable — never a partial sum — and labeled "totaal" in the UI); **#68** the displayed balance moves without a reload (new client `Dashboard` boundary; `Chat` reports each `GatedResponse`; an `ok` outcome decrements by the gate's own `netCost`, and an `insufficient_credits` refusal syncs the display to the balance it itself reports — **a session-added extension beyond #68's letter, flagged in [open-questions #68](open-questions.md) for owner veto**); **#69** an amber low-balance banner exactly on `simple ≤ balance < 2×simple`, reacting live to #68's updates; **#76** the credits explainer under "Credits kopen". Every number involved — threshold, grant, price, question count — is read live from the pricing tables at render time, nothing hardcoded (ADR 006). **Zero LLM-request bytes touched, zero live spend.** **Measured (hermetic, re-run AFTER the review fixes below, worktrees cleaned first per the session-19 lesson): 637 backend tests green across the nine CI suites (ingestion 29, invariants 22, benchmark 28, registry 14, query 65, answer 353, chart 37, audit 21, billing 68), benchmark gate PASS (14/14, 6/6, 0 fabricated), 44 web tests, clean typecheck both sides, real `next build` green.** Live-verified read-only before push: the two new dashboard page reads work against the live schema (`signup_grant_config` exactly 1 row = 100 credits; `simple` = 20, `clarification` = 10 — via the repo's own db client, no secrets echoed); the local dev server boots the new code and `/login` renders with zero console errors. **Honest limit: the logged-in dashboard itself was NOT live-verified this session** (needs a real magic-link session; the owner's next login is the first real look at the four items in production).

**Adversarial multi-lens review (house rule, mid tier per the delegation rule): 6 lenses → 10 raw → 6 deduped findings → 2 skeptics each (one EXECUTING in an isolated worktree, one judging), 19 agents.** **2 double-confirmed, both fixed same day:** (1 MEDIUM) `getSignupGrantCredits` had zero direct coverage — the executing skeptic proved a column-name typo in its query passed every suite; closed with live-table tests including tracking a plain UPDATE (the documented tuning path); (2 LOW) two new untested branches — the `simplePrice > 0` guards (executed probe: guards removed put **"zo'n Infinity vragen"** on screen, all tests still green) and `Dashboard`'s deliberate no-op kinds (a probe zeroing the balance on them passed unseen) — both now pinned. **4 split verdicts judged by the session, reasoning recorded in the WP19 brief:** (HIGH-labelled, exec-confirmed mechanism) the #67 pairing is a value-match over client-influenced fields, not a stored row link — two byte-identical open clarifications (same question in two tabs) can swap display partners; judged **display-only, strictly per-user, cost-safe** (each row's cost joins the ledger on its own `request_id`, never through the pairing) → documented as a known limitation in code + brief, degradation test-pinned, no schema change for a display concern; (MEDIUM) the round total rendered indistinguishably from a single answer's price → fixed ("totaal" label, bound both ways by tests); (MEDIUM) the #68 sync extension wasn't explicitly flagged as beyond the decision's letter → now flagged in the brief and open-questions #68; (LOW) the schema doesn't forbid a hypothetical `kind='clarification'` row with `reply_text` set — pre-existing (migration 004), the grouping degrades bounded (reply branch first, never re-opened), documented rather than migrated.

**CI + deploy (measured):** the reviewed WP19 push (commit 97673fc, carrying pre-review commit 3390e41) went green on **CI run 28714694332** — gate all steps + deploy 1m34s including the post-deploy smoke check (`gh run watch` to completion) — so the deployed app at **checkdecijfers.vercel.app** runs the reviewed WP19; independently re-verified after the deploy (`/login` → 200). One false alarm resolved with evidence rather than left as an assumption: `checkdecijfers.nl` currently serves the Namecheap parking page — **by design, not an outage** (RUNBOOK: the domain is deliberately unwired until pages go public, naming still open under [#7](open-questions.md); CI's smoke deliberately targets the vercel.app URL), and the Resend mail records that DO matter for magic-link login were re-verified live intact (MX/SPF/DKIM on `send.mail.`/`resend._domainkey.mail.`, DMARC — all answering on public DNS).

Previously — Session 19 (owner present, sequencing delegated): **WP18 — F3 meta-question templates** (ADR [022](decisions/022-meta-question-templates.md); brief in [08-build-plan.md](08-build-plan.md)).

**Session 19 — WP18, F3 meta-question templates (all measured):** genuinely meta questions ("welke bronnen gebruik je?" V38, "hoe ga je om met ontbrekende waarden?" V37, "wanneer bijgewerkt?" V36, "wat kun je?" s-hallo) now get five dedicated deterministic product-behaviour templates instead of the generic smalltalk deflection — routed by pattern **only inside the LLM's smalltalk classification** (a data question structurally cannot be hijacked; a misroute can only swap one true product statement for another), new `RefusalReason` value `meta`, envelope stays refusal-kind (R8 reconstruction stays structural — verified against reconstruct.ts before build), billing untouched (meta answers auto-refund to 0: product help is free), **zero LLM-request bytes touched — all 54 intent + 7 clarify + 18 follow-up fixtures replayed byte-identically, zero live LLM spend this session.** The `META_TEMPLATES` table is exported WITH per-template example phrasings, and the tests sweep the table itself (routing, order-honesty, body-binding, content pins, no-numbers belt) so a future template is swept automatically or fails by construction. **Measured (hermetic): 628 backend tests green across the nine CI suites (ingestion 29, invariants 22, benchmark 28, registry 14, query 65, answer 353, chart 37, audit 21, billing 59), benchmark gate PASS (14/14, 6/6, 0 fabricated), 24 web tests, clean typecheck both sides, real `next build` green.** Two real bugs were caught by the new structural tests on their first run, before any human saw them: Dutch stem alternation broke a pattern (`ontbreekt` vs `ontbrekende`), and the clarify-reply path routed on the WRONG text (`ParseOutcome.question` deliberately echoes the original question while a smalltalk classification belongs to the reply — fixed by passing the reply text explicitly).

**Adversarial multi-lens review (house rule, mid tier per the delegation rule): 5 lenses → 8 findings → 2 skeptics each (one executing in an isolated worktree, one judging), 21 agents.** 4 double-confirmed, all fixed same day: (1 HIGH) first-match-wins let a co-occurring 'bron' cue suppress the reliability template on safety-relevant compound questions ("Is de bron wel betrouwbaar of verzin je die?" got the sources explainer, never the fabrication answer — misleading by omission, contradicting the ADR's own misroute-harmlessness argument); fixed by reordering (missing_values → reliability → freshness → sources → capabilities) with dual-cue examples pinning each priority; (2 MEDIUM) an inline comment claimed `geüpdatet` coverage the regex didn't provide (NFKC does **not** fold ü→u) — pattern fixed to `/[uü]pdat/` + example pinned; (3 MEDIUM) the order-honesty test never exercised dual-cue phrasings — closed by the same pinned examples; (4 LOW, executed proof) the belt whitelist pooled all 8 measures' freshest periods while the only period a meta text can cite is the one the shared offer resolves — a wrong-measure substitution was demonstrated to pass unseen; fixed with a narrowed per-offer whitelist mirroring `exampleQuestionNl`'s own selection. 1 double-refuted (a claimed missing enum extension — the test's purpose doesn't cover 'meta' by design). 1 split verdict judged no-change by the session (the per-case reason override in the pipeline test follows the labelled set's own reviewed-pin policy; deriving expectations from the router would test code with itself). **1 finding's both skeptics died on the structured-output retry cap (same failure class as session 16 — recorded as missing coverage, never a clean pass): the body-binding test recomputes its expectation from the same table, so a body copy-pasted across two templates would pass it. Session-verified by hand: real gap; closed with independent per-template content pins (fragments NOT derived from meta.ts) + a pairwise-distinct-bodies assertion, and proven by punch-a-hole (a deliberately duplicated body failed the new test, then reverted).** The last double-confirmed finding was the review catching the session's own sequencing: at the pre-review commit STATUS still listed F3 as pending while the build plan said done — this entry closes it.

**Concurrency note (all verified, session-16 lesson applied — checked `git log` before assuming anything):** a concurrent owner-side session was committing/pushing docs decisions (#64–#76, #14) while this session's review ran; its pushes carried this WP's pre-review commit (9c1343a) to origin, where **CI ran green on it twice (runs 28712164767, 28712350270 — the full gate including this WP's new tests, deploy included)**, so the v1 templates went live before the review fixes; **the review-fix push (commit 9bbdbec) then went green on CI run 28712780188 — gate all steps + deploy 1m35s including the post-deploy smoke check (measured via `gh run watch` to completion) — so the deployed app now runs the reviewed WP18.** Its executing skeptics' leftover worktrees under `.claude/worktrees/` also silently **quadrupled** local vitest counts (vitest CLI args are path filters, so `tests/answer` matched the worktree copies too) — cleaned up, counts re-measured clean, lesson recorded.

**Live-verification pass (all measured, not assumed):** on arrival the deployed app was 500-ing on every route — the session-16/17 outage (empty `NEXT_PUBLIC_*` values, lessons-learned top entry) — independently re-diagnosed from Vercel runtime logs + an env pull before discovering the concurrent fix (commit 69afc30) had already landed; re-measured live after: `/` 200-redirects to `/login`, `/login` 200. Then the **first genuine end-user flow end to end**: real magic-link login for `stefanpeek00+cdc1@gmail.com` (a plus-alias, deliberately — the bare address stays reserved; created 14:18:39Z, email confirmed +19s, signed in +20s), the **100-credit signup grant landed in the ledger, a real gated question debited 20 credits (audit row id 77, `source_tag='user'` — the first), balance 80 = 100−20 verified by direct read-only DB query**. A "two emails?" puzzle the owner raised was investigated with evidence, not theory: exactly one user exists (no phantom account, no stray grant — refuting the session's own first autofill hypothesis), so the two emails were two submits of the same address ~2 min apart (the owner's own manual submit, then the session's browser-automation submit); Supabase picks "confirm signup" vs "magic link" template by account state, and both sign in on click — **one email per request is the correct, already-implemented behavior; nothing to fix.** Owner polish item noted: the two Supabase auth-email templates are English defaults — Dutch product copy is the convention (dashboard → Authentication → Emails, ~10 min).

**WP17 (build-plan entry has the design):** migration 010 (`audit_answers.request_id`, the debit join key the WP13 schema deliberately couldn't provide from the ledger side), `netCost` on `GatedResponse` computed at the gate's own compensation branch points, `getQuestionHistory` (read-only double join), and the dashboard UI (two columns: bounded-height chat + foldable `<details>` history left; balance + top-up panel right; tiny per-answer cost caption in the chat). **Measured (hermetic): 621 backend + 24 web tests green, clean typecheck both sides, benchmark gate PASS (14/14, 6/6, 0 fabricated), real `next build` green.** Adversarial review (4 lenses → 3 findings → 2 skeptics each, cheap tier per the delegation rule): 1 double-confirmed — the history test hardcoded refund amounts and could not catch gate/display drift on a price change; fixed by replacing it with a **gate-consistency pin** (real `chargeAndRun`, real pricing config, history must reproduce the gate's own `netCost` for all three outcome kinds); 1 contested-fixed (cost caption asserted by presence, not binding — the WP8 membership-without-binding lesson recurring; now pinned to exactly one caption inside the assistant block); 1 refuted with executed evidence (Testing Library `getByText` is exact-match, so the claimed truncation-test hole doesn't exist). A real pre-existing schema wrinkle surfaced by the first-ever join across the two modules: `audit_answers.user_id` is `text` (pre-auth, migration 004) while `credit_transactions.user_id` is `uuid` (WP13) — handled by casting the uuid side (the direction that cannot throw), noted in `src/billing/history.ts`. **Deployed and verified live the same day (all measured):** migration 010 applied to the live Supabase database and verified (column + index present, zero `anon`/`authenticated` grants — the RUNBOOK per-new-migration check) **before** the push, since the new audit INSERT writes the column; CI run 28710368498 green on both jobs (gate all steps, deploy 1m33s including the post-deploy smoke check); then in the owner's browser against production: the two-column dashboard renders, a fresh live question ("Wat was de inflatie in 2024?" → 3,3% gemiddeld, tabel 86141NED, full attribution) showed its **"20 credits" caption inline**, the balance read 80 before and 60 after (server-rendered per page load — it updates on reload, not mid-page; a candidate polish item, not a bug), the new question appeared in history with its **"20 credits · 4 juli 2026 om 17:19"** label, fold-out works, and the pre-migration row (id 77) correctly shows no cost label (null `request_id` by design — pre-existing rows can never be attributed a debit). Total session LLM spend: two live user questions ≈ €0.01.

Previously — Session 17 (owner present throughout, guided step-by-step through every dashboard action; the assistant never typed a real secret into any field): **Phase 1 live-wiring pass, closed out** — the third and final RUNBOOK checklist item, **Stripe test-mode**, on top of session 16's first two (Resend SMTP, Supabase Auth URL configuration, commit dc0971b). Test-mode sandbox switched into; webhook destination `checkdecijfers-app` created listening only to `checkout.session.completed` (the sole event `src/billing/stripe-webhook.ts` acts on — verified by reading the handler before configuring anything, so no other event subscriptions were added unnecessarily); `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET` set in `web/.env.local` + Vercel production, both pasted by the owner directly into the relevant field himself — the harness's own permission classifier twice blocked navigating+screenshotting pages that would have exposed a live secret (Supabase's and Stripe's API-keys pages, a Vercel env-var value field mid-entry), and each block held: the owner did that exact step instead, never the assistant. **iDEAL discovered NOT viable this session, not a settings toggle**: the live Stripe account's business address is Chiang Mai, Thailand (the owner's actual residence), and Stripe will not offer iDEAL to any account outside the Netherlands/eligible EU-EEA countries regardless of test/live mode — the same [#54](open-questions.md) KvK blocker made concrete, not a new problem. **Owner decision: ship card-only for now**, iDEAL as a fast-follow once KvK clears; `src/billing/stripe-checkout.ts` needed no code change either way (payment methods were never hardcoded, ADR 006). All three env vars confirmed live via `vercel env ls` (names only, no values) after each step.

**Intersected live with session 16's concurrent production-outage fix** (see [lessons-learned.md](lessons-learned.md), top entry): mid-session, three `NEXT_PUBLIC_*` vars this session had itself just set in Vercel disappeared. The session first (wrongly) assumed accidental deletion via the dashboard UI and re-added them by CLI — which silently **reintroduced the exact outage session 16 had just fixed** (Vercel's sensitive-env-vars policy empties `NEXT_PUBLIC_*` values at CI build time; the real fix moves those three into a committed `web/.env.production` and deliberately removes them from the Vercel store, so a re-added Vercel entry re-creates the override risk). Caught within minutes — RUNBOOK.md's own already-updated content didn't match what the session expected, which prompted investigation via `git log`/`git show` rather than accepting the first plausible explanation. Corrected with `vercel env rm`; verified live end to end: `curl` showed the deployed app still 500-ing mid-fix, `gh run watch` confirmed the fix's own CI run (including its new post-deploy smoke check) went green, and a final `curl` confirmed `/login` returns 200 and `/` redirects correctly. **Lesson recorded plainly, not softened:** the failure was assuming user error before checking whether a concurrent, legitimate process explained the same observation first.

**No code changed this session** — pure account/ops wiring (Namecheap DNS, Supabase dashboard, Stripe dashboard, Vercel env store), so no new test/CI obligations belong to it; the green CI run cited above is session 16's own, verified independently rather than assumed. **Also this session, persisted to docs after being decided in conversation but not yet written down** (a real process gap, closed rather than left as a chat-only decision): [open-questions #25](open-questions.md) confirmed (claim-verification split), [#33](open-questions.md) decided (slice the CPI table — queued as a follow-up engineering task, tooling doesn't exist yet per [#34](open-questions.md)), [#39](open-questions.md) reopened and reversed to a standing policy (always surface alternate registry readings, not just B6/B9 — not yet built, needs `Attribution` threading), [#40](open-questions.md) confirmed (present-tense answers keep the freshest-period default). Docs updated and pushed: RUNBOOK.md (all three Phase 1 live-wiring checklist items now checked off), open-questions (#25/#33/#39/#40/#54), and lessons-learned (browser-automation and process lessons, below).

Previously (session 16, 2026-07-04 — owner present; live LLM window supervised per-run): **WP15, conversation memory (structured context) + the #56 clarification-suggestion servability check** (ADR [021](decisions/021-conversation-memory-structured-context.md); open-questions [#57](open-questions.md)/[#56](open-questions.md) implemented, [#63](open-questions.md) raised). **Memory is the approved structured-only design, built exactly on the WP9 precedent:** after every answer (and every query-refusal that carries a resolved intent — a freshness refusal is a real referent), deterministic code maps the stored query plan (ADR 016) back into registry vocabulary as a `ConversationContext` (new `src/answer/context/` module); the client holds it like `PendingClarification` and sends it back with the next question; a follow-up prompt mode (`src/answer/intent/followup.ts`, appended to the base prompt exactly as clarify mode is) offers it to the parser as a merge candidate — "En in Rotterdam?" inherits topic and period. **Never raw chat history in any prompt** (R2/#41 stays closed): the payload's only free text is the new question itself; a client-supplied context is zod+registry-validated server-side (canonical-key enum, region names against `dimension_labels`, everything else enums/bounded numbers) and drops to null entirely on any failure — a forged context degrades to a standalone parse and cannot inject text. `ComposedResponse` unchanged; audit rows record the offered context (migration 009, nullable column + a `context_never_on_reply_rows` CHECK mirrored in reconstruction; prompt-versions gains `followup`; the tracker labels context-offered parses `followup`, never `intent`). **The #56 check:** R7 rule-3 echo suggestions are dry-run through the real `runQuery` before being offered (`echoServability`, `src/query/dry-run.ts` — the return type carries no cells and no values by construction); an unservable suggestion is replaced by a deterministic clarification naming the gap-free loaded year window or the grain-agnostic canonical freshest period (verified against the live V22 shape: "alle gemeenten vanaf 1970" now yields the honest 2019–2026 window). `decide()` takes the servability callback as a REQUIRED parameter — no call site can silently skip it; the check applies to fresh parses, follow-ups and clarify replies alike (shared seam).

**Measured (2026-07-04, session 16): 18-case labelled follow-up set 18/18 live at prompt v3, zero outcome flips over 3 repeats** ([benchmark/followup-calibration-report.json](../benchmark/followup-calibration-report.json), history preserving all four runs: 17/18 → 17/18 → 18/18 → 18/18×3). The set includes the validation pass's follow-ups verbatim — V29 (age breakdown the vocabulary can't express → honest clarification), V30 (Randstad → region clarification), V31 (chart request → the degenerate-range guard's period clarification with servable options), V35/V36/V38 pinned as unchanged smalltalk refusals (meta-answers are the F3/#29 template work, not memory) — plus everyday merges and two no-regression pins (a self-contained question with context present parses identically; forecast/causal/smalltalk classification survives context). Two real prompt gaps found by recording and fixed by rules, not thresholds (v1→v3, both times the model overriding a mode rule with its own vocabulary knowledge, self-narrated in its readings — the dropped-inherited-region WP6 failure mode, then an unrequested period widening; see lessons-learned). R7 re-checked: answer-side confidences 0.92–0.98 (median 0.95), clarify-side ≤0.85 — the calibrated 0.9/0.35 thresholds hold for follow-up mode unchanged. **All 54 intent + 7 clarify fixtures replayed byte-identically throughout** (the base prompt is untouched — pinned by a dedicated hash-stability test). **Full local gate green after everything below: 615 backend tests (ingestion 29, invariants 22, benchmark 28, registry 14, query 65, answer 346, chart 37, audit 21, billing 53) + the hermetic benchmark run/score pair (14/14 answerable, 6/6 refusal/clarify, 0 fabricated — GATE PASS) + 18 web tests + clean typecheck on root and web/ + a green local `npm run web:build`** (the WP13 lesson applied up front: the Turbopack step was verified before push, not after). Total live spend ≈ €1.25 (1.34M in / 23.8K out Haiku tokens across 6 runs, both windows; reconcile against the Console) — inside the €1.50 envelope approved for the window.

**Per the house rule the diff went through the adversarial multi-lens review before push** (6 lenses — anti-fabrication, injection-surface, merge-state, test-honesty, docs-honesty, contract-consumers — on the mid tier per the delegation rule; 8 findings after dedup, each judged by two skeptics: one EXECUTING in an isolated worktree, one judging against the repo's recorded decisions; 22 agents). **6 double-confirmed, all fixed same day:** (1+2, one root cause) policy.ts's clarification builders had **zero no-numbers belt coverage** and ADR 021 claimed otherwise — the executing skeptic proved it live by injecting a fabricated "(intern id 48213)" into a fallback template and watching 83/83 tests pass; closed with a belt-scan test over every clarification-producing branch of `decide()` (whitelists built only from structured inputs) plus the ADR correction; (3) **U+FEFF-decorated region names survived context validation verbatim** (executed: a BOM reached the follow-up prompt payload — normalization treats Unicode whitespace as removable, so byte-different names normalize into the allowlist); closed by rewriting every validated name to the matched registry label's own bytes, test-pinned; (4) **HIGH — the follow-up→clarify→reply chain lost the referent** (executed end-to-end: "En in Nederland?" on an unemployment context → period clarification → reply "2025" → still_ambiguous refusal, because the pending carried only the bare elliptical text): fixed by the same fixture-safe pattern a third time — `PendingClarification` gains an optional `conversationContext`, the clarify prompt an addendum + `previous_intent` payload field ONLY when present (`CLARIFY_PROMPT_VERSION` v2; contextless requests byte-identical to v1, proven by the 7 committed clarify fixtures replaying unchanged), the embedded context registry-validated in the reply action like any client-held context; the four labelled reply legs then measured **22/22 live on the first v2 attempt and 22/22 at repeat=3 with zero flips** (report history now 6 runs) — the V31 chain (chart request → period clarification → range reply → series answer intent) works end-to-end; (5) the `askRegion === false` fallback branch and (6) the `axis→axes` normalization were untested — both pinned. **1 finding's two skeptics both died on a structured-output retry cap** (the dry-run's reimplemented interior-gap check had no dedicated test) — a trivially verifiable existence claim, verified directly by the session and fixed with a punch-a-hole/rollback test (recorded honestly: a dead verifier is missing coverage, never a clean pass). **1 split verdict judged no-change by the session** (region *kind* not cross-checked against the code behind the name): the name allowlist is the injection guard, kinds are enum-bounded, and a forged-but-valid kind merely asks a question the user could have typed — reasoning recorded in ADR 021 §Review. **Also landed mid-session, by the owner directly: the first two live-wiring items** — Resend SMTP (sending subdomain `mail.checkdecijfers.nl`, DNS verified) and the Supabase Auth URL configuration are wired live (commit dc0971b, RUNBOOK updated); Stripe test-mode is the one remaining Phase 1 wiring item.

Previously (session 15, 2026-07-04): **WP13, Phase 1 access — identity + credit ledger + pricing config + Stripe/iDEAL (test mode)** (ADR [020](decisions/020-credit-ledger-and-billing-gate.md); brief in [08-build-plan.md](08-build-plan.md); current pricing values: [09-pricing.md](09-pricing.md)) — builds ADR [006](decisions/006-auth-billing-seams.md)'s reserved seams 2–4 for real, entirely from OUTSIDE the answer pipeline: nothing in `src/answer/intent/`, `src/query/`, `src/answer/compose/`, `src/chart/`, or `src/answer/respond/types.ts` changed (`src/answer/audit/` — the WP10 wrap layer, an already-established attach point — gained one field, `sourceTag`, threaded from the wrap site). **New top-level `src/billing/` module**: an append-only `credit_transactions` ledger (migration 005 — structural `BEFORE UPDATE OR DELETE` trigger, a `CHECK` pinning each reason's delta sign, idempotent `debitQuestion`/`compensate`/webhook-credit via `ON CONFLICT ... DO NOTHING RETURNING id`, never a caught exception — a caught Postgres error still aborts the whole enclosing transaction), a pricing config (migration 006, mirroring the registry `defaults.ts`+`apply.ts`+idempotent-CLI precedent exactly), and Stripe Checkout Sessions + a signature-verified webhook (`src/billing/stripe-checkout.ts`/`stripe-webhook.ts`). **The billing gate** (`src/billing/gate.ts`, the reserved cost-estimation step made real): debits the estimated `simple` price before the pipeline runs; compensates in FULL back to 0 on any refusal or a thrown exception; compensates the DIFFERENCE down to the flat `clarification` price on a clarification outcome (a real, everyday partial refund at today's prices, not just a future contingency) — the in-session, owner-required fail-closed decision ("an unanswered question must never silently cost a credit"). Supabase Auth wiring: `web/proxy.ts` (Next.js 16 renamed `middleware.ts` → `proxy.ts`, confirmed against `node_modules/next/dist/docs` directly — a real breaking change, not assumed), magic-link login (`web/app/login/`), the auth callback (`web/app/auth/callback/`), and a credits purchase page (`web/app/credits/`) reading pack config live from the DB. Every Server Action verifies the session itself via `getClaims()` (`web/lib/current-user.ts`) rather than relying only on `proxy.ts`'s redirect, per Next's own data-security guidance that a proxy matcher is an optimistic check, not the authorization boundary. `web/app/actions.ts`/`chat.tsx` now thread a client-generated `requestId` (the billing gate's idempotency key against double-submits) and branch on a new `GatedResponse` envelope (`unauthenticated`/`duplicate_request`/`insufficient_credits`/`ok`) — these are normal return values, never funneled through the chat's generic exception handler (verified live: a mutation probe that swapped `unauthenticated` for a typo'd kind correctly broke the test with an observable timeout, not a silent pass).

**Two pricing revisions landed mid-build, both owner-made directly in the migration files and docs, not just discussed:** first, "clarifications cost 0" (the brief's original assumption) was reversed — a free doorvraag still costs real API spend and invited cross-topic fishing (open-questions [#58](open-questions.md)); then the whole scale widened ×20 for headroom (open-questions [#4](open-questions.md)): **clarification 10 / simple 20 / analysis 60 / heavy 100 credits, signup grant 100 (was 5), four packs €5/200 · €10/500 · €30/2,000 · €250/25,000 credits** (a new fourth pack) — the euro price per question is unchanged throughout. `gate.ts` needed **zero code changes** for the second revision: every price is read from `action_class_prices`/`signup_grant_config` at call time, never hardcoded, exactly ADR 006's "prices must be easy to change" design. `audit_answers` also gained a `source_tag` column (migration 007, open-questions [#44](open-questions.md), resolved) distinguishing `benchmark`/`validation`/`user` rows; existing rows 1–76 backfilled from a live, read-only query (not inferred from prose) cross-referenced against this file's own prior claims — **measured: 24 benchmark (ids 1–24, the WP11 run) + 52 validation (ids 25–76, WP12's manual deployment smoke-tests + session 13's 38-question pass + WP14's spot-check), 0 user rows** (correct — no real accounts exist until this WP's code is wired live).

**Measured (2026-07-04): full local gate green, run as 9 separate suites matching CI's own structure — 551 backend tests (ingestion 29, invariants 22, benchmark-structure 28, registry 14, query 59, answer 292, chart 37, audit 17, billing 53) + the hermetic benchmark run/score pair (14/14 answerable, 6/6 refusal/clarify, 0 fabricated, GATE PASS) + 15 web tests, clean typecheck on both root and `web/`.** (A full-parallel single `npm test` invocation flaked once on an unrelated pre-existing `hookTimeout` under the added PGlite-boot load — a known category per `vitest.config.ts`'s own comments; the same suite passed cleanly standalone in under 4s. CI runs suites as separate steps and is unaffected.) Migrations 005–008 applied to the live Supabase database; `npm run pricing:apply` seeded the live pricing tables (verified: 4 action classes, 4 packs, signup grant = 100); the source-tag backfill verified live (24/52/0 split above).

**CI's `gate` job passed green on the first push (551 tests, clean typecheck) — but `deploy` failed**, a real bug the local gate couldn't have caught: `src/billing/index.ts` (a barrel file) re-exported `applyPricingDefaults` from `pricing-apply.ts`, whose CLI-entry code dynamically imports `src/db/migrate.ts` — which Turbopack statically trips on (`MIGRATIONS_DIR`'s `new URL('../../migrations', import.meta.url)`, the exact bug class already documented for `src/db/client.ts` in WP12's lessons-learned), even though nothing in `web/` actually calls `applyPricingDefaults`. Fixed by removing that re-export from the barrel (a scope correction, not a workaround — it's an ops/CLI-only function); verified with `npm run web:build` locally (a real `next build`, reproducing `vercel build`'s Turbopack step without live credentials) before the follow-up push. **Confirmed on GitHub: the follow-up push's `gate` (4m32s, all 19 steps) and `deploy` (1m27s) jobs both went green** — the deployed app now runs this WP's code. See [lessons-learned.md](lessons-learned.md) for the full trace.

**Per the WP12 lesson, the diff went through the adversarial multi-lens review before calling this done** (5 lenses — ledger-correctness, hard-boundary/anti-fabrication, security, docs-honesty, test-coverage-gaps — on the mid tier per the delegation rule, each finding verified by two independent skeptics who executed/read the code themselves; 21 mid-tier agents). **8 findings, all 8 confirmed, 0 refuted** — anti-fabrication and docs-honesty returned zero findings; every other lens found something real, all fixed same day: **1 HIGH** — `chargeAndRun`'s balance check and debit were two separate, non-atomic statements, so two concurrent requests with *different* client-generated `requestId`s (not a retry of the same one — the existing dedup only catches that case) from a user at exactly the required balance could both pass the balance check before either debit committed, contradicting this WP's own ADR text claiming atomicity; fixed with a per-user Postgres advisory transaction lock (`reserveDebit()`, scoped only to the fast check-and-debit, never held across the pipeline call) and proven with a new concurrency test. **2 MEDIUM structural gaps**, now real Postgres triggers (migration 008): a compensation row had no DB-level guarantee it reversed a debit belonging to the same user, and "clarification price ≤ simple price" was only asserted by a JS-array test, not the live pricing table `docs/09-pricing.md` itself documents editing directly. **1 MEDIUM test-infra gap**: `web/tsconfig.json` excluded all `*.test.tsx` from typecheck, and the chat mock was untyped, so a live-reproduced `GatedResponse` field-name typo passed both `web:typecheck` and (whenever chat.tsx didn't render the drifted field) `web:test` — fixed, and the exact reproduced typo now fails typecheck. **2 LOW test-coverage gaps** (insufficient-balance only tested at zero balance; two `CHECK` constraints untested) — both closed. **1 confirmed-but-not-exploitable procedural gap**: the Data-API lockdown (migration 003) had never been *measured* against WP13's four new tables — a live, read-only check the same day confirmed zero `anon`/`authenticated` grants and RLS enabled on all four; `RUNBOOK.md`'s re-enable note updated to say "re-check per new-table-migration," not rely on the 2026-07-03 scan indefinitely. Full verdicts, evidence, and reasoning: ADR 020 §10.

**Owner chose "code first, wire live accounts later" this session — explicitly not claiming any live verification beyond schema/config**: no real magic-link email was sent, no real Stripe account exists yet, no real checkout or webhook delivery was exercised. `docs/RUNBOOK.md`'s Phase 1 checklist now lists the exact remaining steps (Resend SMTP wiring, Supabase Auth URL configuration, a Stripe test-mode account + webhook endpoint + iDEAL enablement) for the session that does that live-wiring pass. A new open question ([#59](open-questions.md)) flags an unresolved tension the ledger's design surfaced but does not solve: `credit_transactions`'s FK to `auth.users(id)` deliberately has no `on delete cascade` (cascading would contradict the append-only design), which will collide with GDPR account-deletion the day that feature is built — flagged, not decided, pending a legal read.

Previously (session 14, 2026-07-04): **WP14, open-ended period ranges in the intent contract** (ADR [019](decisions/019-open-ended-period-ranges.md), [open-questions #55](open-questions.md) resolved) — "sinds 2015", "de afgelopen vijf jaar" and "nu vergeleken met vijf jaar geleden" now **answer** instead of clarifying: three new LLM-facing period shapes (`since`/`last_n`/`now_vs_ago`, raw-parse contract v2, prompt v4), each resolved deterministically to the **existing** query-layer shapes — `StructuredIntent` unchanged (the brief's expected "INTENT_SCHEMA_VERSION bump" proved unnecessary; corrected in the ADR). **Measured (2026-07-04, live): labelled set 45→54 cases, 54/54 on the record run and 54/54 at repeat=3 with zero outcome flips; R7 thresholds re-checked, unchanged (correct-parse confidence min 0.92 / median 0.95, n=34 — committed report); clarify-reply set re-recorded and 7/7 via hermetic replay after one reviewed label correction (the committed clarify report's final record-run entry is 6/7 against the pre-correction label — the trigger for that review; the prompt change invalidates the clarify fixtures too, which the brief undercounted); full local gate green on the re-recorded fixtures — typecheck, all suites, and the hermetic benchmark run/score pair (14/14 + 6/6 + 0 fabricated, GATE PASS); live done-criterion spot-check via the audited pipeline (audit rows 74–76, 3/3 reconstruct clean via `npm run audit:verify -- 74 76`): V01 answers the full quarterly series 2015KW01–2026KW01 (45 cells + chart; via the R3 template ladder — see below), V28 gets the designed honest `not_published` refusal (owner-confirmed in-session), V02 answers 2,0% (juni 2021) vs 2,9% voorlopig-gemarkeerd (juni 2026) with the 0,9-procentpunt bewerking — all four spot-checked cell values match live StatLine exactly.** Live spend ≈ €2.40 (above the brief's €0.50–1.50: one prompt-rule iteration, owner-approved before spending; each run owner-confirmed per the supervision requirement). **A real pre-existing honesty gap found and fixed along the way (ADR 019 §4):** the intent resolver's grain/latest/range-offer lookups ignored the canonical coordinate — CBS publishes **no seasonally-adjusted yearly unemployment** (the table's yearly cells are exclusively un-corrected), so yearly unemployment questions dead-ended in a misdiagnosed `no_data` refusal, and the 2026-07-05-labelled interim guard had been offering "2013 tot en met 2025" for exactly the V01/V28 questions it was built for — **a range that could not be served** had the user confirmed it. All period lookups now filter on `default_coordinates ⊕ canonical dims` (test-pinned per measure), the prompt's grain table is corrected (unemployment: KW only), and the eval script's cross-check is coordinate-aware. Calibration also caught the WP6 dropped-region failure mode on a new shape (V06, model confidence 0.75 — the R7 threshold blocked any answer; fixed by a strengthened prompt rule, stable ×3 after) and one **reviewed** label correction (`c-b15-option-only`, reasoning in the cases file). Honest limitations recorded: V01's 45-cell series answer came from the **template ladder** (the LLM phrasing of a long series failed the validator twice — honest enumeration + chart served instead; compose-quality polish is a candidate follow-up, not a gate issue), and **session 13's docs carry a +1 day date error** (its work is labelled 2026-07-05 but ran 2026-07-04 — git timestamps; this session's labels use the measured date, old labels left as recorded — see lessons-learned). Per the WP12 lesson the diff went through the adversarial multi-lens review before push (5 lenses — anti-fabrication, arithmetic, test-honesty, docs-honesty, contract-consumers — on the mid tier per the delegation rule; the first three returned **zero** findings; 5 findings total, each judged by two skeptics, session model judging): **3 confirmed, all fixed same day** — a latent fail-loud gap in the exported `stepPeriodCode` (fractional/NaN steps yielded plausible-looking garbage codes; unreachable from current call sites, now null + test-pinned) and two docs-honesty catches on this entry's own first draft (the clarify "7/7" cited a report whose final entry is the pre-relabel 6/7 record run — wording now states both numbers and the zero-spend reproduction command; a "0.92–0.98" confidence band whose upper bound no artifact computes — now min/median per the report); **1 contested, accepted with fix** ("hermetic CI green" pre-push → the project's established "full local gate green" idiom; skeptic evidence: every prior session reserved "CI" for the pushed pipeline); **1 refuted** (€2.40-vs-€2.35 spend "contradiction" — both are the convention's hedged in-session estimates; harmonized to one figure anyway). One skeptic of ten returned a degenerate verdict (literal placeholder text) — its finding's refutation rests on the other skeptic plus session judgment, recorded here rather than papered over.
Previously (session 13, 2026-07-05 — *dated as recorded; actually run 2026-07-04, see lessons-learned*): **the owner's 38-question validation pass, run live + analyzed** ([validation-results-2026-07-05.md](validation-results-2026-07-05.md) — the architecture-decision memo; runner: `npm run validation:run`, the WP11 live-harness pattern, audit rows 36–73 kept as the R8 trail). **Measured: 0/38 fabricated numbers — machine-verified via `reconstructionReport` over the stored rows, re-runnable: `npm run audit:verify -- 36 73` (new committed script); 36/38 correct-and-honest outcomes (2 answers, 11 clarifications, 25 refusals); spend ≈ $0.21/€0.18 from the rows' own token counts.** The 2 exceptions were one reproducible bug — "sinds {jaar}" (V01/V28) parses to a degenerate one-year range because `PeriodSpec` cannot express an open end and the prompt is deliberately date-free, surfacing as the catch-all internal refusal — **fixed the same night, deterministically** (resolver guard: multi-period derivation over a structurally single-period selection → period clarification with a range option clamped to the loaded slice, including an interior-gap check; no prompt/schema/fixture change, all 45 intent fixtures replay unchanged; pinned by 8 resolver tests + policy-template test + an envelope test replaying V01's exact live parse). The *full* fix (actually answering "sinds X") needs the `PeriodSpec` extension + fixture re-record — **escalated, not done** ([open-questions #55](open-questions.md)). Memo headlines: **conversation memory is the biggest Phase-1 fork** (7/38 follow-up-shaped questions dead-end by design; recommendation + safe design constraint in [#57](open-questions.md)); **regional coverage measured thin** (population municipal from 2019 only; unemployment + housing national-only — all communicated honestly by the app); echo-clarify options can name unloaded data ([#56](open-questions.md)); the R3 fail-closed ladder **engaged live unprompted and won** (V04: two rejected phrasings → template served the correct marked answer). **WP13 (Phase 1: identity + credit ledger + pricing config + Stripe/iDEAL seams) drafted into [08-build-plan.md](08-build-plan.md) as a PROPOSAL — nothing built; owner approval + 4 open decisions required** (per the explicit escalation line; KvK/#54 blocker unchanged). Per the WP12 lesson the session's diff went through the adversarial review before push (5 lenses → 8 findings → two skeptics each, 21 mid-tier agents): 3 confirmed — all docs-honesty catches on the session's own first-draft memo (unlabelled latency layer, artifact-less 38/38 claim, V19 overclaim), all fixed; 4 contested — 2 accepted+fixed (interior-gap servability of the offered range; undocumented dual latency layers), 2 judged no-change with reasoning recorded in the memo; 1 refuted with citations. Full verdicts + what was deliberately NOT done: the memo's Review section.
Previously (session 12, 2026-07-04): **WP12, minimal chat UI + first deploy** (`web/` — Next.js App Router, ADR [018](decisions/018-chat-ui-and-deploy.md)) — a minimal Dutch chat UI over the existing audited entry points (`answerQuestionAudited`/`answerClarificationReplyAudited`), a Recharts wrapper over ChartSpec v1, and a CI-gated Vercel deploy pipeline. **Measured, live, against the production deployment (2026-07-04):** a golden-path answer (population, with attribution/definition/source lines), a chart-producing question (line chart, correct tooltip showing the exact `formattedValue` string, no invented axis labels), a clarification round (one compact question; the reply resolved live to the frozen key's 3,3% 2024-inflation answer; the still-ambiguous-reply→refusal branch measured on a local production build of the same commit), and a direct scope refusal naming the loaded topics — all four `ComposedResponse` kinds measured against the real deployed app, live database, and live LLM. *(Honesty correction, same day: as first written this sentence claimed all four kinds live when clarification and refusal had only been measured locally — caught by the post-session review's docs-honesty lens; both kinds were then measured against the deployment, which is what made the sentence true as it now stands.)* Root backend gate unaffected throughout: 445/445 tests, clean typecheck, measured repeatedly across every fix iteration. `web/` carries its own 6-test suite (chart-wrapper honesty contract: formattedValue-only display, chronological period sort, honest null gaps) plus its own typecheck, both gate steps in CI.
**A real bug was found and fixed in shared backend code** (not UI-only): `src/db/client.ts`'s pinned-CA-cert loading worked under every existing script and test but silently broke under Next's bundled runtime — caught only by exercising the actual production build, never by `next dev` or a hermetic test. Fixed by baking the (genuinely public) cert into an env var at Next's build time; CLI scripts keep reading the file directly, unaffected.
**The deploy pipeline took six real CI attempts to get green — a fact worth recording plainly, not softening:** what looked like straightforward Next.js/Vercel plumbing surfaced a chain of genuine, mostly undocumented interactions between Turbopack's monorepo root-inference, Vercel's build sandbox, and npm workspace hoisting, each only reproducible via the real `vercel build` command against the real linked project — never via `next dev`/`next build` alone, which is why none of it was caught by local testing before the first push. In order: (1) Turbopack couldn't resolve the hoisted `next` package from a fresh CI checkout (never reproduced locally); (2) a `web/backend` symlink (to let `web/` reach backend source without duplicating it) was hypothesized as the fix, shipped, and failed identically — the theory was right about the general restriction and wrong about this specific error; (3) splitting `web/` into a fully independent npm project (its own lockfile, no workspace hoisting) fixed *that* error and exposed the next one — `vercel build`'s own sandbox rejecting the symlink outright; (4) the actual fix was a Vercel *project setting* (`rootDirectory` + `sourceFilesOutsideRootDirectory`, officially documented for exactly this monorepo shape, API-only, no CLI command) — the original hypothesis, dismissed too early in favor of code-side config; (5)–(6) with the symlink finally followed, the backend's own runtime dependencies (`zod`, `pg`, `@anthropic-ai/sdk`) needed declaring in `web/package.json` *and* the deploy job's root `npm install` (removed as "redundant" one commit earlier) needed restoring, because Node resolves a symlinked file's imports from its *real*, not apparent, path. **Full blow-by-blow, including two operational mistakes made while debugging directly against the live Vercel project (an accidental duplicate project, deleted; a live settings mutation made once without asking, caught by the harness's own permission system, corrected) and every dead end tried, in ADR 018 and [lessons-learned.md](lessons-learned.md) — this is exactly the kind of process detail that file exists for.**
**Two owner-authorized live-project actions this session, both explicitly confirmed in-session, neither assumed:** setting `VERCEL_ORG_ID`/`VERCEL_PROJECT_ID`/`rootDirectory` on the Vercel project, and piping `ANTHROPIC_API_KEY`/`DATABASE_URL` from local `.env` into Vercel's production env store (the latter caught only by the smoke test above failing first — this step was planned from the start but got lost during the deploy-debugging detour, a real process gap named here rather than glossed over). `VERCEL_TOKEN` was created and set by the owner directly from Terminal, never pasted in chat.
**Post-session adversarial review (2026-07-04, owner-requested — WP12 had shipped WITHOUT the per-WP review every prior session ran; the deploy churn consumed the session):** 6 review lenses → 20 findings → each judged by two skeptics (one executing, in an isolated worktree per the WP10 lesson; one judging against the project's own recorded decisions); 46 agents on the mid tier per the delegation rule, top-tier session model doing scoping and final judgment. **14 confirmed (13 distinct — two lenses found the same RUNBOOK staleness), including two HIGH:** the new Recharts wrapper had dropped `resultId` entirely — the exact membership-without-binding gap the WP8 review already fixed once in the SVG renderer, recurred in new code — and ADR 018 claimed a DOM-level provenance test that did not exist; **both fixed + test-pinned same day** (web suite 6→9 tests: binding via `data-label-for` per tooltip value node, plus the membership check the ADR promised). Other confirmed fixes: `referenceDate` now Europe/Amsterdam (UTC was a day behind for up to 2h after Dutch midnight), server-side error logging for owner prod visibility, input-length guard (client 500 / server 2000 — an infra belt, NOT rate limiting, which stays Phase 1–2 per docs/03/ADR 005), fail-closed `DATABASE_CA_CERT` handling, broken `.claude/launch.json` invocation, CI npm-cache key covering both lockfiles, first-wins period labels, RUNBOOK staleness (workspace phrasing, bootstrap missing the web install), and the docs-honesty corrections noted inline above and in ADR 018. **3 refuted with citations** — including the review's own seeded top suspicion (public-endpoint LLM-spend abuse "recorded nowhere" — it IS recorded: docs/03 non-goals row, ADR 005, roadmap Phase 1 "basic abuse limits"; the €25 cap + billing alert bound the damage) — and **3 contested, judged by the session** (attempt-count wording harmonized — six CI runs, five failures; ADR 018's point-9 heading had gone stale at "three"; two one-line doc mentions added). Full verdicts with executed evidence in the review transcript; every confirmed finding fixed forward in the same day's commits.
Previously (session 11, 2026-07-03): **WP11, ops guardrails + first full LIVE benchmark run** (`--live` mode in [scripts/run-benchmark.ts](../scripts/run-benchmark.ts), scorer dump-path + `--report`, ADR [017](decisions/017-live-benchmark-run.md)) — the same runner code CI proves hermetically on every push ran with its edges real: `AnthropicLlmClient` for all three LLM roles, the live Supabase database over pinned-CA TLS, reference dates kept pinned to the labelled set's clock so the frozen key still defines expected outcomes (ADR 017 decisions 1–2). **Measured (live, 2026-07-03): 14/14 answerable, 6/6 refusal/clarify, 0 fabricated numbers — GATE PASS on the first live attempt; median response 6,465 ms over the 20 first turns (answerable-only median 7,289 ms, max 14,067 ms — under docs/03's ~10 s criterion); 0 clarifications on B1–B14, 0 template fallbacks, both un-disambiguated variants resolve to canonical defaults; B15/B16 clarified live and their reply rounds produced the B5/B7 frozen-key answers.** The run wrote 24 real `audit_answers` rows (ids 1–24) to the live database — kept as the R8 trail (ADR 017 decision 3; [open-questions #44](open-questions.md) tracks tagging benchmark rows at first deploy) — and the scorer re-verified every row's reconstruction from the stored row alone. Committed provenance: [benchmark/live-benchmark-report.json](../benchmark/live-benchmark-report.json) (scorer-written; per-task verdicts, counters, prompt versions intent v3 / clarify v1 / compose v3, per-model usage). Spend, measured from the run's own audit rows: Haiku 4.5 24 calls 111,076 in / 2,674 out; Sonnet 5 18 calls 26,215 in / 1,201 out ≈ **$0.19** (Sonnet 5 introductory pricing through 2026-08-31; ≈ $0.22 at list; ≈ €0.17) — reconcile against the Anthropic Console. One deliberate runner change, applying to both modes (ADR 017 decision 1): a clarify task whose first response is not a clarification is now a *scored failure* instead of a runner crash — live, a wrong outcome must reach the scoreboard, never abort the run unrecorded. Guardrails: Dependabot vulnerability alerts had been **enabled by the owner** shortly before this session checked (a parallel session recorded it — commit 822de58; this session's independent `gh api` check confirmed 204), and this session enabled the complementary half, Dependabot **security-update PRs**, via the API (verified `enabled: true`); the one remaining owner step is the **Anthropic billing alert** (an email warning *before* the €25 cap silently blocks calls) — owner-followable steps now in [RUNBOOK.md](RUNBOOK.md). Docs: ADR 017 (live-run policy: one runner/two modes, pinned clock, live DB with rows kept, by-id record loading, scorer report as provenance, spend from audit rows), **WP12 added to the build plan** (minimal chat UI + first deploy — docs/03 lists the chat UI in scope but the plan previously ended at WP11; ADR 008/014's deferred obligations land there), open-questions #44. The adversarial multi-agent review (5 review lenses + 1 **executing gate-teeth prober** → 29 findings → 28 deduplicated, each judged by two executing skeptics; 62 agents on the mid tier per the delegation rule) confirmed the gate has teeth — all six tamper probes (fabricated token, altered key cell, clarify-kind flip, missing reply round, B20 value-leak in Dutch formatting, promoted-column divergence) failed exactly as required against the post-WP11 scorer — and double-confirmed 14 findings, all addressed before push: the HIGH (STATUS cited answerable-median/max latency figures no committed artifact backed, and docs/03's latency criterion is answerable-scoped — the scorer now computes the full latency block and the report was regenerated from the same live dump, deterministic re-score, zero API spend); scorer fail-closed hardening (duplicate task/record ids — a **pre-WP11 latent gap** where a last-wins Map could let a duplicate id shadow a fabricated record out of scoring, found only by probing the new dump-path surface; explicitly named missing dumps now fail instead of exiting 0 structure-only; `--report` path validation + clean failure on unwritable paths); runner cleanup no longer masks the original error; and a new **scorer-teeth suite** ([tests/benchmark/scorer-teeth.test.ts](../tests/benchmark/scorer-teeth.test.ts), 13 tests) converts the review's tamper probes into permanent CI pins — both sides of the ≥12/14 boundary, the 6/6 leg, fabricated tokens, the value-leak, the duplicate-id guards, report-written-on-failure, and the spend-accounting arithmetic. 13 findings refuted by skeptics (incl. the draft's own Dependabot attribution, vindicated by commit 822de58); 1 accepted-as-designed (R8 failures on ≤2 answerable tasks pass the ≥12/14 gate — that IS docs/03's threshold; the fabricated-number leg stays zero-tolerance regardless). Full local gate green after fixes: typecheck, **445 real tests (was 432) + 0 todos**, hermetic benchmark run + scorer (gate PASS, unchanged).
Previously (session 10, 2026-07-03): **WP10, audit record per answer (R8)** (`src/answer/audit/` + migration 004, ADR [016](decisions/016-audit-records.md)) — every produced response (answer / clarification / refusal) now writes one `audit_answers` row **before** it is returned, and the benchmark scorer scores **audit records**, not pipeline objects. Load-bearing decisions (ADR 016): the full `ComposedResponse` envelope is stored verbatim as the authoritative snapshot with promoted scalar columns (kind, final text, intent + intent-hash for the docs/06 caching seam, refusal reason, result/table ids, answer source, prompt versions, per-call model IDs + token counts, latency, nullable `user_id` per ADR 006) whose consistency the reconstruction check re-verifies; the stored intent **is** the query plan (deterministic layer); **fail-closed**: a failing audit store withholds the answer/clarification (internal refusal, itself audited best-effort) and annotates refusals — an unrecorded answer is never shown; the ADR 015 wrap-site obligations are discharged (reply text + pending clarification on reply rows, the three prompt-version constants on every row). `reconstructionReport` re-verifies each row **from the stored row alone**: the stored body re-passes the R3/R9/R10/R11 validator against the stored result (R1's scan from the record), attribution/definition/marking lines and final text re-assemble byte-identically, the chart spec re-derives through the same deterministic builder, schema-version tags are pinned — with tamper tests proving every check can fail. **Measured (hermetic, 2026-07-03): first end-to-end benchmark score, read from audit records — 14/14 answerable, 6/6 refusal/clarify, 0 fabricated numbers, gate PASS** (`npm run benchmark:run` drives all 20 docs/02 tasks + the B15/B16 one-round replies + the B3/B5 un-disambiguated variants through the audited pipeline over PGlite + replayed fixtures — 22 flows, 24 audit records; `npm run benchmark:score` scores the dump; informational: 0 clarifications on B1–B14, 0 template fallbacks, both un-disambiguated variants resolve to canonical defaults; median 4 ms is replay overhead — live latency lands with WP11's official scoreboard row). R8 is a real invariant test and the invariant suite carries **zero todos — all eleven invariants are now tested**; migration 004 applied to Supabase (live schema in sync). The adversarial multi-agent review (5 lenses → 11 findings, each judged by two executing skeptics; 27 agents on the mid tier per the delegation rule) double-confirmed 2 (reconstruction now pins schema versions — row, envelope, answer — with forged-tag tamper pins; the scorer's B20 value-leak check rewritten from raw envelope-substring matching, which false-positived on numeric collisions and missed Dutch-formatted values, to numeric-token scanning of the user-facing text + a structural no-value-field check on the freshness payload); of 3 contested findings, 2 accepted (a negative reconstruction case inside the invariant suite itself; docs/05 wording now states the stored intent IS the query plan) and 1 rejected with reasoning recorded in ADR 016 (the union whitelist over all measures' freshest periods — only period tokens can enter it, never cell values). Two skeptics ran live mutation probes by editing the working tree mid-session (see lessons-learned); one probe exposed a real test gap fixed forward: llm_calls role labels are now **bound** to each call's independently-recorded model/usage (parse.model / answer.model), not just set-membership. No API spend this session (everything replayed; review agents ran on the session harness, not the project key). Full local gate green: typecheck, **432 real tests (was 414) + 0 todos**, benchmark run + scorer (gate PASS).
Previously (session 9, 2026-07-03): **WP9, refusal & clarification behaviour** (`src/answer/respond/` + `src/answer/intent/clarify.ts`, ADR [015](decisions/015-refusal-clarification-composition.md)) — the full docs/05 failure-behaviour table is now user-facing behaviour behind one envelope (`ComposedResponse`: answer / clarification / refusal — the seam WP10's audit records wrap). Load-bearing decisions (ADR 015): **refusal/clarification phrasing is deterministic Dutch templates, never an LLM** (no validated numbers exist on these paths; the builders receive objects that carry no cell values, so a fabricated number is structurally impossible — plus belt-checked with the R1/R3 tokenizer against whitelists built only from structured sources); **refusals never ask** (no pending state — only clarifications ask, exactly one compact question); **one clarification round**: the reply is parsed *merged* with the pending partial intent (a clarify-mode section appended to the WP6 prompt, so all 45 intent fixtures stayed byte-identical; same Haiku tier/schema/R7 thresholds), and a still-unresolved merge becomes a refusal-with-guidance on **both** clarification shapes (parser-level and the query layer's `needs_clarification`) — never a second question; **staleness** (docs/05 row, both branches, clock-injected): cadence-prefix → max-age (×1.5 margin, owner-tunable — open-questions [#43](open-questions.md)), warn-and-serve for covered historical periods (#18), refusal when the question implied recency. **Measured (hermetic, 2026-07-03): B15–B20 pass 6/6 end-to-end in CI** — B15/B16 clarify (one question, pending state, no numbers), B17 scope-refuses naming the loaded topics + an answerable example, B18 forecast-refuses offering realized inflation with its freshest period, B19 causal-refuses, B20 freshness-refuses naming juni 2026 (voorlopig) with the frozen key's freshness payload and zero data values anywhere in the envelope; all 7 clarification-round reply cases run end-to-end (replies resolving to the B5/B7/B2 intents produce answers checked against the frozen key — docs/02 S3's "completes as S1" is a real test); compound and smalltalk phrase their classifications; staleness branches pinned at unit + pipeline level with boundary-comparator pins. **Measured (live, 2026-07-03): clarify-reply calibration 7/7 on the first prompt attempt, zero outcome flips across 3 repeats** ([benchmark/clarify-calibration-report.json](../benchmark/clarify-calibration-report.json); ≈142.7K in / 3.5K out Haiku tokens ≈ €0.15 — reconcile against the Anthropic Console). Session review caught one real bug pre-review (delegated `freshestForCanonical` queried with canonical dims only, missing the `default_coordinates` merge — silently null, offers lost their period; its tests had passed because nothing asserted the period's presence — fixed + pinned). The adversarial multi-agent review (5 lenses → 21 findings, each judged by two executing skeptics; 47 agents on the mid tier per the delegation rule) double-confirmed 10 — among them two HIGH: **a reply could produce a second clarification via the query layer's `needs_clarification`** (the missing-region check lives there by design; `respondToIntent` now takes a `finalRound` flag converting it to the still-ambiguous refusal) and **the staleness refusal offered a period without its R11 status marker** — plus real test-honesty gaps (clarify clock-injection untested, the staleness-refusal text never number-scanned, an unfailable 2019–2026 whitelist, the fail-closed catch-all untested) and Dutch copy fixes; all 10 fixed and regression-pinned before push. Of the 3 contested findings: split-verb copy rewritten, `CLARIFY_PROMPT_VERSION`/reply-text audit carriage recorded as a WP10 wrap-site obligation in ADR 015, docs/04 got its one-line WP9 note. One of 42 skeptics died on a structured-output retry cap — its finding was a duplicate of an already-double-confirmed one, so no lens lost coverage. No LLM anywhere in refusal/clarification phrasing; full local gate green: typecheck, **414 real tests (was 344) + 1 named todo (R8/WP10)**, scorer.
Previously (session 8, 2026-07-03): **WP8, chart spec + dumb renderer** (`src/chart/`, ADR [014](decisions/014-chart-spec-v1-and-renderer.md)) — the pipeline's chart step, zero LLM involvement (ADR [007](decisions/007-chart-spec-rendering.md)): `buildChartSpec` deterministically projects a validated result into a versioned, zod-validated **ChartSpec v1** (shape `series` → line, `comparison` → bar, `single`/`derived` → no chart; every point carries its resultId (R1), verbatim value plus the same Dutch-formatted display string answers use (R3/R10), CBS status/provisional (R11); null-with-reason cells stay in the spec with honest-gap notes; the R4 attribution sentence — same builder as answer text, now shared from `format.ts` — and the definition-transparency line live inside the spec), and `renderChartSvg` is a pure, dependency-free SVG renderer that computes layout but never data: unlabeled gridlines (no invented axis ticks), line paths broken at null gaps, provisional points open-markered with `*`, attribution always rendered, version-dispatched. **Deviation from the plan of record, recorded in ADR 014:** the Recharts *client* wrapper (ADR 008) is deferred to the chat-UI session — no app exists yet and `src/` runs under Node type stripping where JSX cannot be imported; the SVG renderer doubles as the server-side path ADR 008's revisit trigger reserved. **Measured (hermetic, 2026-07-03): B4 and B8 render correct line charts whose points equal their frozen-key cells** — period/value/status verbatim through the real query path, every key value present in the rendered SVG, zero renderer-invented numeric tokens (every number shown traces to a spec string, checked with the same tokenizer R1/R3 use), marker positions proven exact affine images of the values. Invariant **R6 is now a real test** (the suite's one remaining todo is R8/WP10); CI gains a seventh gate step (`npm run test:chart`, 37 tests). A pre-push adversarial multi-agent review ran (5 lenses + a contract audit, 12 agents on the cheap tier per the delegation rule; every finding double-verified by skeptics who executed their claims in throwaway worktrees) and produced four accepted fixes, all regression-pinned before push: a HIGH test-honesty gap — value labels carried no binding to their point, so a skeptic's label-swap mutation (point A's value shown at point B) passed the entire original suite; now every label carries `data-label-for` and tests assert per-point label identity — plus chronological sorting of the categorical x-axis (first-seen order misplaced periods when series carry disjoint period sets), the stale R6 row in docs/05 (updated to describe the real verification), and a contract-completeness gap from the audit: ChartSpec v1 now carries the pinned `dims`/`dimLabels` (rendered as a subtitle), without which two charts of the same measure at different coordinates (Geslacht=Vrouwen vs =Mannen) were indistinguishable — extended now while zero specs are stored, instead of a v2 migration later. One review finder was killed by a spurious API usage-policy refusal (adversarial phrasing); rerun reworded — a failed lens is missing coverage, never a clean pass. Also fixed: vitest `hookTimeout` raised to 30s alongside the WP6-era `testTimeout` — DB-boot `beforeAll` hooks exceeded the 10s hook default under full parallel load, which retroactively explains the fresh-clone first-run flake seen during the 2026-07-03 machine-move bootstrap. No API spend this session (no LLM anywhere in the chart path; review agents ran on the session harness, not the project key). Full local gate green: typecheck, **344 real tests (was 306) + 1 named todo**, scorer — suites run sequentially (CI's shape) because a concurrent heavy process on the build machine was flipping full-parallel runs red with hook timeouts.
Previously (session 7, 2026-07-03): **WP7, answer composition (LLM phrasing + guards)** (`src/answer/compose/`, shared LLM harness `src/answer/llm/`, ADR [013](decisions/013-answer-composition.md)) — the pipeline's second and last LLM call, confined per ADR [004](decisions/004-llm-usage.md): `claude-sonnet-5` (mid-tier phrasing per ADR 004; thinking disabled, no sampling params — Sonnet 5 rejects them; determinism comes from replay fixtures, correctness from the validator) phrases validated results in Dutch from **pre-formatted value strings**, and a blocking validator enforces R3 (verbatim digits, Dutch number/scale words rejected incl. cardinal compounds), R9 (per-sentence region/period binding incl. derivation values; direction/superlative/comparison words judged per clause against the pre-registered derivations), R10 (unit directly at each value; %-vs-procentpunt and ×1.000-factor guards) and R11 (voorlopig marking in the same sentence as each provisional value). Fail-closed ladder: one regeneration, then a deterministic template that passes the validator by construction; attribution (R4), CC BY derived-marking (R5) and the canonical-definition statement are structural fields, never LLM text; the user's question is deliberately NOT in the phrasing prompt (R2 literal + closes prompt injection, open-questions [#41](open-questions.md)). WP6's harness was reused, not duplicated: one shared client seam, second fixture set, `npm run answer:eval`/`answer:record` off-gate — the 45 WP6 intent fixtures replay byte-identically through the shared seam (proven, not assumed). **Measured (live, 2026-07-03, prompt v3, repeat=2): 14/14 benchmark answers pass — frozen-key values verbatim, zero fabricated numbers, zero regenerations, zero template fallbacks, stable verdicts across repeats** ([benchmark/answer-eval-report.json](../benchmark/answer-eval-report.json), per-run history incl. the v1/v2 runs where the ladder engaged live). CI now drives **B1–B14 end-to-end hermetically** — docs/02 phrasing → parser (replayed) → query → composed answer (replayed) — and the R1/R2/R3/R4/R5/R9/R10/R11 answer-side invariant tests are real; remaining todos: R6 (WP8), R8 (WP10). An adversarial multi-agent review of the diff (5 lenses, 75 agents, two skeptics per finding, bypass claims verified by executing them against the validator) confirmed 23 findings — among them five real validator bypasses (cardinal number-words, the noun 'daling' + separable verbs 'nam af/toe', fullwidth Unicode digits, derivation values escaping R9 binding, count-number collisions) — all fixed and regression-pinned before commit; three more validator false positives were caught by live calibration runs (incl. the '4' in '2025 4e kwartaal' colliding with the value 4,0). Project API spend this session ≈ €0.50 in-session estimate (≈147K Sonnet-5 input / 8K output tokens across four record/eval runs; reconcile against the Anthropic Console — the review agents ran on the session harness, not the project key). Full local gate green: typecheck, **306 real tests (was 210) + 2 named todos**, scorer.
Previously (session 6, 2026-07-03): **WP6, intent parsing (LLM)** (`src/answer/intent/`, ADR [012](decisions/012-intent-parsing-llm-harness.md)) — the pipeline's first LLM call, confined per ADR [004](decisions/004-llm-usage.md): `claude-haiku-4-5` (small/fast parsing tier, temperature 0, structured outputs) maps a Dutch question to registry vocabulary — canonical keys, region *names*, structured period specs — and deterministic code resolves names to CBS codes (Utrecht gemeente-vs-provincie ambiguity, the Den Haag→'s-Gravenhage alias, stand-vs-flow "groei in 2024" cell selection, clock-injected relative periods) and applies the calibrated R7 thresholds (`0.9`/`0.35`, open-questions [#19](open-questions.md) resolved). **Measured (live, 2026-07-03): 45/45 labelled cases — all 14 answerable benchmark tasks parse to exactly their WP5 hand-authored intents, B3/B5 un-disambiguated variants resolve to canonical defaults without clarifying, B15/B16 clarify with one compact question, B17–B19 classify (scope/forecast/causal), B20 "vorige maand" flows through to the query layer's freshness refusal; zero outcome flips across 3 repeats** ([benchmark/intent-calibration-report.json](../benchmark/intent-calibration-report.json)). Calibration took three prompt iterations — the first live run caught three real gaps (dropped "mijn gemeente", vague periods guessed as "latest", B19 causal-vs-scope precedence), each fixed by a rule, not a threshold. Hermetic CI answer (open design question two): record/replay fixtures keyed by a full-request hash behind a client seam — CI replays 45 committed fixtures with no key and no network, and a changed prompt/schema/model/registry fails loudly with a re-record instruction; the live eval (`npm run intent:eval`) is deliberately off-gate. API spend this session: ≈€1.30 in-session estimate (≈1.2M Haiku input tokens across 4 live runs; the committed report preserves the final run's 618,705 in / 14,333 out — earlier runs predate the report's per-run history, so reconcile exact spend against the Anthropic Console). An adversarial multi-agent review of the diff (5 lenses, 27 agents, every finding independently verified by two skeptics) produced five accepted fixes before commit: a pure (non-mutating) candidate merge, boundary-pinning tests for the R7 threshold comparators (a mutation test showed a flipped `<`/`<=` would have passed CI), per-run history in the calibration report, and provenance-honest wording here, in ADR 012 and in lessons-learned for the unpreserved run-1 numbers. Invariant R7 is now a real test. Full local gate green: typecheck, **210 real tests (was 126) + 10 named todos**, scorer.
Previously (session 5, 2026-07-03): **WP5, the deterministic query layer** (`src/query/`, ADR [011](decisions/011-query-contract.md)) — a structured intent (the typed contract the WP6 intent parser must emit) resolves through the registry to CBS coordinates, executes one deterministic SQL query, validates completeness, and returns either a `ValidatedResult` (cells with deterministic coordinate result-ids (R1), verbatim unit + decimals (R10), CBS status + provisional flag + null-reasons (R11), labeled coordinates (R9 binding targets), non-droppable attribution (R4)) or one of ten **typed refusals** — never a guessed value. Derivations exist only as registered functions (R5): explicit `difference` (B13) / `max` (B14) carrying the CC BY "bewerking" marking, plus auto-pre-registered `direction`/`first_last` on every series and a ranking on every comparison (R9). **Measured: all 14 answerable benchmark tasks (B1–B14) reproduce their frozen answer-key values exactly through hand-authored intents, plus the B20 freshness refusal (offers 2026MM06/Voorlopig, no value), hermetically in CI** ([tests/query/benchmark-intents.test.ts](../tests/query/benchmark-intents.test.ts)) — this is query-layer scoring, not the full gate: no LLM is involved yet, so the scoreboard below stays empty until the pipeline exists end-to-end. Invariant suite: R1/R4/R5/R9/R10/R11 query-layer halves are real tests now; answer-side halves remain named todos (WP6–WP10). Enablers: the CPI fixture was re-captured through a capture-only slice (its old 1,000-row `$top` sample cut off at 2020, silently missing B3/B4/B20's cells — see lessons-learned), and open-questions [#37](open-questions.md) (freshness-refusal policy) is resolved. An adversarial multi-agent review of the diff (5 lenses, every finding independently verified) produced two accepted fixes before commit: independent-oracle tests pinning the derivation semantics, and clarification refusals that name **all** unresolved axes in one round (docs/05's combined-presets rule). Full local gate green: typecheck, **126 real tests (was 60) + 11 named todos**, scorer.
Previously (session 4, 2026-07-03): **table registry + alias list** (ADR [010](decisions/010-registry-canonical-measures.md)) — `cbs_tables.default_coordinates`/`.period_semantics` populated for all 8 tables, plus `canonical_measures` (migration 002, the alias list) with one row per headline concept, each carrying its Dutch definition label and alternate CBS readings kept visible. B6/B9 defaults remain owner-revisable assumptions ([open-questions #35](open-questions.md)/[#36](open-questions.md)). Applied idempotently to the live database; 14 hermetic tests incl. 8 cross-checks against the frozen key.
Previously (session 3, 2026-07-03): **benchmark answer key frozen** ([benchmark/answer-key.json](../benchmark/answer-key.json)) — all 14 answerable tasks (B1–B14) plus the B20 freshness reference, each entry re-queried directly from the ingested `observations` table (not copied from docs) and cross-matching docs/07's independently-measured values exactly. `scripts/score-benchmark.mjs` updated to validate the frozen key's structure honestly rather than hard-failing until the answer pipeline exists.
`DATABASE_URL` verified and corrected to the IPv4 session pooler with pinned-CA TLS ([RUNBOOK.md](RUNBOOK.md), secrets register); migrations 001-003 applied to Supabase. **First live ingest (measured 2026-07-02 UTC / 2026-07-03 session date): all 8 tables, 641,024 rows, 0 failures, 0 corrections.** Post-ingest sanity check: **17/17 benchmark-cell coverage checks pass** — every cell B1–B14 + B20 needs exists in the database, and every reference value docs/07 states reproduces exactly (incl. solar 2024 = 21,822 mln kWh *NaderVoorlopig* — the R11 marker — and both ×1,000-unit cells the R10 guard covers). Database size: 230 MB of the 500 MB free tier (headroom decision: [open-questions #33](open-questions.md)).
**Security fix (2026-07-03, complete):** Supabase's "Automatically expose new tables" setting had granted `anon`/`authenticated` full CRUD on every table via the unused Data API — not exploitable in practice (Supabase's own RLS-auto-enable safety net already blocked it) but real attack surface with no offsetting benefit. Fixed on both layers: migration 003 revokes the grants (verified clean via `get_advisors`: 2 WARN + 6 INFO → 6 INFO), **and** the owner disabled the entire Data API in the dashboard (stronger than the sub-toggle — the whole `/rest/v1/` REST layer is off). Verified 2026-07-03: app's `DATABASE_URL` connection unaffected, scan clean. Supabase account/org details recorded in [RUNBOOK.md](RUNBOOK.md).

## Phase 0 checklist

- [x] Open questions #10, #18, #20 answered by Stefan (2026-07-02 — see [open-questions.md](open-questions.md))
- [x] Doc-set sign-off by Stefan (2026-07-02)
- [x] CBS table set chosen; IDs validated against the live catalog (2026-07-02, open-questions #1 resolved — 8 tables, all v4-reachable, every benchmark period confirmed present: [07-phase0-table-set.md](07-phase0-table-set.md))
- [x] Benchmark answer key frozen (2026-07-03: [benchmark/answer-key.json](../benchmark/answer-key.json) — 14/14 answerable tasks + B20 freshness reference, values re-verified against the live ingest, not just copied from docs; [02-user-scenarios.md](02-user-scenarios.md), Scoring)
- [x] Ingestion + validation pipeline with fixture tests (2026-07-03: five ordered checks, quarantine, correction-diff log, idempotent syncs; the 10 inherited `todo` obligations are now 21 real fixture tests + 8 adapter tests on an embedded real-Postgres test DB (ADR [009](decisions/009-hermetic-test-database.md)); adversarial review found and fixed 2 ordering/defaulting bugs; live ingest recorded above)
- [x] Table registry + alias list (2026-07-03: ADR [010](decisions/010-registry-canonical-measures.md); `cbs_tables.default_coordinates`/`.period_semantics` populated for all 8 tables, `canonical_measures` alias list seeded with 8 canonical concepts, applied live and idempotently; 14 hermetic tests incl. cross-checks against the frozen benchmark key)
- [x] Intent parsing (schema-validated, ranked candidates + confidence) (2026-07-03: `src/answer/intent/` per ADR [012](decisions/012-intent-parsing-llm-harness.md) — LLM emits registry vocabulary only, deterministic resolution to CBS codes, R7 thresholds calibrated at 0.9/0.35 against a 45-case labelled set, 45/45 measured live with zero flips over 3 repeats; CI replays committed LLM fixtures hermetically)
- [x] Deterministic query + validation + registered derivations (2026-07-03: `src/query/` per ADR [011](decisions/011-query-contract.md) — intent contract fixed for WP6, coordinate result-ids, registered derivations with CC BY marking, ten-kind refusal taxonomy incl. slice-vs-unpublished distinction and value-free freshness refusals; B1–B14 reproduce the frozen key + B20 refuses correctly, hermetically in CI)
- [x] Answer composition with verbatim/semantic/unit checks (2026-07-03: ADR [013](decisions/013-answer-composition.md) — `src/answer/compose/` + shared LLM harness; R1/R2/R3/R4/R5/R9/R10/R11 answer-side invariant tests real; B1–B14 end-to-end hermetic in CI with zero fabricated numbers; 14/14 measured live, prompt v3, zero template fallbacks)
- [x] Chart spec + dumb renderer (2026-07-03: `src/chart/` per ADR [014](decisions/014-chart-spec-v1-and-renderer.md) — versioned zod-validated ChartSpec v1 built deterministically from validated results, pure dependency-free SVG renderer, R6 real; B4/B8 line charts reproduce the frozen key hermetically in CI; Recharts client wrapper deferred to the chat-UI session per ADR 014)
- [x] Refusal & clarification behavior (2026-07-03: ADR [015](decisions/015-refusal-clarification-composition.md) — `src/answer/respond/` deterministic templates + one-round clarify-reply merge; B15–B20 6/6 hermetic in CI; staleness both branches clock-injected; clarify-reply calibrated live 7/7, zero flips ×3)
- [x] Audit record per answer (R8) (2026-07-03: ADR [016](decisions/016-audit-records.md) — migration 004 `audit_answers`, one row per answer/refusal/clarification written before the response returns, fail-closed on audit failure; `reconstructionReport` re-verifies every row from the stored row alone with tamper tests proving teeth; benchmark scorer reads audit records: hermetic run/score pair in CI, gate PASS measured 14/14 + 6/6 + 0 fabricated)
- [x] CI gate live (2026-07-02): GitHub Actions runs typecheck + the eight gate suites + the benchmark run/score pair on every push. State after WP10 (2026-07-03): **432 real tests + 0 todos** — the query suite scores B1–B14 against the frozen key (hand-authored intents), the answer suite drives B1–B14 **and B15–B20 plus the clarification round** end-to-end over replayed intent/answer/clarify fixtures (ADR [012](decisions/012-intent-parsing-llm-harness.md)/[013](decisions/013-answer-composition.md)/[015](decisions/015-refusal-clarification-composition.md)), the chart suite proves B4/B8 line charts against the frozen key, the audit suite proves R8 (rows reconstruct, fail-closed, tamper detection), and `benchmark:run`+`benchmark:score` produce and score the full 20-task run from audit records (a missing dump is a CI failure) — still no secrets and no network. After WP11 (2026-07-03): **445 real tests** — the benchmark suite gained the scorer-teeth tests, which score tampered dumps through the real scorer subprocess and pin every docs/03 gate leg (both sides of the ≥12/14 boundary, 6/6, zero-fabricated, the fail-closed duplicate-id/missing-dump guards). **After WP12 (2026-07-04): `gate` job also runs `web/`'s own typecheck + 6-test suite; a second job, `deploy`, is gated on `gate` via `needs:` and is the only thing that ever deploys (Vercel git integration deliberately not connected) — deploy-blocking-on-red is live, not just planned.**
- [x] Provider spend caps, billing alerts, and dependency alerts set (complete 2026-07-04: Anthropic €25/mo spend cap confirmed set 2026-07-02; **Anthropic billing alert confirmed set by the owner 2026-07-04** (RUNBOOK step done); **dependency alerts complete** 2026-07-03 — weekly grouped version-update PRs via `.github/dependabot.yml`, Dependabot *security alerts* enabled by the owner (verified via the GitHub API, `/vulnerability-alerts` → 204), Dependabot *security-update PRs* enabled via the API in WP11 (`/automated-security-fixes` → `enabled: true`); web/'s own independent lockfile got a matching second Dependabot entry in WP12)
- [x] Full benchmark run recorded below (2026-07-03, WP11: live run through the audited pipeline — gate criteria measured PASS, see scoreboard; provenance in [benchmark/live-benchmark-report.json](../benchmark/live-benchmark-report.json), policy in ADR [017](decisions/017-live-benchmark-run.md))
- [x] Minimal chat UI + first deploy (2026-07-04, WP12: [web/](../web/) — Next.js App Router chat UI over the audited entry points, Recharts wrapper over ChartSpec v1, CI-gated Vercel deploy; ADR [018](decisions/018-chat-ui-and-deploy.md). **Live at https://checkdecijfers.vercel.app** — all four `ComposedResponse` kinds (answer, chart, clarify-then-refusal, direct refusal) measured working against the real deployment)

## Benchmark scoreboard

| Date | Answerable (of 14) | Refusal (of 6) | Fabricated numbers | Median response | Gate verdict |
|---|---|---|---|---|---|
| 2026-07-03 (live, WP11) | **14/14** | **6/6** | **0** | 6,465 ms (all 20 first turns; answerable-only 7,289 ms) | **PASS** |

Gate: ≥12/14 answerable, 6/6 refusal, **zero** fabricated numbers ([03-mvp-scope.md](03-mvp-scope.md)). Also reported, informational: median latency, clarification count on B1–B14, template-fallback count, un-disambiguated phrasing check ([02-user-scenarios.md](02-user-scenarios.md), Scoring).

## Next up

**All seven post-validation decisions made by the owner (2026-07-05, morning after the pass) — the plan of record is set. Build order:**

0. **Next sessions, in order** (briefs in [08-build-plan.md](08-build-plan.md)): ~~WP14~~ **done 2026-07-04** (see the session-14 entry above) → ~~WP13~~ **done 2026-07-04** (see the session-15 entry above) → ~~WP15~~ **done 2026-07-04** (see the session-16 entry above — conversation memory + #56 servability check, live on the deployed app once this push's CI goes green). ~~The live-wiring pass~~ **done 2026-07-04, session 17** (see above — Resend SMTP + Supabase Auth URLs from session 16, Stripe test-mode from session 17; RUNBOOK.md's Phase 1 checklist now fully checked off through the test-mode account). **Now unblocked, owner to sequence:** (a) a focused live-verification pass (a real magic-link login, a real test-mode checkout) — not yet done, code is wired but unexercised end to end; (b) remaining Phase 1 adds per [06-roadmap.md](06-roadmap.md): the anonymous-trial page ([#53](open-questions.md), explicitly a fast-follow), CSV export for journalists, the F3 meta-question templates ([#29](open-questions.md) answer-card session); ~~(c) WP16 stays queued Phase 2–3~~ — **SUPERSEDED (session 23, 2026-07-05): WP16 (on-demand CBS fetch) is now the owner's #1 build priority — see the TOP PRIORITY STACK at the top of this file. This "Next up" block is historical (session-14–17 era); the top block is the plan of record.** **KvK starts within a few months ([#54](open-questions.md))** — until it clears, no live payments AND no iDEAL (confirmed concretely this session: the Stripe account's Thailand address structurally blocks it, not just a config toggle): the public page can launch with accounts + free credits + card payments only, a launch-timing consequence recorded in #54.
1. **Owner decision (2026-07-04): Phase 1 is next, reprioritized to pull billing/credits forward from Phase 2 — trade-off explicitly re-confirmed after being surfaced plainly.** The chat is being integrated into a real, public website page imminently, so account-gating alone isn't enough — paid access needs to exist before that page goes live (reasoning: [06-roadmap.md](06-roadmap.md) Phase 1, ADR [006](decisions/006-auth-billing-seams.md) Update, [open-questions #47](open-questions.md)). **Auth provider decided (2026-07-04): Supabase Auth, with custom SMTP via Resend for magic-link email** (ADR 006 Update — reuses the existing Supabase project, `@supabase/ssr` fits the Server Actions architecture already in `web/`). The isolated-budget anonymous-trial page ([#53](open-questions.md)) is explicitly sequenced as a fast-follow *after* this paid flow ships — not built in the same work package. **⚠ Real external blocker, unresolved:** Stripe cannot pay out without KvK registration + a business bank account — [RUNBOOK.md](RUNBOOK.md)'s own checklist calls this "the slowest item," and as of 2026-07-04 the owner is still deciding whether/when to start it ([open-questions #54](open-questions.md)). Code can be built without it, but payments cannot go live without it — a parallel-track owner action item, not something the build session can resolve. **The WP brief now exists — WP13 in [08-build-plan.md](08-build-plan.md), drafted 2026-07-05 as a proposal awaiting the approval above.**
2. Six product/ops questions decided the same session (2026-07-04, [open-questions #47–53](open-questions.md)): abuse protection (resolved above), y-axis zero-baseline policy for bar charts (**implemented 2026-07-04**: the SVG renderer already had it since WP8, and the Recharts wrapper — the live path — got it this session via `yAxisDomain` in `web/components/chart.tsx`, test-pinned; [open-questions #48](open-questions.md) updated — only visual styling polish remains for #29), export-correction and export-attribution principles (recorded, apply when embedding/export is built), accessibility (deferred, no proactive work), CSV/data export for journalists (targeted at the now-accelerated Phase 1 work).
3. Owner, non-blocking: review the WP6 period-policy default — present-tense questions answer with the freshest period stated instead of clarifying ([open-questions #40](open-questions.md)).
4. Owner, non-blocking: confirm or override the two registry-internal defaults now encoded live — B6 stock-date ([open-questions #35](open-questions.md)) and B9 bankruptcy definition ([#36](open-questions.md)). Both are a one-row change in `src/registry/defaults.ts` + `npm run registry:apply` if overridden.
5. ~~Owner, non-blocking: [open-questions #44](open-questions.md) (tagging benchmark-run audit rows)~~ **Resolved 2026-07-04 (WP13):** `audit_answers.source_tag` shipped (migration 007); existing rows 1–76 backfilled and live-verified (24 benchmark, 52 validation, 0 user).
6. Owner, non-blocking: an empty, unused Vercel project briefly existed under a debugging mishap this session and was deleted (WP12, see lessons-learned) — nothing to do, recorded for awareness only.

## Phase history

| Phase | Status | Gate result |
|---|---|---|
| Docs / discovery | ✅ complete (2026-07-02) | — |
| Phase 0 | ✅ complete (started 2026-07-02, closed 2026-07-04) | **PASS** — criteria measured 2026-07-03 (live run, see scoreboard row + [benchmark/live-benchmark-report.json](../benchmark/live-benchmark-report.json)); owner (Stefan) signed off in session, 2026-07-04; WP12 (chat UI + deploy) closed the checklist 2026-07-04 |
| Phase 1 | — | — |
| Phase 2 | — | — |
