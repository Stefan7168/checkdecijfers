Session 86 kickoff — paste this as the first message
Written at the session-85 close-out, 2026-09-07 (~08:2xZ). Durable copy of the handoff; the chat may be gone, the repo is the source of truth. Every fact below was verified against `git log`, `gh run view --json jobs`, `git status`, `git worktree list`, and `npm run test:docs` at the time of writing — not recalled from memory.

Read in this order before doing anything: `CLAUDE.md` → `docs/STATUS.md` (the top block is the truth) → this file → `docs/status-archive.md` (session-85 entry, top) → `docs/lessons-learned.md` (session-85 entry, top, 4 lessons) → `docs/08-build-plan.md`'s WP202a section (the exact remaining-work list).

Verify yourself first: `date +%Y-%m-%d`, `git log -7 --oneline` (expect `49376b0` at the tip — a `feat(#201/#202): WP202a UI slice, increment 4 -- wire ingestFile` commit, with `52d5fc9`/`d35c87c`/`90c63c0`/`5593f3d`/`6a3f697` right below it — the rest of session 85's 6 commits), `gh run list --branch main -L 6 --json databaseId,status,conclusion` (expect `gate` green on every one of the 6; `deploy` shows `failure` on all — the KNOWN, still-unfixed Route B Vercel-secrets gap, `#132`, confirm via `gh run view <id> --json jobs`, never the top-level conclusion), `git worktree list` (expect ONLY the main checkout), `git status --short` (expect clean). Second-instance check: `ListAgents` / `mcp__ccd_session_mgmt__list_sessions` for any other session that might be active on this repo.

State after session 85 (2026-09-06 into 2026-09-07, owner present throughout — a direct continuation of session 84):

1. **"Chat with your data" (ADR 037/WP202a, #201/#202) is now feature-complete end to end — still NOT live.** Session 84 built the backend design + first slices; session 85 finished the backend (`reconstructDatasetTurn`, the Server Actions) and then built the ENTIRE UI on top of it: thread-kind dispatch (`ThreadSummary.kind`, `listThreads`' dataset-title subselect, `ThreadSidebar`'s prefix), `UserChartView` (the H2-compliant chart renderer, via a type-only `chart.tsx` refactor), `DatasetChat` (the turn loop) + the full dataset-thread resume path (`loadMyThread` dispatch, `replayDatasetTurns`), `Workspace`'s `Handoff` union mounting `DatasetChat`, and wiring "Bestand uploaden" to `ingestFile`. A real user can now, in principle, upload a CSV, chat with it, get a chart, refine it, and resume the thread later — the ONLY thing missing is a way to reach any of it in production.
2. **6 commits, each independently verified and reviewed, all pushed to `main` and CI-green (`gate` job — `deploy` fails on the pre-existing #132 gap, unrelated):** `6a3f697` (reconstruction), `5593f3d` (Server Actions), `90c63c0` (thread-kind dispatch), `d35c87c` (UserChartView), `52d5fc9` (DatasetChat + thread dispatch), `49376b0` (wire ingestFile). Full detail + per-commit findings: `docs/08-build-plan.md`'s WP202a section.
3. **`/code-review` LOW caught 3 real bugs this session, none by pre-existing or even newly-written tests at the time they were found** — worth reading `docs/lessons-learned.md`'s session-85 entry for the specifics, since two of them are genuinely generalizable gotchas (a SQL bound-parameter type-inference trap that depends on how many times the SAME parameter number is used in one statement; a React "same component instance reused unless keyed" staleness bug that no isolated-render test could have caught). The third (client input vs. server-persisted truth diverging) is this codebase's existing R6 rule, just newly confirmed to apply outside the CBS answer pipeline too.
4. **v1 scope is deliberately, visibly smaller than the CBS chat** — read `docs/08-build-plan.md`'s WP202a section for the exact list, but the headline cuts: no dock support for user charts (`VisualDock` has no `userChart` branch — charts always render inline, even on wide screens), no resumed-turn cost captions, no small multiples/table view/per-point value labels/trend headline/CSV export on `UserChartView`, and the "Link toevoegen"/"Databron verbinden" buttons stay disabled because their backends (url_html ingest, OAuth data sources) simply don't exist.
5. **The owner was offered a 3-way choice once the core feature worked end to end** (continue into `VisualDock` / stop here / skip straight to the `ATTACHMENTS_ENABLED` flag + docs sweep) and **chose to stop.** This was NOT a scope decision about what's needed for go-live — it's a pacing choice; nothing was descoped, the remaining-work list is unchanged from what it was mid-session.
6. **A migration-near-miss / SQL-cast lesson worth internalizing before writing more SQL this session touches**: a bound parameter gets ONE inferred type for an entire statement, not per-occurrence — if `$1` is compared against a `uuid` column in one place and a `text` column in another WITHIN THE SAME QUERY, the bare (uncast) comparison fails even though the identical bare form works fine in a query where that parameter number is used only once. Cast explicitly (`$1::uuid`) whenever a parameter number does double duty across differently-typed columns.

▶ NEXT, in order (per `docs/08-build-plan.md`'s WP202a section — confirm with the owner before assuming, since the last steer was "stop here," not "here's what's next"):

1. Ask the owner whether to resume WP202a (and if so, whether to start with `VisualDock`'s dock branch, or skip straight to the `ATTACHMENTS_ENABLED` flag + docs §7 sweep — both were offered last session and the owner picked neither by choosing to stop), or whether something else takes priority this session.
2. If continuing WP202a: `VisualDock`'s `userChart` branch (additive `DockVisual.userChart` field, `deriveDatasetVisuals`, a `UserChartView` render branch in `VisualDock`, plus its own first-ever dedicated test file matching what `ThreadSidebar` got this session) is the one purely-additive, non-owner-supervised piece left. After that, the `ATTACHMENTS_ENABLED` flag itself (`Chat`/`Workspace` are already built and tested against its presence — the remaining work is threading it through `page.tsx`, mirroring `WEBSEARCH_ENABLED`'s exact pattern) is a bigger step that touches production wiring; fixtures (`tests/fixtures/llm/attachments/`, `attachments:record`/`:eval`) and the docs §7 sweep (`docs/05-data-rules.md`'s new U-row section, `docs/09-pricing.md`, `docs/13`, `docs/04-architecture.md`, `docs/03-mvp-scope.md`, `docs/06-roadmap.md`, RUNBOOK) come before the owner-supervised migration apply (026/027 are still FILE-ONLY) and go-live.
3. The 3 `gh secret set` commands for Route B (owner's own terminal, blocking only `deploy`) — pending across many sessions now, still not done, still low urgency.
4. WP30c + #197's three older follow-ups — owner-menu, no rush, untouched this session.

Binding frames, unchanged: principles (a)/(b)/(c); H1/H2 for the attachments feature specifically; no prompt-byte changes to the CBS pipeline without owner sign-off; no live DDL without the owner (migrations 026/027 apply is explicitly owner-supervised); no fixture re-recording without explicit authorization. 8 GB machine: run the verification block solo, read the real exit code, redirect a long-running background command's output STRAIGHT TO A FILE — never pipe it through `tail` — or a real failure's detail gets discarded along with the noise (this session's own near-miss, `docs/lessons-learned.md`). `npm run test:docs` before every docs push; `/code-review` LOW before every code push (docs-only pushes exempt); when a LOW finding is a real bug, verify the fix is real too — temporarily revert it, confirm the regression test fails, then restore it. Owner-present sessions push/merge directly, no per-change approval needed (#118 revision).

Session-end: the full wrap-up ritual ran in this session per `CLAUDE.md`'s "Session wrap-up" section — lessons (4 new entries), memory (1 new file + 2 appended existing files + the MEMORY.md index), the full doc set (STATUS rewritten to a lean session-85 summary, status-archive prepended with the full narrative, open-questions #201/#202/#207 corrected, ADR 037's as-built note added, 04-architecture's forward note corrected), a stale-doc sweep (found and fixed 4 genuinely stale "not yet wired"/"nothing built" spots beyond the ones already caught live during the session), clean-state verification, this kickoff doc, and a final self-audit. A future session's own wrap-up should still run the complete ritual per CLAUDE.md, not assume this one covers it.
