# Overnight autonomous session brief — 2026-07-06

**EXECUTED 2026-07-05 (session 22, autonomous overnight run ~05:00–~07:30) — outcomes in the checkboxes below and the full measured record in [STATUS.md](../STATUS.md). Historical record from here on.**

**One-shot brief, owner-authorized 2026-07-05 (session 21 wrap-up, Stefan present).** The owner is asleep: work through the queue below WITHOUT owner interaction. Anything needing a genuine owner decision → record it in [open-questions.md](../open-questions.md) and move to the next item. Never guess, never wake him with questions. Mark this brief executed (checkboxes below + a one-line note at the top) in your close-out commit — it is a historical record after that, not a standing plan.

Multi-agent orchestration and workflows are **explicitly authorized** for this run (owner instruction). The delegation cost-tier rule from CLAUDE.md applies: cheap/mid tiers for legwork and review lenses, the session's own model for briefs, synthesis, and judgment.

## Hard constraints (non-negotiable, all night)

1. **Zero live product-LLM spend.** Every queue item is display-layer or deterministic-code work. Concretely: NO byte changes to `buildSystemPrompt()`, the clarify/follow-up mode sections, the raw-parse schema, or the answer/compose prompts — any of those forces a supervised ~$2 full fixture re-record (see the session-21 lessons-learned entry). If an item turns out to need one, write its brief, record it in open-questions, skip the build.
2. **No live-database DDL.** New migration files may be *written and hermetically tested* (PGlite applies them in CI), but nothing that requires applying a migration to the production Supabase before deploy may be *pushed* — the deploy would serve code against a schema that isn't there. Brief those items instead (see "brief-only" below).
3. **Every push still earns its green the same way:** invariants suite, hermetic benchmark (gate: ≥12/14 answerable, 6/6 refusal, 0 fabricated — current measured baseline 14/14, 6/6, 0), full local gate (typecheck both sides, all nine backend suites — baseline 675 tests, 76 web tests, real `next build`), the house adversarial multi-lens review per WP (mid tier) BEFORE push, CI watched to completion (`gh run watch`). A red push is fixed before anything else proceeds. Never leave main red.
4. **Concurrent sessions are real** (session 21 had owner-side commits land mid-flight): `git fetch` + check `git log` before every push; expect the worktree to move; never assume your commit is HEAD.
5. **Deploys hit production on every push** — that's the project's model; the smoke check covers it. Keep commits per-WP so a bad deploy is one revert.
6. **Don't touch:** KvK/iDEAL (#54, parked), WP16 table onboarding (big, needs its own supervised session), #97(b) max-over-periods capability (owner phase-gate call pending), Vercel account settings (#96(b) skew protection — owner decision on plan/cost).
7. Scope check per item against [03-mvp-scope.md](../03-mvp-scope.md); definition of done per CLAUDE.md — including docs updated in the same change and honest, measured STATUS entries.

## The queue (ordered; skip forward on blockage, never sideways into undecided work)

- [x] **1. WP21 — CSV export (#52).** SHIPPED (commit 0c365f0, CI 28722219676 green incl. deploy): Dutch-notation CSV, attribution/CC BY inside the file via the one shared builder, round-trip-exact values, explicit derivations with source ids; 4-lens review, all findings closed same-session. The standing next-up; placeholder brief already in [08-build-plan.md](../08-build-plan.md). Decisions are made (Phase 1, CC BY 4.0); **format and placement are delegated to you** — decide them, record the reasoning in the WP brief (an ADR only if genuinely load-bearing). Invariants: R1 (exported numbers are the validated `ResultCell[]` verbatim — no reformatting that changes values; mind the decimal-comma vs CSV-separator trap for Dutch Excel), R4 + CC BY (attribution + derived-marking INSIDE the file, not beside the link), display-side only (the WP20 stat-card/citation pattern is the template; the envelope is already fully readable client-side).
- [x] **2. Live-feedback smalls — ALL THREE SHIPPED** (commits f57ff4a+f7fe91e, closures 491ae3b; CI 28723250006 + 28723661278 green): #95 redirect+banner, #96(a) honest stale-deploy copy via Next's own detector, #97(a) real-gap max clarifications with a CHECKED range option (the review caught the first draft offering a grain 4/7 measures lack — fixed pre-... the fix itself shipped in f7fe91e). (b)-legs untouched per the constraints:**
  - **#95** post-purchase redirect → `/?purchase=success` + dismissible banner (decision recorded in the row).
  - **#96(a)** stale-tab Server-Action 404 → catch it client-side, honest Dutch copy ("De site is net bijgewerkt — ververs de pagina") + refresh affordance. Only leg (a) — leg (b) is an owner decision.
  - **#97(a)** the `max`-without-regions clarification must stop asking "welke gemeente of provincie" when that isn't the gap (misleading on a question about months; doubly wrong on a national-only measure). CAREFUL: the resolver's `region_unknown` reason/text is shared with genuinely-unknown place names — split the max case into its own failure shape or dedicated message rather than editing the shared template; the invariant tests + benchmark (B15/B16 pins) are the guard. This is deterministic refusal/clarification copy — zero LLM.
- [x] **3. Display smalls batch SHIPPED** (commit a9bee3b + closures 491ae3b): #84/#86/#90/#91/#92/#71/#75 built and review-hardened (incl. the meta/smalltalk header catch); **#73 judged brief-not-build** per this brief's own condition — deterministic templates cannot promise a servable chip; recorded in its row, belongs with #66's dry-run clicks. Batch was** from the owner-filtered brainstorm (all "build" decisions already recorded in their rows): **#84** message-type styling, **#86** CBS StatLine deep-link, **#90** source chip, **#91** number typography, **#92** chart-footer rearrangement, **#71** visible "voorlopig" badge beyond the stat card, **#75** example-question chips on an empty chat, and **#73** follow-up suggestion chips **only if** the chips can be built fully deterministically (templates over the existing `ConversationContext`/registry — if they'd need an LLM, brief and skip). Zero pipeline changes; every rendered number/label traces to the validated envelope or registry.
- [x] **4. #64 BUILT** (multi-period enumeration merge, zero prompt bytes — multi-region already worked; design verified against the query contract before building; review round ran before its push — see STATUS). Bug-shaped, owner-confirmed. Needs a small design pass first (what the answer shows when a question legitimately selects several periods AND several regions); display-side of already-validated results. Do a design-brief-then-build if the design is unambiguous; brief-only if you hit a real product fork.
- [x] **5. Stretch: BOTH BRIEF-ONLY as this brief's own conditions dictate** — #53's row records its isolated-budget + tracking design as NOT decided (full brief with recommendations now in 08-build-plan.md); #87 hides a real fetch-window design fork (the needed series is not fetched for single-value answers) — brief with the (a)/(b)/(c) fork for the owner. Original conditions:** (the last item in the session-19 owner order, only after WP21 ships green): build ONLY if every product decision it needs (trial credit count, abuse limiting, copy) is already recorded in its row/docs; otherwise write the full brief and stop. Same for **#87** (historical-range chip, real R5 derivation): build only if you judge the derivation cleanly testable under the benchmark gate; otherwise brief.
- [x] **6. Briefs written** (in 08-build-plan.md): #65 as WP25 (execute-ready, fail-open logging, one migration), #14 (execute-ready, purge+self-service+ledger interaction+docs/05 amendment named), the #70/#79/#89/#90 drill-through cluster (one surface, four depths; #90's chip shipped tonight as depth 0). Original list:** **#65** durable error logging and **#14** GDPR retention + self-service deletion (both need live migrations → next supervised session; #14 is urgent-flagged, so make its brief complete enough to execute immediately), and the **#70/#79/#89/#90 source drill-through cluster** design brief (the batch notes say #79 merges with #70/#90).

## Close-out drill (do this even if only item 1 lands)

STATUS.md session entry with **measured results only** (test counts, benchmark score, CI run ids, live spend = €0 expected); build-plan entries updated; open-questions rows marked built/decided with dates; lessons-learned entry if anything surprised you; this brief's checkboxes ticked with a one-line outcome each; everything committed, pushed, CI green and **watched to completion**; finish with a summary the owner can read over coffee: what shipped, what's briefed, what's blocked on him (keep that last list short and decision-shaped).

**~~Still open for the owner himself~~ — DONE (owner confirmed 2026-07-05, before this brief runs):** both the WP20 "Download als afbeelding" PNG save and "Kopieer als citaat" clipboard copy work in a real browser. Nothing pending from him on WP20.
