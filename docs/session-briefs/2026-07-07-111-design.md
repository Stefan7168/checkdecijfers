# WP27 — frozen design: finder shape-fit gate + candidate fallback (#111)

**Canonical design, frozen session 30 (2026-07-07/08, Fable — owner-authorized), AMENDED after a
3-lens adversarial design review (money / invariants / mechanics — the review corrected a
backwards index claim, a missing carrier chain, error-vs-misfit honesty, legacy-row handling and
stage sequencing).** Decisions + rationale in ADR [027](../decisions/027-finder-shape-fit-gate.md);
evidence in the [dossier](2026-07-07-111-problem-dossier.md). An execute session builds THIS, stage
by stage. **Per #118: autonomous build ⇒ branch + PR + owner review.**

**⚠ AMENDED AGAIN 2026-07-08 (session 31, owner-approved A1–A3 — see ADR 027's Amendments
section for the full measured evidence).** Pre-build measurement against live CBS metadata + the
real 4,858-row catalog showed: the intended "personen met bijstand" stock table does not exist in
v1-deliverable form (all person-level tables have breakdown dimensions ⇒ vocabulary registers zero
measures, and no yearly codes ⇒ a year-explicit question fails the grain gate), and the only
deliverable table — **`37789ksz`**, "Sociale zekerheid; kerncijfers" — sat at recall position 51.
Owner decisions: **A1** the stock target is `37789ksz` (it counts uitkeringen, not persons —
accepted; the answer names the measure verbatim); **A2** recall becomes Regulier-first (20 Regulier
+ 4 historic slots, shortlist 24, membership-not-order); **A3** stage C's fit gate gains two
deterministic deliverability pre-checks (time-only dimensions; bare-year question ⇒ JJ codes
present) that advance to the next candidate on failure. The stage texts below are updated in place
where these land.

## Invariants at stake (name these in every subagent brief)

- **R1/R8**: the fit gate reads METADATA only (`fetchTableSchema`), never observation cells; the
  delivered number still comes exclusively from ingested, validated cells via the audited pipeline.
- **Principle (b)**: all new CBS calls live inside the async job — the synchronous turn gains none.
- **Principle (c)**: every uncertain outcome falls toward next-candidate → honest refund; never a
  wrong table served confidently; the product never asserts a fact it did not verify (ADR 027 D2b:
  all-errored → `failed` with the honest infra message, never "geen passende maat").
- **Money**: the ledger primitives (`debitOnboarding`, `compensate`) and `refundOnboarding` stay
  byte-identical; debit amount, idempotency keys and refund semantics unchanged.
  `triggerOnboarding`/`createPendingRequest` widen only to CARRY `candidate_ids`.
- **Identity**: `pending_table_requests.table_id` is NEVER mutated (ADR 027 D2a) — the fit choice
  lives in the new `resolved_table_id`; the `(user_id, table_id)` active-unique index and the
  asking-twice dedupe are untouched by construction.
- **Prompt-byte discipline**: the intent parser's prompt is UNTOUCHED. The rerank prompt bytes
  change (forces a FULL tablefinder fixture re-record — the documentation constant
  `RERANK_PROMPT_VERSION` bumps to 2 but the re-record is forced by the bytes, not the constant).
  Do NOT touch `RERANK_SCHEMA_VERSION` or the prompt's "version is altijd 1" line — that is the
  OUTPUT-schema literal; changing one side breaks every rerank (validated by z.literal).

## Stage A — question-aware rerank + labelled set, ONE branch (merge needs the supervised record step)

The labelled-set replay test sits ON the CI gate and the new cases' fixtures can only be recorded
AFTER the prompt change — so everything below lands as ONE gate-green unit; its fixture-recording is
a short supervised step (~cents, the session-25 pattern):

1. Fixture baseline **(amended A1, as built)**: add the bijstand family to
   `tests/fixtures/cbs/_catalog.json` via the surgical `--catalog-add` capture mode (existing rows
   stay byte-identical): `85615NED` (flows) + **`37789ksz` (the stock target, owner decision A1)**
   + the realistic Regulier competitors (`85585NED`, `85617NED`, `82015NED`, `82020NED`,
   `85692NED`, `82016NED`) so the hermetic shortlist mirrors live competition. Per-table fixture
   docs: `37789ksz` in full (observations + count — Stage C's e2e delivery test ingests it);
   `85615NED` schema-only with the real live `$count` in the manifest (its 181k observations are
   never read by any test).
2. `benchmark/tablefinder-labelled-set.json` — each case gains an optional `"question"` field
   (eval falls back to `topic` when absent). Note: ALL existing fixtures re-record anyway (the
   prompt bytes changed); the fallback is about eval semantics, not fixture stability. New cases:
   - `bijstand-stock`: topic "bijstand", question *"Hoeveel mensen zaten er in 2023 in de
     bijstand?"* → confident with **`37789ksz` (A1) in the candidate CHAIN (pick +
     alternativeIds, Stage-B cap 3)** and the pick NOT `85615NED`. *(Measured at the first
     record: Haiku reads the stock shape correctly but picks the — undeliverable — person-level
     stock table 85585NED with 37789ksz as alternative; deliverability is invisible in
     title/summary, which is exactly the fit gate's job, so chain membership IS the system-level
     success condition. Exact-pick pinning stays for every other confident case.)*
   - `bijstand-flow`: question *"Hoeveel mensen stroomden er in 2023 de bijstand in?"* → `85615NED`.
   - `inkomen-vaag`, genuinely-ambiguous `disclose`-expected case (closes the #104 gap).
3. Code: `findTable(db, { topic, question }, …)`; `serializeShortlist` renders `Volledige vraag` +
   `Onderwerp`; **widen the confident `FindTableOutcome` variant with the rerank's
   allowlist-sanitized `alternativeIds`** (`src/catalog/types.ts` + `find.ts` — without this,
   Stage B has no data source); thread the question from `resolveUnmatched`'s context into the
   `TableFinder` callback (`src/answer/intent/policy.ts`). **Plus (amendment A2): Regulier-first
   recall quotas in `src/catalog/recall.ts`** — `RECALL_REGULIER_SLOTS` 20 + `RECALL_HISTORIC_SLOTS`
   4 (total 24), membership per class by FTS rank, final order pure relevance; measured driver: the
   only deliverable bijstand table sat at raw position 51 behind 14 discontinued tables.
4. Also update BOTH eval harnesses (review: they were unlisted): `scripts/tablefinder-eval.ts`
   (`LabelledCase` gains `question`) and `tests/catalog/find-replay.test.ts`.
5. Supervised step to merge: re-record rerank fixtures, run the eval — the FULL extended set must
   pass at the calibrated threshold. **Measurement gate (A1/A2 residual): the record must show
   `37789ksz` entering the bijstand-stock candidate chain (pick, or top-3 alternatives once Stage B
   consumes them); if Haiku cannot do this reliably, escalate per the ADR 025/027 ladder (Sonnet)
   before touching any design.** **✅ DONE (2026-07-08, owner present, ~€0.15): 11/11 twice,
   byte-stable — 37789ksz at chain position 3 on bijstand-stock; prompt v2 additionally gained a
   measured vague-question honesty rule (inkomen-vaag now discloses, #104's boundary directly
   measured); full iteration record in ADR 027's Amendments section.**

## Stage B — candidate list through the trigger (hermetic)

Thread `candidateIds` (= confident pick first, then sanitized `alternativeIds`, cap 3) through the
FULL carrier chain — every link named (review finding: skipping one strands the data):
`FindTableOutcome` (done in Stage A) → **`src/ingestion/onboarding-finder.ts` — the link where the
list is CONSTRUCTED: the closure builds `candidateIds = [outcome.pick.tableId,
...outcome.alternativeIds].slice(0, 3)` and puts it on the routing (PR-#17 review, session 31:
the original chain named every carrier but omitted this one constructing file — skip it and
`candidateIds` never populates in production even though everything typechecks)** →
`OnboardingRouting` (`policy.ts`) → the `onboarding`
`ParseOutcome` variant (`intent/types.ts`) → `OnboardingEnvelope` (`respond/types.ts`; update BOTH
construction sites in `respond/respond.ts`) → `web/app/actions.ts` `maybeTriggerOnboarding` →
`TriggerOnboardingInput` (`onboarding-trigger.ts`) → `createPendingRequest` **and `fromRow`**
(`onboarding-store.ts`).

**Migration 015**: `alter table pending_table_requests add column candidate_ids jsonb not null
default '[]'::jsonb, add column resolved_table_id text;` — both outside every index; either deploy
order is safe (old rows/new code: empty list = legacy path; new rows/old code: columns ignored).

## Stage C — the fit gate in the job (hermetic)

New module `src/ingestion/onboarding-fit.ts` (fit prompt + schema + validator + version constant,
mirroring `rerank-*.ts`), driven from `processOneRow`:

```
if row.resolved_table_id is set:            // reclaimed retry after an accepted fit — deterministic
    resume at ingest with resolved_table_id  // resume, never re-run the fit loop (ADR 027 D2a)
else if row.candidate_ids is empty:          // legacy row → EXACTLY today's path, no fit gate,
    proceed with row.table_id as today       // no schema fetch, no fit LLM (ADR 027 D2c)
else:
    verdicts = []
    for candidate of row.candidate_ids:
        schema = source.fetchTableSchema(candidate)          // metadata only — on throw: record 'errored', next
        // AMENDMENT A3 (owner-approved 2026-07-08) — deterministic deliverability
        // pre-checks, BEFORE any LLM call, from metadata the job already fetches:
        //   (a) schema.dimensions must be time-only — any other dimension means the
        //       ingest stores no dims='{}' rows and the vocabulary registers ZERO
        //       measures (the ACTUAL live bijstand failure), and
        //   (b) if row.question_text names a bare year, the candidate's Perioden
        //       code list must contain JJ codes (requireGrain('JJ') fails otherwise).
        // Either failure → record 'undeliverable' (a verdict, groups with 'geen'), next.
        fit    = measureFit(row.question_text, schema)        // Haiku, closed choice + 'geen', hard allowlist
                                                              // on throw/invalid: record 'errored', next
        if fit accepts (>= calibrated threshold):
            setResolvedTable(row, candidate)                  // store helper: DB row AND in-memory object
            break
        verdicts.push('geen')
    if none accepted:
        if every candidate 'errored' → failAndRefund("Onverwachte fout …")        // honest infra failure
        else → unanswerableAndRefund("geen van de onderzochte tabellen bevat een maat die deze vraag beantwoordt")
// then the existing steps, unchanged, all reading (resolved_table_id ?? table_id):
// piggyback check → slice estimate → register+sync → vocab → delivery (gate intact)
```

- **Executor notes (PR-#17 review, session 31):** (a) the pseudocode above uses the DB column
  names (`row.question_text`, `row.candidate_ids`, `row.resolved_table_id`); in TypeScript these
  are the camelCase `PendingTableRequest` fields (`row.questionText`, `row.candidateIds`,
  `row.resolvedTableId` — the latter two land with Stage B's `fromRow` widening). (b) The
  `85615NED` fixture is SCHEMA-ONLY: its manifest carries the real live `$count` (181,440) but
  ZERO observation pages — correct for the fit gate (metadata only), but if any test ever lets
  the job INGEST it, the run dies with a misleading "sync fetched 0 rows … refusing to ingest"
  (that message then means "you ingested the schema-only fixture", not "CBS data problem").
- The fit result (measure code + one-line reading) is recorded on the row (`fit_note text`, same
  migration — diagnostics only; delivery does NOT consume it: the parser + delivery gate still
  decide independently, defense in depth).
- Tests (fixture-driven, `FixtureSource` with the Record<tableId, docs> form serving BOTH bijstand
  tables from Stage A's captures): candidate 1 misfits → candidate 2 delivers; all-'geen' →
  unanswerable with the scoped message; all-errored → failed with the infra message; fit-error on
  1 then accept on 2; legacy row (`[]`) → byte-identical today-path (no schema fetch — pin with a
  throwing-source guard); reclaim-after-accept resumes at ingest (no second fit loop, no second
  table ingested); re-ask during running → `alreadyPending`, no second debit (dedupe pin on the
  UNCHANGED table_id). **A3 additions:** a breakdown-dimension candidate (85615NED's schema) is
  skipped `undeliverable` WITHOUT a fit-LLM call; a bare-year question skips a candidate lacking JJ
  codes; the 37789ksz delivery e2e also MEASURES the #124 risk (18 measures all tagged with the
  topic term — does the delivery parse answer or clarify?).

## Stage D — supervised live step (owner present)

Migration 015 to prod → deploy → record the fit-check fixtures live (~cents) → calibrate the fit
threshold from `benchmark/measurefit-labelled-set.json` (new; seed with the real `85615NED` measure
list → must yield `geen` for the stock question, the stock table's list → must yield its
total-persons measure, + 3–4 more onboarding-plausible tables) → **re-ask the bijstand question
LIVE and watch it ANSWER (the owner's acceptance test)** → RUNBOOK entry.

**Known measurement question for stage C's e2e delivery test (flagged session 31, unmeasured):**
`37789ksz` (amendment A1's target) registers ~18 measures, ALL carrying the topic term as an
everyday term (`onboarding-vocab.ts` behavior), THREE of them bijstand-titled (totaal / tot
AOW-leeftijd / vanaf AOW-leeftijd). The delivery parse must rank "Totaal bijstandsuitkeringen"
clearly above the two sub-splits, or R7 rule 4 fires a clarification → unanswerable+refund despite
a perfect fit-gate pass. The stage-C hermetic e2e test (fixture delivery through the real parser
replay) measures this BEFORE stage D; if rule 4 fires, that finding goes to the owner — do not
silently tune vocab registration to dodge it.

## Done-definition

Full gate green (backend + benchmark 14/14 + 6/6 + 0 fabricated + web + typechecks), finder eval
green on the EXTENDED labelled set incl. both bijstand cases + the disclose case, measure-fit eval
green at its calibrated threshold, zero intent-prompt byte changes (diff-proven), ledger
primitives + `refundOnboarding` byte-identical (diff-proven), live bijstand answer delivered.
Docs: ADR 027 as-built note, STATUS, open-questions #111/#104, RUNBOOK.
