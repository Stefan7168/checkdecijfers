Session 80 kickoff — paste this as the first message
Written at the session-79 close-out, 2026-09-05 (~09:0xZ). Durable copy of the handoff; the chat may be gone, the repo is the source of truth. Every fact below was verified against `git log`, `gh pr list`, `gh run list`, `git worktree list`/`git branch`, and direct `gh run view` inspection at the time of writing.

Read in this order before doing anything: `CLAUDE.md` → `docs/STATUS.md` (the top block is the truth — session 79 has its OWN block, above session 78's) → `docs/session-briefs/2026-09-05-session-80-kickoff.md` (this file) → `docs/status-archive.md` (session-79 entry, top) → `docs/lessons-learned.md` (session-79 entry — read this before touching `EnterWorktree`, `ScheduleWakeup`, or any shared-validator code in `src/answer/compose/validate.ts`).

Verify yourself first: `date +%Y-%m-%d`, `git log -5 --oneline` (expect `31e61fa` on top or later — session 79's last docs push), `gh pr list --state open` (expect exactly THREE Dependabot PRs, #1/#2/#3 — **these are fresh numbers on the RECREATED repo, not the old #124/#125**; #2 is the same npm-all group the old #124 was, still blocked on the same zod/fixture-hash issue), `gh run list --branch main -L 5` (expect `gate` green on every run; `deploy` will show `failure` on most of them — that's the KNOWN Route B secrets gap below, not a real problem, but confirm the `gate` job specifically before assuming so), `git worktree list` (expect the main checkout PLUS TWO other worktrees, both deliberately kept — see below, do not delete either without reading why first) + `git branch`. Second-instance check: `ListAgents`. Red anywhere = understand why before proceeding, not necessarily "fix" — two of the things that will look red on first glance (the deploy failures, the extra worktrees) are known and explained below.

State after session 79 (2026-09-05, OWNER PRESENT throughout, ~6 hours — an interactive session; owner explicitly said "ALL" to a 4-item priority menu early on, so every thread below was worked in parallel rather than sequentially):

1. **Route B executed.** Repo made public again: old history archived to the private `checkdecijfers-pre-rewrite-archief`, a fresh public `Stefan7168/checkdecijfers` created and pushed (807 commits, `refs/pull` confirmed empty). A full secret-exposure scan across all history ran first and found nothing real. **Still needed from the owner, blocking nothing critical but blocking `deploy`:** 3 `gh secret set` commands (`VERCEL_ORG_ID`/`VERCEL_PROJECT_ID`/`VERCEL_TOKEN`) on the new repo — until run, every `deploy` CI job fails at "Pull Vercel project settings," harmlessly (production keeps serving its last successful deployment; confirmed live via `checkdecijfers.vercel.app/api/health` → 200 during session 79). **Side effect worth knowing:** recreating the repo reset Dependabot's PR numbering from 1 — the old #124 (npm-all zod regression) and #125 (web deps) no longer exist as such; the same npm-all group is now **PR #2**, confirmed still bumping zod 4.4.3→4.5.4 among 5 packages (targets have moved slightly since, e.g. `@anthropic-ai/sdk` now to 0.123.0) — still expected to hit the same fixture-hash regression documented in [open-questions #200](../open-questions.md).

2. **An early-session background investigation into the old #124 never finished, status genuinely uncertain — check before trusting or discarding it.** Dispatched near the start of session 79 to check whether a newer zod fixes the fixture-hash regression for free, or failing that, split the Dependabot group to land the 4 safe packages while pinning zod. Its worktree (`.claude/worktrees/agent-aa024a353bfdc08d5`, still present, marked "locked") shows modified `package.json`/`package-lock.json` — real attempted progress — but no commit and no completion notification ever arrived, even after ~6 hours (every OTHER agent dispatched this session reported back cleanly, several after 20-40 minutes). Before doing anything else with Dependabot PR #2: run `git -C .claude/worktrees/agent-aa024a353bfdc08d5 status`/`diff`/`log` to see what it actually has, and decide from there — don't discard the uncommitted changes without looking, and don't assume it's "still working" either. If it's genuinely dead, this needs a fresh investigation against PR #2's current (slightly different) exact versions, reusing the well-established root cause (`docs/open-questions.md` row #200) and the git-stash-controlled-verification technique from the #162 work as a model for how to prove any fix.

3. **#197 chart ideas 6 (hide/show a series) + 8 (small multiples): built, reviewed, fixed, MERGED + LIVE in git** (`main`, multiple commits, `gate` green). Full design: [session-briefs/2026-09-05-chart-ideas-4-6-8-design.md](2026-09-05-chart-ideas-4-6-8-design.md). Idea 4 (the takeaway headline, §3 of that doc) is designed but NOT built — bigger than the original research brief implied (needs a new deterministic sentence formatter plus a small additive `ChartSpec` field), sequenced deliberately last, unscheduled. The build process itself is worth reading about even if you don't touch charts again soon: a final whole-branch review (distinct from and after per-task review) caught a Critical bug — stale hidden-series state leaking across a chart switch at two call sites entirely outside the diff (`visual-dock.tsx`, `chart-toggle.tsx`) — that no task-level review could have seen. Full detail: [status-archive.md](../status-archive.md) and [lessons-learned.md](../lessons-learned.md) session-79 entries, and [ADR 014](../decisions/014-chart-spec-v1-and-renderer.md)'s as-built notes.

4. **#162 (slot-filling A/B): closed out at 91% clean after three real attempts, no phrasing verdict — owner's explicit decision, not a session's.** Went from completely blocked to 31/34 test cases template-consistent across 3 rounds of real, verified work (~$1.14/~€1.05 total spend). The remaining gap needs an edit to a SECOND function in the shared validator (`checkBinding` in `validate.ts`'s R9 pass) that isn't safely patchable after the fact the way the first three fixes were. Given a full plain-language walkthrough of exactly what that means, the owner chose to stop rather than authorize a 4th attempt. **Do not restart this without a fresh owner go** — it's parked, not abandoned. Branch `experiment/162-slot-filling-ab` (worktree `.claude/worktrees/agent-ad8ec20c3cba7eebd`, head `f26d01e`) is kept on purpose as a 91%-fixed foundation for whenever it's revisited. **Separately flagged, not yet acted on:** the Tier-B fix from round 3 (a one-word regex addition to `TEMPORAL_BEFORE` fixing a real, confirmed latent bug in the shared validator that also affects the live production pipeline, not just this experiment) is small, well-tested, and sitting on that same branch — worth considering extracting and shipping on its own regardless of what happens to the rest of the slot-filling idea.

5. **A fourth, unrelated bug found during #162's diagnostic work and fixed on request the same session: negation-blindness in R9's direction-word checker** (`main`, commit `54694ae`). `UP_WORDS`/`DOWN_WORDS`/`FLAT_WORDS` matched a bare trend word with no regard for a preceding negation, so honest prose like "groeide gestaag, zonder tussentijdse dalingen" was rejected as a false decline claim. Fixed, tested (3 new tests, including one proving the fix doesn't mask a genuinely wrong claim sitting next to a correctly-negated true one), fully verified (typecheck, backend suite, benchmark gate, `next build`).

6. **Two new roadmap ideas recorded from the owner mid-session, deliberately not built:** user-connectable Google Sheets ([#201](../open-questions.md), the owner himself named the real blocker — no user accounts/auth exists, and he doesn't want to build that yet) and drag-and-drop chat attachments ([#202](../open-questions.md)). Both cross-referenced to each other and to [ADR 032](../decisions/032-websearch-augmentation.md)'s existing "unofficial content gets its own attributed section" pattern, since both raise the identical honesty-model tension. WP30c stayed at "wait." #199 declined again (owner: leave it parked — consistent with every session since 72).

7. **A real tooling bug, found live and reproduced directly, not just inferred:** `EnterWorktree` in the orchestrating session bound an already-running background agent's OWN sandbox to the wrong worktree, refusing all its commands from that point on. Reproduced directly in the orchestrating session too. `ExitWorktree` (`action: "keep"`) then a fresh `EnterWorktree` clears it, no data lost if everything was already committed. Full detail + the two-part rule: [RUNBOOK.md](../RUNBOOK.md) item 11 (multi-agent gotchas section). **Practical implication for this session:** avoid calling `EnterWorktree` while a background `Agent` is still running elsewhere, including the two worktrees named in points 2 and 4 above if anything is ever dispatched into them again.

**▶ NEXT, in order — nothing urgent, everything below is the owner's call or a "whenever," not a session's default task:**

1. The 3 `gh secret set` commands for Route B's new repo — genuinely blocking (only `deploy`, not production, not any code work).
2. Dependabot PR #2 (the renumbered old #124) — check point 2 above before touching it; do not merge as-is regardless (same zod/fixture-hash regression, `docs/open-questions.md` row #200 has the full bisection).
3. Consider extracting #162's Tier-B validator fix for a standalone ship (point 4 above) — small, ready, independent.
4. #162's `checkBinding` gap — parked at 91%, pick up only on a fresh owner go.
5. #199, WP30c, #197 idea 4, the three older #197 follow-ups (i)-(iii) — all owner-menu items, unscheduled, no rush.

Binding frames, unchanged: principles (a)/(b)/(c); no prompt-byte changes without owner sign-off; no live DDL without the owner; no fixture re-recording without explicit authorization and hard-gate discipline (the #162 work this session is the model: stop at a failed gate, don't speculative-fix-and-retry). 8 GB machine: `npm ci` (root AND `web/`) before any local verification — a FRESH worktree especially, its `node_modules` starts empty (RUNBOOK item 3; hit again directly this session in the chart-work worktree, cost real time to diagnose). `scripts/verify-block.sh` + the vitest mutex (`pgrep -f "[n]ode.*vitest"`, never bare `pgrep -f vitest`) for anything long-running; `npm run test:docs` before every docs push. Autonomous session without an owner answer: still never merge core-product/money-path code (#118(b)) — session 79 was owner-present throughout, so that gate never applied, but it applies again the moment a session runs unattended.

Session-end: the full wrap-up ritual from CLAUDE.md ran in full this session (this file, the status-archive entry, lessons-learned ×2 rounds, memory files, a real stale-doc sweep that found and fixed two genuine misses — a stale "repo is private" RUNBOOK line and the Dependabot renumbering — and this final self-audit). A future session's own wrap-up should do the same, unprompted, per [[feedback_unprompted_session_end_hygiene]] and [[feedback_session_wrapup_ritual]] if those memory files still exist on whatever machine runs it — if not, this paragraph and CLAUDE.md's own "Session wrap-up" section are the durable copy.
