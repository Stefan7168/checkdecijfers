# WP30a executor brief — formalize the source waist (hermetic, CBS byte-identical)

**Written session 36 (2026-07-11), AFTER the completed pre-build adversarial design review
(62 agents incl. the network-interrupted delta-lens re-run; verdicts + the seven amendments A1–A7 in ADR
[030](../decisions/030-multi-source-architecture.md) § Amendments — read that section FIRST,
it corrects the original D-points in load-bearing ways).** Build per
[open-questions #118](../open-questions.md): own branch + PR + owner review; never push code to
`main`; the session never merges without the owner's in-chat approval on a green gate.

## What WP30a is

Give the pipeline a SOURCE IDENTITY without changing a single observable byte: a code-level
source registry, `Attribution.source`, the D3 display consolidation (as amended), and the D4
migration as a FILE. Every existing answer, citation, chart spec, CSV, stored audit row and LLM
request stays byte-identical — `source: 'cbs'` (or ABSENT, per A1) resolves to today's exact
strings. WP30b (conformance harness + guide) is a SEPARATE later session (A5). WP30c (real
second source + public-claim wording) stays owner-deferred (#123).

## Reading order

1. ADR 030 **including § Amendments** (A1 absent-source fallback is the load-bearing one).
2. This brief's work list + pins.
3. docs/05-data-rules.md — R4 (attribution), R8 (audit rows reconstruct forever), R11
   (provisional/verbatim labels).
4. The review inventories (full text in the session-36 review result; summarized below).

## Work list (file-level, from the review's measured inventories)

1. **`src/sources/registry.ts` (new)** — committed constant map, one entry `'cbs'`:
   `{ key, displayName, attributionLabel: 'CBS StatLine', license: 'CC BY 4.0',
   deepLink(tableId) → the statline.ts URL shape, provisionalDisplay (A2 — a MAP:
   Voorlopig → ' (voorlopig cijfer)', NaderVoorlopig → ' (nader voorlopig cijfer)'),
   nullReasonLabels (today's NULL_REASONS from template.ts), adapterFactory }`.
   Plus `resolveSource(source | undefined)` — undefined → the `'cbs'` entry (A1), the ONE
   lookup every consumer uses.
2. **`src/query/types.ts`** — additive `Attribution.source?: string` (the registry KEY, not the
   resolved object — stored rows stay small; resolution happens at display time via
   `resolveSource`). `runQuery` writes `source: 'cbs'` on new results.
3. **Consolidate the display sites onto `resolveSource(...)`** — output byte-identical for
   cbs/absent, pinned:
   - `src/answer/compose/format.ts` `buildAttributionLine` ("CBS StatLine" + license).
   - `src/answer/compose/template.ts` `provisionalSuffix` + `NULL_REASONS`/`nullReasonText`
     (move the tables INTO the registry entry; the functions read via `resolveSource`).
   - `src/chart/build.ts:47` null-note `'… (CBS).'` (A3 — stored in `ChartSpec.nullNotes`,
     R8-re-derived: byte-identical or bust).
   - `web/lib/citation.ts` ("CBS StatLine, tabel …").
   - `web/lib/statline.ts` (deep-link shape → `deepLink`), `web/components/chat.tsx` link label
     ("Bekijk bij CBS StatLine").
   - `web/components/stat-card.tsx` footer + `web/lib/stat-card-data.ts` (A3: additive source
     field on `StatCardData`).
   - `DERIVED_DATA_MARKING`: acknowledged, NOT consolidated (stored verbatim per row inside
     `DerivationRecord.marking` — moving it buys nothing and risks R8; explicitly deferred).
4. **`migrations/016_source_column.sql` (FILE ONLY — no live DDL, owner applies supervised):**
   `alter table cbs_tables add column source text not null default 'cbs'`; same for
   `cbs_catalog`; `check (source = 'cbs' or id like source || ':%')` on both; widen the
   `cbs_tables.platform` CHECK per the ADR. Deploy-order-safe: code never reads the column in
   WP30a (identity comes from the registry constant), so pre-migration production is unaffected.
5. **Benchmark schema** — additive `source` field on tasks/answer-key entries (default 'cbs');
   scorer unchanged.
6. **Chart spec typing** — `attribution.source` optional additive; `CHART_SPEC_VERSION` stays 1
   (A3).

## Pins (the tests that make the byte-identity claim measurable)

- **A1 regression:** a pre-WP30a-shaped `Attribution` (NO `source` key, e.g. via
  `JSON.parse(JSON.stringify(result))` with the field deleted) must re-derive attribution line,
  definition line, marking line, chart spec and full text byte-identically →
  `reconstructionReport` clean. Extend `tests/audit/unit-expansion-r8.test.ts`'s old-row pattern
  or add `tests/audit/source-r8.test.ts`.
- **Golden strings:** `buildAttributionLine`, `nullNote`, `provisionalSuffix`, citation, statline
  URL, chat label, stat-card footer each pinned against today's literal output for cbs AND for
  absent-source input.
- **Zero-LLM-byte proof (A4):** the full hermetic suite + benchmark replay green = every fixture
  hash still matches. The intent prompt and meta 'sources' template are byte-untouched in WP30a
  (grep-diff proof in the PR: `src/answer/intent/prompt.ts` and `src/answer/respond/meta.ts`
  byte-identical unless the meta template's output is proven byte-identical by test).
- Full verification block (STATUS) before the PR: `npm ci` ×2, typecheck ×2, backend suite, web
  suite, benchmark 14/14 + 6/6 + 0 fabricated GATE PASS.

## Explicitly OUT of WP30a

- WP30b harness/guide (own session; D6-as-amended: FIVE validators + four contract families).
- Any `Cbs*`/`cbs-adapter/` rename (A5 — 31+ files of diff noise; alias only if WP30b needs it).
- Intent-prompt / meta-template wording (A4 → WP30c), the public-claim sweep (WP30c, owner-signed).
- `observations`/`canonical_measures`/`pending_table_requests` source columns (derivable from the
  table-id prefix; revisit at WP30c if a real adapter needs it).
- Applying migration 016 (supervised, with the A1 `scripts/verify-audit-rows.ts` re-run against
  real production rows).

## Delta-audit addendum (sites newer than the 2026-07-08 dossier — measured, session 36)

One CONFIRMED design gap → ADR 030 **A6**: `src/catalog/recall.ts:62,64` partitions the finder
shortlist on the literal `'Regulier'` (CBS's CATALOG-lifecycle vocabulary — a different axis
than D2's observation statuses). WP30a work item: add `currentCatalogStatuses` to the registry's
CBS entry (field only — recall.ts itself stays byte-untouched in WP30a; the consultation wiring
+ conformance-contract test are WP30b, per A6).

Recorded deferrals (ADR 030 **A7** — none are WP30a work):
- Two post-dossier CBS-branded LLM prompts (`src/catalog/rerank-prompt.ts` v2,
  `src/ingestion/onboarding-fit.ts`) — fixture-hash-frozen, WP30c wording sweep.
- Fit-gate deliverability pre-checks key on `CbsDimensionKind` ('TimeDimension') — WP30b
  adapter-contract/guide point.
- `parseFactorUnit` (#125a) parses CBS unit spellings; WP29's `baseLabel` assumes CBS's region
  label formatting; `suggestions.ts` adds a third `NL01` literal + hardcoded G4 codes — all
  fail-open, Nederland-scope-consistent; revisit with the first real adapter (WP30c).

